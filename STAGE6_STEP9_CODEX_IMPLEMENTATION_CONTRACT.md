# STAGE6 STEP 9 - CODEX IMPLEMENTATION CONTRACT

STATUS: `DRAFT / PREP_COMPLETE / FROZEN_HASH_BOUND`

BASELINE_MAIN_FOR_FOUNDATION: `7673f3487b0dd52c0c5cb2c7826f4e3fe5cc570e`
FROZEN_JSON_SHA256: `6aa42c46c4bc594d234894dd92f57fd31292cce31953dab78343b653cfbdd45f`

COPY_AUTHORITY: User + ChatGPT

IMPLEMENTATION_ROLE: Codex

MECHANICS_SCOPE: FORBIDDEN

LANGUAGE_PHASE: Russian-first

PROFILE_SET: boomer / genX / millennial / zoomer / alpha

## 1. Trigger

Codex must not implement this package until all of the following exist:

1. explicit user approval/corrections of the consolidated Step 9 review package;
2. `STAGE6_STEP9_FIVE_PROFILE_COPY_FROZEN.md`;
3. `stage6_step9_five_profile_copy_frozen.json`;
4. exact SHA-256 of the frozen JSON bytes (`6aa42c46c4bc594d234894dd92f57fd31292cce31953dab78343b653cfbdd45f`);
5. exact implementation branch/scope from then-current main;
6. required repository model preflight for the actual Codex task.

No draft artifact is implementation authority.

## 2. Required inputs

Codex receives:

1. exact baseline main SHA;
2. exact task branch;
3. frozen JSON SHA-256;
4. exact approved five-profile row set;
5. exact allowed write scope;
6. exact source/docs mirror pairs;
7. known current mirror-drift evidence from `STAGE6_STEP9_FRESH_CURRENT_MAIN_INVENTORY_V1.md`;
8. gap dispositions from `STAGE6_STEP9_GAP_CLOSURE_V1.md`;
9. static/runtime validation commands;
10. user-owned Safari acceptance checklist.

If any required copy row is missing, ambiguous, contradictory or not approved:

- do not invent a replacement;
- stop on that row;
- report the exact gap.

## 3. Technical objective

Implement five first-class UI tone profiles:

- boomer
- genX
- millennial
- zoomer
- alpha

All five route across the same presentation surfaces without changing mechanics.

## 4. Gen X requirements

Gen X must become first-class, not a fallback.

At minimum:

- add `genX` to supported profile registry;
- preserve canonical birth-year band `1965-1980`;
- route normalized `genX` through text-mode selection;
- add explicit Gen X start/onboarding dictionary;
- add explicit Gen X profile text dictionary;
- add Gen X system/profile text routing;
- add Gen X final presentation control/system/toast copy;
- add Gen X NPC profile routing/pools where current architecture profiles NPC speech;
- add Gen X preview/smoke/hidden-label coverage;
- add persistence/reload/profile-switch regression;
- update exact source/docs mirrors for every touched file.

Do not change generation boundaries unless separately user-approved.

## 5. Explicit-profile rule

For every frozen player-facing semantic row:

- resolve exact copy for all five profiles;
- identical copy across profiles is allowed only when the frozen mapping marks it `SHARED_INTENTIONALLY`;
- do not satisfy a profile through accidental spread/fallback inheritance;
- reusable implementation structure is allowed, accidental creative inheritance is not.

In particular:

- Gen X must not fall through to Millennial/Boomer;
- Alpha must not remain merely a Zoomer baseline plus incidental overrides for frozen implementation rows.

## 6. Copy application rule

For every approved row:

- exact TEXT_ID/source location mapping must be deterministic;
- placeholders remain exact;
- numeric values/costs/outcomes remain source-driven;
- punctuation and emoji are part of approved copy;
- profanity/slang must not be softened;
- no synonym substitution;
- no English/Russian translation beyond approved text;
- no post-application "style improvement" pass.

## 7. Intensity rule

Preserve semantic intensity:

- I0_CRITICAL: clarity first;
- I1_GUIDANCE: generational but unambiguous;
- I2_EXPRESSIVE: strong recognisable voice while preserving facts;
- I3_FULL_FREEDOM: full flavour within exact role/semantic meaning.

Do not flatten I2/I3 to neutral copy.

## 8. Unified toast invariant

Preserve current PR #258/final presentation architecture:

- one coalescing stat-toast owner;
- simultaneous/nearby stat changes merge into one toast;
- no stacked duplicate delta cards;
- no duplicate permanent top-bar icons inside toast;
- target REP is not player REP;
- economy/moneyLog meaning remains identical.

Only frozen profile rendering changes.

## 9. P2P product decision

Implement exact product decision:

`HIDE_DISABLED_P2P_BLOCK_UNTIL_FEATURE_ENABLED`

When required P2P capability is disabled:

- do not render the unfinished transfer block;
- do not expose `P2P: анти-абуз.`;
- do not expose the dead `Почему?` affordance;
- do not generationally rewrite technical backlog copy.

Do not enable P2P as part of this task.

## 10. `npcArrest*` cleanup gate

Before deleting any `npcArrest*` key:

1. run exact current repository/callsite search on the implementation baseline;
2. if zero active callsites remain, delete orphan keys in source and mirror within the owned system-copy lane;
3. if any real callsite exists, stop and report the mechanic instead of guessing copy.

No generational `npcArrest*` wording is authorized.

## 11. Mirror drift rule

Known foundation-baseline drift exists in multiple source/docs pairs, including at least:

- `npcs.js`
- `ui/ui-menu.js`
- `ui/ui-boot.js`
- `conflict/conflict-api.js`
- `state.js`
- `ui/ui-loops.js`

Do not copy whole files blindly.

For every touched drifted pair:

- diff source vs docs first;
- isolate Step 9-owned hunks;
- preserve unrelated divergence;
- apply equivalent approved change to both paths;
- prove expected parity or documented deterministic transformation after the wave.

Source/deployed mirrors share one serialized ownership lane.

## 12. Mechanic-canon lock

Codex must not:

- create gameplay rules;
- change economy values;
- change REP/points/influence math;
- change battle outcomes;
- change argument logic/canon;
- change vote counting;
- change cooldown behavior;
- change NPC behavior;
- change persistence state shape beyond required existing uiProfile support;
- store raw birth year;
- add entities/handlers merely for tone;
- alter transfer feature flags;
- infer missing mechanic semantics.

## 13. Accessibility intentional sharing

Critical screen-reader/accessibility labels may remain shared where frozen package marks them `SHARED_INTENTIONALLY / ACCESSIBILITY_CANON`.

Do not force generation slang into ARIA copy merely to increase profile difference.

## 14. System wrapper discipline

For dynamic wrappers:

- preserve placeholders exactly;
- render representative nested values across all five profiles;
- reject duplicate punctuation/slang;
- keep errors/hints understandable;
- never hide a cost, target, outcome or required next action behind opaque slang.

## 15. Structural checks

Static checks must prove:

- supported registry contains all five;
- year resolver maps all five canonical generation bands correctly;
- each profile has explicit presentation authority;
- each frozen semantic intent has five explicit rows or an intentional-sharing declaration;
- Gen X does not fall through;
- accidental Alpha/Zoomer fallback does not satisfy frozen rows;
- profile switch changes presentation only;
- source/docs touched mirrors are deterministic;
- disabled P2P block is hidden without enabling P2P;
- orphan cleanup obeys zero-callsite precheck.

## 16. Runtime checks

Runtime smoke must cover:

- birth-year -> profile for five bands;
- direct profile set/get for five profiles;
- reload persistence for five profiles;
- profile change-later flow;
- persistent UI signature for each profile;
- representative I0/I1/I2/I3 surfaces;
- start/onboarding;
- battle/vote/report/economy/system feedback;
- unified toast behavior under simultaneous deltas;
- NPC role/profile samples;
- no cross-profile leakage;
- no mechanic/economy divergence under the same scripted scenario.

## 17. Hidden-label human acceptance

Automated PASS is insufficient.

Final user Safari acceptance must verify:

- Boomer recognisable;
- Gen X recognisable;
- Millennial recognisable;
- Zoomer recognisable;
- Alpha recognisable;
- Russian cultural lane is natural for each;
- critical actions understandable;
- mechanics identical.

Stage 6 COMPLETE is forbidden until the user explicitly confirms the visible/runtime result.

## 18. Serialized implementation waves

Wave 1 - five-profile plumbing:

- Gen X registry/routing/dictionaries;
- explicit five-profile route model;
- structural tests only.

Wave 2 - signature/persistent copy:

- start/chat/DM/panels/menu/common actions;
- disabled P2P hide;
- hidden-label quick matrix.

Wave 3 - system/economy/feedback:

- system routes;
- economy/report/vote feedback;
- unified stat toasts;
- empty/cooldown/error states.

Wave 4 - NPC/flavour:

- role/profile pools;
- diversity checks;
- `npcArrest*` zero-callsite cleanup gate.

Wave 5 - full regression:

- frozen semantic coverage;
- placeholder invariants;
- same-scenario mechanic invariance;
- mirror audit;
- five-profile previews/smokes.

Wave 6 - user Safari acceptance.

Each wave must be isolated, reviewable and serialized around shared runtime/dev-check ownership.

## 19. Current gate

`CODEX_IMPLEMENTATION_AUTHORIZED = TRUE`

Current authority:

- Step 5 `[x] USER_REVIEW`.
- Step 6 `[x] FREEZE + SHA256`.
- Executable selector state: `IMPLEMENTATION_ALLOWED`.
- Authorized thread: `019f8ffa-074f-7003-bd47-dd02bc7bb3be`.
- Frozen content commit: `e13419c38b9bf23fd601923a6d4a3a500012c8eb`.
- Frozen JSON SHA-256: `6aa42c46c4bc594d234894dd92f57fd31292cce31953dab78343b653cfbdd45f`.

NEXT_ACTION: `CODEX_IMPLEMENTATION_EXECUTING_STEP7_STATIC_WAVES_1_TO_5`.
