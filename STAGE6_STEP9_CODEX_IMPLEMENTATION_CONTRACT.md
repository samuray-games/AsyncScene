# STAGE6 STEP 9 - CODEX IMPLEMENTATION CONTRACT

STATUS: DRAFT / NOT YET AUTHORIZED FOR IMPLEMENTATION
BASELINE_MAIN_FOR_FOUNDATION: 7673f3487b0dd52c0c5cb2c7826f4e3fe5cc570e
COPY_AUTHORITY: User + ChatGPT
IMPLEMENTATION_ROLE: Codex
MECHANICS_SCOPE: FORBIDDEN
LANGUAGE_PHASE: Russian-first
PROFILE_SET: boomer / genX / millennial / zoomer / alpha

## 1. Trigger

Codex must not implement this package until ChatGPT marks the semantic matrix as frozen/approved and supplies the exact approved artifact revision/hash.

## 2. Required inputs

Codex receives:
1. exact baseline main SHA;
2. exact task branch;
3. frozen semantic-matrix artifact SHA/hash;
4. exact approved five-profile row set;
5. exact allowed write scope;
6. mirror pairs;
7. exact static/runtime validation commands;
8. user-owned visual acceptance checklist.

If any required copy row is missing, ambiguous, contradictory, or not approved:
- do not invent a replacement;
- preserve current text for that row;
- report the exact gap.

## 3. Technical objective

Implement five first-class UI tone profiles:
- boomer
- genX
- millennial
- zoomer
- alpha

All five must route across the same presentation surfaces without changing mechanics.

## 4. Gen X requirements

Gen X must become first-class, not a fallback.

At minimum:
- add `genX` to supported profile registry;
- preserve canonical birth-year band 1965-1980;
- route normalized `genX` through text-mode selection;
- add/start Gen X dictionaries;
- add current presentation-layer Gen X control copy;
- add Gen X system/toast copy;
- add Gen X preview/smoke coverage;
- add persistence/reload/profile-switch regression;
- update exact source/docs mirrors.

Do not change the year-band semantics unless a separate user-approved task changes generation boundaries.

## 5. Copy application rule

For every approved row:
- exact TEXT_ID/source location mapping must be deterministic;
- placeholders remain exact;
- numeric values/costs/outcomes remain exact;
- punctuation and emoji are part of the approved copy;
- profanity/slang must not be softened;
- no synonym substitution;
- no English/Russian translation beyond approved text.

## 6. Intensity rule

Implementation must preserve the matrix intensity class:
- I0_CRITICAL: clarity first;
- I1_GUIDANCE: generational but unambiguous;
- I2_EXPRESSIVE: maximum recognisable voice while preserving facts;
- I3_FULL_FREEDOM: full generational flavor.

Codex must not flatten I2/I3 copy into neutral wording.

## 7. Unified toast invariant

Preserve PR #258 architecture:
- one coalescing stat-toast owner;
- simultaneous/nearby stat changes merge into one toast;
- no stacked duplicate delta/stat cards;
- no duplicate permanent top-bar stat icons inside toast;
- target REP is not player REP.

Only profile-specific wording changes.

## 8. Forbidden implementation behavior

Codex must not:
- create new gameplay rules;
- change economy values;
- change REP/points/influence math;
- change battle outcomes or argument logic;
- change NPC behavior;
- change persistence state shape beyond existing uiProfile support;
- store raw birth year;
- add new entities/handlers for tone purposes;
- implement Gen X as alias of Boomer or Millennial;
- infer missing copy;
- auto-sanitize slang/memes/profanity;
- run a "style improvement" pass after applying frozen text.

## 9. Five-profile structural checks

Static checks must prove:
- supported registry contains all five;
- year resolver maps generation bands correctly;
- each profile has a presentation dictionary;
- each approved semantic intent has either five approved rows or an explicitly documented gap;
- Gen X does not fall through to Millennial;
- profile switch changes only presentation;
- source/docs mirrors are deterministic.

## 10. Runtime checks

Runtime smoke must cover:
- birth-year -> profile for five bands;
- direct profile set/get for five profiles;
- reload persistence for five profiles;
- profile change-later flow;
- persistent UI signature for each profile;
- representative I0/I1/I2/I3 surfaces;
- unified toast behavior under simultaneous deltas;
- no cross-profile leakage;
- no mechanic/economy divergence under the same scripted scenario.

## 11. Hidden-label human acceptance

Automated PASS is insufficient.

Final Stage 6 user visual acceptance must verify:
- Boomer is unmistakably Boomer;
- Gen X is unmistakably Gen X;
- Millennial is unmistakably Millennial;
- Zoomer is unmistakably Zoomer;
- Alpha is unmistakably Alpha;
- critical actions remain understandable;
- mechanics behave identically.

Stage 6 COMPLETE is forbidden until the user explicitly confirms the visible game result.

## 12. Suggested implementation waves after copy freeze

Wave A - Gen X plumbing:
- registry/routing/dictionaries/tests only;
- no broad copy migration yet.

Wave B - persistent chrome:
- start/chat/DM/panels/common actions;
- five-profile hidden-label quick test.

Wave C - critical + guidance:
- conflict/vote/report/cooldowns/hints;
- placeholder and clarity validation.

Wave D - expressive surfaces:
- toasts/outcomes/empty states/notifications;
- unified toast regression.

Wave E - NPC/flavor:
- reactions/decorative copy;
- diversity and anti-repetition review.

Wave F - full regression:
- five-profile semantic coverage;
- same-scenario mechanic invariance;
- source/docs parity;
- user visual acceptance.

Each wave must be independently scoped and reviewable.
