# STAGE 6 STEP 9 - FREEZE AND IMPLEMENTATION PREP V1

STATUS: `PREP_COMPLETE / FROZEN_PACKAGE_EMITTED / AWAITING_CODEX_PREFLIGHT`

BASELINE_MAIN: `7673f3487b0dd52c0c5cb2c7826f4e3fe5cc570e`

FOUNDATION_BRANCH: `chatgpt/stage6-step9-five-profile-foundation-20260723`
FROZEN_JSON_SHA256: `6aa42c46c4bc594d234894dd92f57fd31292cce31953dab78343b653cfbdd45f`

## 1. What can be completed before user approval

Completed:

- Alpha Russian-first rewrite;
- five-column Russian cultural authenticity pass;
- fresh current-main source-bound inventory;
- source/product/structural gap closure;
- consolidated user-review gateway;
- freeze procedure;
- implementation wave/scope preparation.

Cannot honestly complete before explicit user approval:

- exact frozen copy package;
- frozen JSON SHA-256;
- Codex implementation;
- final Safari visual/runtime acceptance.

## 2. Freeze procedure after approval

After user review is marked approved:

1. resolve all candidate overlays according to `STAGE6_STEP9_USER_REVIEW_PACKAGE_V1.md` precedence;
2. apply every user correction as highest copy authority;
3. emit one exact Markdown mapping:
   - `STAGE6_STEP9_FIVE_PROFILE_COPY_FROZEN.md`
4. emit one exact machine mapping:
   - `stage6_step9_five_profile_copy_frozen.json`
5. validate JSON determinism and placeholder sets;
6. compute SHA-256 over the exact UTF-8 JSON bytes;
7. record:
   - baseline main SHA;
   - foundation branch/head;
   - frozen JSON SHA-256 (`6aa42c46c4bc594d234894dd92f57fd31292cce31953dab78343b653cfbdd45f`);
   - exact implementation semantic IDs;
   - exact intended shared rows;
   - excluded legacy/dev/mechanic rows;
8. update `STAGE6_STEP9_CODEX_IMPLEMENTATION_CONTRACT.md` with the frozen hash and exact scope;
9. only then create/execute Codex implementation lane under repository preflight rules.

## 3. Expected implementation ownership groups

Final exact files depend on approved row mapping, but current-main inventory proves these ownership groups.

### Core profile/routing owner

Likely required:

- `AsyncScene/Web/data.js`
- `docs/data.js`
- `AsyncScene/Web/system.js`
- `docs/system.js`
- `AsyncScene/Web/ui/ui-profile-visual-tone-repair.js`
- `docs/ui/ui-profile-visual-tone-repair.js`

Responsibilities:

- first-class Gen X registry/text mode;
- explicit five-profile dictionaries/routing;
- frozen start/system/economy/toast presentation;
- remove accidental inheritance where not intentionally shared.

### Persistent UI / start owner

Potentially required where current hardcoded or boot-time text bypasses the final presentation maps:

- `AsyncScene/Web/index.html`
- `docs/index.html`
- `AsyncScene/Web/ui/ui-boot.js`
- `docs/ui/ui-boot.js`
- `AsyncScene/Web/ui/ui-core.js`
- `docs/ui/ui-core.js`
- `AsyncScene/Web/ui/ui-chat.js`
- `docs/ui/ui-chat.js`
- `AsyncScene/Web/ui/ui-dm.js`
- `docs/ui/ui-dm.js`
- `AsyncScene/Web/ui/ui-menu.js`
- `docs/ui/ui-menu.js`

Responsibilities:

- exact persistent labels/controls;
- remove wrong-profile persistent fallbacks;
- disabled P2P block hiding;
- accessibility shared-copy preservation.

### Conflict/event owner

Touch only if frozen semantic mapping cannot be applied through shared dictionaries/presentation routing:

- `AsyncScene/Web/conflict/conflict-core.js`
- `docs/conflict/conflict-core.js`
- `AsyncScene/Web/conflict/conflict-api.js`
- `docs/conflict/conflict-api.js`
- `AsyncScene/Web/conflict/conflict-arguments.js`
- `docs/conflict/conflict-arguments.js`
- `AsyncScene/Web/conflict/conflict-economy.js`
- `docs/conflict/conflict-economy.js`
- `AsyncScene/Web/events.js`
- `docs/events.js`
- `AsyncScene/Web/ui/ui-battles.js`
- `docs/ui/ui-battles.js`
- `AsyncScene/Web/ui/ui-events.js`
- `docs/ui/ui-events.js`

Rule:

Prefer central profile routing over spreading copy edits into mechanic files. Do not touch mechanic code merely because a file contains a visible string.

### NPC owner

Likely required:

- `AsyncScene/Web/npcs.js`
- `docs/npcs.js`
- relevant `data.js` cop/profile pools if current routing still owns them.

Responsibilities:

- five explicit profile lanes;
- role semantics unchanged;
- no generation-wide one-token caricature;
- source/deployed mirror reconciliation.

### Validation/smoke owner

Must be serialized due repository policy:

- `AsyncScene/Web/dev/dev-checks.js`
- `docs/dev/dev-checks.js`
- any current smoke registry/export wiring required by the selected implementation wave.

Do not edit shared dev-checks in parallel with overlapping runtime waves.

## 4. Mirror drift handling

Known current-main drift exists in multiple source/docs pairs.

This prep does not authorize whole-file copy-over.

Before implementation on each touched drifted pair:

1. diff exact source vs docs;
2. identify Step 9-owned hunks;
3. preserve unrelated divergence;
4. apply equivalent approved change to both sides;
5. prove expected parity or documented deterministic transformation after the wave.

## 5. Recommended serialized implementation waves

### Wave 1 - five-profile plumbing

- add first-class Gen X;
- establish one explicit five-profile routing model;
- no broad copy migration beyond what is needed to prove plumbing;
- static routing/profile tests.

### Wave 2 - signature/persistent copy

- start/onboarding;
- chat/DM/panels/menu;
- common battle/vote actions;
- disabled P2P hide decision;
- hidden-label quick acceptance matrix.

### Wave 3 - system/economy/feedback

- system routes;
- economy/report/vote feedback;
- stat toasts;
- empty/cooldown/error states;
- preserve PR #258 unified toast behavior.

### Wave 4 - NPC/flavour

- NPC role/profile pools;
- role diversity;
- orphan `npcArrest*` final zero-callsite precheck and cleanup only if proven safe.

### Wave 5 - full coverage/regression

- zero accidental profile fallbacks on approved implementation rows;
- placeholder checks;
- mirror audit;
- same-scenario mechanic/economy invariance;
- five-profile smoke/preview matrix.

### Wave 6 - user runtime acceptance

User-owned Safari verification:

- all five generation selections;
- persistent signature surfaces;
- representative I0/I1/I2/I3;
- unified toast behavior;
- no wrong-profile leakage;
- mechanics unchanged.

Stage 6 remains NOT COMPLETE until this user acceptance passes.

## 6. Stop gate

Current stop reason is singular and intentional:

`AWAITING_CODEX_MODEL_PREFLIGHT_AFTER_FREEZE`

This is not a source ambiguity, missing inventory or product-decision blocker.

NEXT_ACTION: `USER_REVIEW_STEP9_CONSOLIDATED_COPY_PACKAGE`, then generate exact frozen Markdown/JSON/SHA-256 in the same review cycle.
