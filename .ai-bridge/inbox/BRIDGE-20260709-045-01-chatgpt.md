Use @asynchronia.

Invoke the Asynchronia plugin `task-router` first. Then invoke every required Asynchronia skill for this task, including `scope-isolation-check`, `model-selector`, and `parallel-scope-planner`.

# Slot 2 process correction - remove runtime gate and require Asynchronia plugin for every task

BRIDGE_PROTOCOL: 3.2
THREAD_ID: BRIDGE-20260709-045
LANE_ID: PROCESS-MANDATORY-PLUGIN-NO-RUNTIME-GATE
TASK_ID: TASK-PROCESS-MANDATORY-PLUGIN-NO-RUNTIME-GATE
EXECUTION_EPOCH: PLUGIN-E1-20260709-1735JST
PHASE: CORRECTION_REQUIRED
BRIDGE_SLOT: 2
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260709-045-claim-v1-codex.md
REMOTE_MAIN_BASELINE: 5505f19b379cdf9a4559c1e6d5dd8292160e599b
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-045-02-codex.md
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
SAFARI_STATUS: N/A_PROCESS_ONLY
SUPERSEDES_THREAD: BRIDGE-20260709-044
SUPERSEDES_SLOT: 1
MAILBOX_POLICY_VERSION: CODEX_AUTOPILOT_2026_07_09_FULL_OUTBOX_REPLAY

## Authoritative objective

Make the Asynchronia plugin mandatory for every Asynchronia Codex task and fully remove the obsolete runtime gate from source contracts, active installed package behavior, bridge prompts, validators, routing, bootstrap, recovery, examples, and generated task templates.

This task also retains and completes the three-slot bridge, non-blocking model recommendation, full final-response outbox, publish-before-reply, remote byte verification, and automatic recoverable outbox retry requirements already established by the superseded Slot 1 task.

## Slot ownership

This task runs only in Slot 2.

- Slot 1 is superseded and closed.
- Slot 2 is the sole active owner of this shared process/plugin correction.
- Slot 3 remains closed because this task owns shared root policy, workflow, validator, plugin routing, bootstrap, and report contracts.
- Do not create parallel copies of this task.

## Mandatory plugin-first contract

Every Asynchronia Codex task, whether bridge-based or directly supplied, must start with the exact first line:

`Use @asynchronia.`

After activation, the plugin route is mandatory:

1. `task-router` for every task;
2. `scope-isolation-check` for every implementation or repository-write lane;
3. `model-selector` for every implementation or repository-write lane;
4. `parallel-scope-planner` whenever more than one task, lane, slot, concurrent writer, shared dependency, mirror group, registry, generated output, or documentation owner exists;
5. every specialized Asynchronia skill required by `task-router` for the exact task.

Plugin invocation is not optional guidance. If the Asynchronia plugin cannot be resolved, loaded, or invoked, Codex must stop before implementation with `BLOCKED_PLUGIN_UNAVAILABLE` and provide exact diagnostics. It must not silently fall back to an ungoverned generic workflow.

The final response and outbox must include:

- resolved plugin version and source;
- exact skills invoked in order;
- material result from each skill;
- proof that `task-router` selected the final route;
- proof that required skills were not skipped;
- `USER_SELECTED_UNVERIFIED` for the actual interface model unless externally verified.

A generic sentence such as “plugin used” is not evidence.

## Full removal of runtime gate

The obsolete `runtime-safety-gate` is not part of the active plugin architecture.

Required removal:

- delete `plugins/asynchronia/skills/runtime-safety-gate/SKILL.md` and its source directory if they exist;
- remove it from plugin metadata, routing, examples, bootstrap text, recovery text, task templates, validators, workflow coverage, documentation, installed-package manifests, generated indexes, and active user-level bridge instructions;
- remove the old mandatory prompt `Use @asynchronia runtime-safety-gate.` everywhere in active process surfaces;
- reject reintroduction of `runtime-safety-gate`, `RUNTIME_SAFETY_GATE_REQUIRED`, runtime approval tokens, runtime authorization stops, approval-only stops, or equivalent aliases;
- replace the old gate with active `scope-isolation-check`, exact write ownership, mirror ownership, stable-read dependency checks, shared wiring ownership, and real collision serialization;
- preserve user-owned Safari acceptance without turning it into a pre-implementation gate.

Historical audit artifacts may retain old wording only when clearly marked historical and excluded from active routing. The validator must distinguish historical evidence from active policy and fail closed on active references.

## Installed package requirement

Repository source cleanup alone is insufficient.

After source validation and primary publication, Codex must:

1. build or stage Asynchronia plugin version `1.0.4` from the committed source;
2. install or refresh the active user-level Asynchronia plugin package;
3. verify the active installed package resolves to version `1.0.4`;
4. prove the installed package contains no `runtime-safety-gate` skill, alias, generated index entry, or callable route;
5. run an installed-package smoke that invokes `Use @asynchronia.`, routes through `task-router`, and demonstrates mandatory failure when plugin activation is intentionally unavailable;
6. report installed package path and verification evidence without exposing private credentials.

Historical cache directories may remain as inert audit history only if the active loader cannot select them and validation proves version `1.0.4` is the active package. If stale cache selection is possible, remove or quarantine the stale selectable package safely.

## Three-slot and parallel contract

The root process must permanently expose:

- `мост 1` for Slot 1;
- `мост 2` for Slot 2;
- `мост 3` for Slot 3.

Each open slot has independent thread, task, epoch, inbox, claim, baseline, exact scope, worktree, and outbox.

When multiple tasks or lanes exist, `parallel-scope-planner` assigns them to safe slots. Shared process files, shared docs, mirror pairs, aggregate smoke, registries, exports, globals, boot wiring, generated outputs, and stable-read dependencies require serialization.

A closed slot must be reported as intentionally closed with its collision or dependency reason.

## Model recommendation contract

Only `model-selector` may originate, rank, or name a model and reasoning recommendation.

The user alone selects the actual interface model. The active model remains `USER_SELECTED_UNVERIFIED` unless externally verified.

Recommendation is informational and must not pause, block, authorize, or resume execution. Remove `MODEL_PREFLIGHT_ONLY`, `WAITING_FOR_MODEL_SELECTION`, mandatory `CONTINUE`, model-preflight pauses, valid-preflight resume dependencies, and synonymous hidden stops.

A material scope change recomputes the recommendation in the same response.

## Full outbox contract

The exact expected outbox must contain the complete final response shown to the user. The outbox and user-visible response must be byte-for-byte identical.

Empty, whitespace-only, partial, summary-only, pointer-only, placeholder, receipt-only, and one-line handoff outboxes are forbidden.

Codex must:

1. assemble the complete final response before mailbox publication;
2. validate every required report field;
3. publish the exact response bytes to the expected outbox from a clean current mailbox worktree;
4. push fast-forward;
5. refetch the remote mailbox branch and exact outbox;
6. prove byte-for-byte equality;
7. only then show the same response to the user and instruct them to return to ChatGPT with `мост 2`.

Every recoverable outbox failure must retry automatically. No success or ChatGPT handoff is allowed before remote verification. A non-recoverable external authentication, permission, or service outage returns `BLOCKED_OUTBOX_PUBLICATION` and forbids a ChatGPT handoff.

## Exact primary write scope

Only these primary paths may change or be deleted:

- `AGENTS.md`
- `AGENTS.override.md`
- `PROCESS_ROOT_SYNC.md`
- `ORCHESTRATION.md`
- `BRIDGE.md`
- `CODEX_BRIDGE_BOOTSTRAP.md`
- `CODEX_BRIDGE_RECOVERY.md`
- `GIT_PUSH.md`
- `STAGE6_PARALLEL_EXECUTION_PLAN.md`
- `.github/workflows/orchestration-policy.yml`
- `tools/validate-orchestration-policy.py`
- `plugins/asynchronia/.codex-plugin/plugin.json`
- `plugins/asynchronia/skills/task-router/SKILL.md`
- `plugins/asynchronia/skills/scope-isolation-check/SKILL.md`
- `plugins/asynchronia/skills/parallel-scope-planner/SKILL.md`
- `plugins/asynchronia/skills/model-selector/SKILL.md`
- `plugins/asynchronia/skills/acceptance-pipeline-controller/SKILL.md`
- `plugins/asynchronia/skills/pipeline-state-and-resume-contract/SKILL.md`
- `plugins/asynchronia/skills/evidence-bundle-and-artifact-identity/SKILL.md`
- `plugins/asynchronia/skills/failure-routing-and-corrective-loop/SKILL.md`
- `plugins/asynchronia/skills/runtime-safety-gate/SKILL.md` if present, deletion only

All other primary paths are protected.

User-level Codex instruction and active plugin installation paths may change only as required for the managed bridge block and active Asynchronia package installation. Preserve unrelated user-level content byte-for-byte and create timestamped backups before modification.

## Validator requirements

`tools/validate-orchestration-policy.py` must dynamically discover every current `plugins/asynchronia/skills/*/SKILL.md` and fail closed unless all discovered skills are scanned, frontmatter-validated, and covered by workflow triggers.

It must also:

- require plugin version `1.0.4`;
- require exact first-line `Use @asynchronia.` in active task templates and current bridge inbox contract examples;
- require mandatory `task-router`, `scope-isolation-check`, `model-selector`, and conditional `parallel-scope-planner` routing;
- reject any source or active installed-package occurrence of `runtime-safety-gate`;
- reject the old prompt `Use @asynchronia runtime-safety-gate.`;
- reject hidden runtime approval, model preflight, mandatory `CONTINUE`, and approval-only variants;
- require all three bridge commands and slot-specific ownership;
- require complete report fields, non-empty outbox, publish-before-reply, remote refetch, byte equality, and retry semantics;
- require `BLOCKED_PLUGIN_UNAVAILABLE` before implementation when plugin activation cannot be proven;
- require `BLOCKED_OUTBOX_PUBLICATION` with no ChatGPT handoff for non-recoverable mailbox publication failure;
- print exact discovered and scanned paths.

Add negative controls proving failure for:

- missing `Use @asynchronia.` first line;
- plugin activation skipped;
- `task-router` skipped;
- required skill skipped;
- runtime-safety-gate source restored;
- runtime-safety-gate installed-package entry restored;
- old runtime-gate prompt restored;
- empty or summary-only outbox;
- missing byte equality;
- success before remote refetch;
- missing retry rule;
- missing bridge 1, 2, or 3;
- reintroduced model preflight or `CONTINUE` stop.

## Required validation

Run and report:

- `python3 tools/validate-orchestration-policy.py`;
- all validator negative controls;
- comprehensive active-policy semantic search;
- dynamic skill discovery versus scanned-path equality;
- workflow trigger coverage equality;
- plugin JSON validation for `1.0.4`;
- every changed-skill frontmatter/name validation;
- proof that runtime-safety-gate source is absent;
- active installed-package version and skill inventory proof;
- installed-package plugin-first smoke;
- plugin-unavailable negative smoke;
- exact authorized changed-scope verification;
- `git diff --check`;
- proof that product/runtime files are unchanged.

## Execution and publication

On the first exact `мост 2` in a fresh Codex conversation:

1. fetch current main and mailbox;
2. verify Slot 2, this thread, epoch, inbox, claim, baseline, scope, and expected outbox;
3. activate `@asynchronia` and invoke `task-router` first;
4. invoke `scope-isolation-check`, `model-selector`, `parallel-scope-planner`, and every additional routed skill;
5. execute immediately if this serialized process lane is collision-free;
6. modify only the authorized primary paths and required user-level managed plugin/install surfaces;
7. run all validation and negative controls;
8. publish one direct-child primary commit to main;
9. install and verify active Asynchronia plugin version `1.0.4` without runtime-safety-gate;
10. assemble the complete final response;
11. publish those exact bytes only to `.ai-bridge/outbox/BRIDGE-20260709-045-02-codex.md`;
12. retry every recoverable mailbox failure automatically;
13. refetch and byte-compare the remote outbox;
14. only after equality is proven, return the same complete response with exact next action `Return to ChatGPT and send мост 2`.

Do not modify Step 4.4B product/runtime files. Do not run Safari. Do not stop for model selection, `CONTINUE`, runtime approval, or `APPROVE`.
