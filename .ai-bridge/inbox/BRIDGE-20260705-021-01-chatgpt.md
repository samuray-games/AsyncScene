BRIDGE_PROTOCOL: 1.0
THREAD_ID: BRIDGE-20260705-021
TURN: 01
FROM: CHATGPT
TO: CODEX
TYPE: REQUEST
THREAD_STATUS: WAITING_CODEX
CREATED_AT: 2026-07-05T15:45:00+09:00
REPLY_TO: N/A
PRIMARY_REPO_REF: main
PRIMARY_REPO_COMMIT: f0d2825dfc403ec40c8b93381d6858862a6ae1bd
MAILBOX_PARENT_COMMIT: c1d248c3bb627cee1267d832a288d1ba245115fc
TASK_ID: MAILBOX-BRANCH-GUARD-V1-FIX1
MODE: MODEL_PREFLIGHT_ONLY

Use @asynchronia runtime-safety-gate.

## Summary

Repair the impossible self-hash requirement introduced by `MAILBOX-BRANCH-GUARD-V1` while preserving every effective fail-closed branch, ancestry, path-scope and explicit-push safeguard.

This is documentation/policy only. Stage 6 remains paused until this Fix1 is accepted.

## Mandatory plugin preflight

Return `MODEL_PREFLIGHT_ONLY` only. Do not edit files or publish an outbox before valid same-thread `CONTINUE`.

Model selection belongs exclusively to `model-selector`. The coordinator supplies no preferred model family, reasoning level, starting pair or anchor.

Return the complete current contracts for:

1. `task-router`, including `status: TASK_ROUTE`, classification, flags, objective, exact read/write scope, audits, audit order, required skills, execution mode, gates, serialization, prerequisites, blockers, assumptions, confidence and exact next action.
2. `model-selector`, including `status: MODEL_RECOMMENDATION`, objective `MINIMIZE_EXPECTED_USER_TOKEN_COST`, evaluated pair count `12/12`, full pair matrix, frontier, sufficient and rejected candidates, cheapest plausible and cheapest rejected candidates, concrete failure mode, retry risk, bottlenecks, overprovisioning check, confidence and `actual active model: USER_SELECTED_UNVERIFIED`.

Report source manifest version, marketplace version, installed-plugin status and exact current-thread resolver/load evidence. Missing evidence returns `BLOCKED_PLUGIN_NOT_LOADED` with no `CONTINUE` block.

## Verified current state

- Current main: `f0d2825dfc403ec40c8b93381d6858862a6ae1bd`.
- Current accepted guard changed exactly `AGENTS.md` and `BRIDGE.md`.
- Thread 020 outbox reached mailbox in commit `1c128e0d8870acaa7e981f7d553c4b84842d2521` as a direct child of `47503ef11c673e1ced0560cecba0cebfe2f3ac3c` and changed exactly `.ai-bridge/outbox/BRIDGE-20260705-020-02-codex.md`.
- Coordinator closure decision: `BRIDGE-20260705-020-03-chatgpt.md`, commit `c1d248c3bb627cee1267d832a288d1ba245115fc`.
- User reported a deployment failure, but the mailbox report arrived and no served runtime files changed. Deployment is not an acceptance dependency for this policy-only repair.

## Proven defect

The current policy requires a committed outbox report to contain its own final commit SHA and the final remote mailbox head SHA equal to that commit.

That is a cryptographic fixed-point requirement and is not operationally satisfiable: changing the report to insert the commit SHA changes the tree and therefore changes the commit SHA.

## Authorized primary files after CONTINUE

Edit exactly:

- `AGENTS.md`
- `BRIDGE.md`

No other primary file may change.

## Required corrected contract

Preserve all existing guard requirements except the impossible same-commit self-reference. The corrected policy must require:

1. Immediately before preparing a mailbox write, fetch `origin/coordination/chatgpt-codex-bridge` and record the fetched head as `MAILBOX_PARENT_COMMIT`.
2. Use an isolated checkout whose checked-out branch is exactly `coordination/chatgpt-codex-bridge`.
3. Before editing, prove current branch equals the mailbox branch and `HEAD == MAILBOX_PARENT_COMMIT`.
4. Never create a mailbox commit from main, a primary baseline, detached primary lineage or any non-mailbox branch.
5. The mailbox commit must be a direct child of the recorded mailbox parent.
6. Before push, prove the diff contains only the exact mailbox path authorized by the active inbox.
7. Push explicitly to `refs/heads/coordination/chatgpt-codex-bridge`, never an implicit upstream.
8. Refetch after push and locally prove remote mailbox head equals the newly created mailbox commit.
9. When primary write scope is `NONE`, prove `origin/main` still equals the authorized primary baseline. When primary writes are authorized, prove main equals the exact accepted primary commit and contains only the authorized primary paths.
10. Any local mismatch returns exactly `BLOCKED_MAILBOX_BRANCH_GUARD`; no success claim, no fallback to main and no guessed repair.
11. If a mailbox file is written to main, return exactly `FAIL_MAILBOX_WRITTEN_TO_MAIN`.
12. The committed outbox must include all non-self-referential evidence available before its commit:
    - mailbox parent SHA;
    - exact authorized mailbox path;
    - checked-out mailbox branch identity;
    - current main SHA before mailbox publication;
    - exact primary commit and authorized primary path list when applicable;
    - explicit statement that the mailbox commit is not based on main;
    - statement that post-push remote-head verification is mandatory.
13. The committed outbox must not be required to contain its own final commit SHA or its own final remote-head SHA.
14. After publication, ChatGPT must independently:
    - resolve the remote mailbox branch head;
    - verify it is exactly one direct child of the reported mailbox parent;
    - verify the commit changes only the authorized mailbox path;
    - verify current main state against the task contract;
    - record the exact mailbox commit/head SHA in the immutable coordinator closure decision.
15. A thread cannot close PASS until the independent coordinator verification in item 14 succeeds.

## One-time publication waiver for this Fix1

Because the current policy defect is the subject of this repair, the `BRIDGE-20260705-021-02-codex.md` outbox is explicitly not required to self-embed its own commit SHA or final remote-head SHA.

It must include all non-self-referential evidence listed above. ChatGPT will independently resolve and record the final mailbox commit/head in the closure decision.

## Validation after CONTINUE

- Show exact diff for `AGENTS.md` and `BRIDGE.md`.
- Prove no other primary path changed.
- Prove all effective branch/ancestry/path/explicit-push safeguards remain present.
- Prove the self-hash requirement is removed from both files.
- Include a written negative test showing why a same-commit self-SHA field is impossible and how coordinator post-push verification closes the evidence loop.
- Verify action-token formatting rules remain intact.
- Runtime gate: `N/A - documentation/policy only`.
- Safari smoke: `N/A`.
- Deployment: `N/A - no served runtime artifact change`.

## Write and publication sequence

After valid same-thread `CONTINUE`:

1. Commit the policy correction on `main`, changing exactly `AGENTS.md` and `BRIDGE.md`.
2. Verify the exact primary commit and two-file diff.
3. Publish the final report only from an isolated mailbox worktree satisfying the corrected guard.
4. The only mailbox path allowed is `.ai-bridge/outbox/BRIDGE-20260705-021-02-codex.md`.
5. The outbox commit must be a direct child of the then-current remote mailbox head and must not descend from the primary policy commit.

## Required final report

Include:

- status;
- plugin-derived model and reasoning;
- plugin availability evidence;
- exact primary parent and primary commit;
- exact primary changed-path list and diff summary;
- mailbox parent SHA;
- exact authorized mailbox path;
- checked-out mailbox branch identity;
- main SHA before mailbox publication;
- explicit non-main-lineage statement;
- statement that post-push remote-head verification was performed locally;
- negative-test result for self-hash impossibility;
- failures and missing coverage;
- runtime, Safari and deployment status;
- exact next user action.

Do not include or guess the outbox commit's own SHA inside the outbox.
