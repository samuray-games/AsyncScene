BRIDGE_PROTOCOL: 1.0
THREAD_ID: BRIDGE-20260705-022
TURN: 01
FROM: CHATGPT
TO: CODEX
TYPE: REQUEST
THREAD_STATUS: WAITING_CODEX
CREATED_AT: 2026-07-07T12:46:00+09:00
REPLY_TO: N/A
PRIMARY_REPO_REF: main
PRIMARY_REPO_COMMIT: 4f26c72f9d1c49784a10b635007cc4c8f18767c6
MAILBOX_PARENT_COMMIT: 4ef79befc2d96df7a9ee0f1e1d8c11778ad2329d
TASK_ID: MAILBOX-BRANCH-GUARD-V1-FIX2
MODE: MODEL_PREFLIGHT_ONLY

Use @asynchronia runtime-safety-gate.

## Goal

Remove the remaining impossible same-outbox final-SHA requirement while preserving every effective fail-closed mailbox branch, ancestry, path-scope, explicit-push, refetch and main-state safeguard.

This is documentation/policy only. It does not modify or reopen the independently accepted S6-010A4 product result.

## Mandatory preflight

Return `MODEL_PREFLIGHT_ONLY` only. Do not edit, commit, push or publish an outbox before valid same-thread `CONTINUE`.

Model selection belongs exclusively to `model-selector`. The coordinator provides no model or reasoning anchor. Report the complete current task-router and model-selector contracts, full `12/12` model/reasoning matrix, current-thread plugin resolver/load evidence, and `actual active model: USER_SELECTED_UNVERIFIED`.

Missing plugin evidence returns `BLOCKED_PLUGIN_NOT_LOADED` with no `CONTINUE` block.

## Current authoritative state

- Current main: `4f26c72f9d1c49784a10b635007cc4c8f18767c6`.
- Parent policy commit: `f0d2825dfc403ec40c8b93381d6858862a6ae1bd`.
- Current main differs from that parent only in `AGENTS.md` and `BRIDGE.md`.
- Thread 021 is closed `FAIL_INCOMPLETE_SELF_HASH_FIX_NO_OUTBOX`.
- No thread 021 outbox exists.
- PR #187 is open and unmerged and changes only `TASKS.md` and `PROJECT_MEMORY.md`; do not touch, merge, rebase or absorb it.

## Proven remaining defect

Current policy still says that the committed mailbox final report must include:

- `the mailbox commit SHA if one was created`; and
- `the verified remote mailbox head SHA after push`.

For an outbox that is itself committed as the mailbox commit, these are facts created only after the outbox content is frozen. Requiring them inside that same outbox is circular and impossible.

## Authorized primary files after CONTINUE

Edit exactly:

- `AGENTS.md`
- `BRIDGE.md`

No other primary file may change.

## Exact corrected contract

Preserve the existing branch guard, but replace the report-evidence section so it states all of the following unambiguously:

1. Before preparing a mailbox write, fetch the remote mailbox branch and record the fetched head as `MAILBOX_PARENT_COMMIT`.
2. Use an isolated checkout on exactly `coordination/chatgpt-codex-bridge` with `HEAD == MAILBOX_PARENT_COMMIT` before editing.
3. The mailbox commit must be a direct child of that parent and may change only the exact authorized outbox path.
4. Push explicitly to `refs/heads/coordination/chatgpt-codex-bridge`.
5. Refetch after push and locally verify that the remote mailbox head equals the newly created mailbox commit.
6. Verify the task's required main-state invariant after mailbox publication.
7. Any mismatch returns exactly `BLOCKED_MAILBOX_BRANCH_GUARD`; no success claim or fallback.
8. If a mailbox file is written to main, return exactly `FAIL_MAILBOX_WRITTEN_TO_MAIN`.
9. The committed outbox must contain only evidence knowable before its own commit:
   - mailbox parent SHA;
   - exact authorized outbox path;
   - checked-out mailbox branch identity;
   - exact primary commit and authorized primary path list when applicable;
   - current main SHA immediately before mailbox publication;
   - explicit statement that the mailbox commit is not based on main;
   - statement that local post-push verification and independent coordinator verification are mandatory.
10. The committed outbox must NOT be required to contain:
   - its own final mailbox commit SHA;
   - its own final remote mailbox head SHA;
   - current main SHA after mailbox publication.
11. After publication, ChatGPT independently resolves the remote mailbox head, verifies direct-child ancestry, exact path scope and main state, then records:
   - exact mailbox commit/head SHA;
   - current main SHA after publication;
   - final PASS or FAIL
   in the immutable coordinator closure decision.
12. No thread closes PASS before that coordinator verification succeeds.

## Validation

After CONTINUE:

- show exact diff for `AGENTS.md` and `BRIDGE.md`;
- prove no other primary path changed;
- prove all branch, ancestry, path, explicit-push, refetch and fail-closed safeguards remain present;
- prove all requirements for the outbox to contain its own final commit/head or post-publication main SHA are removed;
- run `git diff --check` and focused text searches;
- runtime gate: `N/A - documentation/policy only`;
- Safari smoke: `N/A - documentation/policy only`;
- deployment: `N/A - no served runtime artifact change`.

## Required publication sequence

1. Commit the corrected policy on `main`, changing exactly `AGENTS.md` and `BRIDGE.md`.
2. Verify exact parent, commit and two-file diff.
3. Publish `.ai-bridge/outbox/BRIDGE-20260705-022-02-codex.md` only from an isolated mailbox checkout based on the then-current fetched mailbox head.
4. Do not guess or self-embed the outbox commit SHA.
5. Perform local post-push verification, but leave exact final mailbox commit/head and post-publication main SHA for independent ChatGPT resolution and closure.

## Final report content

The committed outbox must include:

- status;
- plugin-derived model and reasoning;
- plugin availability evidence;
- exact primary parent and primary commit;
- exact primary changed paths and diff summary;
- mailbox parent SHA;
- exact authorized outbox path;
- checked-out mailbox branch identity;
- main SHA immediately before mailbox publication;
- explicit non-main-lineage statement;
- statement that local post-push verification is mandatory and was performed outside the immutable outbox content lifecycle;
- failures and missing coverage;
- runtime, Safari and deployment status;
- exact next user action.

It must not include or claim to know its own final mailbox commit SHA, final remote head SHA, or post-publication main SHA.
