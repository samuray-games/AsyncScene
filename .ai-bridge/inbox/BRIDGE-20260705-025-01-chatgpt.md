BRIDGE_PROTOCOL: 1.0
THREAD_ID: BRIDGE-20260705-025
TURN: 01
FROM: CHATGPT
TO: CODEX
TYPE: REQUEST
THREAD_STATUS: WAITING_CODEX
CREATED_AT: 2026-07-07T13:27:00+09:00
REPLY_TO: N/A
PRIMARY_REPO_REF: main
PRIMARY_REPO_COMMIT: c3e2bcccf982934c46d1ac12a3250576ad89e647
MAILBOX_PARENT_COMMIT: 6306b6b1192714a89795d63cf7c70da52e1ddad8
TASK_ID: MAILBOX-BRANCH-GUARD-V1-FIX3-PUBLICATION-SYNC
MODE: MODEL_PREFLIGHT_ONLY
USER_COMMAND_ALIAS: мост

Use @asynchronia runtime-safety-gate.

## Goal

Complete the remaining mailbox outbox evidence contract and keep Codex edit completion separate from later user publication.

The exact bridge command alias is now `мост` for both Codex and ChatGPT. The old phrase `проверь мост` is superseded and must not be presented as an active command.

This is documentation/policy only. Do not touch product branches, PR #187, runtime, served docs, TASKS.md, or PROJECT_MEMORY.md.

## Mandatory preflight

Return `MODEL_PREFLIGHT_ONLY` only. Do not edit, stage, commit, push, or publish an outbox before a valid same-thread `CONTINUE`.

Model selection belongs exclusively to `model-selector`. Report the complete task-router and model-selector contracts, full 12/12 model/reasoning matrix, current-thread plugin resolver/load evidence, and `actual active model: USER_SELECTED_UNVERIFIED`.

## Authorized primary files after CONTINUE

Edit exactly:

- `AGENTS.md`
- `BRIDGE.md`

The alias in both files must be exactly `мост`.

## Remaining policy correction

Preserve every existing mailbox branch, ancestry, path-scope, explicit-push, refetch and fail-closed safeguard. Restore all required pre-commit outbox evidence and exclude facts knowable only after the immutable outbox commit exists.

## Publication synchronization

Reports must distinguish Codex local edit completion from later user publication. `commit: N/A` is valid before user publication and must not later be described as an unreported Codex commit.

## Final report and publication

After same-thread `CONTINUE`, validate exact scope and publish `.ai-bridge/outbox/BRIDGE-20260705-025-02-codex.md` only through the mailbox branch guard. Runtime, Safari and deployment are N/A because this is policy only.
