Use @asynchronia.

# Implement the closed-loop source contract

BRIDGE_PROTOCOL: 3.3
CYCLE_ID: CYCLE-20260709-001
CYCLE_GENERATION: 7
THREAD_ID: BRIDGE-20260709-052
LANE_ID: PROCESS-CLOSED-LOOP-SOURCE-CONTRACT-CORRECTION
TASK_ID: TASK-PROCESS-CLOSED-LOOP-SOURCE-CONTRACT-CORRECTION
EXECUTION_EPOCH: CLOSED-LOOP-SOURCE-R1-20260709-2138JST
TASK_NONCE: CLV1-052-SOURCE-9B17-2138
BRIDGE_SLOT: 3
PHASE: CORRECTION_REQUIRED
BASELINE: 9b170097e1ff0889ae0cb1e127516c51440c4c3d
COORDINATOR_MEMORY_REV: 2026-07-09-2138-JST
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260709-052-claim-v1-codex.md
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-052-02-codex.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
NO_SOURCE_DELTA_POLICY: .ai-bridge/NO_SOURCE_DELTA_POLICY_V1.md
FRESH_CODEX_CONVERSATION_REQUIRED: true
SAFARI_STATUS: N/A_PROCESS_ONLY
SUPERSEDES_THREAD: BRIDGE-20260709-051

## Verifier verdict for thread 051

`CORRECTION_REQUIRED_FALSE_NO_DELTA`.

- Claim 051 required a primary write and forbade verified no-delta.
- Outbox 051 returned illegal `BLOCKED_NO_SOURCE_DELTA`.
- Remote main remained at the baseline.
- The existing validator passing does not satisfy the requested source expansion.
- Outbox 051 also omitted required generation, nonce, coordinator memory, primary commit/parent, exact paths, skill route, tests, negative controls, repairs, retries, missing coverage, remote outbox identity and byte equality.
- Thread 051 must not continue.

## Atomic objective

This task is source phase 1 only. Do not install the user-level plugin in this epoch. Implement and publish the complete repository source contract so a later fresh task can perform installed-package proof and canary work.

## Exact required source changes

### `tools/closed_loop_contract.py`

Implement executable constants and functions for:

- exactly the 12 legal states: CLOSED, PREPARING, READY_FOR_CODEX, EXECUTING, PRIMARY_PUBLISHED, OUTBOX_PUBLISHING, AWAITING_CHATGPT_VERIFICATION, ACCEPTED, CORRECTION_REQUIRED, RECOVERY_REQUIRED, BLOCKED_EXTERNAL, SUPERSEDED;
- an explicit legal transition table;
- required identity fields including cycle, generation, thread, lane, task, epoch, nonce, slot, coordinator memory, baseline, inbox, claim and expected outbox;
- required outbox report fields from the active publication policy;
- phase-aware outbox rules: absent at fresh startup passes, required after terminal publication;
- forbidden outcomes including BLOCKED_NO_REMOTE_OUTBOX and BLOCKED_NO_SOURCE_DELTA;
- identity validation;
- transition validation;
- report-schema validation;
- recovery classification for correction, report recovery, publication recovery and blocked external;
- implementation acceptance gating a separate canary;
- product work blocked until canary acceptance;
- named positive-control inventory;
- at least 50 named negative controls;
- one deterministic self-check callable by the policy validator.

### `tools/test_closed_loop_contract.py`

Replace the three trivial tests with deterministic tests covering every legal transition, illegal transitions, identity mismatch, memory pending, orphan artifacts, missing inbox/claim, startup outbox absence, foreign/pre-existing outbox, premature schema/byte checks, forbidden no-source/no-outbox verdicts, recovery selection, canary gate and all named positive/negative controls.

Use generic fixtures or thread 052 identity. No stale thread 048 fixture.

### `tools/validate-orchestration-policy.py`

Import the shared contract and execute its self-check. Verify dynamically:

- exact legal states and transition table;
- report schema;
- positive and negative control inventories;
- task-router bridge route;
- source runtime-gate absence;
- plugin version 1.0.5;
- workflow commands and path triggers.

A marker-string-only implementation fails.

### `plugins/asynchronia/skills/task-router/SKILL.md`

Every numbered bridge task must route in order through task-router, scope-isolation-check, model-selector, conditional parallel-scope-planner, closed-loop-controller, failure-routing-and-corrective-loop, then routed skills.

### `plugins/asynchronia/skills/closed-loop-controller/SKILL.md`

Replace the descriptive shell with the executable contract requirements: full identity, legal states/transitions, memory gate, outbox phase rules, recovery selection, fresh-chat rotation, canary gate and forbidden no-delta escape.

### `CLOSED_LOOP_PROTOCOL.md`

Document the same coordinator, Codex and verifier transactions and the same legal machine without contradicting the shared Python contract.

### `.github/workflows/orchestration-policy.yml`

On push and pull request, trigger on both new Python files and run:

- `python3 -m py_compile tools/closed_loop_contract.py tools/test_closed_loop_contract.py tools/validate-orchestration-policy.py`
- `python3 tools/validate-orchestration-policy.py`
- `PYTHONPATH=. python3 -m unittest tools.test_closed_loop_contract`

## Frozen primary scope

Only:

- `CLOSED_LOOP_PROTOCOL.md`
- `tools/closed_loop_contract.py`
- `tools/test_closed_loop_contract.py`
- `tools/validate-orchestration-policy.py`
- `.github/workflows/orchestration-policy.yml`
- `plugins/asynchronia/skills/task-router/SKILL.md`
- `plugins/asynchronia/skills/closed-loop-controller/SKILL.md`
- `PROJECT_MEMORY.md`
- `TASKS.md`

All product/runtime files and every other path are protected.

## Required validation

- exact changed paths within scope;
- direct-child commit from baseline;
- Python compile;
- policy validator;
- full unit suite;
- `git diff --check`;
- count and names of positive/negative controls;
- protected-path equality;
- clean task-owned worktree.

## Publication

Publish one direct-child source commit. Then publish a complete immutable report to `.ai-bridge/outbox/BRIDGE-20260709-052-02-codex.md`, refetch it and prove byte equality.

`BLOCKED_NO_SOURCE_DELTA`, `PASS_VERIFIED_NO_DELTA`, and `NO_DELTA_REQUIRED` are illegal. If validation fails inside scope, repair it in this chat, up to three passes per root cause.

Exact next instruction: open a fresh ChatGPT conversation and send `мост 3`.

Do not run Safari. Acceptance of this source phase must create a separate installed-plugin/evidence task, not the final canary yet.
