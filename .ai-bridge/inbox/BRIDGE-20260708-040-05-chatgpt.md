Use @asynchronia runtime-safety-gate.

# Bridge Slot 1 - Wave V-A3 truthful derivation correction after root sync

BRIDGE_PROTOCOL: 3.1
ORCHESTRATION_VERSION: 3.1
ROOT_CAUSE_SYNC: REQUIRED
ROOT_PROCESS_SYNC_STATUS: COMPLETE
BRIDGE_SLOT: 1
THREAD_ID: BRIDGE-20260708-040
LANE_ID: S6-V5A3-BOOMER-STATIC-AUDIT
TASK_ID: TASK-S6-PAR-V5A3
PHASE: CORRECTION_REQUIRED
PUBLICATION_MODE: CODEX_AUTO_PULL_PUSH
REMOTE_MAIN_BASELINE: 22f8b51150cd02f81c519fe822b6d6423d073062
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260708-040-claim-v2-codex.md
REJECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260708-040-02-codex.md
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260708-040-04-codex.md
RECOMMENDED_MODEL: GPT-5.4-Mini / Medium
CONFIRMATION: MATCHING_NUMBERED_BRIDGE_COMMAND
RUNTIME_CLASSIFICATION: STATIC_AUDIT_ONLY
SAFARI_RUNTIME_STATUS: PENDING_USER

## Root-process hardening already complete

Main now contains synchronized root authority:

- `PROCESS_ROOT_SYNC.md`;
- `AGENTS.override.md`;
- `ORCHESTRATION.md`;
- `BRIDGE.md`;
- `GIT_PULL.md`;
- `GIT_PUSH.md`;
- `tools/validate-orchestration-policy.py`.

Mailbox publication policy is also synchronized.

Do not modify any process file in this task. The process-only commits are accepted baseline history.

## Coordinator verdict

The previous result remains rejected for two defects:

1. The old outbox reported nonexistent primary SHA `57dae3fad0f7b1be8ef14e0aa66e90d6d0bb3e8b` instead of the actual historical primary SHA `57dae3f7942ba60996604c39115dad0cb4fa2238`.
2. The generator still embedded observed `currentText` for live rows that must be derived independently from current production registries and resolvers.

The new outbox must derive the final primary SHA and parent from freshly fetched remote refs. Manual SHA transcription is forbidden.

## Exact write scope

Only:

1. `tools/generate-boomer-step4-4-economy-conflict-audit.py`
2. `UI_PROFILE_BOOMER_STEP_4_4_ECONOMY_CONFLICT_TERMINOLOGY_AUDIT.md`
3. `docs/UI_PROFILE_BOOMER_STEP_4_4_ECONOMY_CONFLICT_TERMINOLOGY_AUDIT.md`

Any other changed primary path is automatic FAIL.

All production/runtime files, process files, validators, TASKS.md, PROJECT_MEMORY.md and mailbox STATE are protected.

## Required truthful derivation correction

Preserve two independent values for every live row:

- `currentText` from current production/profile source;
- `acceptedBoomerTarget` from frozen authority.

The accepted-target overlay may affect only `acceptedBoomerTarget` and authority. It must never supply `currentText`.

Correct these proven derivation defects:

### A. Battle fallback rows

For `AUD_0153`, `AUD_0154`, and `AUD_0155`, derive current Boomer text by semantic key directly from `BATTLE_FALLBACK_TEXTS.boomer`.

Do not embed these observed values in row specifications:

- `Решает голосование`
- `Выйти`
- `Вызов отклонён`

### B. Report submit states

For `AUD_0145` and `AUD_0146`, derive current values from parsed current `DM_REPORT_SUBMIT_LABELS`.

Do not embed `Проверка...` or `Повторите позже` as observed current values.

### C. Escape payment surface

For `AUD_0125`, derive current text through the current System/profile route for `notifications.escapePaid` or the exact production resolver that owns the surface.

Do not embed `Выйти за 1 💰.` as observed current text.

### D. Data.SYS result templates

For `AUD_0132` and `AUD_0133`, extract or structurally prove current `Data.SYS.npcBattleEndWin` and `Data.SYS.npcBattleEndDraw` templates from current source.

Do not use accepted targets as current values. If extraction cannot be proven, record a structural failure.

### E. Preserve truthful current routes

Keep current extraction from:

- `SYSTEM_PROFILE_TEXT_COPY.boomer` plus `SYSTEM_PROFILE_TEXT_ROUTE_MAP`;
- `DM_ACTION_LABEL_BOOMER`;
- `DM_REPORT_SURFACE_COPY.boomer`;
- `TOPBAR_STAT_TITLES.boomer`;
- `BATTLE_FALLBACK_TEXTS.boomer`;
- current Data/System dictionaries and resolvers.

Keep `make_row` authoritative. Do not hardcode PASS, counts, status or empty failure arrays.

## Frozen accepted targets

The exact `stage6_accepted_decision:<AUD_ID>` overlay from `.ai-bridge/inbox/BRIDGE-20260708-040-01-chatgpt.md` remains unchanged. Do not revise copy.

## Required derived result

Only if actual derivation supports it:

- `auditStatus: STATIC_PASS / READY_FOR_RUNTIME_SMOKE`
- `auditedRowCount: 147`
- `passRowCount: 147`
- `failRowCount: 0`
- `structuralFailureCount: 0`
- `nonLiveEvidenceCount: 8`
- all live classifications `allowed_exact`
- root/docs mirrors byte-identical

Do not force these values.

## Required checks

Run and report:

1. `python3 -B tools/generate-boomer-step4-4-economy-conflict-audit.py .`
2. `python3 -B tools/validate-boomer-step4-4-economy-conflict-audit.py .`
3. `python3 -m py_compile tools/generate-boomer-step4-4-economy-conflict-audit.py`
4. root/docs byte comparison
5. deterministic second generation with zero diff
6. exact changed-path check
7. `git diff --check`
8. protected production blob verification for every source file read by the generator
9. temporary negative controls, with every mutation restored before commit:
   - mutate one `BATTLE_FALLBACK_TEXTS.boomer` value in a disposable copy and prove the corresponding current row changes or fails;
   - mutate `DM_REPORT_SUBMIT_LABELS.pending` in a disposable copy and prove `AUD_0145` changes or fails;
   - mutate one routed `SYSTEM_PROFILE_TEXT_COPY.boomer` value in a disposable copy and prove the corresponding current row changes or fails.

## Automatic publication

On the next `мост 1`:

1. fetch current remote refs and read current root authority, policy, STATE, this inbox and the v2 claim;
2. use a clean task-owned worktree from exact baseline `22f8b51150cd02f81c519fe822b6d6423d073062`;
3. apply only this three-path correction;
4. run all checks;
5. create one atomic direct-child primary commit;
6. push main fast-forward and refetch it;
7. machine-derive the exact primary SHA and actual parent from fetched remote main;
8. write those exact values into `.ai-bridge/outbox/BRIDGE-20260708-040-04-codex.md`;
9. push the outbox fast-forward and refetch mailbox;
10. return `PASS_PUSHED` only after both refs and the outbox contents are verified.

Do not repeat model preflight. Do not request `CONTINUE`, `APPROVE`, separate `пул`, separate `пуш`, payload copy, merge, rebase, reset, stash, clean, cherry-pick, amend or force-push.

Exact next action after publication: return to ChatGPT and send `мост 1`.
