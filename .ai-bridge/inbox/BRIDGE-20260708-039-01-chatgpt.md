Use @asynchronia runtime-safety-gate.

# Bridge Slot 1 - Stage 6 Wave V-A2 Boomer runtime surface correction

BRIDGE_PROTOCOL: 3.0
ORCHESTRATION_VERSION: 3.0
BRIDGE_SLOT: 1
THREAD_ID: BRIDGE-20260708-039
LANE_ID: S6-V5A2-BOOMER-RUNTIME-SURFACES
TASK_ID: TASK-S6-PAR-V5A2
PHASE: MODEL_PREFLIGHT_ONLY
USER_COMMAND: мост 1
PUBLICATION_MODE: CHATGPT_CONNECTOR_ONLY
PUBLICATION_POLICY: .ai-bridge/PUBLICATION_POLICY.md
CURRENT_MAIN_BASELINE: d15fe4dd34e8c431b02fb5a690982e38e6210fc5
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260708-039-02-codex.md
RUNTIME_CLASSIFICATION: RUNTIME_SENSITIVE_PRESENTATION_ONLY
MODEL_SELECTION: PENDING_CODEX_PREFLIGHT_12_OF_12
CONFIRMATION_RULE: SAME_THREAD_CONTINUE_CONFIRMS_MODEL_AND_EXACT_FROZEN_SCOPE
SAFARI_RUNTIME_STATUS: PENDING_USER

## Atomic objective

Make exactly four accepted Boomer-visible surfaces real at runtime without changing default, Millennial, Zoomer or Alpha copy and without changing mechanics:

1. reputation stat title: `Репутация`
2. points stat title: `Баланс`
3. report input placeholder: `Имя токсика, бандита или мафиози.`
4. report hint: `Сообщите о токсике, бандите или мафиози.`

This task fixes production routing only. It does not edit the Step 4.4A audit generator or audit artifacts.

## Exact write scope

Only these source/deployed mirror groups:

1. `AsyncScene/Web/ui/ui-core.js`
2. `docs/ui/ui-core.js`
3. `AsyncScene/Web/ui/ui-dm.js`
4. `docs/ui/ui-dm.js`
5. `AsyncScene/Web/index.html`
6. `docs/index.html`

Any other changed primary path is automatic FAIL.

Protected:

- all economy, battle, NPC, state, persistence and handler semantics;
- both `dev/dev-checks.js` mirrors;
- all `system.js`, `data.js`, conflict and event files;
- all audit generators and audit artifacts;
- `TASKS.md`, `PROJECT_MEMORY.md`, mailbox STATE;
- all creative copy other than the four exact targets above.

## Required behavior

### Top-bar stat titles

Add profile-aware presentation routing for stat title attributes.

Required matrix:

- default/Millennial/Zoomer/Alpha:
  - influence: `Влияние`
  - reputation: `⭐`
  - points: `💰`
  - wins: `Победы`
- Boomer:
  - influence: `Влияние`
  - reputation: `Репутация`
  - points: `Баланс`
  - wins: `Победы`
- unknown profile: exact default behavior.

Do not use the mutable `title` attribute as the structural selector for stat anchors after profile routing. `statAnchor(kind)` must resolve through stable structure such as `data-profile-stat` and the nested `.statIcon`, so changing titles cannot break stat toasts.

Apply title routing during normal render/startup and after a profile change. Do not require page reload if the existing profile-change flow already triggers UI refresh.

### Report placeholder and hint

Add profile-aware report-surface copy routing in `ui-dm.js`.

Required matrix:

- default/Millennial/Zoomer/Alpha:
  - placeholder: `Ник бандита или токсика.`
  - hint: `Сдай токсика, бандита или мафиози.`
- Boomer:
  - placeholder: `Имя токсика, бандита или мафиози.`
  - hint: `Сообщите о токсике, бандите или мафиози.`
- unknown profile: exact default behavior.

Use the existing profile resolution path. Do not duplicate game-state logic. Keep the existing Boomer report button labels unchanged:

- open/idle: `Сообщить`
- pending: `Проверка...`
- cooldown: `Повторите позже`

### Mirror and cache-bust rules

- source/deployed JS mirrors must be byte-identical by pair;
- source/deployed `index.html` mirrors must be byte-identical;
- update only the `ui-core.js` and `ui-dm.js` cache-bust query values needed to serve the new artifacts;
- update buildTag/commit markers only if required by existing project convention, with exact source/deployed identity;
- no unrelated cache-bust churn.

## Static acceptance checks

After implementation, prove:

1. exact six changed paths only;
2. all three source/deployed mirror pairs byte-identical;
3. JavaScript syntax PASS for both changed JS modules;
4. no changed mechanics/economy/state/handler paths;
5. exact four-profile matrix plus default/unknown fallback;
6. `statAnchor` no longer depends on profile-mutated title values;
7. the two report strings are produced by profile routing, not hardcoded as universal current text;
8. existing Alpha and Boomer DM action labels remain exact;
9. `git diff --check` PASS.

## Runtime evidence package

Do not edit shared dev-checks in this task.

Provide one concise Safari console probe using current public/runtime-accessible objects or a narrowly task-owned helper inside the changed module pair. The probe must observe:

- default/Millennial titles and report surfaces;
- Boomer titles and report surfaces;
- Alpha unchanged report surfaces;
- unknown fallback to default;
- stat anchor resolution still returns non-null for influence, rep, points and wins under Boomer.

The task can be statically accepted before Safari, but runtime PASS remains user-owned.

## Model protocol

Evaluate all 12 available model/reasoning pairs and recommend the cheapest reliable pair for this narrow but runtime-sensitive profile-routing correction.

Return compact preflight only. End with exactly one fenced `CONTINUE` block and nothing after it.

No edits, tests or publication payload before the user selects the model and sends `CONTINUE` in the same Codex thread.

## Publication

Codex must not run GitHub write or authentication commands.

After implementation and validation, return `READY_FOR_CONNECTOR_PUBLICATION` with the complete bundle required by `.ai-bridge/PUBLICATION_POLICY.md`, including full exact UTF-8 contents for all six changed files and the complete intended outbox.

ChatGPT will publish through the GitHub connector.

Exact next action after the bundle: return to ChatGPT and write `мост 1`.
