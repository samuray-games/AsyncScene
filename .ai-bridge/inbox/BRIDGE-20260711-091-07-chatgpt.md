Use @asynchronia.

# BRIDGE-20260711-091 - Fix6 correction after independent ChatGPT rejection

ISSUE: 216
CYCLE: CYCLE-20260711-003
GENERATION: 14
SLOT: 2
THREAD_ID: BRIDGE-20260711-091
LANE_ID: S6-BOOMER-STEP4_4B-FIX4-VALIDATION-CORRECTION
TASK_ID: TASK-S6-BOOMER-STEP4_4B-FIX4-ADVERSARIAL
EXECUTION_EPOCH: S6-BOOMER-4_4B-FIX4-R4-20260711-2322JST
TASK_NONCE: S6B44B-091-ADV-2322
BRIDGE_PROTOCOL: 4.0
BRANCH_MAILBOX: coordination/chatgpt-codex-bridge-2
BRANCH_TASK: bridge/2/BRIDGE-20260711-091
AUTHORIZED_PRIMARY_BASELINE: 25bee74c99fa19ae5914e1739e2ff6a46eb8aeec
REJECTED_TASK_HEAD: baf51aeb1407a3b288d7afd0d07966e1a323bc8a
SUPERSEDES_EXECUTION_EPOCH: S6-BOOMER-4_4B-FIX4-R3-20260711-1730JST
PREVIOUS_OUTBOX: .ai-bridge/outbox/BRIDGE-20260711-091-05-codex.md
PREVIOUS_RECEIPT: .ai-bridge/receipts/BRIDGE-20260711-091-06-codex.md
CLAIM_PATH: .ai-bridge/claims/BRIDGE-20260711-091-claim-v3-codex.md
EXPECTED_OUTBOX_PATH: .ai-bridge/outbox/BRIDGE-20260711-091-08-codex.md
EXPECTED_RECEIPT_PATH: .ai-bridge/receipts/BRIDGE-20260711-091-09-codex.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
REQUIRE_COMPLETE_SCOPE_DELTA: true
MODEL_RECOMMENDATION_PAUSE_REQUIRED: true
SAME_THREAD_CONTINUE_REQUIRED: true
THREAD_ROTATION_REQUIRED: true
SAFARI_STATUS: PENDING_USER

## Independent verifier verdict

The generation-13 result is rejected and must not be integrated or sent to Safari.

Exact failures:

1. The published outbox and receipt declared PASS while the mandatory command `python3 tools/validate-boomer-step4-4-economy-conflict-audit.py .` was reported as `FAIL_SCOPE_MISMATCH_FOR_STEP4_4B_RUNTIME_PATHS`. Repository policy forbids success unless required validations pass.
2. The task required preserving the accepted Fix4 snapshot, finally-path restoration, before/after digest logic, and production comparator. Rejected head `baf51aeb1407a3b288d7afd0d07966e1a323bc8a` instead removed the accepted `stableStringify`, `cloneRuntimeValue`, `restoreObjectInPlace`, `captureLocalStorage`, `makeRuntimeStateTracker`, per-category restoration fields, and comparator path, replacing them with a new JSON-string comparator. That is not a minimal adversarial-proof correction and is not the production comparator required by the task.
3. The claimed harness booleans therefore do not establish the frozen objective. They test the newly introduced self-contained comparator rather than proving that the accepted production smoke comparator rejects category-local mutations.

## Mandatory fresh model preflight

This is a fresh corrective epoch. The first `мост 2` command is read-only. Reconcile active installed Asynchronia 1.0.7, run task-router, scope-isolation-check, and model-selector, then stop at `WAITING_FOR_MODEL_SELECTION` with one standalone fenced `CONTINUE` block. No mutation before exact same-thread `CONTINUE`.

Because generation 13 failed after a large replacement of accepted runtime safety logic, start the recommendation frontier at GPT-5.5 / High. Downgrade only with a concrete proof that the cheaper selection can safely preserve the accepted restoration implementation while making the narrow comparator-fixture correction.

## Frozen write scope

Exactly these four paths may change on `bridge/2/BRIDGE-20260711-091`:

- `AsyncScene/Web/dev/dev-checks.js`
- `docs/dev/dev-checks.js`
- `AsyncScene/Web/index.html`
- `docs/index.html`

No fifth path may change. Direct task writes to main are forbidden.

## Corrective objective after CONTINUE

Create exactly one corrective commit whose parent is rejected head `baf51aeb1407a3b288d7afd0d07966e1a323bc8a`.

The final tree relative to baseline `25bee74c99fa19ae5914e1739e2ff6a46eb8aeec` must:

1. Restore the accepted Fix4 implementation of `smokeBoomerEconomyConflictTerminologyOnce()` from the baseline, including:
   - `stableStringify`;
   - `cloneRuntimeValue`;
   - `restoreObjectInPlace`;
   - localStorage capture and restoration;
   - `makeRuntimeStateTracker`;
   - its exact digest, restore, and compare paths;
   - the existing `try/finally` restoration path;
   - before/after digest evidence;
   - `profileRestored`, `economyRestored`, `battleStateRestored`, `moneyLogRestored`, `persistenceRestored`, `localStorageRestored`, and aggregate restoration requirements.
2. Replace only the defective generation-12/13 adversarial fixture proof.
3. For each category `profile`, `economy`, `battleState`, `moneyLog`, `persistence`, and `localStorage`:
   - clone the valid control digest;
   - mutate only that category;
   - invoke the exact accepted production comparator `stateTracker.compare` used by the smoke;
   - require the mutated digest to be rejected;
   - require an untouched cloned control to be accepted.
4. Do not create a second standalone comparator or substitute `JSON.stringify` inequality for the production comparator.
5. Expose and require:
   - `profileComparatorRejectsMutation`;
   - `economyComparatorRejectsMutation`;
   - `battleStateComparatorRejectsMutation`;
   - `moneyLogComparatorRejectsMutation`;
   - `persistenceComparatorRejectsMutation`;
   - `localStorageComparatorRejectsMutation`;
   - `allComparatorsRejectMutation`.
6. Overall smoke PASS must require every comparator field and every existing restoration field to be true.
7. Fixtures must operate only on cloned or sandboxed digest data and never mutate live runtime state.
8. Keep source/docs dev-check files byte-identical and both index files byte-identical.
9. Move identity and the single matching cache-bust from rejected Fix5 to Fix6. Do not reuse `step4_4b_boomer_runtime_aggregate_fix5_20260711a`.

## Required validation

Run all validation from a clean committed task worktree after the corrective commit. The Step 4.4A validator reads `git status --short`; running it against an uncommitted runtime diff is invalid evidence.

All of the following must return PASS or the terminal status must not be PASS:

- `node --check AsyncScene/Web/dev/dev-checks.js`;
- `node --check docs/dev/dev-checks.js`;
- `python3 tools/generate-boomer-step4-4-economy-conflict-audit.py .`;
- `python3 tools/validate-boomer-step4-4-economy-conflict-audit.py .` from the clean committed worktree;
- `python3 tools/validate-orchestration-policy.py`;
- current Boomer static generator and validator outputs remain deterministic and unchanged outside the frozen runtime scope;
- exact final four-path diff from `25bee74c99fa19ae5914e1739e2ff6a46eb8aeec`;
- corrective commit parent equals `baf51aeb1407a3b288d7afd0d07966e1a323bc8a`;
- source/docs dev-check blob equality;
- source/docs index blob equality;
- exact new Fix6 cache-bust in both index files;
- executable ephemeral harness against both source and docs runtime code;
- untouched control accepted by the accepted production comparator;
- each category-local mutation rejected by the accepted production comparator;
- all restoration fields remain true;
- `git diff --check`;
- clean committed task worktree before publication;
- no direct push to main.

A failed required validator must be reported as a failure and blocks success publication. Do not relabel or explain away a non-zero result as PASS.

## Publication

Publish implementation only to `bridge/2/BRIDGE-20260711-091`.

Publish the new outbox and receipt as separate fast-forward commits only on `coordination/chatgpt-codex-bridge-2`. Include final refetch, exact parent, exact final diff, blob equality, clean-worktree evidence, every command exit status, active Asynchronia 1.0.7 path and invoked skill hashes.

Codex must not edit `.ai-bridge/STATE.md`.

Terminal action after successful publication: `OPEN_FRESH_CHATGPT_VERIFIER_AND_SEND_SAME_BRIDGE_COMMAND`.