Use @asynchronia runtime-safety-gate.

# Bridge Slot 1 - Wave IV final narrow correction

BRIDGE_PROTOCOL: 3.0
ORCHESTRATION_VERSION: 3.0
BRIDGE_SLOT: 1
THREAD_ID: BRIDGE-20260708-036
LANE_ID: S6-I4-DATA-BATTLE-CONFLICT-PRESENTATION
TASK_ID: TASK-S6-PAR-I4
PHASE: EXECUTE_NOW_CORRECTION
CORRECTION_OF: .ai-bridge/outbox/BRIDGE-20260708-036-06-codex.md
CURRENT_MAIN_BASELINE: 96ecd05826550ab4c2dccf5f8a426b3079173452
EXPECTED_CORRECTION_OUTBOX: .ai-bridge/outbox/BRIDGE-20260708-036-08-codex.md
MODEL_PROTOCOL: SAME_THREAD_CORRECTION_NO_NEW_PREFLIGHT
CONFIRMATION: ALREADY_SATISFIED_BY_PRIOR_CONTINUE

## Decision

Wave IV is not accepted yet.

The exact `battle.*` keys and sentence-note routing are now correct, but one visible pending escape-vote line still bypasses the resolver.

Current code in `startEscapeVote`:

- `b.note` is profile-routed;
- `b.resultLine` remains hardcoded as `Отвали?` or `Свалить?`.

This produces mixed-profile visible copy:

- Alpha escape prompt still shows `Свалить?` instead of `Уйти?`;
- Boomer escape prompt still shows `Свалить?` instead of `Выйти?`;
- Boomer ignored prompt still shows `Отвали?` instead of `Вызов отклонён?`.

This violates the original Wave IV requirement to route existing visible hardcoded fallback assignments through the fallback resolver and the current requirement that all owned fallback presentation paths use the exact semantic keys.

## Exact correction

Change only:

- `AsyncScene/Web/conflict/conflict-core.js`
- `docs/conflict/conflict-core.js`

In the pending escape-vote block, derive one exact semantic key from `modeNorm`:

- `off` -> `battle.ignored_fallback`
- otherwise -> `battle.escaped_fallback`

Use that same key for both:

- `b.note`, preserving the existing `prefix: outcome?` structure;
- `b.resultLine`, preserving the existing question-mark structure as `outcome?`.

Required pending `resultLine` matrix:

- default/Millennial/Zoomer/unknown escaped: `Свалить?`
- default/Millennial/Zoomer/unknown ignored: `Отвали?`
- Alpha escaped: `Уйти?`
- Alpha ignored: `Отвали?`
- Boomer escaped: `Выйти?`
- Boomer ignored: `Вызов отклонён?`

Final resolved result lines must remain bare outcome text without added punctuation.

Do not change any other copy, helper behavior, profile resolution, handlers, economy, REP, influence, crowd, timers, report flow, persistence, battle state, semantic results, SystemCopy routing, `data.js`, or either `ui-battles.js` file.

## Required proof

- exact two-file diff only;
- source/deployed conflict-core mirrors byte-identical;
- pending note and pending resultLine matrices across default/Millennial/Zoomer/Alpha/Boomer/unknown;
- resolved bare result lines unchanged;
- no hardcoded `b.resultLine = ... "Свалить?"` / `"Отвали?"` assignment remains in the owned conflict files;
- exact three `battle.*` keys remain the only registered fallback keys;
- data and ui-battles blobs unchanged;
- node checks and diff check pass;
- empty `failures`, `forbiddenRemaining`, `missingCoverage`, `failedChecks`.

## Publication

- Publish one direct-child correction commit to `main` from `96ecd05826550ab4c2dccf5f8a426b3079173452`.
- Only the two conflict-core mirror files may change.
- Refetch `origin/main` and report fresh remote blob SHAs for all six Wave IV ownership files.
- Publish immutable outbox `.ai-bridge/outbox/BRIDGE-20260708-036-08-codex.md`.
- Do not edit mailbox STATE.
- No new preflight, no new `CONTINUE`, no second approval.
- Exact next action after publication: return to ChatGPT and write `мост 1`.
