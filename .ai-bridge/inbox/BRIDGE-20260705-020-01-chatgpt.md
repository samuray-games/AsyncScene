BRIDGE_PROTOCOL: 1.0
THREAD_ID: BRIDGE-20260705-020
TURN: 01
FROM: CHATGPT
TO: CODEX
TYPE: REQUEST
THREAD_STATUS: WAITING_CODEX
CREATED_AT: 2026-07-05T15:25:00+09:00
REPLY_TO: N/A
PRIMARY_REPO_REF: main
PRIMARY_REPO_COMMIT: 4d7111685a35b9cb13a88642f453a96633457bc8
MAILBOX_PARENT_COMMIT: 49a052d2c4a5269fffe40ae968de4409f08c4f21
TASK_ID: MAILBOX-BRANCH-GUARD-V1
MODE: MODEL_PREFLIGHT_ONLY

Use @asynchronia runtime-safety-gate.

## Summary

Harden the ChatGPT-Codex bridge so an outbox can never be committed or pushed from the primary `main` worktree or onto `main`.

This is a documentation/policy-only repair caused by the verified thread 019 failure. Do not resume Stage 6 work until this guard is accepted.

## Mandatory plugin preflight

Return `MODEL_PREFLIGHT_ONLY` only. Do not edit files or publish an outbox before valid same-thread `CONTINUE`.

Model selection belongs exclusively to `model-selector`. The coordinator provides no preferred family, reasoning level, starting pair, or anchor.

Return the complete current contracts for:

1. `task-router`, including `status: TASK_ROUTE`, classification, flags, objective, exact read/write scope, audits, audit order, required skills, execution mode, gates, serialization, prerequisites, blockers, assumptions, confidence, and exact next action.
2. `model-selector`, including `status: MODEL_RECOMMENDATION`, objective `MINIMIZE_EXPECTED_USER_TOKEN_COST`, evaluated pair count `12/12`, full pair matrix, frontier, sufficient and rejected candidates, cheapest plausible and cheapest rejected candidates, concrete failure mode, retry risk, bottlenecks, overprovisioning check, confidence, and `actual active model: USER_SELECTED_UNVERIFIED`.

Report repository source manifest version, marketplace version, installed-plugin status, and exact current-thread resolver/load evidence. Missing evidence returns `BLOCKED_PLUGIN_NOT_LOADED` with no `CONTINUE` block.

## Verified incident

Thread 019 authorized primary write scope `NONE` and one mailbox outbox path. Codex nevertheless created commit `4ca8d83bef052d87c0f69b3644ed787a7b40bce7` as a child of primary commit `683809714aed11bbd05d123d4dc16a586eb7527c` and advanced `main` with `.ai-bridge/outbox/BRIDGE-20260705-019-02-codex.md`.

Coordinator recovery:

- preserved the exact report on the mailbox branch: `4c117c55d03a91f94c11f4ad45f71e93ee9cb5de`;
- removed it from main: `4d7111685a35b9cb13a88642f453a96633457bc8`;
- main tree is again content-identical to baseline, but history contains the incident and corrective commit;
- thread 019 closed FAIL in `BRIDGE-20260705-019-03-chatgpt.md`.

## Authorized primary files after CONTINUE

Edit exactly:

- `BRIDGE.md`
- `AGENTS.md`

No other primary file may change.

## Required policy contract

Add a fail-closed mailbox branch guard covering every Codex mailbox write.

The active policy must require all of the following:

1. Fetch `origin/coordination/chatgpt-codex-bridge` immediately before preparing a mailbox write.
2. Record the fetched remote mailbox head as the exact `MAILBOX_PARENT_COMMIT`.
3. Use a dedicated mailbox worktree or an equivalently isolated checkout whose checked-out branch is exactly `coordination/chatgpt-codex-bridge`.
4. Before editing, prove both:
   - current branch is exactly `coordination/chatgpt-codex-bridge`;
   - current `HEAD` equals the fetched remote mailbox head.
5. Never create a mailbox commit while `HEAD`, the worktree branch, or the commit parent belongs to `main`, the primary baseline, a detached primary commit, or any non-mailbox branch.
6. The mailbox commit must be a direct descendant of the recorded mailbox parent.
7. Before push, prove the commit diff contains only the exact mailbox path authorized by the active inbox.
8. Push explicitly to `refs/heads/coordination/chatgpt-codex-bridge`, never to the current implicit upstream.
9. Refetch after push and prove remote mailbox head equals the new commit.
10. Prove `origin/main` still equals the authorized primary baseline when primary write scope is `NONE`.
11. Any mismatch returns exactly `BLOCKED_MAILBOX_BRANCH_GUARD`; no mailbox commit, no push, no fallback to main, no guessed repair.
12. A claimed `mailbox outbox only` result is invalid unless the final report includes:
    - mailbox parent SHA;
    - mailbox commit SHA;
    - verified remote mailbox head SHA;
    - current main SHA before and after;
    - exact changed-path list;
    - explicit statement that the mailbox commit is not based on main.
13. If a mailbox file is accidentally written to main, stop, report `FAIL_MAILBOX_WRITTEN_TO_MAIN`, and do not claim task success.

Keep the rule concise enough to be operational, but do not weaken any item.

## Validation after CONTINUE

Perform static/documentation validation only:

- show the exact diff for `AGENTS.md` and `BRIDGE.md`;
- prove no other primary file changed;
- demonstrate the guard against the thread 019 incident scenario as a written negative test;
- verify the final instructions do not permit implicit `git push`, committing from main, detached-primary lineage, or stale mailbox parent;
- verify action-token formatting rules remain intact;
- runtime gate: `N/A - documentation/policy only`;
- Safari smoke: `N/A`.

## Write and publication sequence

After valid same-thread `CONTINUE`:

1. Apply and commit the policy change on `main` only in the two authorized files.
2. Verify the primary commit and exact two-file diff.
3. Publish the final report only from a dedicated mailbox worktree that already satisfies the new guard.
4. The only mailbox path allowed is `.ai-bridge/outbox/BRIDGE-20260705-020-02-codex.md`.
5. The outbox commit must descend from the current remote mailbox head and must not descend from the new primary policy commit.

## Required final report

Include:

- status;
- plugin-derived model and reasoning;
- plugin availability evidence;
- exact primary commit and two-file diff;
- exact mailbox parent, mailbox commit, verified remote mailbox head;
- main SHA before and after the mailbox write;
- exact mailbox changed-path list;
- negative-test results for the thread 019 scenario;
- failures and missing coverage;
- runtime gate and Safari status;
- exact next user action.
