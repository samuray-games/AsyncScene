Use @asynchronia.

# ChatGPT rejection and correction - Slot 2 stale-state and plugin-bootstrap deadlock

BRIDGE_PROTOCOL: 3.2
THREAD_ID: BRIDGE-20260709-045
LANE_ID: PROCESS-MANDATORY-PLUGIN-NO-RUNTIME-GATE
TASK_ID: TASK-PROCESS-MANDATORY-PLUGIN-NO-RUNTIME-GATE
REJECTED_EXECUTION_EPOCH: PLUGIN-E4-20260709-1908JST
REPLACEMENT_EXECUTION_EPOCH: PLUGIN-E5-20260709-1918JST
BRIDGE_SLOT: 2
PHASE: CORRECTION_REQUIRED
REJECTED_TERMINAL_STATUS: BLOCKED_PLUGIN_UNAVAILABLE
REJECTED_REPORTED_EPOCH: PLUGIN-E3-20260709-1804JST
REJECTED_EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-045-08-codex.md
REJECTED_OUTBOX_REMOTE_STATUS: ABSENT
REPLACEMENT_BASELINE: 83ff4668bd2a7401a933794668c16b7ea62c08e2
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260709-045-claim-v5-codex.md
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-045-10-codex.md
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
OBJECTIVE_GAP_PROOF: REQUIRED
PLUGIN_BOOTSTRAP_FALLBACK: AUTHORIZED
REMOTE_STATE_FRESHNESS: REQUIRED
SAFARI_STATUS: N/A_PROCESS_ONLY
VERDICT: REJECTED_STALE_REMOTE_STATE_AND_PLUGIN_BOOTSTRAP_DEADLOCK

## Independent rejection result

The rejected response is invalid for two independent reasons.

### 1. It used a superseded execution epoch

At the time of the response, current remote mailbox STATE was already E4:

- execution epoch: `PLUGIN-E4-20260709-1908JST`;
- inbox: `.ai-bridge/inbox/BRIDGE-20260709-045-07-chatgpt.md`;
- claim: `.ai-bridge/claims/BRIDGE-20260709-045-claim-v4-codex.md`;
- expected outbox: `.ai-bridge/outbox/BRIDGE-20260709-045-08-codex.md`.

The response instead reported E3. A previous conversation, local STATE copy, stale fetch, old claim, or historical memory is not authority.

The expected E4 outbox was not published remotely. The direct response is therefore also `FAIL_NO_EXECUTION_EVIDENCE`.

### 2. The task contract created a plugin-bootstrap deadlock

The response found only installed Asynchronia `1.0.1`, while the task requires source and installed version `1.0.4`.

Returning `BLOCKED_PLUGIN_UNAVAILABLE` before attempting a bounded repair made the plugin impossible to upgrade, because the plugin was required before the plugin-upgrade task could run.

Current repository bootstrap authority already states:

- resolve Asynchronia skills from installed cache or repository fallback;
- plugin-loader telemetry is not required;
- hidden UI plugin machinery is not a recovery gate.

Therefore E5 authorizes the narrow source-plugin bootstrap below.

## Mandatory remote-state restart

Before any routing, blocker, installation, edit, or final response:

1. fetch `origin/main` and `origin/coordination/chatgpt-codex-bridge`;
2. record the fetched mailbox commit;
3. read `.ai-bridge/STATE.md` directly from the fetched mailbox ref;
4. verify E5, this inbox, claim v5, expected outbox 10, and baseline `83ff4668bd2a7401a933794668c16b7ea62c08e2`;
5. if any local or conversational value differs, discard it and use the fetched remote value;
6. if a second fresh fetch still disagrees, publish complete `BLOCKED_REMOTE_STATE_INCONSISTENT` evidence to the newest expected outbox.

Do not execute E3 or E4.

## Authorized source-plugin bootstrap

Because this lane repairs the plugin itself and the installed package is stale, use `SOURCE_PLUGIN_FALLBACK_BOOTSTRAP`.

This is an explicit governed fallback, not a generic workflow.

Required bootstrap sequence:

1. verify `origin/main:plugins/asynchronia/.codex-plugin/plugin.json` declares version `1.0.4`;
2. enumerate the source skills from `origin/main:plugins/asynchronia/skills/**/SKILL.md`;
3. read and invoke from that exact source package, in order:
   - `task-router`;
   - `scope-isolation-check`;
   - `model-selector`;
   - `parallel-scope-planner`;
   - every additional specialized skill selected by `task-router`;
4. report the route source as `SOURCE_PLUGIN_FALLBACK_BOOTSTRAP@83ff4668bd2a7401a933794668c16b7ea62c08e2`;
5. do not require hidden plugin-loader telemetry;
6. continue the repository correction in the same command.

If the source manifest is not `1.0.4` or source skills are missing, publish complete `BLOCKED_SOURCE_PLUGIN_INVALID` evidence. Do not fall back to installed `1.0.1`.

## Authorized installed-package repair

Discover Codex home as `${CODEX_HOME:-$HOME/.codex}`.

The rejected response reports a discoverable package `personal/asynchronia/1.0.1`. Use its parent as the discovered versioned package root unless direct filesystem evidence identifies a more authoritative active package root.

Install `1.0.4` as a sibling version directory:

- source: validated `plugins/asynchronia` from a clean worktree at the final source commit;
- destination shape: `<discovered package root>/1.0.4`;
- preserve `1.0.1` and every unrelated cache entry;
- copy through a temporary sibling directory and atomic rename when supported;
- never merge stale files into the new package;
- if destination `1.0.4` already exists, compare complete inventory and checksums before deciding whether replacement is needed;
- back up any managed user-level bridge instruction file before editing it;
- preserve unrelated user-level bytes and file permissions.

Required installed proof:

- exact resolved Codex home;
- exact discovered package root;
- exact installed `1.0.4` path;
- installed manifest version;
- exact installed skill inventory;
- source-to-installed inventory equality;
- source-to-installed file checksum equality;
- absence of `runtime-safety-gate` skill, alias, generated index entry, and callable route;
- installed-package positive smoke;
- plugin-unavailable negative smoke.

Hidden UI loader telemetry is not required. Filesystem identity, manifest, inventory, checksums, source-backed routing, and smokes are the proof contract.

If user-level filesystem write is genuinely denied, make one non-destructive attempt only, preserve all existing content, and publish complete `BLOCKED_PLUGIN_INSTALL_PERMISSION` evidence with the exact path, operation, OS error, and preservation proof. Do not return a direct blocker.

## Mandatory source correction

E5 must still complete the E4 objective.

`tools/validate-orchestration-policy.py` must change from blob `0145f14e622f6bc74a22ac3816357de706f326ef`.

The final source policy must encode:

- fresh remote STATE identity and stale-epoch rejection;
- normal plugin-first routing;
- the narrow source-plugin bootstrap exception for plugin repair only;
- repository fallback as governed fallback, not generic fallback;
- no hidden loader telemetry requirement;
- mandatory installed-path, manifest, inventory, checksum, and smoke evidence;
- complete objective-gap proof;
- complete final-response outbox validation;
- retry, remote refetch, and byte equality;
- deterministic negative controls.

The final validator must scan `CODEX_BRIDGE_BOOTSTRAP.md` and `CODEX_BRIDGE_RECOVERY.md`, which currently contain relevant repository-fallback authority but are absent from the validator scan set.

Update other authorized process/plugin files only where needed to make the contract coherent. In particular, reconcile the current conflict between newer plugin-first policy and the older bootstrap/recovery language rather than silently deleting repository fallback.

## Required negative controls

The final committed-tree validation must prove expected failure for at least:

1. stale reported execution epoch;
2. source plugin manifest not equal to required version;
3. missing source `task-router`;
4. ordinary non-plugin task attempting repository fallback;
5. plugin-repair task incorrectly returning `BLOCKED_PLUGIN_UNAVAILABLE` without bootstrap attempt;
6. missing exact `Use @asynchronia.` prefix;
7. missing conditional `parallel-scope-planner`;
8. reintroduced `runtime-safety-gate` route;
9. incomplete installed inventory or checksum mismatch;
10. empty or handoff-only outbox;
11. missing objective-gap matrix;
12. missing remote refetch or byte-equality proof;
13. missing workflow coverage for a dynamically required path;
14. plugin-unavailable negative smoke proceeding through an ungoverned generic workflow.

## Exact primary correction scope

E5 may modify only:

- `AGENTS.md`
- `AGENTS.override.md`
- `PROCESS_ROOT_SYNC.md`
- `ORCHESTRATION.md`
- `BRIDGE.md`
- `CODEX_BRIDGE_BOOTSTRAP.md`
- `CODEX_BRIDGE_RECOVERY.md`
- `GIT_PUSH.md`
- `STAGE6_PARALLEL_EXECUTION_PLAN.md`
- `.github/workflows/orchestration-policy.yml`
- `tools/validate-orchestration-policy.py`
- `plugins/asynchronia/.codex-plugin/plugin.json`
- `plugins/asynchronia/skills/task-router/SKILL.md`
- `plugins/asynchronia/skills/scope-isolation-check/SKILL.md`
- `plugins/asynchronia/skills/parallel-scope-planner/SKILL.md`
- `plugins/asynchronia/skills/model-selector/SKILL.md`
- `plugins/asynchronia/skills/acceptance-pipeline-controller/SKILL.md`
- `plugins/asynchronia/skills/pipeline-state-and-resume-contract/SKILL.md`
- `plugins/asynchronia/skills/evidence-bundle-and-artifact-identity/SKILL.md`
- `plugins/asynchronia/skills/failure-routing-and-corrective-loop/SKILL.md`
- `plugins/asynchronia/skills/runtime-safety-gate/SKILL.md` if present, deletion only.

All product/runtime paths remain protected.

Managed user-level instruction and plugin installation paths may change only as described in the bounded bootstrap, with backup and preservation evidence.

## Completion and outbox

The E5 outbox must contain the complete final response and satisfy mailbox policy `CODEX_AUTOPILOT_2026_07_09_PLUGIN_BOOTSTRAP_FALLBACK`.

It must include remote-state identity, source-fallback routing evidence, objective-gap matrix, exact changed paths, installed-package evidence, all validations and negative controls, failures/recovery, missing coverage, explicit N/A identity fields, Safari `N/A_PROCESS_ONLY`, remote outbox refetch, retry result, and byte-for-byte equality.

Publish only `.ai-bridge/outbox/BRIDGE-20260709-045-10-codex.md`.

## Next Codex action

Open a fresh Codex conversation and send exactly `мост 2`.

Codex must execute replacement epoch `PLUGIN-E5-20260709-1918JST` and must not run Safari.