BRIDGE_PROTOCOL: 1.0
THREAD_ID: BRIDGE-20260705-019
TURN: 01
FROM: CHATGPT
TO: CODEX
TYPE: REQUEST
THREAD_STATUS: WAITING_CODEX
CREATED_AT: 2026-07-05T14:35:00+09:00
REPLY_TO: N/A
PRIMARY_REPO_REF: main
PRIMARY_REPO_COMMIT: 683809714aed11bbd05d123d4dc16a586eb7527c
MAILBOX_PARENT_COMMIT: c054d867bc7b7103d3ff7fe57d62fe863a9ea54c
TASK_ID: TASK-S6-001-R10
MODE: MODEL_PREFLIGHT_ONLY

Use @asynchronia runtime-safety-gate.

## Summary

Perform a focused plugin-backed read-only correction audit for the Zoomer acceptance conflict missed by `BRIDGE-20260705-018`.

Do not edit the Zoomer final-package documents. First determine whether their Step 8 runtime-PASS reference is accurate, overbroad, ambiguous, or stale after reconciling accepted user-owned Safari evidence with exact repository smoke identities.

## Mandatory plugin preflight

Return `MODEL_PREFLIGHT_ONLY` only before any audit work. Do not write the outbox before valid same-thread `CONTINUE`.

Model selection belongs exclusively to `model-selector`. The coordinator supplies no preferred model family, reasoning level, starting pair, or anchor.

The preflight must include the complete current output contracts for:

1. `task-router`, including `status: TASK_ROUTE`, classification, flags, objective, exact read scope, exact write scope, audits, audit order, required skills, execution mode, gates, serialization, prerequisites, blockers, assumptions, confidence, and exact next action.
2. `model-selector`, including `status: MODEL_RECOMMENDATION`, objective `MINIMIZE_EXPECTED_USER_TOKEN_COST`, evaluated pair count `12/12`, full pair matrix, frontier, sufficient candidates, rejected candidates, cheapest plausible candidate, cheapest rejected candidate, concrete failure mode, retry risk, bottlenecks, overprovisioning check, confidence, and `actual active model: USER_SELECTED_UNVERIFIED`.

Plugin evidence must separately report source manifest version, marketplace version, installed-plugin version/status, current-thread resolver/load evidence, exact evidence sources, and whether this thread can actually resolve and use the required skills. Missing resolver/load evidence returns `BLOCKED_PLUGIN_NOT_LOADED` with no `CONTINUE` block.

## Authoritative coordinator-supplied evidence

Treat the following as current accepted cross-chat decision evidence that must be reconciled, not ignored:

1. Live `ASYNCHRONIA - PROJECT MEMORY`
   - `MEMORY_REV: 2026-07-05-1421-JST`
   - revision `2026-07-05-0117-JST`
   - exact accepted decision: `Zoomer Step [6.2] Fix4 и aggregate [6.5] приняты по Safari runtime PASS; Pages deployment восстановлен без изменения исходников; lock Fix4 освобождён.`
   - recorded evidence source: user Safari runtime log plus GitHub Pages API evidence.
2. Stage 6 Control Board `S6-CONTROL-2026-07-05-10`
   - `LAST_SAFARI_RESULT: Zoomer Step 6.2 Fix4 and aggregate 6.5 PASS`
   - current profile state: `Zoomer: aggregate Safari PASS accepted.`
3. Stage 6 Execution Plan `S6-PLAN-2026-07-05-02`
   - Zoomer Step [6.2] Fix4 and aggregate Step [6.5] received user Safari PASS.
   - Zoomer is closed except final cross-profile regression.

These accepted decisions do not by themselves prove that every sentence in `UI_PROFILE_ZOOMER_FINAL.md` is perfectly scoped. They do prove that repository absence of the user Safari log is not sufficient to call Zoomer runtime acceptance unsupported.

## Audit scope after valid same-thread CONTINUE

Read-only. No primary writes, no runtime execution, no new Safari smoke, no locks.

Inspect at minimum:

- `AGENTS.md`, `TASKS.md`, `PROJECT_MEMORY.md`;
- `UI_PROFILE_ZOOMER_FINAL.md` and `docs/UI_PROFILE_ZOOMER_FINAL.md`;
- all repository artifacts that define or reference Zoomer Step 8, Step [6.2] Fix4, aggregate [6.5], final acceptance, buildTag, smokeVersion, deployment lineage, and smoke commands;
- relevant runtime smoke implementations and registrations, read-only only;
- relevant historical repository status documents needed to map the accepted decision to an exact smoke identity;
- current `main`, local/remote parity, worktree diff, and mailbox state.

Use complete applicable plugin contracts, including `canon-audit`, `mirror-audit`, `smoke-orchestrator` for evidence classification, `deployment-verifier`, and `acceptance-evidence-gate`.

## Required questions

Answer each explicitly:

1. What exact repository smoke identity or chain corresponds to accepted Zoomer Step [6.2] Fix4?
2. What exact repository smoke identity or chain corresponds to accepted aggregate Step [6.5]?
3. Does the accepted user Safari decision support the Step 8 sentence in `UI_PROFILE_ZOOMER_FINAL.md` exactly as written?
4. If not exact, is the sentence overbroad, ambiguous, stale, or mapped to a different historical numbering scheme?
5. Is any documentation correction required? If yes, provide the smallest exact wording correction and exact files, but do not implement it in this task.
6. Does the corrected Zoomer result change the recommended next Stage 6 atomic task from the matrix in outbox 018-02?

Do not convert missing mapping evidence into FAIL or PASS. Use `AMBIGUOUS` or `BLOCKED` with the precise missing link when appropriate.

## Required result

Publish one focused correction report containing:

- status: `PASS`, `PARTIAL_PASS`, `FAIL`, or `BLOCKED`;
- plugin-derived selected model and reasoning;
- plugin availability and exact evidence;
- inspected files;
- changed files: mailbox outbox only;
- accepted coordinator evidence consumed;
- repository smoke identity map;
- exact answers to the six required questions;
- corrected Zoomer lane row for the Stage 6 reconciliation matrix;
- impact on the rest of outbox 018-02;
- proven conflicts and uncertainties;
- missing evidence;
- exact next atomic task, exact files, runtime-gate status, and plugin-derived model/reasoning recommendation for that next task;
- commit: `N/A - read-only primary audit`;
- Safari status: `N/A - evidence reconciliation only`;
- exact next user action.

## Write scope

After valid same-thread `CONTINUE`, the only permitted write is:

- `.ai-bridge/outbox/BRIDGE-20260705-019-02-codex.md`

Do not edit primary files, `TASKS.md`, `PROJECT_MEMORY.md`, runtime JavaScript, smoke registries, deployment files, locks, the Control Board, or the Execution Plan.

## Runtime and acceptance

- Runtime safety gate: `N/A - read-only evidence reconciliation`.
- Safari smoke: `N/A - do not rerun or manufacture evidence`.
- Current accepted user-owned Safari evidence must remain accepted unless a newer authoritative source explicitly supersedes it.
