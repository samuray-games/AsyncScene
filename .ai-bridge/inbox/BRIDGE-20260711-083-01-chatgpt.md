Use @asynchronia.

# BRIDGE-20260711-083 - Stage 6 Boomer Step 4.4B Fix3 publication recovery

ISSUE: 216
CYCLE: CYCLE-20260711-003
GENERATION: 4
SLOT: 2
THREAD_ID: BRIDGE-20260711-083
LANE_ID: S6-BOOMER-STEP4_4B-PUBLICATION-RECOVERY
TASK_ID: TASK-S6-BOOMER-STEP4_4B-FIX3-PUBLISH
EXECUTION_EPOCH: S6-BOOMER-4_4B-FIX3-PUB-R1-20260711-1003JST
TASK_NONCE: S6B44B-083-PUB-1003
PHASE: RECOVER_VALIDATE_AND_PUBLISH
COORDINATOR_MEMORY_REVISION: 2026-07-11-1003-JST
AUTHORIZED_PRIMARY_BASELINE: c5ef0ecea5c96a54604e6d130207ac4fd52789df
REPORTED_LOCAL_COMMIT: 0f7ecd69fb5857dbb965e6b4ee57dc2737f16f29
SUPERSEDES_THREAD: BRIDGE-20260711-082
PREVIOUS_ATTEMPT_STATUS: BLOCKED_REMOTE_PUBLICATION_NO_REMOTE_COMMIT_NO_OUTBOX_NO_RECEIPT
CLAIM_PATH: .ai-bridge/claims/BRIDGE-20260711-083-claim-v1-codex.md
EXPECTED_OUTBOX_PATH: .ai-bridge/outbox/BRIDGE-20260711-083-02-codex.md
EXPECTED_RECEIPT_PATH: .ai-bridge/receipts/BRIDGE-20260711-083-03-codex.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
PLUGIN_INVOCATION_REQUIRED: true
PLUGIN_IDENTIFIER: asynchronia
VALIDATOR_APPLICABILITY_GATE: REQUIRED
RUNTIME_CLASSIFICATION: RUNTIME_SMOKE_INFRASTRUCTURE
SAFARI_STATUS: PENDING_USER
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true
MODEL_PREFLIGHT_GATE: RETIRED
MODEL_RECOMMENDATION_OWNER: ASYNCHRONIA_MODEL_SELECTOR
ACTUAL_MODEL_STATUS: USER_SELECTED_UNVERIFIED

## Verified remote state

- Remote `main` still resolves to `c5ef0ecea5c96a54604e6d130207ac4fd52789df`.
- GitHub does not contain commit `0f7ecd69fb5857dbb965e6b4ee57dc2737f16f29`.
- Bridge 082 published neither its expected outbox nor its expected receipt.
- Therefore no source or mailbox acceptance occurred.

## Correction to the previous validation interpretation

`tools/validate-boomer-step4-4-economy-conflict-audit.py` is task-applicable and remains a required PASS gate.

Current repository source shows that this validator does not invoke `tools/validate-orchestration-policy.py`. It calls `git status --short` and rejects dirty paths outside its historical Step 4.4A `ALLOWED_SCOPE`.

Therefore the reported failure was caused by running the validator while the four runtime files were still visible as working-tree changes, not by the excluded Bridge 062 orchestration validator.

Run this validator only after the final primary commit exists and from a clean task-owned worktree. It must PASS.

`tools/validate-orchestration-policy.py` remains separately excluded as `NOT_APPLICABLE` under the exact Bridge 082 exclusion evidence.

## Atomic objective

Recover the already-created four-file Fix3 content, prove its provenance and exact scope, obtain one direct-child exact-four-path commit from the authorized baseline, run all task-applicable checks from a clean committed tree, push with an explicit non-force refspec, then publish complete outbox and receipt evidence.

Do not rewrite source merely because the prior push command was malformed. Do not use merge, rebase, cherry-pick, reset, amend, force-push, stash, clean, or the user's primary checkout.

## Mandatory plugin route and evidence

Invoke the installed Asynchronia plugin and use material results in this exact order:

1. task-router
2. scope-isolation-check
3. model-selector
4. parallel-scope-planner
5. mirror-audit
6. smoke-orchestrator
7. deployment-verifier
8. closed-loop-controller
9. failure-routing-and-corrective-loop
10. acceptance-evidence-gate

Both outbox and receipt must contain the complete top-level `pluginInvocationEvidence` object. Every skill must appear exactly once, in order, with PASS status, non-empty material result, and concrete evidence references.

## Frozen source scope

Exactly these four paths must differ from the authorized baseline:

- AsyncScene/Web/dev/dev-checks.js
- docs/dev/dev-checks.js
- AsyncScene/Web/index.html
- docs/index.html

No other primary path may change.

## Local commit provenance gate

Locate local commit `0f7ecd69fb5857dbb965e6b4ee57dc2737f16f29` without assuming it is valid.

Prove and record:

- exact commit object exists locally;
- exact parent SHA;
- exact changed paths;
- clean tree snapshot for all four paths;
- no fifth changed path;
- Fix3 buildTag, commit label, smokeVersion, and cache-bust;
- source/docs semantic mirror status and intentional docs Alpha difference.

If the commit parent is exactly `c5ef0ecea5c96a54604e6d130207ac4fd52789df` and the changed paths are exactly the frozen four, retain the exact commit and publish it.

If its parent is not the authorized baseline, do not rebase or cherry-pick it. Create a new clean detached task-owned worktree at the authorized baseline, copy only the exact four file blobs from `0f7ecd69fb5857dbb965e6b4ee57dc2737f16f29`, independently review the resulting diff, and create one new direct-child exact-four-path commit.

## Required clean-tree validations

Run all checks only after the final primary commit exists and the task-owned worktree is clean:

- `git status --short` must be empty;
- `python3 tools/generate-boomer-step4-4-economy-conflict-audit.py .`;
- `python3 tools/validate-boomer-step4-4-economy-conflict-audit.py .`;
- `node --check AsyncScene/Web/dev/dev-checks.js`;
- `node --check docs/dev/dev-checks.js`;
- exact direct-parent proof;
- exact four-path proof from baseline to final commit;
- protected-path proof;
- source/docs semantic mirror verification with the intentional Alpha difference preserved;
- served entrypoint, Fix3 identity, and Fix3 cache-bust verification for both index files;
- adversarial fixture proof for every new evidence evaluator family;
- restoration proof for profile/text mode, points, REP, Influence, battle state, moneyLog, persistence, and relevant localStorage keys;
- `git diff --check c5ef0ecea5c96a54604e6d130207ac4fd52789df...<FINAL_PRIMARY_SHA>`.

The static audit must remain `STATIC_PASS / READY_FOR_RUNTIME_SMOKE`, 147 PASS, 0 FAIL, 0 structural failures, and 8 non-live rows.

Any non-passing task-applicable validation forbids `PASS_PUSHED`.

## Explicit push recovery

Immediately before pushing:

- fetch `origin/main`;
- prove `git ls-remote origin refs/heads/main` equals the authorized baseline;
- prove the final primary commit has that baseline as its sole parent;
- prove the baseline is an ancestor of the final primary commit.

Push with an explicit non-force source-to-destination refspec:

`git push origin <FINAL_PRIMARY_SHA>:refs/heads/main`

Do not rely on the current branch name or upstream configuration.

If the explicit push is rejected, capture exact stderr, immediately refetch and record `ls-remote`, and return a publication blocker. Never force-push.

## Excluded validator evidence

Preserve the Bridge 082 exclusion exactly:

- validator: `tools/validate-orchestration-policy.py`
- status: `NOT_APPLICABLE`
- pinnedIdentity: `BRIDGE-20260710-062 / CLOSED-LOOP-CLOUD-PR-R1-20260710-1348JST / 32513f02daf5943c41f24328e1ae251d6bc85ccc`
- activeIdentityMismatch: `BRIDGE-20260711-083 / S6-BOOMER-4_4B-FIX3-PUB-R1-20260711-1003JST / c5ef0ecea5c96a54604e6d130207ac4fd52789df`
- reason: historical Bridge 062 frozen scope and historical-base repository diff are unrelated to the current four-path runtime lane
- replacementControls: every required clean-tree validation listed above

## Publication contract

After a successful primary push:

- refetch remote `main` and prove exact final SHA, first parent, and four paths;
- publish only `.ai-bridge/outbox/BRIDGE-20260711-083-02-codex.md` as a fast-forward child of freshly fetched mailbox head;
- refetch the outbox, then publish only `.ai-bridge/receipts/BRIDGE-20260711-083-03-codex.md` as a separate fast-forward child;
- refetch mailbox and return bytes identical to the remote receipt;
- preserve `validationResults`, `excludedValidationEvidence`, and `pluginInvocationEvidence` exactly in the receipt;
- put receipt-only checks in `publicationValidationResults`;
- do not edit `.ai-bridge/STATE.md`.

## Acceptance boundary

Codex must not claim Safari PASS. Safari remains `PENDING_USER` until ChatGPT independently verifies the remote commit, exact scope, source behavior, outbox, receipt, plugin evidence, validation preservation, exclusion evidence, and deployed identity.

Terminal success action code: `OPEN_FRESH_CHATGPT_VERIFIER_AND_SEND_SAME_BRIDGE_COMMAND`.
