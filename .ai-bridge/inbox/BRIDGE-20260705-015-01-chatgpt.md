BRIDGE_PROTOCOL: 1.0
THREAD_ID: BRIDGE-20260705-015
TURN: 01
FROM: CHATGPT
TO: CODEX
TYPE: REQUEST
THREAD_STATUS: WAITING_CODEX
CREATED_AT: 2026-07-05T12:48:00+09:00
REPLY_TO: N/A
PRIMARY_REPO_REF: main
PRIMARY_REPO_COMMIT: f5bc642bff40953e11b86ad3592c49c669573e67
MAILBOX_PARENT_COMMIT: 980815abe392f12801ec8b51006fcba173cda938
TASK_ID: TASK-S6-001-R7
MODE: MODEL_PREFLIGHT_ONLY

## Summary

Run the complete read-only Stage 6 Tone Profiles reconciliation audit with mandatory use of the repository-tracked Asynchronia plugin workflow. This thread supersedes thread 014 before execution because that request named a stale plugin version.

## Context checked

- Accepted main baseline: `f5bc642bff40953e11b86ad3592c49c669573e67`.
- Current source manifest `plugins/asynchronia/.codex-plugin/plugin.json` declares version `1.0.0`.
- `.agents/plugins/marketplace.json` still declares `0.4.1`; treat that as metadata drift.
- Installed plugin availability and exact installed version must be verified separately from repository source metadata.
- Execution Plan: https://docs.google.com/document/d/17YDaMlYRlJIRwOekm6gt1prDCDNCQvrXF5A2T7PbeUM/edit
- Control Board: https://docs.google.com/document/d/1ywPtHLwi8yhXoQILcrElsKqv03y4S64N1alzlYdBRC0/edit
- Current Project Memory expected at or after `MEMORY_REV: 2026-07-05-1245-JST`; verify directly.
- Historical lock `BOOMER-B4T44A-ECONFLICT-AUDIT-20260705T0223JST` is stale and grants no write authority.

## Message

First return `MODEL_PREFLIGHT_ONLY` only. Do not run the audit before same-thread `CONTINUE`.

Preflight requirements:
1. verify local and remote `main`, branch, Git status, worktrees and policies;
2. verify mailbox `STATE.md`, this inbox and that thread 015 is the sole open thread;
3. verify the repository plugin source workflow is readable;
4. verify current plugin availability and installed version separately, or report unavailable / unverified;
5. report the source `1.0.0` versus marketplace `0.4.1` discrepancy;
6. identify and use the applicable plugin components: `task-router`, `model-selector`, `canon-audit`, `mirror-audit`, `parallel-scope-planner`, `smoke-orchestrator` for evidence classification only, `deployment-verifier`, and `acceptance-evidence-gate`;
7. recommend the cheapest reliable model and reasoning level;
8. list the exact read scope, permissions and blockers;
9. confirm primary repository changed files remain none and the only eventual write is `.ai-bridge/outbox/BRIDGE-20260705-015-02-codex.md`;
10. end with exactly one standalone fenced text code block containing only `CONTINUE`, with no text after it.

After same-thread `CONTINUE`, audit:
- current repository baseline, dirty state, worktrees, branches and locks;
- canonical Stage 6 boundary and separation from future Stage 8 Living World work;
- Millennial, Zoomer, Boomer, Alpha and UI Profile Selection implementation and acceptance state;
- exact source files, root/docs or source/deployed counterparts, lexicons, taboo lists, mappings, inventories, audits, generators and validators;
- boot entrypoints, cache-bust wiring and deployed reachability;
- runtime-sensitive files and serialization groups;
- stable-read dependencies, write overlaps, task order, copy-decision prerequisites and model recommendations;
- corrections required in the Execution Plan and Control Board;
- exact next atomic task.

Do not implement, repair, invent copy, acquire a lock, edit primary files, run runtime smoke or modify closed/superseded threads.

Mirror rules:
- distinguish byte parity, semantic parity, deterministic transformation and deployment-wiring defects;
- do not reduce this to a generic mirror audit;
- matching contents do not prove runtime reachability, plugin availability or acceptance.

## Required response

Follow current `AGENTS.md`. Distinguish mailbox, primary repository, plugin-source, installed-plugin and local-worktree evidence; static validation; runtime acceptance; missing evidence; and exact next user action.

Recommended starting model: `GPT-5.4 / High`, subject to model preflight.
Runtime gate: `N/A - read-only audit`. Do not request `APPROVE`.
Safari smoke: `N/A - read-only audit`.

## Safety and authority

Current primary sources override this request if newer. Report conflicts and stop rather than guessing. Threads 011, 013 and 014 are superseded and must not be resumed.