Use @asynchronia runtime-safety-gate.

# Bridge Slot 1 - Stage 6 serialized Wave IV data, battle and conflict presentation

BRIDGE_PROTOCOL: 3.0
ORCHESTRATION_VERSION: 3.0
BRIDGE_SLOT: 1
THREAD_ID: BRIDGE-20260708-036
LANE_ID: S6-I4-DATA-BATTLE-CONFLICT-PRESENTATION
TASK_ID: TASK-S6-PAR-I4
PHASE: MODEL_PREFLIGHT_ONLY
USER_COMMAND: мост 1
CURRENT_MAIN_BASELINE: 689ee3e9b50539268a509c49b5c377b403d44c58
DECISION_FREEZE: .ai-bridge/decisions/STAGE6_WAVE2_COPY_FREEZE.md
PRIMARY_WRITE_SCOPE:
- AsyncScene/Web/data.js
- docs/data.js
- AsyncScene/Web/ui/ui-battles.js
- docs/ui/ui-battles.js
- AsyncScene/Web/conflict/conflict-core.js
- docs/conflict/conflict-core.js
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260708-036-02-codex.md
RUNTIME_CLASSIFICATION: RUNTIME_SENSITIVE
CONFIRMATION_RULE: SAME_THREAD_CONTINUE_CONFIRMS_MODEL_AND_EXACT_FROZEN_RUNTIME_SCOPE
MODEL_SELECTION: PENDING_CODEX_PREFLIGHT_12_OF_12

## Atomic objective

Implement only Wave IV profile presentation in the data, battle UI and conflict mirror group:

1. separate Alpha text resolution from Zoomer without changing the existing Zoomer output;
2. apply only the frozen Alpha and Boomer data-copy decisions listed below;
3. route only the approved conflict fallback presentation through a five-profile resolver;
4. preserve every handler, economy rule, battle state transition, REP rule, point transfer, crowd rule, timer and argument rule;
5. restore byte parity for all three source/deployed mirror pairs, treating current source files as canonical for unchanged behavior.

Do not start Wave V dev-checks, boot, HTML or cache-bust work.

## Mandatory authorities

Before preflight, read fresh remote versions of:

- `AGENTS.override.md`
- `ORCHESTRATION.md`
- `AGENTS.md`
- `BRIDGE.md`
- `TASKS.md`
- `PROJECT_MEMORY.md`
- mailbox `STATE.md`
- this inbox and its immutable claim
- `.ai-bridge/decisions/STAGE6_WAVE2_COPY_FREEZE.md`
- `.ai-bridge/outbox/BRIDGE-20260707-031-02-codex.md`
- `.ai-bridge/outbox/BRIDGE-20260707-032-02-codex.md`
- `.ai-bridge/inbox/BRIDGE-20260708-035-07-chatgpt.md`
- all six target files from current `origin/main`

## Required model protocol

Evaluate all 12 model/reasoning pairs and recommend the cheapest reliable pair.

Return compact preflight only. End with exactly one fenced `CONTINUE` block and nothing after it.

No edits, tests, commits or pushes before the user selects the model and sends `CONTINUE` in the same Codex thread.

Under Protocol 3.0, that `CONTINUE` confirms both model selection and this exact frozen runtime scope. Do not request a second `APPROVE`.

## Baseline mirror evidence

Current source/deployed blobs differ in all three ownership groups:

- source `data.js`: `06427577465deef080e07551b9325794ff5f1a9b`
- deployed `docs/data.js`: `a5f236cbd85f1c5d9ebdd658e7889f474bb52a0b`
- source `ui-battles.js`: `9b11eeffeff3091c4933130d73d96843301f0b08`
- deployed `docs/ui/ui-battles.js`: `d528ff8fd802c016d0fd827d6683a576777aadac`
- source `conflict-core.js`: `b574cd7e1b451118ef4be025f78dd56395f5d247`
- deployed `docs/conflict/conflict-core.js`: `701fc501e47b9cfac5d8d33887246bc0c130c8d0`

Before changing copy, classify the existing drift. No approved source/deployed difference is known. Preserve the current source behavior and reconcile each deployed mirror to the final source byte-for-byte.

## A. Five-profile data resolution

Current `data.js` collapses Alpha into Zoomer. Replace that only as needed so text resolution distinguishes:

- `default`
- `millennial`
- `zoomer`
- `alpha`
- `boomer`

Required invariants:

- default and Millennial remain byte-for-byte equivalent in visible output;
- Zoomer retains the exact complete baseline object currently reached by both Zoomer and Alpha;
- Alpha receives its own immutable table based on that exact Zoomer baseline plus only the approved Alpha overrides below;
- Boomer retains its baseline table plus only the approved Boomer overrides below;
- unknown values fall back to default/Millennial behavior;
- `Data.getUiProfile()`, profile selection, birth-year selection and stored profile identity are not changed;
- `resolveUiTextMode` / `resolveUiTextProfileName` may be adjusted only to preserve five distinct presentation identities.

A safe implementation pattern is:

1. preserve the existing old `Data.TEXTS.alpha` object as the Zoomer table;
2. assign that preserved table to `Data.TEXTS.zoomer`;
3. create a new immutable Alpha table by spreading the preserved Zoomer table and applying only the approved Alpha overrides.

Do not rewrite unrelated profile copy.

## B. Exact Alpha data overrides

Apply exactly:

- `escape_button_label`: `Уйти: −{X}💰`
- `teach_sent_dm`: `{student}: {arg}. Цена {cost} 💰.`
- `teach_sent_chat`: `Аргумент: {teacher} → {student}.`
- `invite_open_hint`: `Указать имя`
- `invite_invalid`: `Игрок: нет`

Preserve placeholders, the Unicode minus, arrow and resource token exactly.

All other Alpha keys inherit the frozen Zoomer baseline unchanged in this wave.

## C. Exact Boomer data overrides

Apply exactly:

- `conflict_win`: `Победа`
- `conflict_loss`: `Поражение`
- `invite_invalid`: `Игрок отсутствует.`
- `escape_button_label`: `Выйти за {X} 💰.`
- `COP_TEMPLATES_BOOMER.cooldownReplies[7]`: `Сейчас занят. Вернусь позже.`

Do not change other Boomer entries.

The dynamic escape placeholder must remain `{X}`. Rendering with `X = 1` must produce the accepted target `Выйти за 1 💰.`.

## D. Battle UI contract

The existing escape button must continue to render through:

- semantic key `escape_button_label`;
- variable `{X}` sourced from the existing canonical escape-cost resolver;
- the existing escape handler and mode.

Do not hardcode a fixed cost. Do not change `oneCost`, escape cost calculation, `tryEscapeBattle`, the `off` action, influence gating, click handlers, toasts, battle rendering, rematch behavior or battle state.

`ui-battles.js` source code should remain semantically unchanged unless a minimal presentation-only adjustment is strictly required. Regardless, the final source and deployed mirror must be byte-identical.

## E. Conflict fallback presentation resolver

Add one stable immutable five-profile resolver for exactly these three semantic presentation keys:

- `battle.draw_fallback`
- `battle.escaped_fallback`
- `battle.ignored_fallback`

Exact matrix:

### default / Millennial / Zoomer / unknown

- `battle.draw_fallback`: `Толпа решает`
- `battle.escaped_fallback`: `Свалить`
- `battle.ignored_fallback`: `Отвали`

### Alpha

- `battle.draw_fallback`: preserve `Толпа решает`
- `battle.escaped_fallback`: `Уйти`
- `battle.ignored_fallback`: preserve `Отвали`

### Boomer

- `battle.draw_fallback`: `Решает голосование`
- `battle.escaped_fallback`: `Выйти`
- `battle.ignored_fallback`: `Вызов отклонён`

Route only the existing visible hardcoded fallback assignments through this resolver.

For places that currently use a sentence-style note with a trailing period and a bare result line, preserve that punctuation structure: resolver returns the bare target, note formatting may append the existing period, and result line remains the bare target.

Do not change semantic result values such as `draw`, `escaped`, `ignored`, `win`, `lose` or `resolved`.

Do not duplicate or replace the accepted Wave I `SystemCopy` battle-result routing. The existing system event route and `systemSay(...)` calls remain untouched.

## F. Mechanics and state freeze

Do not change:

- battle creation or target eligibility;
- attack/defense argument selection or canonical matching;
- escape, dismiss or rematch eligibility;
- escape/rematch cost calculations or price multipliers;
- points, REP, influence, moneyLog or transfer reasons;
- battle result semantics or winner/loser identity;
- crowd voting, NPC voting, caps, timers or synchronization;
- report flow;
- cop behavior except the exact Boomer cooldown copy row;
- persistence, save shape or state fields;
- DOM structure, event handlers or accessibility behavior;
- dev-checks, smoke registries, boot, exports or globals outside existing file-local helpers.

Do not add persistent state fields.

## Exact allowed changed paths

- `AsyncScene/Web/data.js`
- `docs/data.js`
- `AsyncScene/Web/ui/ui-battles.js`
- `docs/ui/ui-battles.js`
- `AsyncScene/Web/conflict/conflict-core.js`
- `docs/conflict/conflict-core.js`

Any other changed path is automatic FAIL.

## Required verification after `CONTINUE`

1. Prove current main parent is exactly `689ee3e9b50539268a509c49b5c377b403d44c58`.
2. Report the exact changed paths and explain any authorized source file that remained unchanged while its mirror was reconciled.
3. Prove all three source/deployed mirror pairs are byte-identical.
4. Run `node --check` on all six files.
5. Probe profile resolution for default, Millennial, Zoomer, Alpha, Boomer and unknown.
6. Prove the full Zoomer table/output is unchanged from baseline.
7. Prove default and Millennial output is unchanged.
8. Prove all five exact Alpha data overrides and all five exact Boomer data overrides.
9. Prove no other Alpha or Boomer data key changed.
10. Render the escape label for representative dynamic costs `1`, `2` and `3` under all five profiles; prove costs and handlers are unchanged.
11. Probe all three conflict fallback keys across all five profiles plus unknown.
12. Prove semantic battle result values and state transitions are unchanged.
13. Prove existing SystemCopy battle-result routing is untouched.
14. Prove report flow, vote logic, crowd logic, economy, REP, influence, timers and persistence are untouched.
15. Run relevant existing static/profile/battle smoke commands without editing dev-checks or boot.
16. Run `git diff --check`.
17. Refetch `origin/main` after push and report fresh remote blob SHAs for all six files.
18. PASS requires empty `failures`, `forbiddenRemaining`, `missingCoverage` and `failedChecks`.

## Publication

- Publish one direct-child implementation commit to `main` according to current policy.
- Publish one immutable outbox at the expected mailbox path.
- Do not edit mailbox STATE.
- On credentials failure, use Protocol 3.0 `BLOCKED_PUSH_AUTH` with the complete recovery bundle, including full UTF-8 contents for every changed text file and the complete outbox payload.
- If main publishes but outbox publication fails, do not reimplement. Report `MAIN_PUBLISHED_AWAITING_OUTBOX` plus the complete outbox recovery payload.

## Required outbox fields

Include:

- status;
- selected model and reasoning;
- 12/12 preflight summary;
- exact main parent and implementation commit;
- exact changed paths;
- six fresh post-push remote blob SHAs and mirror equality;
- baseline mirror-drift classification;
- complete profile resolution matrix;
- exact Alpha and Boomer override proof;
- unchanged default/Millennial/Zoomer proof;
- dynamic escape-label proof;
- conflict fallback matrix;
- mechanics/state/SystemCopy/report-flow preservation proof;
- commands and results;
- `failures`, `forbiddenRemaining`, `missingCoverage`, `failedChecks`;
- runtime classification;
- Safari status `N/A - later serialized deployment smoke`;
- mailbox parent and outbox commit, or complete recovery bundle;
- exact next action: return to ChatGPT and write `мост 1`.
