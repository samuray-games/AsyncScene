# Stage 6 Wave V-B - Step 4.4B execution under Protocol 3.2

BRIDGE_PROTOCOL: 3.2
THREAD_ID: BRIDGE-20260709-041
LANE_ID: S6-V5B-BOOMER-RUNTIME-AGGREGATE
TASK_ID: TASK-S6-PAR-V5B
EXECUTION_EPOCH: S6V5B-E3-20260709-1350JST
PHASE: EXECUTE_AND_PUBLISH
BRIDGE_SLOT: 1
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260709-041-claim-v4-codex.md
REMOTE_MAIN_BASELINE: cf2558d782d614d97bc66deec3a69017f5085d73
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-041-09-codex.md
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
SAFARI_STATUS: PENDING_USER
SUPERSEDES_INBOX: .ai-bridge/inbox/BRIDGE-20260709-041-06-chatgpt.md
SUPERSEDES_CLAIM: .ai-bridge/claims/BRIDGE-20260709-041-claim-v3-codex.md
SUPERSEDES_EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-041-05-codex.md
PROCESS_CORRECTION_CLOSURE: .ai-bridge/inbox/BRIDGE-20260709-042-12-chatgpt.md

## Frozen objective

Add one isolated Step 4.4B runtime aggregate smoke for the accepted Boomer economy and conflict terminology. Do not change product copy, economy, battle, NPC, persistence, state, routing, profile selection or gameplay behavior.

Public Safari command:

`Game.__DEV.smokeBoomerEconomyConflictTerminologyOnce()`

The smoke must cover Points / 💰, REP / ⭐, Influence, voting, majority/minority outcomes, rematch, NPC-vs-NPC conflict text, conflict results, DM, reports and report resolution.

Accepted static prerequisite remains `STATIC_PASS / READY_FOR_RUNTIME_SMOKE`: 147 PASS, 0 FAIL, 0 structural failures and 8 non-live rows.

## Exact write scope

Only these four primary paths may change:

- `AsyncScene/Web/dev/dev-checks.js`
- `docs/dev/dev-checks.js`
- `AsyncScene/Web/index.html`
- `docs/index.html`

All other repository paths are protected. Existing helper fixtures may be called but not edited. No `TASKS.md`, `PROJECT_MEMORY.md`, audit, lexicon, mapping, resolver, economy, battle, NPC, state, persistence or profile-routing file belongs to this commit.

## Frozen runtime contract

The aggregate must:

- install and export `Game.__DEV.smokeBoomerEconomyConflictTerminologyOnce` through the existing `Game.Dev` / `Game.__DEV` pattern;
- run only against the Boomer presentation path and verify default/unknown fallback remains non-Boomer;
- call existing pure helper fixtures where available and actual runtime resolvers where required;
- cover all frozen feature zones;
- verify placeholders, protected numbers and emoji;
- detect Millennial, Zoomer or Alpha leakage and accepted Boomer taboo phrases;
- preserve game state, points, REP, Influence, battle state, money log, persistence and local storage;
- perform no synthetic economy transfer and no gameplay action;
- include `location.href` and loaded dev-checks script URL deployment evidence;
- fail closed on missing helper, missing feature zone, stale build, export failure, state drift or mirror-target drift;
- return `ok:true` only when `failures`, `forbiddenRemaining`, `missingCoverage` and `failedChecks` are empty.

Required result fields: `ok`, `buildTag`, `commit`, `smokeVersion`, `profile`, `checkedCount`, `coveredFeatureZones`, `stateRestored`, `runtimeSourceUrl`, `pageUrl`, `failures`, `forbiddenRemaining`, `missingCoverage`, `failedChecks`.

Frozen identity:

- `buildTag`: `build_2026_07_09_step4_4b_boomer_runtime_aggregate`
- `commit`: `step4_4b_boomer_runtime_aggregate`
- `smokeVersion`: `boomer_step4_4b_runtime_aggregate_v20260709_001`
- cache-bust: `step4_4b_boomer_runtime_aggregate_20260709a`

The task-owned smoke block and exports must be byte-equivalent across both `dev-checks.js` copies. Both index files must be byte-identical and reference the frozen cache-bust. Preserve unrelated pre-existing differences and do not replace either large file wholesale.

## Protocol 3.2 execution

On the first exact `мост 1` in a fresh Codex conversation:

1. Fetch current main and mailbox and verify this epoch, inbox, claim, baseline and expected outbox.
2. Run scope-isolation classification. A frozen collision-free lane executes immediately. Do not stop for model preflight, `CONTINUE`, `APPROVE`, runtime confirmation or a separate runtime slot.
3. Return `BLOCKED_SCOPE_COLLISION` only for a real current overlap or dependency, with exact paths, owners and dependencies.
4. Implement only the four authorized paths.
5. Syntax-check both changed JavaScript files.
6. Prove the exact four-file changed-path set, mirrored aggregate/export parity, index byte parity and frozen cache-bust.
7. Run the existing Step 4.4A generator and validator without changing their outputs and run relevant non-Safari repository validators.
8. Create one direct-child primary commit, push main fast-forward, publish only `.ai-bridge/outbox/BRIDGE-20260709-041-09-codex.md` from a clean current mailbox worktree, and refetch both refs.
9. Return `PASS_PUSHED` with current identities, actual parent, exact changed paths, validations, recommended model/reasoning, actual selection status, and `Safari smoke status: PENDING_USER`.

Model recommendation is informational and must not become a stopping gate. No separate pull or push command applies.