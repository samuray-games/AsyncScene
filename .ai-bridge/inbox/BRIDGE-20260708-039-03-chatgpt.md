Use @asynchronia runtime-safety-gate.

# Bridge Slot 1 - connector payload completion

BRIDGE_PROTOCOL: 3.0
BRIDGE_SLOT: 1
THREAD_ID: BRIDGE-20260708-039
LANE_ID: S6-V5A2-BOOMER-RUNTIME-SURFACES
TASK_ID: TASK-S6-PAR-V5A2
PHASE: CONNECTOR_PAYLOAD_COMPLETION_NOW
PUBLICATION_MODE: CHATGPT_CONNECTOR_ONLY
REMOTE_MAIN_BASELINE: d15fe4dd34e8c431b02fb5a690982e38e6210fc5
MODEL_PREFLIGHT: ALREADY_COMPLETE
CONFIRMATION: ALREADY_SATISFIED
IMPLEMENTATION: ALREADY_COMPLETE
VALIDATION: ALREADY_COMPLETE

## Coordinator finding

The previous `READY_FOR_CONNECTOR_PUBLICATION` response is a valid execution report, but not a complete connector publication bundle.

It contains:

- changed paths;
- validation results;
- target blob SHA values;
- intended outbox;
- behavior summary.

It does not contain the full exact UTF-8 contents of the six changed files. The three reported blob SHAs are not present in the remote GitHub object database, so ChatGPT cannot reconstruct the commit from SHA alone.

## Required response

Do not rerun implementation, tests, preflight, CONTINUE, Git authentication, push, pull, merge, rebase or history operations.

Return exactly one payload beginning:

`CONNECTOR_FILE_BODIES_BEGIN`

and ending:

`CONNECTOR_FILE_BODIES_END`

Include the complete untruncated UTF-8 content of exactly these three unique files:

1. `AsyncScene/Web/ui/ui-core.js`
2. `AsyncScene/Web/ui/ui-dm.js`
3. `AsyncScene/Web/index.html`

For each file include:

- path;
- UTF-8 byte length;
- expected Git blob SHA;
- one fenced code block with the entire exact file and no ellipses.

Then state these mirror rules:

- `docs/ui/ui-core.js = BYTE_IDENTICAL_COPY_OF AsyncScene/Web/ui/ui-core.js`
- `docs/ui/ui-dm.js = BYTE_IDENTICAL_COPY_OF AsyncScene/Web/ui/ui-dm.js`
- `docs/index.html = BYTE_IDENTICAL_COPY_OF AsyncScene/Web/index.html`

Do not repeat the outbox or validation report. ChatGPT already has them.

Exact next action: paste the complete response into ChatGPT and write `мост 1`.
