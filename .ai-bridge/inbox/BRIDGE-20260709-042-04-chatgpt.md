# Correct runtime-gate removal publication from a clean current baseline

BRIDGE_PROTOCOL: 3.1
THREAD_ID: BRIDGE-20260709-042
LANE_ID: PROCESS-RUNTIME-GATE-REMOVAL
TASK_ID: TASK-PROCESS-RUNTIME-GATE-REMOVAL
EXECUTION_EPOCH: PRGR-E2-20260709-0346JST
PHASE: CORRECTION_REQUIRED
BRIDGE_SLOT: 1
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260709-042-claim-v2-codex.md
REMOTE_MAIN_BASELINE: c0e2f891076f3e8e280941edbe0e241d9931dd0f
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-042-05-codex.md
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
SAFARI_STATUS: N/A_PROCESS_ONLY
SUPERSEDES_INBOX: .ai-bridge/inbox/BRIDGE-20260709-042-01-chatgpt.md
SUPERSEDES_CLAIM: .ai-bridge/claims/BRIDGE-20260709-042-02-codex.md
SUPERSEDES_EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-042-03-codex.md

## Failure being corrected

The previous Codex execution created local commit `86632a4cc22d8552c4a30b72df52817db99a4de9` with parent `33ca1fb7ef716385acce2ecd73e7b591571149dc` instead of the required direct child of current remote baseline `c0e2f891076f3e8e280941edbe0e241d9931dd0f`. Its push was rejected non-fast-forward and no outbox was published.

That local commit, its parent, old task worktree, old claim and old expected outbox are superseded. They are not publication evidence and must not be reused as commit ancestry.

## Objective

Complete the unchanged process/plugin task from the current remote baseline. Remove the obsolete blocking runtime gate from active root policy and plugin execution paths, replace it with non-blocking scope isolation, validate, push main fast-forward, publish the exact current outbox and refetch both refs.

## Exact primary write scope

Only these primary paths may change:

1. `AGENTS.md`
2. `AGENTS.override.md`
3. `ORCHESTRATION.md`
4. `BRIDGE.md`
5. `PROCESS_ROOT_SYNC.md`
6. `tools/validate-orchestration-policy.py`
7. `.github/workflows/orchestration-policy.yml`
8. `STAGE6_PARALLEL_EXECUTION_PLAN.md`
9. `plugins/asynchronia/.codex-plugin/plugin.json`
10. `plugins/asynchronia/skills/runtime-safety-gate/SKILL.md` - delete
11. `plugins/asynchronia/skills/scope-isolation-check/SKILL.md` - create
12. `plugins/asynchronia/skills/task-router/SKILL.md`
13. `plugins/asynchronia/skills/parallel-scope-planner/SKILL.md`
14. `plugins/asynchronia/skills/canon-audit/SKILL.md`
15. `plugins/asynchronia/skills/mirror-audit/SKILL.md`
16. `plugins/asynchronia/skills/economy-invariant-audit/SKILL.md`
17. `plugins/asynchronia/skills/acceptance-pipeline-controller/SKILL.md`
18. `plugins/asynchronia/skills/failure-routing-and-corrective-loop/SKILL.md`

Do not touch game runtime, product copy, economy, battle, NPC, persistence, state, routing, deployment assets, `TASKS.md`, or `PROJECT_MEMORY.md`.

## Required correction procedure

On the first exact `мост 1` in a fresh Codex conversation:

1. Fetch `origin/main` and `origin/coordination/chatgpt-codex-bridge`.
2. Verify remote main is exactly `c0e2f891076f3e8e280941edbe0e241d9931dd0f` and read current STATE, this inbox and the current replacement claim.
3. Do not continue inside the old task worktree. Create a new clean task-owned worktree whose initial `HEAD` is exactly the fetched remote baseline.
4. Do not cherry-pick, merge, rebase, amend, reset onto, or publish local commit `86632a4cc22d8552c4a30b72df52817db99a4de9`. Its diff may be consulted only as non-authoritative reference after all changes are independently checked against this exact scope.
5. Recreate the authorized change in the clean worktree. Update root process markers coherently to Protocol 3.2 or one equivalent coherent version, retire the blocking gate, create `scope-isolation-check`, update plugin metadata and fail-closed validation, and preserve bridge publication, exact-scope checks, mirror parity, Safari ownership, no-op guards and accepted game canon.
6. Run at least:
   - `python3 tools/validate-orchestration-policy.py`
   - JSON validation for `plugins/asynchronia/.codex-plugin/plugin.json`
   - frontmatter/name validation for every changed skill
   - active repository search for removed blocking markers outside `.ai-bridge/**`
   - repository search proving `scope-isolation-check` is referenced by active router/planner/audits
   - exact changed-path verification
   - `git diff --check`
7. Before commit, prove the worktree began from the exact baseline and contains no unrelated paths.
8. Create one commit whose actual first parent is exactly `c0e2f891076f3e8e280941edbe0e241d9931dd0f`.
9. Fetch `origin/main` again immediately before push. If it remains the baseline, push the new commit fast-forward.
10. If a non-fast-forward rejection occurs because local ancestry is wrong, repair it within this same execution by discarding only the superseded task worktree, recreating a clean baseline worktree and recommitting. Do not send the user back to ChatGPT for a repair that Codex can perform locally.
11. If remote main genuinely moved, return only `BLOCKED_MAIN_BASELINE_MOVED` with fetched evidence. If authenticated publication still fails after one permitted authentication repair, return only `BLOCKED_PUSH_AUTH` with complete evidence. Do not append `return to ChatGPT and write мост 1` to a blocker.
12. After main is successfully pushed and refetched, publish only `.ai-bridge/outbox/BRIDGE-20260709-042-05-codex.md` from a separate clean mailbox worktree based on the latest fetched mailbox head.
13. Refetch both refs and return `PASS_PUSHED` with machine-derived primary SHA, actual parent, exact changed paths, validation results and `Safari smoke status: N/A_PROCESS_ONLY`.

No model preflight, runtime approval, continuation token, separate pull command, separate push command or extra bridge command applies.