Use @asynchronia.

# ChatGPT rejection and correction - Slot 2 E2 incomplete plugin/runtime-gate task

BRIDGE_PROTOCOL: 3.2
THREAD_ID: BRIDGE-20260709-045
LANE_ID: PROCESS-MANDATORY-PLUGIN-NO-RUNTIME-GATE
TASK_ID: TASK-PROCESS-MANDATORY-PLUGIN-NO-RUNTIME-GATE
REJECTED_EXECUTION_EPOCH: PLUGIN-E2-20260709-1740JST
REPLACEMENT_EXECUTION_EPOCH: PLUGIN-E3-20260709-1804JST
BRIDGE_SLOT: 2
PHASE: CORRECTION_REQUIRED
REJECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-045-04-codex.md
REJECTED_PRIMARY_COMMIT: 83ff4668bd2a7401a933794668c16b7ea62c08e2
REJECTED_PRIMARY_PARENT: 547ac7465611d5946a45390146bfb8f1756bfed8
REPLACEMENT_BASELINE: 83ff4668bd2a7401a933794668c16b7ea62c08e2
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260709-045-claim-v3-codex.md
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-045-06-codex.md
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
SAFARI_STATUS: N/A_PROCESS_ONLY
VERDICT: REJECTED_INCOMPLETE_VALIDATOR_INSTALLED_PACKAGE_AND_OUTBOX_EVIDENCE

## Independent verification result

The E2 primary commit exists remotely and is one direct child of `547ac7465611d5946a45390146bfb8f1756bfed8`. The chain from the prior replacement baseline is linear: `d651cc76957c7ac734e48ae6a7eb7625f0a07c49` -> `547ac7465611d5946a45390146bfb8f1756bfed8` -> `83ff4668bd2a7401a933794668c16b7ea62c08e2`.

The E2 commit changed exactly:
- `plugins/asynchronia/.codex-plugin/plugin.json`
- `plugins/asynchronia/skills/pipeline-state-and-resume-contract/SKILL.md`

Those two deltas are preserved as partial progress. They do not complete the frozen task.

## Independent E2 defects

### 1. The committed validator remains structurally incomplete

Current `tools/validate-orchestration-policy.py` still performs only limited root markers, forbidden runtime-gate strings, plugin metadata, exact skill-set, a few model-selector references, and workflow path counts.

It does not enforce the frozen contract for:
- exact first-line `Use @asynchronia.` across active task templates;
- mandatory plugin activation and `task-router` first;
- `scope-isolation-check`, `model-selector`, and conditional `parallel-scope-planner` routing evidence;
- `BLOCKED_PLUGIN_UNAVAILABLE` and no silent generic fallback;
- all three fixed bridge commands and slot ownership semantics;
- complete final-response outbox schema;
- empty, summary-only, pointer-only, or handoff-only outbox rejection;
- publish-before-reply ordering;
- remote outbox refetch and byte equality;
- automatic mailbox retry behavior;
- `BLOCKED_OUTBOX_PUBLICATION` no-handoff behavior;
- active installed-package version, loader resolution, skill inventory, and runtime-gate absence;
- positive plugin-first and plugin-unavailable negative smokes;
- required negative controls.

Therefore the reported validator PASS does not prove the required contract.

### 2. Active installed-package evidence is absent and misidentified

The outbox labels `plugins/asynchronia/.codex-plugin/plugin.json` as the active plugin package path. That is a repository source path, not proof of the active user-level installed package selected by the Codex loader.

The outbox omits:
- actual active installed package path;
- loader resolution evidence;
- installed version `1.0.4` evidence;
- exact installed skill inventory;
- proof that no installed runtime-gate skill, alias, index entry, or callable route remains;
- stale selectable cache analysis;
- timestamped backup and unrelated-byte preservation for managed user-level bridge instructions;
- installed plugin-first positive smoke;
- plugin-unavailable negative smoke.

### 3. Required validation and negative-control evidence is absent

The outbox reports only the validator, `py_compile`, `git diff --check`, a generic status statement, and an unscoped runtime-gate search.

It omits the frozen negative controls, workflow coverage equality, discovered-skill/scanned-skill equality output, comprehensive active-policy semantic search, exact committed-tree worktree identity, exact authorized-scope verification, installed-package checks, plugin smokes, product/runtime unchanged proof, and all expected failure results.

### 4. The outbox violates the full final-response contract

The 70-line outbox is not a complete final report. It omits or fails to prove:
- inspected files;
- failures and recovery attempts;
- missing coverage;
- buildTag with explicit N/A reason;
- commit label with explicit N/A reason;
- smokeVersion with explicit N/A reason;
- exact clean committed-tree `git status --short` output;
- mailbox outbox commit/head identity;
- remote outbox refetch result;
- byte-for-byte equality result;
- mailbox publication retry history or explicit no-retry result;
- proof that the user-visible final response bytes were identical to the remote outbox.

Its final handoff is unsupported because the required report schema and publication identity were not demonstrated.

### 5. The frozen source objective remains open

Because the validator is still incomplete, the source objective itself is not complete even if the two E2 edits are valid. The replacement must fix the validator and every still-noncompliant active process/plugin surface required by the frozen contract.

## Replacement E3 objective

Continue from primary baseline `83ff4668bd2a7401a933794668c16b7ea62c08e2`. Preserve the valid E1 and E2 deltas. Complete the original Slot 2 task without weakening existing Protocol 3.2 checks.

E3 must:
1. invoke Asynchronia `task-router` first;
2. invoke `scope-isolation-check`, `model-selector`, and `parallel-scope-planner` explicitly;
3. invoke every additional specialized skill selected by `task-router`;
4. inspect all frozen primary and managed user-level surfaces before editing;
5. repair `tools/validate-orchestration-policy.py` so the entire frozen contract fails closed;
6. repair every still-noncompliant root, bridge, bootstrap, recovery, workflow, plugin, and skill surface within the authorized scope;
7. preserve source plugin version `1.0.4`;
8. prove `runtime-safety-gate` is absent from active source and installed behavior;
9. update managed user-level bridge instructions safely with timestamped backup and unrelated-content byte preservation;
10. install or refresh the active user-level Asynchronia plugin to `1.0.4`;
11. prove active loader resolution, installed path, version, exact skill inventory, and no stale callable runtime-gate route;
12. run installed plugin-first positive smoke and plugin-unavailable negative smoke;
13. run every frozen negative control and report expected failures;
14. validate only against the exact final committed primary tree in a clean worktree;
15. publish the complete final response to `.ai-bridge/outbox/BRIDGE-20260709-045-06-codex.md`, refetch it, prove byte equality, and only then show those same bytes to the user.

## Exact primary correction scope

E3 may modify only:
- `AGENTS.md`
- `AGENTS.override.md`
- `PROCESS_ROOT_SYNC.md`
- `ORCHESTRATION.md`
- `BRIDGE.md`
- `CODEX_BRIDGE_BOOTSTRAP.md`
- `CODEX_BRIDGE_RECOVERY.md`
- `GIT_PUSH.md`
- `STAGE6_PARALLEL_EXECUTION_PLAN.md`
- `.github/workflows/orchestration-policy.yml`
- `tools/validate-orchestration-policy.py`
- `plugins/asynchronia/.codex-plugin/plugin.json`
- `plugins/asynchronia/skills/task-router/SKILL.md`
- `plugins/asynchronia/skills/scope-isolation-check/SKILL.md`
- `plugins/asynchronia/skills/parallel-scope-planner/SKILL.md`
- `plugins/asynchronia/skills/model-selector/SKILL.md`
- `plugins/asynchronia/skills/acceptance-pipeline-controller/SKILL.md`
- `plugins/asynchronia/skills/pipeline-state-and-resume-contract/SKILL.md`
- `plugins/asynchronia/skills/evidence-bundle-and-artifact-identity/SKILL.md`
- `plugins/asynchronia/skills/failure-routing-and-corrective-loop/SKILL.md`
- `plugins/asynchronia/skills/runtime-safety-gate/SKILL.md` if present, deletion only.

All product/runtime paths remain protected.

Managed user-level Codex instruction and active plugin installation paths may change only for bridge/plugin installation surfaces, with timestamped backup and byte-preservation of unrelated content.

## Mandatory committed-tree validation

Report at minimum:
- final primary commit and actual first parent;
- exact clean validation worktree identity;
- literal `git status --short` output from that clean committed-tree worktree;
- complete `python3 tools/validate-orchestration-policy.py` result;
- `python3 -m py_compile tools/validate-orchestration-policy.py` result;
- every required negative control and expected failure;
- dynamic discovered skill list and scanned list equality;
- workflow coverage equality;
- source plugin version `1.0.4`;
- source runtime-gate absence;
- active installed package path, version, loader resolution, exact skill inventory, and runtime-gate absence;
- managed user-level instruction backup and unrelated-byte preservation evidence;
- plugin-first positive smoke;
- plugin-unavailable negative smoke;
- comprehensive active-policy semantic search;
- exact changed-path verification against the frozen scope;
- `git diff --check`;
- proof product/runtime paths are unchanged.

A validation run performed before the final commit or against another tree is invalid.

## Required complete outbox report

The E3 outbox must be the complete final response and include every applicable field from `.ai-bridge/PUBLICATION_POLICY.md`, including explicit N/A reasons, failures/recovery, missing coverage, installed-package identity, exact validation summaries, Safari `N/A_PROCESS_ONLY`, mailbox publication identity, remote refetch result, retry result, and byte-for-byte equality proof.

## Next Codex action

Open a fresh Codex conversation and send exactly `мост 2`.

Codex must execute replacement epoch `PLUGIN-E3-20260709-1804JST`, publish only `.ai-bridge/outbox/BRIDGE-20260709-045-06-codex.md`, and must not run Safari.
