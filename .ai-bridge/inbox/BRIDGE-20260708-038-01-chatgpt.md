Use @asynchronia runtime-safety-gate.

# Bridge Slot 1 - Stage 6 Wave V-A Boomer static gate repair

BRIDGE_PROTOCOL: 3.0
ORCHESTRATION_VERSION: 3.0
BRIDGE_SLOT: 1
THREAD_ID: BRIDGE-20260708-038
LANE_ID: S6-V5A-BOOMER-STATIC-GATE
TASK_ID: TASK-S6-PAR-V5A
PHASE: MODEL_PREFLIGHT_ONLY
USER_COMMAND: мост 1
MAILBOX_PARENT_COMMIT: 2e1df65ee9f127e258cafd0de912a78e39bfc2ee
CURRENT_MAIN_BASELINE: 41ce4aa7ca6b9d287d47aee7d5a6384673054a36
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260708-038-02-codex.md
RUNTIME_CLASSIFICATION: NON_RUNTIME_STATIC_AUDIT_REPAIR
CONFIRMATION_RULE: SAME_THREAD_CONTINUE_CONFIRMS_MODEL_AND_EXACT_FROZEN_SCOPE
MODEL_SELECTION: PENDING_CODEX_PREFLIGHT_12_OF_12
SAFARI_RUNTIME_STATUS: PENDING_USER

## Atomic objective

Repair only the stale Boomer Step 4.4A deterministic audit contract so it recognizes the already accepted profile-aware battle fallback implementation, then regenerate and validate the two audit mirrors.

This task does not change production code and does not add runtime smoke.

## Exact write scope

Only:

1. `tools/generate-boomer-step4-4-economy-conflict-audit.py`
2. `UI_PROFILE_BOOMER_STEP_4_4_ECONOMY_CONFLICT_TERMINOLOGY_AUDIT.md`
3. `docs/UI_PROFILE_BOOMER_STEP_4_4_ECONOMY_CONFLICT_TERMINOLOGY_AUDIT.md`

Any other changed primary path is automatic FAIL.

Protected and forbidden in this task:

- both `conflict-core.js` files;
- both `dev/dev-checks.js` files;
- both `index.html` files;
- all runtime, UI, state, persistence, economy, battle, profile resolver and copy files;
- `TASKS.md` and `PROJECT_MEMORY.md`.

## Confirmed stale generator defect

The generator's three manual fallback rows still expect removed literal implementation snippets:

- `return conflictResultText("conflict_draw") || "Толпа решает";`
- `if (r === "escaped") return "Свалить";`
- `if (r === "ignored") return "Отвали";`

Current accepted production uses the immutable five-profile fallback table and exact semantic keys:

- `battle.draw_fallback`
- `battle.escaped_fallback`
- `battle.ignored_fallback`

The generator must audit that current architecture rather than forcing the old literals back into production.

## Required generator correction

For the three fallback manual surfaces:

1. keep stable semantic locators for draw, escaped and ignored fallback presentation;
2. describe the resolution path through `BATTLE_FALLBACK_TEXTS`, `battleFallbackText(...)`, and the active UI text profile resolver;
3. use the exact current Boomer visible values:
   - draw: `Решает голосование`
   - escaped: `Выйти`
   - ignored: `Вызов отклонён`
4. retain the corresponding default/Millennial source terms:
   - `Толпа решает`
   - `Свалить`
   - `Отвали`
5. replace fragile old-literal evidence with evidence proving both:
   - the exact Boomer registry entry for the semantic key;
   - at least one real production callsite using the same exact `battle.*` key;
6. do not weaken `ensure_evidence`, structural failure handling, row classification, STATIC_PASS rules or deterministic output checks;
7. do not exclude these three rows from live totals;
8. do not hardcode a PASS verdict or counts.

## Required execution after `CONTINUE`

- use a clean detached worktree at exact baseline `41ce4aa7ca6b9d287d47aee7d5a6384673054a36` so mailbox/untracked `.ai-bridge` files cannot contaminate changed-path validation;
- run the generator;
- regenerate both audit artifacts from the generator output;
- prove root/docs audit artifacts are byte-identical;
- run `python3 tools/validate-boomer-step4-4-economy-conflict-audit.py .`;
- run `python3 -m py_compile tools/generate-boomer-step4-4-economy-conflict-audit.py tools/validate-boomer-step4-4-economy-conflict-audit.py` without leaving tracked bytecode;
- run `git diff --check`;
- report exact changed paths;
- prove protected production blobs, especially both conflict-core mirrors, remain unchanged.

## Acceptance

Expected only if derived truth supports it:

- `auditStatus: STATIC_PASS / READY_FOR_RUNTIME_SMOKE`;
- `auditedRowCount: 147`;
- `failRowCount: 0`;
- `passRowCount: 147`;
- `structuralFailureCount: 0`;
- `nonLiveEvidenceCount: 8`;
- root/docs audit mirrors byte-identical;
- validator PASS;
- failures, forbiddenRemaining, missingCoverage and failedChecks empty.

If any other live row still fails after the generator repair:

- do not broaden the write scope;
- publish `FAIL_STATIC_REMAINDER`;
- list every remaining row ID, source locator, current text, accepted target, classification and smallest later correction scope.

## Required model protocol

Evaluate all 12 available model/reasoning pairs and recommend the cheapest reliable pair for this narrow deterministic Python/audit task.

Return compact preflight only. End with exactly one fenced `CONTINUE` block and nothing after it.

No edits, tests, commits or pushes before the user selects the model and sends `CONTINUE` in the same Codex thread. No second approval is required.

## Publication

- publish one direct-child primary commit from `41ce4aa7ca6b9d287d47aee7d5a6384673054a36` if and only if the task passes or produces a valid scoped static remainder;
- only the three authorized primary paths may change;
- publish immutable outbox `.ai-bridge/outbox/BRIDGE-20260708-038-02-codex.md`;
- do not edit mailbox STATE;
- report fresh `origin/main`, three changed blob SHAs, protected conflict-core blob SHAs, commands/results and complete failure arrays;
- exact next action after publication: return to ChatGPT and write `мост 1`.
