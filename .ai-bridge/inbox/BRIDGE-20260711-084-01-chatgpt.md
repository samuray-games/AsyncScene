Use @asynchronia.

# BRIDGE-20260711-084 - Fix3 publication recovery without fake plugin telemetry

ISSUE: 216
CYCLE: CYCLE-20260711-003
GENERATION: 5
SLOT: 2
THREAD_ID: BRIDGE-20260711-084
LANE_ID: S6-BOOMER-STEP4_4B-PUBLICATION-RECOVERY
TASK_ID: TASK-S6-BOOMER-STEP4_4B-FIX3-PUBLISH2
EXECUTION_EPOCH: S6-BOOMER-4_4B-FIX3-PUB-R2-20260711-1037JST
TASK_NONCE: S6B44B-084-PUB2-1037
PHASE: RECOVER_VALIDATE_AND_PUBLISH
COORDINATOR_MEMORY_REVISION: 2026-07-11-1037-JST
AUTHORIZED_PRIMARY_BASELINE: c5ef0ecea5c96a54604e6d130207ac4fd52789df
REPORTED_LOCAL_COMMIT: 0f7ecd69fb5857dbb965e6b4ee57dc2737f16f29
SUPERSEDES_THREAD: BRIDGE-20260711-083
PREVIOUS_ATTEMPT_STATUS: INVALID_BLOCKED_PLUGIN_UNAVAILABLE
CLAIM_PATH: .ai-bridge/claims/BRIDGE-20260711-084-claim-v1-codex.md
EXPECTED_OUTBOX_PATH: .ai-bridge/outbox/BRIDGE-20260711-084-02-codex.md
EXPECTED_RECEIPT_PATH: .ai-bridge/receipts/BRIDGE-20260711-084-03-codex.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
PLUGIN_IDENTIFIER: asynchronia
PLUGIN_CONTEXT_MODE: USER_ATTACHED_OR_REPOSITORY_FALLBACK
PLUGIN_INVOCATION_REQUIRED: false
PLUGIN_TOOL_TELEMETRY_REQUIRED: false
SKILL_APPLICATION_EVIDENCE_REQUIRED: true
VALIDATOR_APPLICABILITY_GATE: REQUIRED
RUNTIME_CLASSIFICATION: RUNTIME_SMOKE_INFRASTRUCTURE
SAFARI_STATUS: PENDING_USER
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true
MODEL_PREFLIGHT_GATE: RETIRED
MODEL_RECOMMENDATION_OWNER: ASYNCHRONIA_MODEL_SELECTOR
ACTUAL_MODEL_STATUS: USER_SELECTED_UNVERIFIED

## Root-cause correction

The prior `BLOCKED_PLUGIN_UNAVAILABLE` result is invalid and superseded.

Primary repository authority proves:

- `plugins/asynchronia/.codex-plugin/plugin.json` exposes `skills: ./skills/` and declares no callable tools or tool server;
- `CODEX_BRIDGE_BOOTSTRAP.md` requires skill resolution from installed cache or repository fallback and states that plugin-loader telemetry is never required;
- the user manually attached Asynchronia in the Codex interface beside `мост 2`;
- repository fallback skill files are present and readable.

Therefore absence of a separately callable Asynchronia tool is expected, not a blocker. Do not return `BLOCKED_PLUGIN_UNAVAILABLE` for this lane.

The plugin package currently has a non-gating version discrepancy: plugin manifest `1.0.5`, marketplace declaration `1.0.0`. Record it as maintenance evidence only. Do not edit plugin or marketplace files in this lane.

## Atomic objective

Publish the already-created exact-four-path Fix3 commit, or reconstruct its exact four file blobs as one direct child of the authorized baseline only if its parent is wrong. Run all task-applicable validations from a clean committed tree, push `main` using an explicit non-force SHA refspec, then publish immutable outbox and receipt evidence.

Do not rewrite valid source work merely because previous bridge metadata was defective.

## Skill resolution and application

Resolve the Asynchronia skills from the user-attached plugin context when available and from repository fallback under `plugins/asynchronia/skills/**` as authoritative fallback.

Apply these skills in this dependency order:

1. task-router
2. scope-isolation-check
3. model-selector
4. parallel-scope-planner
5. closed-loop-controller
6. failure-routing-and-corrective-loop
7. mirror-audit
8. deployment-verifier
9. smoke-orchestrator
10. acceptance-evidence-gate

Do not look for a callable `asynchronia` tool. Do not claim plugin invocation telemetry.

Both outbox and receipt must contain `skillApplicationEvidence` with:

- `pluginIdentifier: asynchronia`;
- `resolutionMode`: `USER_ATTACHED_PLUGIN`, `REPOSITORY_FALLBACK`, or `ATTACHED_PLUS_FALLBACK`;
- `pluginManifestPath: plugins/asynchronia/.codex-plugin/plugin.json`;
- `pluginManifestVersion: 1.0.5`;
- `actualModelStatus: USER_SELECTED_UNVERIFIED`;
- one exact ordered entry per required skill containing `skill`, `sourcePath`, `status: APPLIED`, non-empty `materialResult`, and concrete `evidenceRefs`.

Evidence must describe decisions actually used. It must not pretend that a tool call occurred.

## Frozen source scope

Exactly these four paths may differ from the baseline:

- AsyncScene/Web/dev/dev-checks.js
- AsyncScene/Web/index.html
- docs/dev/dev-checks.js
- docs/index.html

No other primary path may change.

## Local commit provenance

Inspect local commit `0f7ecd69fb5857dbb965e6b4ee57dc2737f16f29`.

The previous Codex report states that it:

- exists locally;
- has sole parent `c5ef0ecea5c96a54604e6d130207ac4fd52789df`;
- changes exactly the frozen four paths;
- passes clean-tree generator, validator, both node syntax checks, and diff-check.

Reverify these facts locally. If true, retain and publish the exact commit. Do not manufacture a replacement SHA.

Only if the parent or path proof fails may you create a new detached task-owned worktree at the authorized baseline and copy only the four file blobs from the reported local commit. Rebase, cherry-pick, merge, amend, reset, stash, clean, and force-push are forbidden.

## Required clean-tree validations

Run from the final committed tree with empty `git status --short`:

- `python3 tools/generate-boomer-step4-4-economy-conflict-audit.py .`;
- `python3 tools/validate-boomer-step4-4-economy-conflict-audit.py .`;
- `node --check AsyncScene/Web/dev/dev-checks.js`;
- `node --check docs/dev/dev-checks.js`;
- exact sole-parent proof;
- exact four-path proof from baseline to final commit;
- protected-path proof;
- source/docs semantic mirror verification with intentional docs Alpha differences preserved;
- served entrypoint, Fix3 identity, and Fix3 cache-bust verification for both index files;
- adversarial fixture evidence for every new evaluator family;
- restoration evidence for profile/text mode, points, REP, Influence, battle state, moneyLog, persistence, and relevant localStorage keys;
- `git diff --check c5ef0ecea5c96a54604e6d130207ac4fd52789df...<FINAL_PRIMARY_SHA>`.

The static audit must remain `STATIC_PASS / READY_FOR_RUNTIME_SMOKE`, 147 PASS, 0 FAIL, 0 structural failures, and 8 non-live rows.

`tools/validate-boomer-step4-4-economy-conflict-audit.py` is required and must PASS from the clean committed tree.

## Excluded validator evidence

Only `tools/validate-orchestration-policy.py` is excluded:

- validator: `tools/validate-orchestration-policy.py`
- status: `NOT_APPLICABLE`
- pinnedIdentity: `BRIDGE-20260710-062 / CLOSED-LOOP-CLOUD-PR-R1-20260710-1348JST / 32513f02daf5943c41f24328e1ae251d6bc85ccc`
- activeIdentityMismatch: `BRIDGE-20260711-084 / S6-BOOMER-4_4B-FIX3-PUB-R2-20260711-1037JST / c5ef0ecea5c96a54604e6d130207ac4fd52789df`
- reason: historical Bridge 062 process scope and historical-base repository diff are unrelated to the current four-path runtime lane
- replacementControls: every required clean-tree validation listed above

## Explicit primary publication

Immediately before pushing:

- fetch `origin/main`;
- prove `git ls-remote origin refs/heads/main` equals the authorized baseline;
- prove the final commit has that baseline as its sole parent;
- prove the baseline is an ancestor of the final commit.

Push without force using:

`git push origin <FINAL_PRIMARY_SHA>:refs/heads/main`

Do not rely on branch name or upstream configuration.

If rejected, capture exact stderr and immediately refetch `refs/heads/main`. Return only a real publication blocker. Lack of a callable plugin tool is not a blocker.

## Mailbox publication

After successful source push:

- refetch remote main and prove exact SHA, parent, and four paths;
- publish only `.ai-bridge/outbox/BRIDGE-20260711-084-02-codex.md` as a fast-forward child of freshly fetched mailbox head;
- refetch it;
- publish only `.ai-bridge/receipts/BRIDGE-20260711-084-03-codex.md` as a separate fast-forward child;
- refetch mailbox and return bytes identical to the remote receipt;
- preserve `validationResults`, `excludedValidationEvidence`, and `skillApplicationEvidence` exactly;
- put receipt-only checks in `publicationValidationResults`;
- do not edit `.ai-bridge/STATE.md`.

## Acceptance boundary

Codex must not claim Safari PASS. Safari remains `PENDING_USER` until ChatGPT independently verifies remote source, scope, validations, skill application evidence, outbox, receipt, and deployed identity.

Terminal success action code: `OPEN_FRESH_CHATGPT_VERIFIER_AND_SEND_SAME_BRIDGE_COMMAND`.
