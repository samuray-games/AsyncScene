Use @asynchronia.

# Complete the closed-loop implementation after rejected epoch 046

BRIDGE_PROTOCOL: 3.3
CYCLE_ID: CYCLE-20260709-001
CYCLE_GENERATION: 2
THREAD_ID: BRIDGE-20260709-047
LANE_ID: PROCESS-CLOSED-LOOP-ORCHESTRATION
TASK_ID: TASK-PROCESS-CLOSED-LOOP-IMPLEMENTATION-CORRECTION-R1
EXECUTION_EPOCH: CLOSED-LOOP-R1-E1-20260709-1932JST
TASK_NONCE: CLV1-047-R1-CA2A-362B77
BRIDGE_SLOT: 2
PHASE: CORRECTION_REQUIRED
BASELINE: ca2a3f88ac00a7e4fae47459758e7b09099a3f41
COORDINATOR_MEMORY_REV: 2026-07-09-1932-JST
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260709-047-claim-v1-codex.md
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-047-02-codex.md
PUBLICATION_POLICY: .ai-bridge/PUBLICATION_POLICY_CLOSED_LOOP_V1.md
CLOSED_LOOP_PROTOCOL: .ai-bridge/CLOSED_LOOP_PROTOCOL.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
PLUGIN_BOOTSTRAP_FALLBACK: AUTHORIZED
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true
SAFARI_STATUS: N/A_PROCESS_ONLY
SUPERSEDES_THREAD: BRIDGE-20260709-046
SUPERSEDES_EPOCH: CLOSED-LOOP-E1-20260709-1930JST

## Verifier verdict for epoch 046

Epoch 046 is rejected as `REJECTED_INCOMPLETE_PRIMARY_MISSING_OUTBOX_AND_MEMORY_IDENTITY_MISMATCH`.

Fresh remote evidence:

- mailbox STATE referenced coordinator memory revision `2026-07-09-1945-JST`;
- the activated epoch 046 inbox and claim both contained stale coordinator memory revision `2026-07-09-1918-JST`;
- expected outbox `.ai-bridge/outbox/BRIDGE-20260709-046-02-codex.md` is absent remotely;
- primary main advanced from `83ff4668bd2a7401a933794668c16b7ea62c08e2` to `ca2a3f88ac00a7e4fae47459758e7b09099a3f41`;
- that commit changed only `.github/workflows/orchestration-policy.yml`, `CODEX_BRIDGE_BOOTSTRAP.md`, `CODEX_BRIDGE_RECOVERY.md`, and `tools/validate-orchestration-policy.py`;
- root `CLOSED_LOOP_PROTOCOL.md` is absent;
- plugin manifest remains version `1.0.4`;
- `plugins/asynchronia/skills/closed-loop-controller/SKILL.md` is absent;
- `tools/closed_loop_contract.py` is absent.

Do not resume or reuse the epoch 046 Codex conversation. Do not reset, amend, revert, or rewrite commit `ca2a3f88ac00a7e4fae47459758e7b09099a3f41`. Treat it as the immutable current baseline and complete the required implementation in one fresh correction conversation.

## Absolute priority

The completed resumable ChatGPT-Codex cycle remains the highest-priority project task. Product work and Safari remain suspended.

This correction task incorporates the complete objective of epoch 046 plus the verifier defects above. One task instance equals one fresh Codex conversation. The conversation expires after its final response.

## Required startup and plugin route

Freshly fetch `origin/main` and `coordination/chatgpt-codex-bridge`, then read remote STATE and only this inbox and its named claim. Verify cycle, generation, thread, task, epoch, nonce, slot, baseline, coordinator memory revision, and expected outbox before any work.

Invoke, in order:

1. `task-router`;
2. `scope-isolation-check`;
3. `model-selector`;
4. `parallel-scope-planner` because this task owns shared root process, plugin, validator, workflow, documentation, installation and recovery surfaces;
5. `closed-loop-controller` from governed source after creating or staging it when necessary;
6. `failure-routing-and-corrective-loop`;
7. every additional skill selected by `task-router`.

The installed plugin may be older than the required final package. This task explicitly authorizes governed `SOURCE_PLUGIN_FALLBACK_BOOTSTRAP` from the validated baseline source plugin, then requires building and installing final Asynchronia plugin version `1.0.5` as a sibling version while preserving older versions and unrelated user content.

## Implementation objective

Complete one deterministic fresh-thread ChatGPT-Codex closed loop that enforces:

- one atomic execution epoch per fresh Codex conversation;
- one independent outbox verification per fresh ChatGPT conversation;
- conversation history never being authoritative state;
- unique cycle, generation, thread, task, epoch and nonce;
- inbox and claim publication and remote verification before STATE activation;
- STATE-last activation;
- live memory synchronization and refetch before Codex handoff;
- Codex remote-state fetch before routing;
- complete immutable outbox publication, remote refetch and byte equality before handoff;
- rejected work becoming a new task instance rather than old-chat continuation;
- missing outbox becoming correction, report recovery or publication recovery after primary inspection;
- implementation acceptance automatically creating a separate canary task;
- product work remaining blocked until implementation and canary are independently accepted.

## Required primary design

- Create root `CLOSED_LOOP_PROTOCOL.md` matching the active mailbox protocol and make it authoritative from AGENTS, ORCHESTRATION, BRIDGE, bootstrap and recovery.
- Create plugin skill `closed-loop-controller` validating task identity, legal transition, fresh-chat isolation, memory readiness, publication ordering, failure classification, recovery choice and canary gating.
- Bump the plugin manifest to version `1.0.5` and install the exact validated package as a sibling under the discovered user-level Asynchronia package root.
- Create a machine-readable Python closed-loop contract and deterministic tests.
- Make `tools/validate-orchestration-policy.py` call or import the contract instead of duplicating drifting string checks.
- Preserve and validate the useful partial delta already present at baseline `ca2a3f88ac00a7e4fae47459758e7b09099a3f41`.

## Required state transitions

Recognize only:

- CLOSED
- PREPARING
- READY_FOR_CODEX
- EXECUTING
- PRIMARY_PUBLISHED
- OUTBOX_PUBLISHING
- AWAITING_CHATGPT_VERIFICATION
- ACCEPTED
- CORRECTION_REQUIRED
- RECOVERY_REQUIRED
- BLOCKED_EXTERNAL
- SUPERSEDED

Illegal transitions fail closed. Unreferenced artifacts are inert. STATE referencing missing artifacts requires recovery. `MEMORY_SYNC_PENDING` forbids execution.

## Automatic repair rules

Before terminal output, perform bounded repair for:

- stale remote state through refetch and identity restart;
- main or mailbox non-fast-forward through refetch and clean worktree recreation;
- moved main with disjoint writes and stable reads through safe patch reapplication and full revalidation;
- moved main with overlap or changed dependency through complete blocker evidence;
- dirty worktree through a new task-owned worktree without stash, reset or clean of unrelated work;
- stale plugin during plugin repair through pinned source fallback and atomic sibling installation;
- validation failure inside scope through diagnosis and up to three distinct repair passes per root cause;
- transient connector, fetch or push failure through up to three retries with remote identity revalidation;
- outbox absence or byte mismatch through preservation of immutable response bytes and up to three clean republication attempts.

When repair is impossible, publish a complete blocker outbox whenever mailbox publication works. If mailbox publication itself remains impossible, return `BLOCKED_OUTBOX_PUBLICATION`, preserve the complete report locally and do not instruct a ChatGPT handoff.

## Required failure coverage

Deterministic controls must cover at least:

1. memory unavailable;
2. memory stale against primary sources;
3. memory sync pending;
4. mailbox STATE split-brain;
5. STATE referencing missing inbox;
6. STATE referencing missing claim;
7. orphan inbox or claim;
8. duplicate task identity or nonce;
9. wrong bridge slot;
10. stale thread, epoch or nonce;
11. previous conversation identity reuse;
12. claim collision;
13. plugin missing on ordinary task;
14. source fallback used without authorization;
15. invalid source plugin manifest;
16. missing task-router or required skill;
17. reintroduced runtime-safety-gate;
18. moved main with safe disjoint reapply;
19. moved main with overlap;
20. dirty unrelated worktree;
21. source change outside frozen scope;
22. validator claim not reproducible on committed tree;
23. false no-delta inference;
24. failing tests requiring outside scope;
25. primary push non-fast-forward;
26. mailbox push non-fast-forward;
27. empty, partial or handoff-only outbox;
28. wrong outbox identity or nonce;
29. historical outbox reuse;
30. absent remote outbox after local success;
31. remote byte mismatch;
32. missing retry evidence;
33. missing remote refetch evidence;
34. primary commit missing, wrong parent or outside main ancestry;
35. main advanced after a valid task commit;
36. duplicate bridge command after closure;
37. missing Safari evidence when required;
38. product task attempted before canary acceptance;
39. external auth, permission or service outage;
40. report-recovery and publication-recovery selection;
41. coordinator memory revision mismatch among memory, STATE, inbox and claim;
42. partial primary publication with missing required artifacts.

Positive controls must prove legal coordinator, Codex, verifier, correction, report-recovery, publication-recovery and canary transitions.

## Exact primary scope

Only these primary paths may change or be created:

- `CLOSED_LOOP_PROTOCOL.md`
- `AGENTS.md`
- `AGENTS.override.md`
- `PROCESS_ROOT_SYNC.md`
- `ORCHESTRATION.md`
- `BRIDGE.md`
- `CODEX_BRIDGE_BOOTSTRAP.md`
- `CODEX_BRIDGE_RECOVERY.md`
- `GIT_PUSH.md`
- `STAGE6_PARALLEL_EXECUTION_PLAN.md`
- `PROJECT_MEMORY.md`
- `TASKS.md`
- `.github/workflows/orchestration-policy.yml`
- `tools/validate-orchestration-policy.py`
- `tools/closed_loop_contract.py`
- `tools/test_closed_loop_contract.py`
- `plugins/asynchronia/.codex-plugin/plugin.json`
- `plugins/asynchronia/skills/closed-loop-controller/SKILL.md`
- `plugins/asynchronia/skills/task-router/SKILL.md`
- `plugins/asynchronia/skills/scope-isolation-check/SKILL.md`
- `plugins/asynchronia/skills/parallel-scope-planner/SKILL.md`
- `plugins/asynchronia/skills/model-selector/SKILL.md`
- `plugins/asynchronia/skills/acceptance-pipeline-controller/SKILL.md`
- `plugins/asynchronia/skills/pipeline-state-and-resume-contract/SKILL.md`
- `plugins/asynchronia/skills/evidence-bundle-and-artifact-identity/SKILL.md`
- `plugins/asynchronia/skills/failure-routing-and-corrective-loop/SKILL.md`
- `plugins/asynchronia/skills/runtime-safety-gate/SKILL.md` if present, deletion only.

All product and runtime paths are protected.

Managed user-level Codex instruction and plugin installation paths may change only for the closed-loop managed block and versioned Asynchronia `1.0.5` installation, with backup, permission preservation and unrelated-byte preservation.

## Validation required on the exact final committed tree

Report and prove:

- final commit and actual first parent;
- clean worktree status;
- Python compile for all changed Python files;
- full orchestration validator;
- full closed-loop unit test suite;
- all positive and negative controls;
- dynamic discovered skill inventory and workflow coverage equality;
- plugin manifest `1.0.5` and frontmatter validation;
- source and installed runtime-gate absence;
- installed `1.0.5` path, inventory and source checksum equality;
- plugin-first positive smoke and unavailable-plugin negative smoke;
- exact changed paths versus frozen scope;
- protected product/runtime blob equality;
- `git diff --check`.

Do not use validation performed before the final commit as completion evidence.

## Publication and acceptance

Publish one direct-child exact-scope correction commit on baseline `ca2a3f88ac00a7e4fae47459758e7b09099a3f41`, or safely rebuild on a newer disjoint main only under the publication policy.

Prepare one complete immutable final response and publish the exact bytes only to `.ai-bridge/outbox/BRIDGE-20260709-047-02-codex.md`. Refetch remote mailbox and prove report schema and byte equality before showing the same response.

The exact next instruction after verified outbox publication is: open a fresh ChatGPT conversation and send `мост 2`.

Do not run Safari. This correction task does not itself declare the closed loop complete. After independent acceptance, ChatGPT must create a separate canary task with a fresh identity.