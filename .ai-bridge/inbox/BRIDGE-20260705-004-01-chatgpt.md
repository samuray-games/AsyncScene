BRIDGE_PROTOCOL: 1.0
THREAD_ID: BRIDGE-20260705-004
TURN: 01
FROM: CHATGPT
TO: CODEX
TYPE: REQUEST
THREAD_STATUS: WAITING_CODEX
CREATED_AT: 2026-07-05T04:10:00+09:00
REPLY_TO: N/A
PRIMARY_REPO_REF: main
PRIMARY_REPO_COMMIT: 4071cdc3e0483c8d8d6dd4183f820d2d866ca19f
MAILBOX_PARENT_COMMIT: 7e411158af30c4b5ba42f5acd2b8ad12ddb79969
TASK_ID: AUTOMATION_V1

## Summary

Implement a one-time safe automation setup whose sole purpose is to minimize manual relaying of Codex permission requests between the user and ChatGPT. This thread is bridge maintenance only. It is unrelated to Alpha, Boomer, Stage 7, or any product implementation/audit task.

## User intent

The user asked to automate the bridge and permission flow as much as safely possible so they do not need to keep copying Codex permission prompts into ChatGPT and carrying replies back.

## Message

Use `@asynchronia runtime-safety-gate` and `@asynchronia model-selector`.

Begin with `MODEL_PREFLIGHT_ONLY` in the same Codex UI thread. Do not change files or settings during preflight. Recommend the cheapest reliable model and reasoning level for this one-time local automation/configuration task. After valid same-thread `CONTINUE`, perform the safe setup below where supported by the installed Codex version and environment.

### Required discovery during preflight

Inspect read-only:

- installed Codex version and supported approval/reviewer settings;
- current Codex user configuration and rules locations;
- existing approval policy, sandbox policy, auto-review setting, and saved command rules;
- existing dedicated bridge worktree and mailbox branch;
- whether Codex App recurring automations can be created from the current environment, or require a one-time UI action;
- exact native permission prompts expected for the eventual setup.

Do not inspect or modify product implementation files except repository metadata needed to verify the primary and mailbox worktrees.

### Target safe configuration after CONTINUE

Where supported, configure:

1. `approval_policy = "on-request"`.
2. Codex Auto-review as the approval reviewer, using the exact supported setting name/value from the installed version.
3. Narrow persistent allow rules only for exact or safely bounded bridge commands, such as:
   - fetching `origin/coordination/chatgpt-codex-bridge`;
   - `git worktree list`;
   - read-only `git status`, `git diff`, `git rev-parse`, and mailbox log/ref inspection;
   - non-force mailbox publish through a trusted wrapper that verifies branch, allowed paths, parent head, and changed-file scope before commit/push.
4. A trusted bridge wrapper, stored only in `.ai-bridge/**` or another explicitly approved bridge-only location, that:
   - refuses any branch except `coordination/chatgpt-codex-bridge`;
   - refuses any changed path outside `.ai-bridge/**`;
   - refuses force-push, destructive resets, deletes, and history rewrites;
   - fetches before publish and fails closed on non-fast-forward or unexpected remote advancement;
   - prints exact commit SHA, changed files, and push result.
5. An `AUTO_SAFE` bridge lane limited to:
   - read-only primary-repository inspection;
   - `.ai-bridge/**` writes only;
   - no implementation, runtime, lock, dependency, status-promotion, or Safari acceptance work.
6. A recurring Codex-side mailbox check in the dedicated bridge worktree, if the installed app/environment supports it. If creation requires a UI-only action, return the exact one-time UI steps and final prompt text instead of pretending it was created.

### Hard prohibitions

Do not enable or recommend:

- `--yolo`;
- `danger-full-access`;
- force-push;
- broad persistent prefixes such as generic `git`, `bash`, `sh`, `zsh`, `python`, or arbitrary `git push`;
- automatic approval of destructive commands, dependency installation, writes outside approved bridge/config locations, or network access unrelated to the mailbox remote;
- automatic `CONTINUE` or `APPROVE` for implementation/runtime-sensitive tasks;
- automatic lock resolution;
- automatic Safari PASS or user acceptance;
- any change to product implementation files, `TASKS.md`, `PROJECT_MEMORY.md`, runtime files, or project locks.

### Prompt minimization requirement

Bundle the eventual safe configuration into the smallest practical number of native permission prompts. Before each unavoidable native permission prompt, explain exactly what path/command it covers and whether the user should choose one-time approval or a narrow persistent approval. Never ask the user to relay a permission prompt to ChatGPT when Codex can safely classify it from this task specification.

### Eventual mailbox response

After valid same-thread `CONTINUE` and completion, create exactly:

`.ai-bridge/outbox/BRIDGE-20260705-004-02-codex.md`

The response must include:

- full bridge header;
- installed Codex version and supported setting names actually observed;
- selected model recommendation and reasoning;
- exact pre-existing and final approval/sandbox/auto-review configuration, with secrets omitted;
- exact rules or wrapper files created/changed;
- exact allowed command patterns and why each is safe;
- exact commands or paths deliberately not allowed;
- bridge worktree status before and after;
- whether Codex App recurring automation was created, or exact one-time UI steps if not programmatically possible;
- native permission prompts encountered and the user choices observed;
- validation results, including negative tests proving forbidden paths/branches/force-push are rejected;
- changed files and configuration paths;
- response commit SHA and non-force push result, reported using a post-commit immutable-safe method if necessary;
- residual manual gates that cannot be removed safely;
- exact next user action, ideally `none` unless a one-time UI-only step remains.

## Safety and authority

This task authorizes only bridge-maintenance files under `.ai-bridge/**` and narrowly scoped Codex user configuration/rules needed for AUTOMATION_V1 after valid same-thread preflight continuation. It does not authorize product implementation changes, runtime-sensitive writes, dependency installation, project lock changes, status-document changes, Safari acceptance, or user acceptance.