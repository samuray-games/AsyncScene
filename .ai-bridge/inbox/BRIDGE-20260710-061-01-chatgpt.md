# Recover Codex Cloud repository remote and complete the exact semantic correction

BRIDGE_PROTOCOL: 3.3
CYCLE_ID: CYCLE-20260709-001
CYCLE_GENERATION: 16
THREAD_ID: BRIDGE-20260710-061
LANE_ID: PROCESS-CLOSED-LOOP-CLOUD-REMOTE-RECOVERY
TASK_ID: TASK-PROCESS-CLOSED-LOOP-CORE-COMPLETION
EXECUTION_EPOCH: CLOSED-LOOP-CLOUD-REMOTE-R1-20260710-1339JST
TASK_NONCE: CLV1-061-CLOUD-3251-1339
BRIDGE_SLOT: 3
PHASE: RECOVERY_REQUIRED
BASELINE: 32513f02daf5943c41f24328e1ae251d6bc85ccc
COORDINATOR_MEMORY_REV: 2026-07-10-1339-JST
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260710-061-claim-v1-codex.md
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260710-061-02-codex.md
EXPECTED_RECEIPT: .ai-bridge/receipts/BRIDGE-20260710-061-03-codex.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
CLOUD_REPOSITORY: samuray-games/AsyncScene
ORIGIN_URL: https://github.com/samuray-games/AsyncScene.git
REMOTE_REPAIR_ALLOWED: true
PLUGIN_DELIVERY_LANE: SEPARATE_EXTERNAL_NON_GATING
PLUGIN_INVOCATION_REQUIRED: false
PLUGIN_SOURCE_CHANGES_AUTHORIZED: false
SAFARI_STATUS: N/A_PROCESS_ONLY
SUPERSEDES_THREAD: BRIDGE-20260710-060

## Independent verdict on thread 060

`BLOCKED_EXTERNAL_CLOUD_CHECKOUT_WITHOUT_ORIGIN`.

The cloud task reported that `git fetch origin main coordination/chatgpt-codex-bridge` failed because the checkout had no configured `origin`. It made no authorized source delta, created no commit, and published no mailbox evidence.

Fresh independent remote verification after that response proved:

- remote `main` remains exactly `32513f02daf5943c41f24328e1ae251d6bc85ccc`;
- remote `coordination/chatgpt-codex-bridge` remains exactly `11eddd74a5692bc3eaa53074d347f5cb65a5ef16` before preparation of this thread;
- thread 060 produced no remote primary commit, outbox, or receipt.

Thread 060 is consumed and superseded. Its generic checkout and pre-existing unrelated `node_modules` changes are not authority and must not be reused.

## Mandatory cloud repository bootstrap

This task must run in the Codex Cloud environment connected to GitHub repository `samuray-games/AsyncScene`.

Before reading or changing project files:

1. Run `git rev-parse --show-toplevel` and identify the repository root.
2. Run `git remote get-url origin`.
3. If `origin` is absent, add exactly:
   `git remote add origin https://github.com/samuray-games/AsyncScene.git`
4. If `origin` exists but points elsewhere, stop with `BLOCKED_WRONG_CLOUD_REPOSITORY` and make no changes.
5. Fetch exactly:
   `git fetch --prune origin main coordination/chatgpt-codex-bridge`
6. Verify both `refs/remotes/origin/main` and `refs/remotes/origin/coordination/chatgpt-codex-bridge` exist.
7. Verify `origin/main` equals baseline `32513f02daf5943c41f24328e1ae251d6bc85ccc` before implementation.
8. If fetch or push authentication fails, use at most one non-interactive repair available from the configured Codex Cloud GitHub integration or credential helper. Never request or print a user token.
9. Do not use any dirty generic checkout. Create clean task-owned worktrees from the fetched remote refs.

A missing `origin` by itself is repairable in this thread and is not a terminal blocker. A genuine absence of repository authorization after the one allowed repair is `BLOCKED_CLOUD_GITHUB_AUTHORIZATION`.

## Frozen objective

Implement the exact semantic correction rejected in thread 059:

1. One immutable active identity containing exact cycle, generation, slot, thread, lane, task, epoch, nonce, memory revision, baseline, inbox, claim, expected outbox, and expected receipt.
2. Generic executable identity comparison against the active identity supplied by STATE, with no hard-coded passing stale thread.
3. Separate exact status-specific outbox schemas and a separate exact receipt schema.
4. Distinct exact fields: `stateBlobSha`, `mailboxParentCommit`, `outboxPublicationCommit`, `outboxBlobSha`, `primaryCommitSha`, and `primaryParent`.
5. Outbox contains only evidence knowable before its own publication. Receipt contains actual outbox publication evidence and does not require its own commit SHA.
6. Exact terminal tuples for status, completion mode, phase, verifier classification, recovery classification, next action code, and next action text.
7. Success action code exactly `OPEN_FRESH_CHATGPT_VERIFIER_AND_SEND_SAME_BRIDGE_COMMAND`.
8. `BLOCKED_NO_REMOTE_OUTBOX`, `BLOCKED_NO_SOURCE_DELTA`, and `BLOCKED_PLUGIN_UNAVAILABLE` are illegal for this primary-required loop-only task.
9. Strict lowercase `[0-9a-f]{40}` SHA validation. Reject uppercase and repeated synthetic 40-hex placeholders.
10. Exact changed-path equality against actual commit evidence and frozen scope.
11. An immutable canonical 12-state transition oracle independent from the implementation transition table. Test every legal and illegal pair against the independent oracle.
12. Every declared route, identity, schema, path, publication, status, and acceptance control must call real validation behavior. Unknown and unmapped controls fail closed. No unconditional-success control branches.
13. Real mutation tests must patch, remove, invert, or bypass every evaluator family and prove validation failure.
14. Remove stale 052-060 identities from passing fixtures. Do not use repeated `cafebabe`, `deadbeef`, `feedface`, `a111...`, or `b222...` values as passing SHA fixtures.
15. Closed-loop completion requires independent implementation acceptance and separate canary acceptance. Plugin installation is outside this gate.

## Frozen primary scope

Only:

- `AGENTS.md`
- `AGENTS.override.md`
- `PROCESS_ROOT_SYNC.md`
- `ORCHESTRATION.md`
- `BRIDGE.md`
- `CODEX_BRIDGE_BOOTSTRAP.md`
- `CODEX_BRIDGE_RECOVERY.md`
- `CLOSED_LOOP_PROTOCOL.md`
- `tools/closed_loop_contract.py`
- `tools/test_closed_loop_contract.py`
- `tools/validate-orchestration-policy.py`
- `TASKS.md`
- `PROJECT_MEMORY.md`

All other primary paths are protected. Plugin paths, product/runtime files, Safari surfaces, mailbox artifacts, and pre-existing `node_modules` changes are not primary scope.

## Validation

Run from the exact committed primary tree:

- `python3 -m py_compile tools/closed_loop_contract.py tools/test_closed_loop_contract.py tools/validate-orchestration-policy.py`
- `python3 -m unittest tools.test_closed_loop_contract`
- `python3 tools/validate-orchestration-policy.py`
- `git diff --check`

The suite must execute adversarial and mutation proofs. Passing labels or prose are not evidence.

## Mandatory publication transaction

1. Create a clean primary worktree from exact `origin/main` baseline.
2. Implement and validate the complete frozen objective.
3. Create exactly one direct-child primary commit and push it fast-forward to `origin/main`.
4. Refetch and prove remote main, first parent, and exact changed paths.
5. Create a clean mailbox worktree from the latest fetched mailbox head.
6. Fetch the actual current `.ai-bridge/STATE.md` blob SHA.
7. Publish immutable outbox only at `.ai-bridge/outbox/BRIDGE-20260710-061-02-codex.md` as one fast-forward commit changing only that path.
8. Refetch and record actual outbox publication commit and outbox blob SHA.
9. Publish receipt only at `.ai-bridge/receipts/BRIDGE-20260710-061-03-codex.md` as the immediate fast-forward child changing only that path.
10. Refetch main, STATE, outbox, receipt, and mailbox head through remote refs.
11. Return user-visible bytes exactly identical to the remote receipt.

Do not open a pull request. Do not stop after local commits. Do not use `::git-stage`, `::git-commit`, or local filesystem links as publication evidence.

## Next action after successful publication

The receipt must instruct the user to open a fresh ChatGPT verifier and send exactly `мост 3`.
