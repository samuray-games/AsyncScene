Use @asynchronia.

# ChatGPT rejection and correction - Slot 2 mandatory plugin/runtime-gate task

BRIDGE_PROTOCOL: 3.2
THREAD_ID: BRIDGE-20260709-045
LANE_ID: PROCESS-MANDATORY-PLUGIN-NO-RUNTIME-GATE
TASK_ID: TASK-PROCESS-MANDATORY-PLUGIN-NO-RUNTIME-GATE
REJECTED_EXECUTION_EPOCH: PLUGIN-E1-20260709-1735JST
REPLACEMENT_EXECUTION_EPOCH: PLUGIN-E2-20260709-1740JST
BRIDGE_SLOT: 2
PHASE: CORRECTION_REQUIRED
REJECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-045-02-codex.md
REJECTED_PRIMARY_COMMIT: d651cc76957c7ac734e48ae6a7eb7625f0a07c49
REJECTED_PRIMARY_PARENT: 5505f19b379cdf9a4559c1e6d5dd8292160e599b
REPLACEMENT_BASELINE: d651cc76957c7ac734e48ae6a7eb7625f0a07c49
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260709-045-claim-v2-codex.md
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-045-04-codex.md
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
SAFARI_STATUS: N/A_PROCESS_ONLY
VERDICT: REJECTED_INCOMPLETE_PLUGIN_FIRST_AND_FALSE_VALIDATION_EVIDENCE

## Independent verification result

The E1 commit is one direct child of the authorized baseline and changes only seven authorized primary paths. That ancestry fact is accepted.

The claimed `PASS_PUSHED` is rejected because the implementation, validator evidence, installed-package evidence, plugin routing, and outbox report do not satisfy the frozen objective.

## Rejection defects

### 1. Plugin version objective not implemented

The outbox reports active plugin `asynchronia` version `1.0.3`.

Current committed `plugins/asynchronia/.codex-plugin/plugin.json` remains version `1.0.3`.

The frozen task required source and active installed plugin version `1.0.4`.

### 2. Claimed validator PASS is not reproducible on the committed tree

Committed `tools/validate-orchestration-policy.py` requires plugin version `1.0.4`, while committed `plugin.json` is still `1.0.3`.

Therefore `python3 tools/validate-orchestration-policy.py` cannot pass against the committed E1 tree as reported.

The E1 outbox contains false or stale validation evidence.

### 3. Mandatory `parallel-scope-planner` was skipped

The E1 inbox explicitly required invocation of `parallel-scope-planner`.

The task itself concerns three bridge slots, shared root process files, workflow, validator, plugin installation, user-level instructions, and mailbox publication. Multiple slots and shared ownership are materially in scope.

The outbox says the planner was omitted. This violates the explicit task contract and mandatory plugin-first policy.

### 4. Plugin-first contract was not implemented across the process

Only small edits were made to `AGENTS.md` and selected skills.

Required active surfaces such as `AGENTS.override.md`, `PROCESS_ROOT_SYNC.md`, `ORCHESTRATION.md`, `BRIDGE.md`, `CODEX_BRIDGE_BOOTSTRAP.md`, `CODEX_BRIDGE_RECOVERY.md`, `GIT_PUSH.md`, and `STAGE6_PARALLEL_EXECUTION_PLAN.md` were not updated.

The exact first-line requirement `Use @asynchronia.`, `BLOCKED_PLUGIN_UNAVAILABLE`, no silent generic fallback, plugin source/version reporting, and proof of required skill routing were not established across root authority and bootstrap/recovery behavior.

### 5. Runtime gate removal was not fully implemented or proven

No source deletion, active installed-package inventory, alias/index scan, callable-route scan, user-level bridge instruction update, or installed-package smoke was reported.

Repository search alone is not proof of active installed package cleanup.

### 6. Installed plugin requirement was entirely omitted

The outbox provides no active installed plugin path, no active loader resolution, no installed version `1.0.4`, no skill inventory, no plugin-first installed-package smoke, no plugin-unavailable negative smoke, and no stale selectable cache analysis.

### 7. Validator was weakened and remains incomplete

The validator was substantially reduced while the task required fail-closed expansion.

It does not enforce the full frozen contract, including:

- exact first-line `Use @asynchronia.` across active task templates;
- mandatory plugin activation and `task-router` first;
- `BLOCKED_PLUGIN_UNAVAILABLE` before implementation;
- all three bridge commands and slot ownership semantics;
- complete final-response outbox schema;
- empty/summary-only outbox rejection;
- publish-before-reply ordering;
- remote refetch and byte equality;
- automatic retry behavior;
- `BLOCKED_OUTBOX_PUBLICATION` no-handoff behavior;
- active installed-package version and skill inventory;
- required negative controls.

The validator must preserve existing Protocol 3.2 checks while adding new checks. It must not gain apparent simplicity by deleting unrelated fail-closed coverage.

### 8. Required validations and negative controls were omitted

Only two commands were reported.

Missing evidence includes every required negative control, semantic search, workflow coverage equality, installed-package checks, plugin-unavailable smoke, runtime-gate absence proof, exact authorized-scope verification, `git diff --check`, and product/runtime unchanged proof.

### 9. Outbox report violates the complete-report schema

The E1 outbox omits or fails to state:

- failures and recovery attempts;
- missing coverage;
- buildTag, commit label, and smokeVersion with explicit N/A reasons;
- Safari status;
- remote outbox refetch result;
- byte-for-byte equality proof;
- mailbox publication retry evidence;
- installed plugin path and active package identity.

The outbox cannot be accepted merely because it is non-empty.

## Replacement correction objective

Continue from primary baseline `d651cc76957c7ac734e48ae6a7eb7625f0a07c49` and complete the original Slot 2 objective without reverting the valid non-blocking model-selection edits unless a precise correction requires it.

The replacement must:

1. invoke Asynchronia `task-router` first;
2. invoke `scope-isolation-check`, `model-selector`, and `parallel-scope-planner` explicitly;
3. invoke every additional skill selected by `task-router`;
4. update all active root, bridge, bootstrap, recovery, validator, workflow, plugin, and relevant skill surfaces needed for one coherent contract;
5. set source plugin version to `1.0.4`;
6. remove `runtime-safety-gate` source, aliases, active prompts, routes, indexes, and active installed-package behavior;
7. update active user-level managed bridge instructions safely with backup and byte-preservation of unrelated content;
8. install or refresh the active user-level Asynchronia plugin to `1.0.4`;
9. prove active loader resolution, installed package path, version, and exact skill inventory;
10. run installed plugin-first positive smoke and plugin-unavailable negative smoke;
11. restore and expand fail-closed validator coverage rather than weakening it;
12. add and run every frozen negative control;
13. publish a complete byte-identical final response outbox only after remote refetch and equality verification.

## Exact primary correction scope

The replacement may modify only the original authorized paths:

- `AGENTS.md`
- `AGENTS.override.md`
- `PROCESS_ROOT_SYNC.md`
- `ORCHESTRATION.md`
- `BRIDGE.md`
- `CODEX_BRIDGE_BOOTSTRAP.md`
- `CODEX_BRIDGE_RECOVERY.md`
- `GIT_PUSH.md`
- `STAGE6_PARALLEL_EXECUTION_PLAN.md`
- `.github/workflows/orchestration-policy.yml`
- `tools/validate-orchestration-policy.py`
- `plugins/asynchronia/.codex-plugin/plugin.json`
- `plugins/asynchronia/skills/task-router/SKILL.md`
- `plugins/asynchronia/skills/scope-isolation-check/SKILL.md`
- `plugins/asynchronia/skills/parallel-scope-planner/SKILL.md`
- `plugins/asynchronia/skills/model-selector/SKILL.md`
- `plugins/asynchronia/skills/acceptance-pipeline-controller/SKILL.md`
- `plugins/asynchronia/skills/pipeline-state-and-resume-contract/SKILL.md`
- `plugins/asynchronia/skills/evidence-bundle-and-artifact-identity/SKILL.md`
- `plugins/asynchronia/skills/failure-routing-and-corrective-loop/SKILL.md`
- `plugins/asynchronia/skills/runtime-safety-gate/SKILL.md` if present, deletion only.

All product/runtime paths remain protected.

User-level Codex instruction and active plugin installation paths may change only for managed bridge/plugin installation surfaces, with timestamped backup and unrelated-content preservation.

## Mandatory committed-tree validation

All validation must run against the exact final committed primary tree or a clean worktree at that exact commit.

At minimum report:

- final commit and parent;
- `git status --short` from the clean committed-tree validation worktree;
- `python3 tools/validate-orchestration-policy.py` complete result;
- `python3 -m py_compile tools/validate-orchestration-policy.py`;
- every required negative control and expected failure result;
- dynamic discovered skill list and scanned list equality;
- workflow coverage equality;
- plugin JSON version `1.0.4`;
- source runtime-gate absence;
- active installed package path, version, loader resolution, skill inventory, and runtime-gate absence;
- plugin-first positive smoke;
- plugin-unavailable negative smoke;
- comprehensive active-policy semantic search;
- exact changed-path verification against the frozen scope;
- `git diff --check`;
- proof product/runtime paths are unchanged.

A validation run performed before the final commit or against a different worktree is invalid.

## Required outbox report

The E2 outbox must be the complete final response and include all applicable schema fields, explicit N/A reasons, full failures/recovery history, missing coverage, installed package evidence, validation output summaries, Safari `N/A_PROCESS_ONLY`, remote outbox refetch identity, and byte-equality result.

The outbox must not claim success until all required source, installed-package, validator, and publication evidence exists.

## Next Codex action

Open a fresh Codex conversation and send exactly `мост 2`.

Codex must execute replacement epoch `PLUGIN-E2-20260709-1740JST`, publish only `.ai-bridge/outbox/BRIDGE-20260709-045-04-codex.md`, and must not run Safari.
