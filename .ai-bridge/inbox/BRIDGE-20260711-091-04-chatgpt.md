Use @asynchronia.

# BRIDGE-20260711-091 - Fix5 adversarial comparator correction, baseline-resynced attempt

ISSUE: 216
CYCLE: CYCLE-20260711-003
GENERATION: 13
SLOT: 2
THREAD_ID: BRIDGE-20260711-091
LANE_ID: S6-BOOMER-STEP4_4B-FIX4-VALIDATION-CORRECTION
TASK_ID: TASK-S6-BOOMER-STEP4_4B-FIX4-ADVERSARIAL
EXECUTION_EPOCH: S6-BOOMER-4_4B-FIX4-R3-20260711-1730JST
TASK_NONCE: S6B44B-091-ADV-1730
COORDINATOR_MEMORY_REVISION: 2026-07-11-1700-JST
AUTHORIZED_PRIMARY_BASELINE: 25bee74c99fa19ae5914e1739e2ff6a46eb8aeec
PREVIOUS_AUTHORIZED_BASELINE: 42c56c023e3b60f3b9e534dbb720057e78ef0c77
BRIDGE_PROTOCOL: 4.0
BRANCH_MAILBOX: coordination/chatgpt-codex-bridge-2
BRANCH_TASK: bridge/2/BRIDGE-20260711-091
SUPERSEDES_EXECUTION_EPOCH: S6-BOOMER-4_4B-FIX4-R2-20260711-1415JST
PREVIOUS_ATTEMPT_STATUS: BLOCKED_MAIN_BASELINE_MOVED_BEFORE_MUTATION
CLAIM_PATH: .ai-bridge/claims/BRIDGE-20260711-091-claim-v2-codex.md
EXPECTED_OUTBOX_PATH: .ai-bridge/outbox/BRIDGE-20260711-091-05-codex.md
EXPECTED_RECEIPT_PATH: .ai-bridge/receipts/BRIDGE-20260711-091-06-codex.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
REQUIRE_COMPLETE_SCOPE_DELTA: true
MODEL_RECOMMENDATION_PAUSE_REQUIRED: true
SAME_THREAD_CONTINUE_REQUIRED: true
THREAD_ROTATION_REQUIRED: true
SAFARI_STATUS: PENDING_USER

## Baseline synchronization

The previous attempt correctly stopped before mutation because origin/main moved. The dedicated task branch had no task-owned commits and was exactly the old baseline. It has now been fast-forwarded without force from 42c56c023e3b60f3b9e534dbb720057e78ef0c77 to 25bee74c99fa19ae5914e1739e2ff6a46eb8aeec.

The new main is a descendant of the accepted Fix4 predecessor, so the accepted restoration implementation remains present. Do not replay, cherry-pick, rebase, reset, or reconstruct the old branch.

This is a fresh execution epoch. Any model recommendation or CONTINUE token from the superseded attempt is stale and must not be reused.

## Bridge v4 isolation

Read bridge control only from current main and coordination/chatgpt-codex-bridge-2. Do not read, write, compare, or depend on bridge 1 or bridge 3 mailbox refs.

Perform implementation only on bridge/2/BRIDGE-20260711-091. Direct task-lane writes to main are forbidden.

## Installed Asynchronia reconciliation

Repository authority at the authorized baseline declares Asynchronia 1.0.7 in both:

- plugins/asynchronia/.codex-plugin/plugin.json
- .agents/plugins/marketplace.json

The superseded attempt observed only /Users/User/.codex/plugins/cache/personal/asynchronia/1.0.5/ and no installed 1.0.7 directory.

Before any task mutation and before relying on plugin-produced evidence:

1. Read the two repository version declarations above and confirm both equal 1.0.7.
2. Inspect the active installed Asynchronia package and its required skill files.
3. If installed 1.0.7 is absent, refresh or install the personal/asynchronia package from the current repository-local marketplace source without modifying repository files, the task branch, or any mailbox artifact.
4. If the current Codex session cannot activate the refreshed 1.0.7 package without a user reload or native UI action, return exactly BLOCKED_ACTIVE_PLUGIN_RELOAD_REQUIRED with the exact single user action. Do not mutate task files.
5. Continue only after active installed readback proves version 1.0.7 and records the exact installed path plus hashes for the invoked task-router, scope-isolation-check, and model-selector skill files.
6. Historical 1.0.5 cache contents are not acceptance evidence and must not be reported as the active package.

## Mandatory fresh model preflight

The first fresh мост 2 command for this epoch is read-only. Apply task-router, scope-isolation-check, and model-selector after the plugin reconciliation above. Show the recommendation, stop at WAITING_FOR_MODEL_SELECTION, and end with one standalone fenced CONTINUE block. No mutation before exact same-thread CONTINUE.

Start the cost frontier at GPT-5.4 / Light. Promote only for a concrete task-specific failure mode.

## Frozen task-branch scope

Exactly these four paths must change on bridge/2/BRIDGE-20260711-091:

- AsyncScene/Web/dev/dev-checks.js
- docs/dev/dev-checks.js
- AsyncScene/Web/index.html
- docs/index.html

No fifth path may change.

## Atomic objective after CONTINUE

Preserve the accepted Fix4 snapshot, finally-path restoration, and before/after digest logic. Replace only the inequality-only adversarial proof with production-comparator rejection fixtures, then bump runtime identity and cache-bust to Fix5.

For each category profile, economy, battleState, moneyLog, persistence, and localStorage:

- clone the valid control digest;
- mutate only that category;
- invoke the exact production comparator or evaluator used by the smoke;
- require false for the mutated digest;
- require true for an untouched control digest.

Expose and require:

- profileComparatorRejectsMutation
- economyComparatorRejectsMutation
- battleStateComparatorRejectsMutation
- moneyLogComparatorRejectsMutation
- persistenceComparatorRejectsMutation
- localStorageComparatorRejectsMutation
- allComparatorsRejectMutation

Overall smoke PASS is forbidden unless every comparator field and every restoration field is true. Fixtures must use cloned or sandboxed data and must never mutate live player state.

Source and docs Boomer blocks must remain semantically identical. Both index files must remain byte-identical and move to one new Fix5 cache-bust.

## Validation

After one direct-child task commit based on 25bee74c99fa19ae5914e1739e2ff6a46eb8aeec, run from a clean committed task worktree:

- node --check on both dev-check files;
- current Boomer static generator and validator;
- exact four-path diff from 25bee74c99fa19ae5914e1739e2ff6a46eb8aeec;
- sole-parent and direct-child proof from that baseline;
- source/docs dev-check blob equality;
- index blob equality and exact Fix5 cache-bust;
- executable ephemeral harness against source and docs code;
- untouched control returns true through the production comparator;
- each category-local mutation returns false through the production comparator;
- all restoration fields remain true;
- git diff --check;
- clean committed task worktree before final publication.

## Publication

Publish implementation only to bridge/2/BRIDGE-20260711-091. Do not push the task commit directly to main.

Publish the exact current outbox and receipt as separate fast-forward commits only on coordination/chatgpt-codex-bridge-2. The receipt must include final refetch and byte-equality evidence, not a pending publication claim.

Codex must not edit .ai-bridge/STATE.md.

Terminal action after successful publication: OPEN_FRESH_CHATGPT_VERIFIER_AND_SEND_SAME_BRIDGE_COMMAND.
