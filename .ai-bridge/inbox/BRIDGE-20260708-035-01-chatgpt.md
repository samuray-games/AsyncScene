Use @asynchronia runtime-safety-gate.

# Bridge Slot 1 - Stage 6 serialized Wave III event vote presentation routing

BRIDGE_PROTOCOL: 3.0
ORCHESTRATION_VERSION: 3.0
BRIDGE_SLOT: 1
THREAD_ID: BRIDGE-20260708-035
LANE_ID: S6-I3-EVENT-VOTE-PRESENTATION
TASK_ID: TASK-S6-PAR-I3
PHASE: MODEL_PREFLIGHT_ONLY
USER_COMMAND: мост 1
MAILBOX_PARENT_COMMIT: 78f220f18b9d708a74b4a990bee9518337582681
CURRENT_MAIN_BASELINE: 23fd4ab42d62c999d426ea07c79c84837a71a48b
DECISION_FREEZE: .ai-bridge/decisions/STAGE6_WAVE2_COPY_FREEZE.md
PRIMARY_WRITE_SCOPE:
- AsyncScene/Web/events.js
- docs/events.js
- AsyncScene/Web/ui/ui-events.js
- docs/ui/ui-events.js
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260708-035-02-codex.md
RUNTIME_CLASSIFICATION: RUNTIME_SENSITIVE
CONFIRMATION_RULE: SAME_THREAD_CONTINUE_CONFIRMS_MODEL_AND_EXACT_FROZEN_RUNTIME_SCOPE
MODEL_SELECTION: PENDING_CODEX_PREFLIGHT_12_OF_12

## Atomic objective

Implement only Wave III event-vote presentation routing:

1. give Alpha and Boomer exact profile-specific vote feedback in `ui-events.js` through one stable resolver;
2. preserve default, Millennial and Zoomer visible behavior;
3. restore byte parity between `AsyncScene/Web/events.js` and `docs/events.js` by treating the current source file as canonical;
4. preserve all vote mechanics, economy, REP, caps, timers, NPC simulation, battle synchronization, handlers and event state behavior.

Do not implement or duplicate the DM report state machine. Report flow remains owned by the accepted Wave II `ui-dm.js` implementation and later runtime acceptance smoke.

Do not start Wave IV or later work.

## Mandatory authorities

Before preflight, read fresh remote versions of:

- `AGENTS.override.md`
- `ORCHESTRATION.md`
- `AGENTS.md`
- `BRIDGE.md`
- `TASKS.md`
- `PROJECT_MEMORY.md`
- mailbox `STATE.md`
- this inbox
- `.ai-bridge/decisions/STAGE6_WAVE2_COPY_FREEZE.md`
- `.ai-bridge/outbox/BRIDGE-20260707-031-02-codex.md`
- `.ai-bridge/outbox/BRIDGE-20260707-032-02-codex.md`
- `docs/UI_PROFILE_ALPHA_NEW_FEATURES.md`
- all four target files from current `origin/main`

## Required model protocol

Evaluate all 12 model/reasoning pairs and recommend the cheapest reliable pair.

Return compact preflight only. End with exactly one fenced `CONTINUE` block and nothing after it.

No edits, tests, commits or pushes before the user selects the model and sends `CONTINUE` in the same Codex thread.

Under Protocol 3.0, that `CONTINUE` confirms both the model selection and this exact frozen runtime scope. Do not request a second `APPROVE`.

## Frozen implementation contract

### A. Event profile resolution

In `ui-events.js`, replace the current Boomer-only branch with one stable event-vote presentation resolver that distinguishes:

- `default`
- `millennial`
- `zoomer`
- `boomer`
- `alpha`

Unknown profiles fall back safely to `default`.

Alpha must not resolve through the Zoomer presentation bucket. Boomer must not use ad hoc render-site conditionals.

Prefer immutable base and profile override objects, following the accepted `ui-dm.js` registry architecture.

### B. Stable presentation keys

Use stable semantic keys for exactly these two visible signals:

- `vote.disabled`
- `vote.no_points`

No other event copy is approved for profile rewriting in this wave.

### C. Exact resolved matrix

`vote.disabled`:

- default: preserve the current `systemSay("errors", "unavailable")` result
- millennial: preserve the current `systemSay("errors", "unavailable")` result
- zoomer: preserve the current `systemSay("errors", "unavailable")` result
- boomer: `Вы уже проголосовали.`
- alpha: `Голос: уже принято`
- unknown: same as default

`vote.no_points`:

- default: `Мало 💰`
- millennial: `Мало 💰`
- zoomer: `Мало 💰`
- boomer: `Недостаточно 💰.`
- alpha: `Мало 💰`
- unknown: `Мало 💰`

The Alpha targets come from accepted mappings NFS_0058 and NFS_0059. The Boomer targets come from the accepted Boomer decision package.

### D. Exact call sites

Route only the existing visible vote feedback call sites through the resolver:

- locked/disabled vote-button toast;
- pre-click insufficient-points toast and stat toast;
- post-`helpEvent` insufficient-points toast and stat toast;
- existing `Game.__DEV.__smokeBoomerTermsStep42Events` compatibility output may be refactored to consume the resolver but must preserve its existing Boomer and non-Boomer results.

Do not change button labels, vote side labels, event titles, timer copy, result copy, escape copy or crowd tally copy in this wave.

### E. `events.js` source/mirror reconciliation

Current main has mirror drift:

- source blob: `447bdc2d362785dc9655c208a20cecb4cdd66aad`
- deployed mirror blob: `d81dd9c69c742ece4e7cbcc1b0c41876146d7cb1`

Treat current `AsyncScene/Web/events.js` as canonical and reconcile `docs/events.js` to it byte-for-byte.

Preserve the source security-blocker diagnostics and reason-bearing hint exactly. Do not simplify, remove or rewrite them.

Do not otherwise modify `AsyncScene/Web/events.js` unless strictly necessary for deterministic mirror parity. The expected source file after this task should remain semantically and preferably byte-identical to the baseline source.

### F. Mechanics and state freeze

Do not change:

- `helpEvent` signature or success/failure semantics;
- one-player-vote lock;
- `crowd.voters`, `aVotes`, `bVotes`, mirrored vote fields;
- vote cost calculation or transfer reasons;
- point charging, refunds or moneyLog behavior;
- participation REP or majority/minority REP;
- NPC voting continuation after player vote;
- NPC-share/crowd caps;
- timers, expiry or resolution;
- battle/crowd synchronization;
- render scheduling;
- event creation, filtering or ordering;
- report handlers, report states, rewards, penalties or cooldowns.

Do not add new persistent state fields.

## Exact allowed changed paths

- `AsyncScene/Web/events.js`
- `docs/events.js`
- `AsyncScene/Web/ui/ui-events.js`
- `docs/ui/ui-events.js`

Any other changed path is automatic FAIL.

## Required verification after `CONTINUE`

1. Prove current main parent is exactly `23fd4ab42d62c999d426ea07c79c84837a71a48b`.
2. Prove exactly four authorized paths changed.
3. Prove `events.js` source and deployed mirror are byte-identical.
4. Prove `ui-events.js` source and deployed mirror are byte-identical.
5. Run `node --check` on all four files.
6. Probe the resolver matrix for all five profiles plus unknown fallback.
7. Prove default/Millennial/Zoomer output is unchanged.
8. Prove exact Boomer and Alpha targets above.
9. Prove all three visible vote-feedback call-site classes use the resolver and no profile conditional remains at those call sites.
10. Prove `Game.__DEV.__smokeBoomerTermsStep42Events` remains compatible.
11. Diff `AsyncScene/Web/events.js` against baseline and prove no semantic change; if byte-identical, report that explicitly.
12. Prove protected mechanics and function signatures remain unchanged with targeted static probes.
13. Prove no report-flow code changed.
14. Run relevant existing event/profile smoke commands without editing dev-checks or boot.
15. Run `git diff --check`.
16. PASS requires empty `failures`, `forbiddenRemaining`, `missingCoverage` and `failedChecks`.

## Publication

- Publish one direct-child implementation commit to `main` according to current policy.
- Publish one immutable outbox at the expected mailbox path.
- Do not edit mailbox STATE.
- On credentials failure, follow Protocol 3.0 `BLOCKED_PUSH_AUTH` and include the complete recovery bundle, including full UTF-8 contents for every changed text file and the complete outbox payload.
- If main publishes but outbox publication fails, do not reimplement. Report `MAIN_PUBLISHED_AWAITING_OUTBOX` plus the complete outbox recovery payload.

## Required outbox fields

Include:

- status;
- selected model and reasoning;
- 12/12 preflight summary;
- exact main parent and implementation commit;
- exact changed paths;
- all four final blob SHAs and mirror equality;
- full resolved matrix;
- unchanged-profile proof;
- call-site routing proof;
- baseline `events.js` semantic/byte comparison;
- protected mechanics proof;
- report-flow untouched proof;
- commands and results;
- `failures`, `forbiddenRemaining`, `missingCoverage`, `failedChecks`;
- runtime classification;
- Safari status `N/A - later serialized deployment smoke`;
- mailbox parent and outbox commit, or complete recovery bundle;
- exact next action: return to ChatGPT and write `мост 1`.
