Use @asynchronia.

# Recover the executable controls and terminal publication protocol

BRIDGE_PROTOCOL: 3.3
CYCLE_ID: CYCLE-20260709-001
CYCLE_GENERATION: 10
THREAD_ID: BRIDGE-20260710-055
LANE_ID: PROCESS-CLOSED-LOOP-EXECUTABLE-CONTROLS-AND-PUBLICATION-RECOVERY
TASK_ID: TASK-PROCESS-CLOSED-LOOP-EXECUTABLE-CONTROLS-AND-PUBLICATION-RECOVERY
EXECUTION_EPOCH: CLOSED-LOOP-RECOVERY-R1-20260710-0029JST
TASK_NONCE: CLV1-055-RECOVERY-3880-0029
BRIDGE_SLOT: 3
PHASE: RECOVERY_REQUIRED
BASELINE: 388048b8878b6b362dda9518a8ea9f079277ade9
COORDINATOR_MEMORY_REV: 2026-07-10-0029-JST
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260710-055-claim-v1-codex.md
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260710-055-02-codex.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
BRANCH_SEPARATION_POLICY: .ai-bridge/PRIMARY_MAILBOX_SEPARATION_POLICY_V1.md
FRESH_CODEX_CONVERSATION_REQUIRED: true
SAFARI_STATUS: N/A_PROCESS_ONLY
SUPERSEDES_THREAD: BRIDGE-20260709-054

## Independent verifier verdict for thread 054

`RECOVERY_REQUIRED_SCHEMA_CONTRADICTION_AND_FALSE_CONTROLS`.

Accepted partial progress:
- remote main is `388048b8878b6b362dda9518a8ea9f079277ade9`;
- it is exactly one direct child of `708bc8f1380f2fb4ba687ecfa2706494b3c969d9`;
- the six changed paths are within the authorized source scope;
- leaked outbox 052 was removed from main;
- active inbox, claim, STATE and outbox 054 are absent from main;
- workflow triggers on push and pull request and runs py_compile, unittest and the validator.

Rejected source behavior:
1. `evaluate_control()` returns `True` for every unimplemented control, so most positive and negative controls are names without executable semantics.
2. `self_check()` validates only two transitions and then loops through permissive controls; it does not execute the full legal table or the complete illegal 12x12 matrix.
3. Unit tests check only three illegal transitions, do not execute each control with a dedicated fixture, and still accept stale 052/053 paths and synthetic repeated `cafebabe`, `deadbeef` and `feedface` SHA values.
4. `validate_report_schema()` does not validate `verifiedPrimarySha`, `remoteMailboxCommit`, `remoteStateSha` or `baselineSha` as real 40-hex SHAs, does not compare report identity to the active state, does not enforce exact path-set equality, and permits forbidden `BLOCKED_NO_SOURCE_DELTA`.
5. `classify_recovery()` depends on undeclared `publicationRecovery` and `reportRecovery` keys that the strict schema rejects as extras.
6. `accept_product_work()` requires only `canaryAccepted` and `currentState`; it does not separately require implementation acceptance and installed-package acceptance.
7. The validator counts controls and searches substrings, but does not prove every illegal pair, every control evaluator, schema negatives, exact route order, exact transition table, or primary mailbox-artifact absence.
8. The task-router does not encode the full required numbered-bridge order: `task-router`, `scope-isolation-check`, `model-selector`, conditional `parallel-scope-planner`, `closed-loop-controller`, `failure-routing-and-corrective-loop`, then routed skills.

Rejected publication evidence:
- outbox 054 reports `remoteMailboxCommit: 1dfc50a480add321425a67304a78bc2506ad9608`;
- that commit modified only `.ai-bridge/STATE.md` and predates outbox publication;
- actual mailbox head and outbox-publication commit is `33396b717e16dcbf3570d59b50a8849cea39ce65`;
- therefore outbox 054 contains false remote publication evidence.

## Protocol contradiction to recover

A terminal outbox cannot contain the SHA of the Git commit that contains those same bytes: the commit SHA depends on the tree and blob containing the field. Requiring the final response to be byte-identical to that outbox makes `remoteMailboxCommit` self-referential.

Replace this impossible field contract with a deterministic two-artifact publication proof:
1. Codex publishes immutable terminal outbox bytes containing the fetched mailbox parent before publication, exact expected outbox path, exact primary evidence and all known terminal fields.
2. Codex refetches the resulting outbox publication commit and blob.
3. Codex publishes a separate receipt at `.ai-bridge/receipts/BRIDGE-20260710-055-03-codex.md` containing the actual outbox publication commit, outbox blob SHA, outbox path, mailbox parent, active identity and byte-equality result.
4. The final Codex response is byte-identical to the receipt, while the receipt cryptographically identifies the immutable outbox bytes.
5. STATE and coordinator verification treat outbox plus receipt as the complete terminal evidence pair.

Do not invent another self-referential field. If repository policy exposes a simpler non-self-referential transaction with equivalent proof, implement it consistently across protocol, contract, tests, validator and skills.

## Required source correction

- Give every declared positive and negative control a real deterministic evaluator. Unknown or unimplemented control names must fail closed.
- Make `self_check()` execute every legal transition, every illegal pair in the exact 12x12 matrix, every positive control and every negative control using meaningful fixtures.
- Add unit tests for the full illegal matrix, every control, exact identity mismatch, all SHA fields, path equality, publication evidence, recovery consistency and three-stage release gating.
- Use thread 055 or generic fixtures only. Remove stale thread 052/053 paths and synthetic placeholder SHA fixtures from passing samples.
- Define exact status-specific terminal schemas. Forbidden outcomes include `BLOCKED_NO_REMOTE_OUTBOX` and `BLOCKED_NO_SOURCE_DELTA`.
- Validate exact active identity: cycle, generation, slot, thread, lane, task, epoch, nonce, coordinator memory revision, baseline, inbox, claim, expected outbox and expected receipt.
- Validate all commit and blob SHAs as 40 lowercase hexadecimal values and reject known synthetic repeated placeholders.
- Enforce actual commit changed paths, frozen authorized scope, branch separation and absence of inbox, claim, STATE, outbox and receipt artifacts from main.
- Make recovery classification derive only from declared schema fields and enforce consistency between status, completion mode and recovery class.
- Require separate `implementationAccepted`, `installedPackageAccepted` and `canaryAccepted` gates before product release.
- Make validator prove the exact legal state set and exact transition table, full legal and illegal matrices, every control, all schema negative cases, exact ordered route, plugin 1.0.5, retired runtime-gate absence, workflow triggers/commands and primary branch separation.
- Align `CLOSED_LOOP_PROTOCOL.md`, controller skill and task-router with the executable contract.

## Frozen primary scope

Only:
- `CLOSED_LOOP_PROTOCOL.md`
- `tools/closed_loop_contract.py`
- `tools/test_closed_loop_contract.py`
- `tools/validate-orchestration-policy.py`
- `plugins/asynchronia/skills/task-router/SKILL.md`
- `plugins/asynchronia/skills/closed-loop-controller/SKILL.md`
- `TASKS.md`
- `PROJECT_MEMORY.md`

All other primary paths are protected. Do not modify the workflow unless a demonstrated missing trigger or command requires it; current push/pull_request triggers and three validation commands are already accepted.

## Validation

Run from the exact committed tree:
- `python3 -m py_compile tools/closed_loop_contract.py tools/test_closed_loop_contract.py tools/validate-orchestration-policy.py`
- `python3 -m unittest tools.test_closed_loop_contract`
- `python3 tools/validate-orchestration-policy.py`
- `git diff --check`

Add explicit mutation-style proofs showing that deleting or bypassing any evaluator, release gate, identity check, path check or publication check makes validation fail.

## Publication transaction

- Publish exactly one direct-child primary correction commit from baseline `388048b8878b6b362dda9518a8ea9f079277ade9` to `main`.
- Publish the immutable outbox only to `.ai-bridge/outbox/BRIDGE-20260710-055-02-codex.md`.
- Publish the non-self-referential receipt only to `.ai-bridge/receipts/BRIDGE-20260710-055-03-codex.md`.
- Refetch main, STATE, outbox, receipt and mailbox head.
- The final response must be byte-identical to the remote receipt.
- Do not continue thread 054.
- Do not run Safari.

## Model recommendation

Recommended: GPT-5.5 with Extra High reasoning.
Reason: repeated shallow corrections, cross-file executable invariants, complete state-matrix testing and a cryptographic publication-transaction redesign make weaker settings likely to produce another cosmetically passing but logically false contract.

Exact next instruction after complete publication: open a fresh ChatGPT conversation and send `мост 3`.