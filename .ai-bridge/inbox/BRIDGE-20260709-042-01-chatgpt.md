# Remove the obsolete runtime gate from active Asynchronia workflow

BRIDGE_PROTOCOL: 3.1
THREAD_ID: BRIDGE-20260709-042
LANE_ID: PROCESS-RUNTIME-GATE-REMOVAL
TASK_ID: TASK-PROCESS-RUNTIME-GATE-REMOVAL
EXECUTION_EPOCH: PRGR-E1-20260709-0305JST
PHASE: EXECUTE_AND_PUBLISH
BRIDGE_SLOT: 1
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260709-042-02-codex.md
REMOTE_MAIN_BASELINE: c0e2f891076f3e8e280941edbe0e241d9931dd0f
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-042-03-codex.md
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
SAFARI_STATUS: N/A_PROCESS_ONLY
SUPERSEDES_ACTIVE_THREAD: BRIDGE-20260709-041
SUSPENDED_TASK: TASK-S6-PAR-V5B

## User decision

The runtime safety gate is obsolete. It existed to prevent collisions while the user manually ran parallel work. Current bridge lanes already use exact scopes, mirror ownership, dependency ordering and serialization. The gate now adds only blocking approval loops.

Remove the blocking runtime gate from every active authority and plugin execution path. Preserve collision prevention through exact ownership and serialization.

## Exact write scope

Only these primary paths may change:

1. `AGENTS.md`
2. `AGENTS.override.md`
3. `ORCHESTRATION.md`
4. `BRIDGE.md`
5. `PROCESS_ROOT_SYNC.md`
6. `tools/validate-orchestration-policy.py`
7. `.github/workflows/orchestration-policy.yml`
8. `STAGE6_PARALLEL_EXECUTION_PLAN.md`
9. `plugins/asynchronia/.codex-plugin/plugin.json`
10. `plugins/asynchronia/skills/runtime-safety-gate/SKILL.md` - delete
11. `plugins/asynchronia/skills/scope-isolation-check/SKILL.md` - create
12. `plugins/asynchronia/skills/task-router/SKILL.md`
13. `plugins/asynchronia/skills/parallel-scope-planner/SKILL.md`
14. `plugins/asynchronia/skills/canon-audit/SKILL.md`
15. `plugins/asynchronia/skills/mirror-audit/SKILL.md`
16. `plugins/asynchronia/skills/economy-invariant-audit/SKILL.md`
17. `plugins/asynchronia/skills/acceptance-pipeline-controller/SKILL.md`
18. `plugins/asynchronia/skills/failure-routing-and-corrective-loop/SKILL.md`

Do not touch game runtime, product copy, economy, battle, NPC, persistence, state, routing, deployment assets, `TASKS.md`, or `PROJECT_MEMORY.md`.

## Required result

1. Delete the blocking `runtime-safety-gate` skill.
2. Remove active requirements for:
   - `Use @asynchronia runtime-safety-gate.`
   - `RUNTIME_SAFETY_GATE_REQUIRED`
   - `RUNTIME_GATE_REQUIRED`
   - runtime approval tokens or an approval-only stop
   - opening an isolated runtime slot merely because a file is runtime-sensitive
3. Replace the gate with a non-blocking `scope-isolation-check` skill that verifies:
   - exact write ownership
   - mirror ownership
   - stable-read dependencies
   - shared wiring ownership
   - collision-free parallel lanes
   - serialization only when scopes actually overlap or depend on each other
4. Runtime-sensitive files may execute immediately when the lane is already frozen, collision-free and serialized where necessary.
5. Unresolved overlap must return `BLOCKED_SCOPE_COLLISION` with exact paths and owners. It must not request an approval token.
6. Update root process version markers consistently to Protocol 3.2 or an equivalent single coherent version.
7. Update plugin metadata and bump the plugin patch version.
8. Update the fail-closed validator so it:
   - requires the new scope-isolation policy markers
   - rejects active blocking gate markers
   - checks every changed root policy path in workflow filters
9. Preserve bridge publication, exact-scope validation, mirror parity, Safari ownership, no-op guards and accepted game canon.

## Active-text cleanup

Outside immutable historical audit artifacts under `.ai-bridge/**`, active repository search must return no execution requirement for the removed gate. Historical records may retain old text but must not be named by current STATE as authority.

## Validation

Run at least:

- `python3 tools/validate-orchestration-policy.py`
- JSON validation for `plugins/asynchronia/.codex-plugin/plugin.json`
- frontmatter/name validation for every changed skill
- repository search for removed blocking markers outside `.ai-bridge/**`
- repository search proving `scope-isolation-check` is referenced by active router/planner/audits
- exact changed-path verification
- `git diff --check`

## Execution and publication

On the first exact `мост 1` in a fresh Codex conversation:

1. fetch current main and mailbox;
2. read this task and claim;
3. execute the complete policy/plugin change without model preflight, runtime approval, continuation token or extra bridge command;
4. commit one direct child of the exact baseline;
5. push main fast-forward;
6. publish the exact expected outbox;
7. refetch both refs;
8. return `PASS_PUSHED` with machine-derived SHA, parent, exact paths, validations and `Safari smoke status: N/A_PROCESS_ONLY`.

After acceptance ChatGPT will reopen the suspended Step 4.4B task on the new baseline without the removed gate.