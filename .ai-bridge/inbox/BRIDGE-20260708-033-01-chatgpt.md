Use @asynchronia runtime-safety-gate.

# Bridge Slot 1 - Stage 6 serialized Wave I system resolver/copy implementation

BRIDGE_PROTOCOL: 2.4
BRIDGE_SLOT: 1
THREAD_ID: BRIDGE-20260708-033
LANE_ID: S6-I1-SYSTEM-RESOLVER
TASK_ID: TASK-S6-PAR-I1
PHASE: MODEL_PREFLIGHT_ONLY_THEN_RUNTIME_SENSITIVE_IMPLEMENTATION
USER_COMMAND: мост 1
MAILBOX_PARENT_COMMIT: e2109a4dfe7c168df9974199779804bf75f51baa
CURRENT_POLICY_HEAD: 873e67387e7c4db88c1b07772ef928ee17187210
AUTHORIZED_PRODUCT_BASELINE: 873e67387e7c4db88c1b07772ef928ee17187210
DECISION_FREEZE: .ai-bridge/decisions/STAGE6_WAVE2_COPY_FREEZE.md
PRIMARY_WRITE_SCOPE:
- AsyncScene/Web/system.js
- docs/system.js
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260708-033-02-codex.md
RUNTIME_CLASSIFICATION: RUNTIME_SENSITIVE
RUNTIME_SAFETY_CONFIRMATION_REQUIRED: YES
MODEL_SELECTION: PENDING_CODEX_PREFLIGHT_12_OF_12

## Atomic objective

Implement serialized Wave I only: create correct Boomer and Alpha system-copy routing in `system.js` and its deployed mirror, apply the frozen Boomer system-copy decisions, preserve frozen Alpha copy, and prove profile isolation without changing mechanics or any file outside the exact mirror pair.

Do not start Wave II or any later wave.

## Mandatory authorities

1. Freshly fetch `origin/main` and `origin/coordination/chatgpt-codex-bridge`.
2. Read current mailbox STATE and immutable claim.
3. Read `.ai-bridge/decisions/STAGE6_WAVE2_COPY_FREEZE.md`.
4. Read accepted collision package `.ai-bridge/outbox/BRIDGE-20260707-030-02-codex.md`.
5. Read accepted Boomer package `.ai-bridge/outbox/BRIDGE-20260707-031-02-codex.md`.
6. Read accepted Alpha package `.ai-bridge/outbox/BRIDGE-20260707-032-02-codex.md`.
7. Inspect current `AsyncScene/Web/system.js` and `docs/system.js` from fresh `origin/main`.

## Required model protocol

Before any edit, evaluate all 12 available model/reasoning pairs:

- GPT-5.4-Mini: Light, Medium, High, Extra High
- GPT-5.4: Light, Medium, High, Extra High
- GPT-5.5: Light, Medium, High, Extra High

Optimize for the cheapest reliable pair. Return compact preflight only and stop with exactly one fenced `CONTINUE` block. The user selects the model in the Codex UI. Same-thread `CONTINUE` is the explicit runtime-safety confirmation for this exact two-file task.

No edit, claim mutation, test, commit or push before same-thread `CONTINUE`.

## Required implementation

### Profile routing

- `activeSystemTextProfile()` must distinguish at least `millennial`, `zoomer`, `boomer`, and `alpha`.
- `alpha` must not resolve through the Zoomer bucket.
- `boomer` must not resolve through the Millennial bucket.
- Unknown profiles retain the existing safe fallback behavior.
- Existing Millennial and Zoomer visible output must remain unchanged unless an accepted frozen row explicitly requires otherwise. No such non-Boomer/non-Alpha rewrite is authorized here.

### Frozen Boomer system-copy targets

Apply these exact visible targets on Boomer profile routes while preserving placeholders and semantics:

- `errors.insufficientPoints` -> `Недостаточно 💰.`
- `errors.pointsLowBattle` -> `Недостаточно 💰 для баттла.`
- `errors.reportNo` -> `Сообщение отклонено. Штраф: -5 💰.`
- `notifications.pointsDeltaPlusOne` -> `+1 💰`
- `notifications.repDeltaPlusOne` -> `+1 ⭐`
- `notifications.pointsDeltaVoteCost` -> `Участие в голосовании: -{voteCost} 💰.`
- `notifications.rematchCost` -> `Реванш: -{rematchCost} 💰.`
- `notifications.pointsDeltaRefund` -> `Возврат: +1 💰.`
- `notifications.pointsDeltaRefundMajority` -> `Возврат большинству: +1 💰.`
- `notifications.pointsDeltaRemainderWin` -> `Остаток победителю: +1 💰.`
- `systemEvents.dmReaction` -> `Реакция {name} на {target}.`
- `systemEvents.dmInvite` -> `Приглашение от {name}: {guest} к {target}.`
- `systemEvents.battleResult` -> `Баттл с {oppName}: {text}.`

Do not implement Boomer rows owned by other files in this wave.

### Frozen Alpha system-copy targets

- `warnings.escapeNeedsPoints` -> `Мало 💰.`
- `notifications.escapePaid` -> `Уйти: 1💰`
- `notifications.escapeVoteCost` -> `Уйти: −{escapeCost}💰`
- `notifications.trainingSent` remains exactly `Аргумент: {teacher} → {student}.`
- Alpha `systemEvents.dmReaction` and `systemEvents.dmInvite` remain unchanged in this wave because the accepted Alpha package explicitly kept them pending a separate shared-system-copy decision.
- Alpha Vote and Report runtime behavior is not implemented here; preserve current keys and semantics so later waves can use the accepted contracts.

### Architecture constraints

- Use the existing `Game.System.say` / SystemCopy architecture. Do not create a second notification engine.
- Preserve taxonomy codes, public APIs, handlers, action IDs, placeholders, economy values, state transitions and fallback behavior.
- Root and deployed mirror must remain byte-identical.
- No new slang or cross-profile leakage.
- No broad refactor.

## Exact allowed changed paths

- `AsyncScene/Web/system.js`
- `docs/system.js`

Any other changed path is an automatic FAIL. In particular, do not change:

- `data.js`
- `ui-dm.js`
- `events.js`
- `ui-events.js`
- `ui-battles.js`
- `conflict-core.js`
- `index.html`
- `dev-checks.js`
- boot/cache-bust markers
- `TASKS.md`
- `PROJECT_MEMORY.md`
- mailbox STATE

## Required verification

After same-thread `CONTINUE` and implementation:

1. Prove source/mirror byte equality.
2. Run syntax/static validation suitable for both files.
3. Run targeted profile-resolution checks for all four profiles.
4. Prove Alpha does not resolve through Zoomer and Boomer does not resolve through Millennial.
5. Prove every exact frozen target above resolves on its intended profile.
6. Prove all placeholders remain byte-for-byte intact.
7. Prove Millennial and Zoomer representative outputs are unchanged.
8. Prove unknown-profile fallback remains safe.
9. Run existing relevant system-language/system-copy smoke commands if they can run without editing dev-checks or boot.
10. Return empty `failures`, `forbiddenRemaining`, `missingCoverage`, and `failedChecks` for PASS.

Do not claim Safari acceptance. This wave requires later deployment verification and user Safari smoke after the serialized implementation chain reaches the smoke wave.

## Publication

- Commit and push the exact two-file primary change to `main` according to current repository policy.
- Publish one mailbox outbox at the expected path.
- On credentials-only mailbox publication failure, return the complete intended UTF-8 outbox payload and exact path.
- Do not edit mailbox STATE.

## Required outbox fields

Include:

- status;
- selected model and reasoning;
- preflight 12/12 summary;
- exact main parent and implementation commit;
- exact changed paths;
- source/mirror blob SHAs and equality result;
- profile-routing proof;
- exact target matrix with actual resolved text;
- representative unchanged Millennial/Zoomer proof;
- commands/tests and results;
- failures, forbiddenRemaining, missingCoverage, failedChecks;
- runtime classification;
- Safari status `N/A - later serialized deployment smoke`;
- mailbox parent/commit or complete auth-fallback payload;
- exact next user action `Return to ChatGPT and write мост 1`.
