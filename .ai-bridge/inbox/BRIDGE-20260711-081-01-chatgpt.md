Use @asynchronia.

# BRIDGE-20260711-081 - Stage 6 Boomer Step 4.4B Fix2

ISSUE: 216
CYCLE: CYCLE-20260711-003
GENERATION: 2
SLOT: 2
THREAD_ID: BRIDGE-20260711-081
LANE_ID: S6-BOOMER-STEP4_4B-CORRECTION
TASK_ID: TASK-S6-BOOMER-STEP4_4B-FIX2
EXECUTION_EPOCH: S6-BOOMER-4_4B-FIX2-R1-20260711-0500JST
TASK_NONCE: S6B44B-081-FIX2-0500
PHASE: EXECUTE_AND_PUBLISH
COORDINATOR_MEMORY_REVISION: 2026-07-11-0500-JST
AUTHORIZED_PRIMARY_BASELINE: c5ef0ecea5c96a54604e6d130207ac4fd52789df
SUPERSEDES_THREAD: BRIDGE-20260711-080
REJECTED_PRIMARY_COMMIT: c5ef0ecea5c96a54604e6d130207ac4fd52789df
CLAIM_PATH: .ai-bridge/claims/BRIDGE-20260711-081-claim-v1-codex.md
EXPECTED_OUTBOX_PATH: .ai-bridge/outbox/BRIDGE-20260711-081-02-codex.md
EXPECTED_RECEIPT_PATH: .ai-bridge/receipts/BRIDGE-20260711-081-03-codex.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
PLUGIN_INVOCATION_REQUIRED: true
PLUGIN_IDENTIFIER: asynchronia
RUNTIME_CLASSIFICATION: RUNTIME_SMOKE_INFRASTRUCTURE
SAFARI_STATUS: PENDING_USER
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true
MODEL_PREFLIGHT_GATE: RETIRED
MODEL_RECOMMENDATION_OWNER: ASYNCHRONIA_MODEL_SELECTOR
ACTUAL_MODEL_STATUS: USER_SELECTED_UNVERIFIED

## Rejection of Bridge 080

Bridge 080 is superseded and must not be accepted or reused as current authority.

Its outbox used `PASS_PUSHED` while required validations contained failures, changed only two paths instead of the exact four-path frozen scope, omitted mandatory plugin invocation evidence, and its receipt silently omitted the failed validation entries. The source commit also leaves the deployed cache-bust stale and does not satisfy the required state restoration and independent runtime-evidence contract.

## Atomic objective

Replace the rejected Fix1 aggregate with a complete evidence-derived Fix2 aggregate on current `main`. Produce one direct-child exact-four-path primary commit, prove every required validation from the committed tree, then publish immutable outbox and receipt artifacts that preserve all evidence without omission.

The current baseline commit `c5ef0ecea5c96a54604e6d130207ac4fd52789df` is repository history but is not accepted and is not authorized for Safari.

## Mandatory plugin route and evidence

Invoke the installed Asynchronia plugin and use material results in this exact order:

1. task-router
2. scope-isolation-check
3. model-selector
4. parallel-scope-planner
5. mirror-audit
6. smoke-orchestrator
7. deployment-verifier
8. closed-loop-controller
9. failure-routing-and-corrective-loop
10. acceptance-evidence-gate

The model recommendation is informational only. Do not stop for `MODEL_PREFLIGHT_ONLY`, `WAITING_FOR_MODEL_SELECTION`, `CONTINUE`, `APPROVE`, or any equivalent pause. Active model status remains `USER_SELECTED_UNVERIFIED`.

Both outbox and receipt must contain a top-level `pluginInvocationEvidence` object with:

- `pluginIdentifier: asynchronia`
- `invocationPrefix: Use @asynchronia.`
- `actualModelStatus: USER_SELECTED_UNVERIFIED`
- `skillResults`: exactly ten entries in the order above

Every skill result must contain the exact skill name, a passing status, a non-empty material result actually used by execution, and non-empty evidence references. A prefix line, installed files, or prose stating that the plugin was used is not invocation evidence.

Any absent, duplicated, reordered, renamed, skipped, blocked, or failed skill forbids `PASS_PUSHED`.

## Frozen primary write scope

The primary commit must change exactly all four paths below, no fewer and no more:

- AsyncScene/Web/dev/dev-checks.js
- docs/dev/dev-checks.js
- AsyncScene/Web/index.html
- docs/index.html

The source and docs counterparts are one serialized mirror ownership group. Shared smoke registration, exports, globals, runtime identity, and cache-bust wiring are owned only by this Slot 2 task for this epoch.

## Read-only evidence scope

- AGENTS.md
- TASKS.md
- PROJECT_MEMORY.md
- .ai-memory/CURRENT.md
- STAGE6_PARALLEL_EXECUTION_PLAN.md
- STAGE6_TRACEABILITY_CORRECTIONS.md
- UI_PROFILE_BOOMER_STEP_4_4_ECONOMY_CONFLICT_TERMINOLOGY_AUDIT.md
- docs/UI_PROFILE_BOOMER_STEP_4_4_ECONOMY_CONFLICT_TERMINOLOGY_AUDIT.md
- tools/generate-boomer-step4-4-economy-conflict-audit.py
- tools/validate-boomer-step4-4-economy-conflict-audit.py
- current plugin and orchestration authority required by routing
- exact runtime modules needed to derive and verify snapshots and runtime coverage, without editing them

## Protected scope

Do not change product copy, Data tone tables, system/profile resolvers, UI behavior, economy, battle, NPC, state, persistence, save behavior, action handlers, Stage 4.4A generator/validator/artifacts, Alpha implementation, shared project status documents, plugin source, bridge policy, or mailbox STATE.

## Required Fix2 correction evidence

1. Derive `checkedCount` from independently executed runtime checks with stable unique check identities. Do not infer coverage from a declared zone array or from the mere presence of boolean entries.
2. Each frozen feature zone must have its own real evidence source and verdict: points/💰, REP/⭐, Influence, voting, majority/minority outcomes, rematch, NPC-vs-NPC conflict text, conflict results, DM, reports, and report resolution.
3. Derive `forbiddenRemaining` and `missingCoverage` from structured failing evidence. Never hard-code empty arrays or make emptiness depend only on a final aggregate boolean.
4. Verify fallback resolution, placeholders, protected numbers, emoji contracts, cross-profile leakage, and taboo terms against the actual Boomer runtime outputs or resolvers used by the corresponding feature zones. A broad scan of static tone tables with heuristic words is insufficient.
5. Snapshot all mutable state required by the issue: profile/text mode, points, REP, Influence, battle state, `moneyLog`, persistence state, and relevant `localStorage` keys.
6. Restore every snapshot category, including battle state, `moneyLog`, persistence, and `localStorage`. Merely observing those fields without restoring them is a failure.
7. Compare restoration only after the restore operation. A pre-restore equality check cannot establish `stateRestored`.
8. Exercise isolated fixtures or reversible runtime paths that produce evidence. Do not manufacture permanent gameplay mutations and do not accept static label-to-check mappings as runtime coverage.
9. Preserve intentional source/docs differences. In `docs/dev/dev-checks.js`, keep the unrelated Alpha smoke block at its docs-specific `SOURCE_PATH` and `publishRoot` values.
10. Preserve the public command `Game.__DEV.smokeBoomerEconomyConflictTerminologyOnce()`.
11. Update both entrypoints so the published runtime loads the new Fix2 cache-bust and identity. Stale Fix1 or pre-Fix1 cache-busts fail deployment verification.

## Frozen new identity

- buildTag: build_2026_07_11_step4_4b_boomer_runtime_aggregate_fix2
- commit label: step4_4b_boomer_runtime_aggregate_fix2
- smokeVersion: boomer_step4_4b_runtime_aggregate_fix2_v20260711_001
- cache-bust: step4_4b_boomer_runtime_aggregate_fix2_20260711a

## Required validation

Run every command from a clean task-owned worktree on the final committed tree:

- python3 tools/generate-boomer-step4-4-economy-conflict-audit.py .
- python3 tools/validate-boomer-step4-4-economy-conflict-audit.py .
- node --check AsyncScene/Web/dev/dev-checks.js
- node --check docs/dev/dev-checks.js
- python3 tools/validate-orchestration-policy.py
- exact four-path changed-scope proof
- protected-path proof
- source/docs semantic mirror verification with the intentional Alpha difference preserved
- served entrypoint, runtime identity, and Fix2 cache-bust verification for both index files
- adversarial fixture proof for every new evidence evaluator family
- restoration proof for every snapshot category
- git diff --check

The Step 4.4A regeneration must remain `STATIC_PASS`, 147 PASS, 0 FAIL, 0 structural failures, with the existing 8 non-live checks preserved.

A dirty unrelated base checkout is not a passing validation result. Use the required clean task-owned worktree and rerun. Any required validation containing `FAIL`, `BLOCKED`, `ERROR`, `SKIPPED`, `NOT_RUN`, or equivalent forbids `PASS_PUSHED`.

## Outbox and receipt evidence contract

- `changedPaths` and `authorizedPaths` must both equal the exact four frozen paths.
- `validationResults` must include every required validation, including failures when present.
- The receipt must preserve the outbox `validationResults` byte-for-byte and in the same order.
- Receipt-only publication checks belong in a separate `publicationValidationResults` array.
- The receipt must preserve the complete `pluginInvocationEvidence` object from the outbox without omission or rewriting.
- No successful status may omit, summarize away, or convert a failed source validation.

## Publication contract

- Freshly fetch `origin/main` and `origin/coordination/chatgpt-codex-bridge` before execution.
- Use a clean task-owned primary worktree at the authorized baseline and a separate clean mailbox worktree.
- Do not touch the user's primary checkout.
- Create exactly one direct-child primary commit from `c5ef0ecea5c96a54604e6d130207ac4fd52789df` with exactly all four frozen write paths.
- Push fast-forward, refetch main, and prove first parent plus exact paths.
- Publish the exact outbox as a fast-forward child of the freshly fetched mailbox head.
- Refetch it, then publish the exact receipt as a separate fast-forward child.
- Refetch mailbox and return bytes identical to the remote receipt.
- Codex must not edit `.ai-bridge/STATE.md`.

## Acceptance boundary

Codex may report implementation, static, mirror, and deployment readiness only when every gate above passes. It must not claim Safari PASS. Final Safari status remains `PENDING_USER` until ChatGPT independently verifies the new primary commit, outbox, receipt, plugin evidence, validation preservation, exact scope, and deployed identity, and then gives the user the exact smoke command.

Terminal success action code: `OPEN_FRESH_CHATGPT_VERIFIER_AND_SEND_SAME_BRIDGE_COMMAND`.
