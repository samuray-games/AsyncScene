Use @asynchronia.

# Complete the closed-loop state machine, validator, routing and evidence contract

BRIDGE_PROTOCOL: 3.3
CYCLE_ID: CYCLE-20260709-001
CYCLE_GENERATION: 5
THREAD_ID: BRIDGE-20260709-050
LANE_ID: PROCESS-CLOSED-LOOP-COMPLETENESS-CORRECTION
TASK_ID: TASK-PROCESS-CLOSED-LOOP-COMPLETENESS-CORRECTION
EXECUTION_EPOCH: CLOSED-LOOP-COMPLETE-R1-20260709-2118JST
TASK_NONCE: CLV1-050-COMPLETE-9B17-2118
BRIDGE_SLOT: 3
PHASE: CORRECTION_REQUIRED
BASELINE: 9b170097e1ff0889ae0cb1e127516c51440c4c3d
COORDINATOR_MEMORY_REV: 2026-07-09-2118-JST
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260709-050-claim-v1-codex.md
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-050-02-codex.md
PUBLICATION_POLICY: .ai-bridge/PUBLICATION_POLICY_CLOSED_LOOP_V1.md
CLOSED_LOOP_PROTOCOL: .ai-bridge/CLOSED_LOOP_PROTOCOL.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
PLUGIN_BOOTSTRAP_FALLBACK: AUTHORIZED
FRESH_CODEX_CONVERSATION_REQUIRED: true
SAFARI_STATUS: N/A_PROCESS_ONLY
SUPERSEDES_THREAD: BRIDGE-20260709-049
ACCEPTED_PARTIAL_PRIMARY: 9b170097e1ff0889ae0cb1e127516c51440c4c3d

## Independent verifier verdict for thread 049

`CORRECTION_REQUIRED`.

Accepted facts:

- primary commit `9b170097e1ff0889ae0cb1e127516c51440c4c3d` is now remote main;
- it is one direct child of `ca2a3f88ac00a7e4fae47459758e7b09099a3f41`;
- its changed paths are within the frozen process/plugin/tool scope;
- expected outbox 049 exists remotely and reports byte equality;
- source plugin manifest is `1.0.5`;
- source `runtime-safety-gate/SKILL.md` is absent.

Rejected or missing facts:

1. The outbox violates the active required report schema. It omits cycle id, generation, nonce, coordinator MEMORY_REV, plugin source/version, ordered skill results, model-selector result, negative controls, failures, repair attempts, retry counts, missing coverage, protected-path proof, installed-package identity and checksum proof.
2. `tools/closed_loop_contract.py` is only a dataclass plus an outbox suffix check. It has no legal state machine, transition validation, generation/nonce/memory identity, artifact activation, orphan handling, recovery classification, retry policy, canary gate or product-release gate.
3. `tools/test_closed_loop_contract.py` contains only three trivial tests, uses stale thread 048 fixtures and does not cover the required positive and negative controls.
4. Root `CLOSED_LOOP_PROTOCOL.md` is only 38 lines and does not encode the coordinator transaction, Codex transaction, verifier transaction, state transitions, failure routing, recovery semantics or completion gate.
5. `closed-loop-controller` is descriptive only and explicitly says it does not implement orchestration. It omits cycle, generation, nonce, coordinator MEMORY_REV, claim identity, legal transitions, memory synchronization and canary gating.
6. `task-router` was not updated to route every bridge task through `closed-loop-controller` and `failure-routing-and-corrective-loop` in the required order.
7. `tools/validate-orchestration-policy.py` still validates the old phase set (`SCOPE_FREEZE`, `EXECUTE_AND_PUBLISH`, and similar) instead of the closed-loop legal states. It checks markers rather than deterministic transitions and recovery controls.
8. The workflow only runs the policy validator. It does not run closed-loop unit tests or Python compile, and the commit has no remote CI status evidence.
9. Installed Asynchronia 1.0.5 path, inventory, checksums, atomic install, old-version preservation, plugin-first smoke and unavailable-plugin negative smoke are absent.
10. Installed runtime-gate removal proof is absent.
11. Protected product/runtime blob equality is absent.

The useful partial commit is the immutable new baseline. Do not revert it and do not rebuild the same files from scratch without first auditing current bytes.

## Mandatory route

Freshly fetch main and mailbox, then verify this exact generation, thread, task, epoch, nonce, memory revision, baseline, claim and outbox.

Invoke in order:

1. `task-router` first;
2. `scope-isolation-check`;
3. `model-selector`;
4. `parallel-scope-planner` because shared process/plugin authorities are involved;
5. current source `closed-loop-controller` as an audited input, then replace it with the complete contract;
6. `failure-routing-and-corrective-loop`;
7. `acceptance-pipeline-controller`;
8. `pipeline-state-and-resume-contract`;
9. `evidence-bundle-and-artifact-identity`;
10. every additional routed skill.

Use governed source-plugin bootstrap when the installed plugin is not the required final 1.0.5 package. Never use a generic fallback.

## Required machine-readable identity

The shared contract and report schema must require at least:

- cycle id;
- cycle generation;
- thread id;
- lane id;
- task id;
- execution epoch;
- task nonce;
- slot;
- coordinator MEMORY_REV;
- baseline;
- inbox;
- claim;
- expected outbox;
- fetched mailbox commit;
- STATE blob SHA;
- plugin source and version;
- ordered skill results;
- model-selector recommendation and actual-model status;
- completion mode;
- primary commit and actual parent;
- exact changed paths;
- protected-path proof;
- tests, validators and negative controls;
- failures, repairs and retry counts;
- missing coverage;
- Safari status;
- remote outbox identity and byte equality;
- exact next action.

Missing, extra-identity-conflicting or malformed fields must fail closed.

## Required legal state machine

Only these states are legal:

- `CLOSED`
- `PREPARING`
- `READY_FOR_CODEX`
- `EXECUTING`
- `PRIMARY_PUBLISHED`
- `OUTBOX_PUBLISHING`
- `AWAITING_CHATGPT_VERIFICATION`
- `ACCEPTED`
- `CORRECTION_REQUIRED`
- `RECOVERY_REQUIRED`
- `BLOCKED_EXTERNAL`
- `SUPERSEDED`

Implement an explicit transition table. Every illegal transition fails. Include terminal and replacement-task rules.

The contract must enforce:

- STATE as the only activation pointer;
- unreferenced inbox/claim/outbox artifacts are inert;
- STATE referencing absent inbox or claim becomes recovery-required;
- `MEMORY_SYNC_PENDING` forbids execution;
- coordinator publishes inbox and claim first, STATE last, then memory;
- Codex verifies remote identity before routing;
- one atomic epoch per fresh Codex conversation;
- one independent verification per fresh ChatGPT conversation;
- old conversations cannot receive corrections or another task;
- every correction/recovery gets a new identity;
- primary commit without valid outbox selects report recovery;
- mailbox publication failure selects publication recovery;
- wrong implementation selects correction;
- external dependency selects blocked external;
- implementation acceptance automatically gates a separate canary;
- product work remains blocked until canary acceptance.

## Required deterministic controls

Positive controls must cover coordinator preparation/activation, Codex execution/publication, verifier acceptance, correction rotation, report recovery, publication recovery and canary acceptance.

Negative controls must cover at least:

1. missing memory;
2. stale memory;
3. memory sync pending;
4. STATE split-brain;
5. missing referenced inbox;
6. missing referenced claim;
7. orphan inbox/claim/outbox;
8. duplicate task identity;
9. duplicate nonce;
10. wrong slot;
11. stale thread;
12. stale epoch;
13. stale nonce;
14. old-conversation reuse;
15. claim collision;
16. plugin missing on ordinary task;
17. unauthorized source fallback;
18. invalid source plugin;
19. missing task-router-first evidence;
20. missing required routed skill;
21. runtime-gate reintroduction;
22. moved main with safe disjoint reapply;
23. moved main with overlap;
24. dirty unrelated worktree;
25. write outside frozen scope;
26. protected-path change;
27. non-reproducible validator claim;
28. false no-delta;
29. outside-scope test requirement;
30. primary non-fast-forward;
31. mailbox non-fast-forward;
32. empty outbox;
33. partial or handoff-only outbox;
34. wrong identity or nonce in outbox;
35. historical outbox reuse;
36. absent remote outbox;
37. remote byte mismatch;
38. missing retry evidence;
39. missing remote refetch evidence;
40. missing commit;
41. wrong parent;
42. lost main ancestry;
43. main advancement after task commit;
44. duplicate bridge after closure;
45. missing required Safari evidence;
46. product work before canary;
47. external auth/permission/service outage;
48. correction/report/publication recovery selection;
49. installed package identity mismatch;
50. source-installed checksum mismatch.

Tests must use current generic fixtures or thread 050 identity, not stale thread 048 fixtures.

## Routing and root authority corrections

- Update `task-router` so every numbered bridge task routes through `closed-loop-controller` and `failure-routing-and-corrective-loop` after the required core order.
- Update root authorities so the same legal states, identity schema, fresh-chat rule, memory-sync rule, publication transaction, recovery selection and canary gate are consistent.
- Remove conflicting old phase names from active closed-loop validation, or explicitly namespace legacy phases where they remain necessary.
- Fix cross-file protocol drift such as `ORCHESTRATION_VERSION: 3.3` paired with stale bridge markers.

## Validator and workflow

`tools/validate-orchestration-policy.py` must import the shared contract and actually execute deterministic contract validation rather than merely checking marker strings.

It must dynamically verify:

- exact legal state set and transition table;
- report schema;
- positive and negative control inventory;
- task-router mandatory bridge route;
- all required root authorities;
- exact discovered plugin skill inventory;
- workflow path coverage;
- absence of source runtime-gate aliases/routes/indexes;
- plugin manifest 1.0.5.

The workflow must run, on push and pull request:

- `python3 -m py_compile` for every changed contract/validator/test Python file;
- `python3 tools/validate-orchestration-policy.py`;
- `PYTHONPATH=. python3 -m unittest tools.test_closed_loop_contract`.

Include `tools/closed_loop_contract.py` and `tools/test_closed_loop_contract.py` in workflow path triggers.

## Installed plugin completion

Discover the user-level Asynchronia package root. Install source version 1.0.5 as a versioned sibling using temporary staging and atomic rename where supported.

Preserve:

- older versions;
- unrelated user files;
- file permissions;
- user-level managed instruction bytes outside the owned block.

Report:

- installed path;
- manifest version;
- exact source and installed skill inventory;
- checksums for manifest and every installed skill;
- source-installed equality;
- absence of installed runtime-gate skill, alias, route and index;
- plugin-first positive smoke;
- plugin-unavailable negative smoke;
- backup and retry evidence.

## Exact primary scope

Only these repository paths may change:

- `CLOSED_LOOP_PROTOCOL.md`
- `AGENTS.md`
- `AGENTS.override.md`
- `PROCESS_ROOT_SYNC.md`
- `ORCHESTRATION.md`
- `BRIDGE.md`
- `CODEX_BRIDGE_BOOTSTRAP.md`
- `CODEX_BRIDGE_RECOVERY.md`
- `GIT_PULL.md`
- `GIT_PUSH.md`
- `STAGE6_PARALLEL_EXECUTION_PLAN.md`
- `PROJECT_MEMORY.md`
- `TASKS.md`
- `.github/workflows/orchestration-policy.yml`
- `tools/validate-orchestration-policy.py`
- `tools/closed_loop_contract.py`
- `tools/test_closed_loop_contract.py`
- `plugins/asynchronia/.codex-plugin/plugin.json`
- `plugins/asynchronia/skills/closed-loop-controller/SKILL.md`
- `plugins/asynchronia/skills/task-router/SKILL.md`
- `plugins/asynchronia/skills/scope-isolation-check/SKILL.md`
- `plugins/asynchronia/skills/parallel-scope-planner/SKILL.md`
- `plugins/asynchronia/skills/model-selector/SKILL.md`
- `plugins/asynchronia/skills/acceptance-pipeline-controller/SKILL.md`
- `plugins/asynchronia/skills/pipeline-state-and-resume-contract/SKILL.md`
- `plugins/asynchronia/skills/evidence-bundle-and-artifact-identity/SKILL.md`
- `plugins/asynchronia/skills/failure-routing-and-corrective-loop/SKILL.md`
- `plugins/asynchronia/skills/runtime-safety-gate/SKILL.md` if present, deletion only.

All product/runtime paths are protected.

## Exact final validation

Validate the exact final committed tree and report:

- final commit and actual parent;
- clean task-owned worktree;
- complete changed paths versus frozen scope;
- protected product/runtime blob equality;
- Python compile;
- full orchestration validator;
- full closed-loop unit suite;
- exact number and names of positive and negative controls;
- plugin manifest and skill frontmatter;
- source and installed runtime-gate absence;
- installed path/inventory/checksum equality;
- plugin positive and unavailable negative smokes;
- `git diff --check`;
- failures, repair passes and retries.

## Publication

Publish one direct-child exact-scope commit from baseline `9b170097e1ff0889ae0cb1e127516c51440c4c3d`, unless a later disjoint main is safely adopted under policy.

Prepare one complete immutable report satisfying every field in the active publication policy. Publish exactly those bytes only to `.ai-bridge/outbox/BRIDGE-20260709-050-02-codex.md`, refetch remote main, mailbox and outbox, validate the report schema and prove byte equality before returning the same response.

The exact next instruction is: open a fresh ChatGPT conversation and send `мост 3`.

Do not run Safari. This correction cannot declare the loop complete. Independent acceptance must create a separate canary task.
