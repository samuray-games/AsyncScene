# Correct false semantic controls and exact receipt contract

BRIDGE_PROTOCOL: 3.3
CYCLE_ID: CYCLE-20260709-001
CYCLE_GENERATION: 15
THREAD_ID: BRIDGE-20260710-060
LANE_ID: PROCESS-CLOSED-LOOP-EXACT-SEMANTIC-CORRECTION
TASK_ID: TASK-PROCESS-CLOSED-LOOP-CORE-COMPLETION
EXECUTION_EPOCH: CLOSED-LOOP-SEMANTIC-R1-20260710-1322JST
TASK_NONCE: CLV1-060-SEMANTIC-3251-1322
BRIDGE_SLOT: 3
PHASE: CORRECTION_REQUIRED
BASELINE: 32513f02daf5943c41f24328e1ae251d6bc85ccc
COORDINATOR_MEMORY_REV: 2026-07-10-1322-JST
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260710-060-claim-v1-codex.md
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260710-060-02-codex.md
EXPECTED_RECEIPT: .ai-bridge/receipts/BRIDGE-20260710-060-03-codex.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
PLUGIN_DELIVERY_LANE: SEPARATE_EXTERNAL_NON_GATING
PLUGIN_INVOCATION_REQUIRED: false
PLUGIN_SOURCE_CHANGES_AUTHORIZED: false
MODEL_RECOMMENDATION_REQUIRED: false
SAFARI_STATUS: N/A_PROCESS_ONLY
SUPERSEDES_THREAD: BRIDGE-20260710-059

## Independent verdict on thread 059

`CORRECTION_REQUIRED_FALSE_SEMANTIC_CONTROLS_STALE_IDENTITY_AND_INCONSISTENT_SUCCESS_RECEIPT`.

Accepted publication evidence:

- remote main is exactly `32513f02daf5943c41f24328e1ae251d6bc85ccc`;
- its first parent is exact baseline `e599beddbbd03c8585f9c44f0f7c190338e123e7`;
- exact changed paths are the six reported primary paths;
- immutable outbox 059 exists with publication commit `98681bbfa1a5d624f67444dbcc9f940539b9975d` and blob `b935a3f21c4d5df0189b66bed3c7d3bedfb506f6`;
- receipt 059 exists as the next mailbox commit and records the actual outbox publication identity;
- the user-supplied JSON matches the remote receipt content.

Rejected source and receipt semantics:

1. The active thread-059 objective required exact equality against thread 059, but contract, tests, validator, and passing fixtures remain pinned to superseded thread 058.
2. `REQUIRED_IDENTITY_FIELDS` omits cycle id, generation, expected receipt, and other exact active identity fields.
3. The outbox schema still uses old ambiguous keys such as `verifiedPrimarySha`, `remoteMailboxCommit`, and `remoteStateSha`, while actual outbox and receipt use `primaryCommitSha`, `mailboxParentCommit`, and `stateBlobSha`.
4. `REPORT_SCHEMA_KEYS` remains only the old universal outbox schema. The declared `REQUIRED_RECEIPT_FIELDS` is not used as an exact executable receipt schema.
5. `validate_report_schema()` still permits forbidden `BLOCKED_NO_SOURCE_DELTA` and does not enforce exact success/status/recovery/action combinations.
6. SHA validation lowercases before checking and therefore accepts uppercase SHA values. Repeated synthetic 40-hex placeholders are not rejected.
7. Route and identity controls still return unconditional `True`.
8. Many controls only check presence or non-emptiness rather than executing the real validator against adversarial mutations.
9. The implementation transition table is also used as the validation oracle. No independent immutable canonical transition table exists.
10. Tests contain no mutation-style tests and no dedicated real-behavior test for every declared control.
11. Passing fixtures still contain superseded thread 058, stale thread 052, and synthetic `cafebabe`, `deadbeef`, `feedface`, `a111...`, and `b222...` SHA values.
12. `accept_product_work()` checks only `canaryAccepted` and `currentState`; it does not express the required independent implementation plus canary acceptance gate for this loop.
13. Receipt 059 reports `status: PASS_PUSHED` and `completionMode: PRIMARY_DELTA` together with `recoveryClassification: CORRECTION_REQUIRED`. That success combination is internally inconsistent.
14. Receipt 059 contains only free-text `nextAction`; it lacks exact action code `OPEN_FRESH_CHATGPT_VERIFIER_AND_SEND_SAME_BRIDGE_COMMAND`.
15. Root authorities were not fully aligned with the loop-only exact receipt contract.

Thread 059 publication shape is accepted as transport progress, but implementation is not accepted. Thread 059 must not continue.

## Frozen objective

Implement an executable, generic closed-loop contract that validates the exact active task package and terminal evidence without hard-coding a superseded execution identity.

### 1. Exact active identity

- Define one immutable identity schema containing exact cycle id, generation, slot, thread, lane, task, epoch, nonce, coordinator memory revision, baseline, inbox, claim, expected outbox, and expected receipt.
- Load the active identity from explicit input and compare every terminal artifact by exact equality.
- Current valid fixture identity is thread 060, generation 15, epoch `CLOSED-LOOP-SEMANTIC-R1-20260710-1322JST`.
- Remove all 052-059 identities from passing fixtures.

### 2. Exact separate schemas

- Implement separate exact status-specific outbox schemas.
- Implement a separate exact receipt schema and a separate validator entry point.
- Use unambiguous fields: `stateBlobSha`, `mailboxParentCommit`, `outboxPublicationCommit`, `outboxBlobSha`, `primaryCommitSha`, `primaryParent`, `outboxPath`, and `receiptPath`.
- Reject old ambiguous aliases in new terminal artifacts.
- Receipt must not require its own publication commit SHA.

### 3. Exact terminal combinations

For success require an exact consistent tuple equivalent to:

- `status: PASS_PUSHED`;
- `completionMode: PRIMARY_DELTA`;
- `recoveryClassification: NONE`;
- `nextActionCode: OPEN_FRESH_CHATGPT_VERIFIER_AND_SEND_SAME_BRIDGE_COMMAND`;
- exact human instruction to independently verify remote main, outbox, receipt, ancestry, paths, source semantics, and byte equality.

Define exact tuples for every blocked or recovery status. Arbitrary combinations fail closed.

`BLOCKED_NO_REMOTE_OUTBOX`, `BLOCKED_NO_SOURCE_DELTA`, and `BLOCKED_PLUGIN_UNAVAILABLE` are illegal for this primary-required loop-only task.

### 4. Executable controls

- Remove every unconditional-success branch.
- Each declared control must call the actual identity, route, schema, SHA, path, publication, status, transition, or acceptance validator.
- Unknown and unmapped controls fail closed.
- Presence and non-empty checks are not sufficient semantic controls.

### 5. Independent transition oracle

- Define an immutable canonical 12-state transition table separately from the implementation table.
- Test every legal and illegal pair by comparing implementation behavior against the independent canonical oracle.
- Mutating either table independently must make tests fail.

### 6. SHA and path proof

- Require exact lowercase `[0-9a-f]{40}` without normalization.
- Reject uppercase.
- Reject repeated synthetic patterns, including repeated short words or repeated single-character patterns.
- Compare actual primary changed paths exactly with frozen authorized paths.
- Validate protected paths and branch separation from explicit evidence inputs.
- Verify active STATE, inbox, claim, outbox, and receipt are absent from main.

### 7. Mutation tests

Add real tests using mocking or patching that independently bypass, remove, invert, or replace:

- identity equality;
- SHA validation;
- outbox schema validation;
- receipt schema validation;
- status tuple validation;
- path equality;
- protected-path proof;
- branch ancestry proof;
- transition validator;
- route-order validator;
- implementation acceptance gate;
- canary acceptance gate.

Every mutation must cause a failing validation or failing test. Inventory counts are not mutation proof.

### 8. Acceptance gate

- Source implementation acceptance and separate canary acceptance are distinct required facts.
- The loop becomes COMPLETE only when both are independently accepted and exact accepted state is recorded.
- Plugin installation and installed-package acceptance remain outside this loop gate.

### 9. Root synchronization

Align all affected root authorities and remove contradictory old field names, old outbox-only completion wording, plugin gating for loop-only tasks, and stale thread-specific examples.

## Frozen primary scope

Only:

- `AGENTS.md`
- `AGENTS.override.md`
- `PROCESS_ROOT_SYNC.md`
- `ORCHESTRATION.md`
- `BRIDGE.md`
- `CODEX_BRIDGE_BOOTSTRAP.md`
- `CODEX_BRIDGE_RECOVERY.md`
- `CLOSED_LOOP_PROTOCOL.md`
- `tools/closed_loop_contract.py`
- `tools/test_closed_loop_contract.py`
- `tools/validate-orchestration-policy.py`
- `TASKS.md`
- `PROJECT_MEMORY.md`

All other primary paths are protected. Plugin source, manifests, marketplace, cache, installation paths, product/runtime files, and Safari surfaces are protected.

## Required validation

Run from the exact committed tree:

- `python3 -m py_compile tools/closed_loop_contract.py tools/test_closed_loop_contract.py tools/validate-orchestration-policy.py`
- `python3 -m unittest tools.test_closed_loop_contract`
- `python3 tools/validate-orchestration-policy.py`
- `git diff --check`

The test suite must visibly contain and execute the required mutation tests. Validator PASS alone is not acceptance.

## Mandatory publication transaction

1. Freshly fetch main and mailbox.
2. Use clean task-owned worktrees from exact baseline `32513f02daf5943c41f24328e1ae251d6bc85ccc`.
3. Publish exactly one direct-child exact-scope primary commit.
4. Refetch and prove remote main, first parent, exact paths, and protected paths.
5. Publish immutable outbox only at `.ai-bridge/outbox/BRIDGE-20260710-060-02-codex.md`.
6. Refetch and record actual outbox publication commit and blob.
7. Publish separate receipt only at `.ai-bridge/receipts/BRIDGE-20260710-060-03-codex.md`.
8. Refetch main, STATE, outbox, receipt, and mailbox head.
9. Return user-visible bytes exactly identical to remote receipt.

No local-only completion, no IDE-local links, no `::git-stage`, and no `::git-commit` as evidence.

## Next action after successful publication

The exact success receipt must use action code `OPEN_FRESH_CHATGPT_VERIFIER_AND_SEND_SAME_BRIDGE_COMMAND` and instruct the user to open a fresh ChatGPT verifier and send exactly `мост 3`.
