Use @asynchronia.

# BRIDGE-20260711-087 - Complete automatic model preflight root sync and installed package parity

ISSUE: 216
CYCLE: CYCLE-20260711-003
GENERATION: 8
SLOT: 2
THREAD_ID: BRIDGE-20260711-087
LANE_ID: ASYNCHRONIA-PREFLIGHT-RESIDUAL-ROOT-SYNC
TASK_ID: TASK-ASYNCHRONIA-PREFLIGHT-RESIDUAL-SYNC
EXECUTION_EPOCH: ASYNCHRONIA-PREFLIGHT-R2-20260711-1135JST
TASK_NONCE: ASPF-087-1135
PHASE: MODEL_PREFLIGHT_REQUIRED
COORDINATOR_MEMORY_REVISION: 2026-07-11-1135-JST
AUTHORIZED_PRIMARY_BASELINE: 26493abe8b66dfe29b0dd799129c1f11acb77238
PARTIAL_PREDECESSOR_PR: 217
PARTIAL_PREDECESSOR_COMMIT: 26493abe8b66dfe29b0dd799129c1f11acb77238
SUPERSEDES_THREAD: BRIDGE-20260711-086
PREVIOUS_ATTEMPT_STATUS: BLOCKED_MAIN_BASELINE_MOVED_PARTIAL_IMPLEMENTATION_ALREADY_ON_MAIN
CLAIM_PATH: .ai-bridge/claims/BRIDGE-20260711-087-claim-v1-codex.md
EXPECTED_OUTBOX_PATH: .ai-bridge/outbox/BRIDGE-20260711-087-02-codex.md
EXPECTED_RECEIPT_PATH: .ai-bridge/receipts/BRIDGE-20260711-087-03-codex.md
MODEL_PREFLIGHT_POLICY: .ai-bridge/MODEL_PREFLIGHT_POLICY.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
PLUGIN_IDENTIFIER: asynchronia
PLUGIN_AUTO_START_REQUIRED: true
ACTIVE_INSTALLED_PLUGIN_SYNC_REQUIRED: true
PLUGIN_TOOL_TELEMETRY_REQUIRED: false
MODEL_RECOMMENDATION_REQUIRED: true
MODEL_RECOMMENDATION_PAUSE_REQUIRED: true
SAME_THREAD_CONTINUE_REQUIRED: true
CONTINUE_TOKEN: CONTINUE
ACTUAL_MODEL_STATUS: USER_SELECTED_UNVERIFIED
SAFARI_STATUS: N/A_PROCESS_AND_PLUGIN_ONLY
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true

## Baseline movement resolution

The Bridge 086 `BLOCKED_MAIN_BASELINE_MOVED` result is valid. Do not execute Bridge 086 against its stale baseline.

Current main `26493abe8b66dfe29b0dd799129c1f11acb77238` is a direct child of the old Bridge 086 baseline and contains PR 217, `Require automatic Asynchronia model preflight`.

Treat PR 217 as partial accepted input, not as a reason to repeat its edits and not as complete closure.

Independent verification found residual contradictions:

1. `plugins/asynchronia/.codex-plugin/plugin.json` is version `1.0.6`, but `.agents/plugins/marketplace.json` still declares `1.0.0`.
2. `AGENTS.md` still says model selection is informational, non-blocking, and must not become a pause or resume token.
3. `plugins/asynchronia/skills/task-router/SKILL.md` still says model selection is not a required approval stop and must not become a pause or resume token.
4. `plugins/asynchronia/skills/acceptance-pipeline-controller/SKILL.md` still says model selection is not a separate approval gate and explicitly forbids `CONTINUE`.
5. `plugins/asynchronia/skills/pipeline-state-and-resume-contract/SKILL.md` still says model recommendation evidence must not be a blocking preflight gate.
6. `ORCHESTRATION.md`, `BRIDGE.md`, `CODEX_BRIDGE_BOOTSTRAP.md`, and `CODEX_BRIDGE_RECOVERY.md` retain lower-authority no-preflight or immediate-execution clauses.
7. `tools/validate-asynchronia-auto-model-preflight.py` mostly checks literal strings in three files. It does not execute a declared transition model proving same-thread resume, wrong-thread rejection, malformed-token rejection, stale-scope rejection, or no mutation before resume.
8. Active local installed-plugin/cache identity and user-level bridge-block identity remain unverified.

Google Drive memory that labels PR 217 `PASS_REPOSITORY` is superseded for current implementation completeness by these primary repository facts and must be corrected after this lane.

## Mandatory phase 1 - recommendation and pause

On the first fresh `мост 2` for Bridge 087:

1. Freshly fetch main and mailbox.
2. Read current root authority, mailbox policy, model preflight policy, STATE, this inbox, and the claim.
3. Perform read-only discovery only.
4. Apply `task-router`, `scope-isolation-check`, and `model-selector` automatically.
5. Print the complete visible model recommendation for this exact residual scope.
6. Compare all available model/reasoning pairs according to `model-selector`; recommend the cheapest reliable pair and give one concrete failure mode for the next cheaper plausible pair.
7. Do not edit files, create worktrees, install or update a plugin, create commits, push refs, or publish mailbox artifacts.
8. Stop with `WAITING_FOR_MODEL_SELECTION`.
9. End with one standalone fenced block containing exactly `CONTINUE`, with no text after it.

## Resume boundary

Implementation begins only after exact trimmed `CONTINUE` in the same Codex conversation.

After `CONTINUE`, freshly refetch both remotes and verify unchanged thread, task, claim, baseline, and frozen scope. Any change invalidates the prior recommendation and requires a new preflight pause.

## Atomic residual objective after CONTINUE

Complete, rather than duplicate, PR 217:

- synchronize every remaining conflicting root authority and plugin skill;
- make manifest and marketplace package identity match;
- strengthen the validator into an actual deterministic preflight/resume transition contract with positive and adversarial fixtures;
- bump the plugin package for the new residual skill changes;
- synchronize and verify the active installed plugin/cache and the user-level bridge managed block;
- publish one direct-child exact-scope primary commit and immutable mailbox evidence.

Do not touch runtime game files. Runtime smoke correction remains a later separate lane.

## Frozen primary write scope

Exactly these primary paths may change:

- AGENTS.md
- ORCHESTRATION.md
- BRIDGE.md
- CODEX_BRIDGE_BOOTSTRAP.md
- CODEX_BRIDGE_RECOVERY.md
- .agents/plugins/marketplace.json
- plugins/asynchronia/.codex-plugin/plugin.json
- plugins/asynchronia/skills/task-router/SKILL.md
- plugins/asynchronia/skills/acceptance-pipeline-controller/SKILL.md
- plugins/asynchronia/skills/pipeline-state-and-resume-contract/SKILL.md
- tools/validate-asynchronia-auto-model-preflight.py

No other primary path may change.

Stable read dependencies that must not be rewritten unless a fresh contradiction is proved:

- AGENTS.override.md
- plugins/asynchronia/skills/model-selector/SKILL.md
- .github/workflows/asynchronia-auto-model-preflight.yml
- .github/workflows/orchestration-policy.yml

## Required final semantics

Every frozen authority and skill must agree that:

1. Asynchronia routing starts automatically for every project task.
2. Read-only discovery may identify scope before recommendation.
3. No implementation or mutable command occurs before preflight completion and exact same-thread `CONTINUE`.
4. `task-router`, `scope-isolation-check`, and `model-selector` are the mandatory ordered preflight route.
5. The recommendation is blocking and returns `WAITING_FOR_MODEL_SELECTION`.
6. The response ends with exactly one standalone fenced `CONTINUE` block and no trailing text.
7. Wrong thread, wrong token, quoted token, token embedded in longer text, stale task, changed scope, changed baseline, or changed claim cannot resume.
8. Resume freshly verifies authority before mutation.
9. A blocked response contains no `CONTINUE`.
10. The actual active model remains `USER_SELECTED_UNVERIFIED` unless independently proven.
11. Manual plugin attachment is optional context; repository fallback is mandatory when installed attachment cannot be resolved.
12. Installed package, repository manifest, and marketplace version are coherent.

Remove or rewrite every contradictory clause in frozen scope. Do not rely only on AGENTS.override precedence while leaving plugin skills internally self-contradictory.

## Package identity

Because this lane changes additional plugin skills beyond PR 217, bump the plugin package from `1.0.6` to `1.0.7` unless fresh main already contains a newer coherent version.

`plugins/asynchronia/.codex-plugin/plugin.json` and `.agents/plugins/marketplace.json` must declare the same exact version.

## Validator expansion

Expand `tools/validate-asynchronia-auto-model-preflight.py` into a deterministic contract validator.

It must define and execute a transition table or equivalent state model covering at least:

- `NEW_TASK -> PREFLIGHT_DISCOVERY`;
- `PREFLIGHT_DISCOVERY -> WAITING_FOR_MODEL_SELECTION`;
- exact same-thread `CONTINUE -> IMPLEMENTATION_ALLOWED`;
- wrong thread -> rejected;
- wrong token -> rejected;
- whitespace-trimmed exact token -> accepted;
- token embedded in longer text -> rejected;
- quoted token -> rejected;
- blocked state -> no token and no resume;
- changed task, scope, baseline, or claim -> stale recommendation and repeated preflight;
- no mutable action before resume;
- fresh authority verification before first mutation;
- second resume attempt after implementation start does not duplicate the transition.

The validator must also:

- parse both manifest and marketplace JSON;
- assert exact version equality and expected package version;
- scan every frozen authority and skill for required semantics;
- reject all known obsolete non-blocking/no-preflight clauses;
- verify the terminal fenced token contract structurally rather than only with one substring;
- include adversarial fixtures whose expected rejection is itself asserted;
- print bounded machine-readable or unambiguous PASS/FAIL output.

Simple presence of magic phrases is insufficient.

## Active installation and bridge-block synchronization

After the primary commit is pushed and remotely verified:

1. Determine `${CODEX_HOME:-$HOME/.codex}`.
2. Discover the active installed Asynchronia package/cache and active user-level instruction file.
3. Preserve unrelated user configuration byte-for-byte.
4. Use the supported non-destructive installation/update mechanism to synchronize installed Asynchronia to repository package `1.0.7`.
5. Run the current bootstrap to update the managed bridge block to the current protocol.
6. Verify installed manifest version, hashes for modified skills, and managed-block identity against committed repository files.
7. Record exact paths and hashes.

If no installed plugin copy exists but repository fallback is the active supported mode, prove that mode from current Codex configuration and synchronize the managed block. Do not invent an installed path.

After one supported non-destructive attempt, unresolved installation identity returns `BLOCKED_ACTIVE_PLUGIN_SYNC` with exact commands, paths, stdout, stderr, and preserved source commit evidence.

## Required validation after CONTINUE

Run from the final clean committed tree:

- `python3 tools/validate-asynchronia-auto-model-preflight.py`;
- `python3 -m py_compile tools/validate-asynchronia-auto-model-preflight.py`;
- JSON parsing for manifest and marketplace;
- manifest/marketplace exact version equality;
- positive transition fixtures;
- every adversarial rejection fixture;
- conflicting-clause scan across every frozen authority and skill;
- exact frozen-scope changed-path proof;
- direct-child proof from baseline `26493abe8b66dfe29b0dd799129c1f11acb77238`;
- proof no mutation occurred before same-thread `CONTINUE`;
- active installed package or proved fallback identity;
- user-level managed-block identity;
- `git diff --check 26493abe8b66dfe29b0dd799129c1f11acb77238...<FINAL_PRIMARY_SHA>`.

No validation may be represented as PASS unless its actual command or deterministic control was run successfully.

## Publication

Immediately before source push, refetch `origin/main` and prove it still equals the authorized baseline.

Push one direct-child commit by explicit non-force SHA refspec to `refs/heads/main`.

After source and installation verification, publish only:

- `.ai-bridge/outbox/BRIDGE-20260711-087-02-codex.md`;
- then `.ai-bridge/receipts/BRIDGE-20260711-087-03-codex.md` as a separate fast-forward mailbox commit.

Preserve in outbox and receipt:

- preflight recommendation and evaluated-pair evidence;
- exact same-thread `CONTINUE` evidence;
- proof of no mutation before resume;
- primary commit, parent, and exact paths;
- transition-table and adversarial fixture results;
- repository manifest and marketplace identity;
- active installed package or proved fallback identity;
- managed-block identity;
- ordered skill application evidence;
- no Safari claim.

Codex must not edit `.ai-bridge/STATE.md`.

Terminal success action code: `OPEN_FRESH_CHATGPT_VERIFIER_AND_SEND_SAME_BRIDGE_COMMAND`.
