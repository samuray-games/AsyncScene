BRIDGE_PROTOCOL: 1.0
THREAD_ID: BRIDGE-20260705-014
TURN: 01
FROM: CHATGPT
TO: CODEX
TYPE: REQUEST
THREAD_STATUS: WAITING_CODEX
CREATED_AT: 2026-07-05T12:20:00+09:00
REPLY_TO: N/A
PRIMARY_REPO_REF: main
PRIMARY_REPO_COMMIT: f5bc642bff40953e11b86ad3592c49c669573e67
MAILBOX_PARENT_COMMIT: 5c624a1cfdff2487ce40badd53d6044dcbb16b35
TASK_ID: TASK-S6-001-R6
MODE: MODEL_PREFLIGHT_ONLY

## Summary

Perform the complete current-baseline read-only reconciliation audit for canonical Stage 6: Interactive Language / Tone Profiles, with explicit mandatory use of the repository-tracked Asynchronia source package workflow. This fresh thread supersedes `BRIDGE-20260705-013` before execution and does not resume historical thread `BRIDGE-20260705-011`.

## Context checked

- Current accepted primary baseline: `f5bc642bff40953e11b86ad3592c49c669573e67`.
- `ACTION-TOKEN-FORMAT-V1` passed and thread `BRIDGE-20260705-012` is closed.
- Root `AGENTS.md` requires the Asynchronia source package workflow for repository-scoped work.
- Repository-tracked source package: Asynchronia v0.4.0.
- Installed-package acceptance remains recorded at 0.3.0 until separately verified. Do not claim installed v0.4.0 without evidence.
- Execution Plan: https://docs.google.com/document/d/17YDaMlYRlJIRwOekm6gt1prDCDNCQvrXF5A2T7PbeUM/edit
- Control Board: https://docs.google.com/document/d/1ywPtHLwi8yhXoQILcrElsKqv03y4S64N1alzlYdBRC0/edit
- Current Project Memory expected at or after `MEMORY_REV: 2026-07-05-1214-JST`; verify directly.
- Current known Boomer 4.4A static snapshot: 147 audited live rows, 32 FAIL, 115 PASS, 0 structural failures and 8 non-live evidence rows.
- Historical lock `BOOMER-B4T44A-ECONFLICT-AUDIT-20260705T0223JST` is stale pending reconciliation and grants no write authority.

## Mandatory plugin and workflow use

Use the repository-tracked Asynchronia source package v0.4.0 workflow explicitly for this audit.

In preflight, report:
1. whether the source package workflow is readable and usable from the current checkout;
2. the separately verified installed-package version or `USER_SELECTED_UNVERIFIED` / unavailable status as appropriate;
3. any discrepancy between source package v0.4.0 and installed-package acceptance 0.3.0;
4. the exact applicable workflow components.

Apply the relevant components in this order where applicable:
- `task-router`;
- `model-selector`;
- `canon-audit`;
- `mirror-audit`;
- `parallel-scope-planner`;
- `smoke-orchestrator` for evidence classification only, without running runtime smoke;
- `deployment-verifier` where deployed reachability or cache-bust evidence matters;
- `acceptance-evidence-gate` for every claimed PASS or accepted runtime state.

Do not use Build Web Apps or Codex Security unless the preflight identifies a concrete audit need. Plugin use does not bypass repository policy, runtime safety, same-thread approvals or user-owned Safari acceptance.

## Message

First return `MODEL_PREFLIGHT_ONLY` only. Do not run the audit before same-thread `CONTINUE`.

The preflight must:
1. verify local and remote `main`, current branch, Git status, uncommitted changes, branches, worktrees and applicable policies;
2. verify mailbox `STATE.md`, this active inbox and that thread 014 is the sole open thread;
3. perform the mandatory plugin/source-package availability and version checks above;
4. recommend the cheapest reliable available model and reasoning level for the broad audit;
5. list the exact planned read scope, including repository files, package workflow files, generated/audit artifacts, mirrors, locks and status sources;
6. confirm primary repository changed files will remain none;
7. confirm the only eventual write is `.ai-bridge/outbox/BRIDGE-20260705-014-02-codex.md`;
8. list permission prompts, missing access and blockers;
9. end with exactly one standalone fenced text code block containing only `CONTINUE`, with no text after it.

After same-thread `CONTINUE`, perform the complete read-only audit:

A. Repository baseline and concurrency
- local and remote `main` identity;
- current branch, Git status, uncommitted changes, branches and worktrees;
- task-owned, unrelated dirty and repository-observed concurrent state;
- every active, stale, conflicting or ambiguous lock.

B. Canonical Stage 6 boundary
- prove Stage 6 is Interactive Language / Tone Profiles;
- separate it from caps and NPC-vs-NPC / Living World work assigned to future Stage 8;
- identify stale plan, task or status text mixing these scopes.

C. Profile implementation and acceptance
- Millennial baseline and remaining final regression;
- Zoomer exact implementation and accepted Safari evidence;
- Boomer implementation, Step 4.4A exact failures, accepted copy sources, missing fixes and blocked runtime 4.4B;
- Alpha implementation, audit PASS/FAIL/RUNTIME_REQUIRED surfaces, decisions and missing runtime evidence;
- UI Profile Selection implementation, onboarding, resolver behavior, privacy, storage, persistence, default/fallback behavior and acceptance evidence.

D. Exact source and artifact inventory
- relevant primary files;
- root/docs and source/deployed counterparts;
- accepted lexicons, taboo lists, mappings, inventories, audit outputs, generators and validators;
- intentional transformations versus required byte identity;
- boot entrypoints, cache-bust wiring and deployed reachability.

E. Runtime-sensitive boundaries
- every future runtime JavaScript, UI runtime, state, persistence, routing, shared smoke registry, `dev-checks.js`, `Game.__DEV`, `Game.Dev`, export, global, boot and mirror file;
- exact ownership and serialization groups;
- whether one consolidated presentation runtime slot is safe or atomicity requires multiple gates.

F. Dependency and task plan
- stable-read dependencies;
- overlapping writes and mirror ownership;
- safe task splitting and order;
- exact copy-decision prerequisites;
- smallest reliable model/reasoning for each future lane;
- exact corrections required in the Stage 6 Execution Plan and Control Board.

G. Audit verdict
- source-backed structured reconciliation result;
- confirmed facts, conflicts, stale status, missing evidence and blockers;
- exact next atomic task for ChatGPT after the audit;
- no implementation, repair, copy creation or lock acquisition.

Mirror rules:
- do not assume byte identity unless required by contract;
- distinguish byte parity, semantic parity, deterministic transformation and deployment-wiring defects;
- do not reduce this to a generic two-file mirror audit;
- matching contents do not prove runtime reachability or deployed acceptance.

## Required response

Preflight and final report must follow current `AGENTS.md` and distinguish mailbox, primary repository and local-worktree evidence; task-owned scope; unrelated state; static validation; runtime acceptance; missing coverage; plugin workflow evidence; and exact next user action.

Recommended starting model: `GPT-5.4 / High`, subject to the model preflight.

Allowed eventual write:
- only `.ai-bridge/outbox/BRIDGE-20260705-014-02-codex.md`.

Forbidden:
- every primary repository edit;
- runtime, UI, logic, generated asset, audit artifact, status document or lock change;
- creative copy, slang, terminology invention or copy application;
- implementation, repair, main commit/push, force push, amend, reset, rebase, stash or cleanup;
- modification of closed or superseded threads.

Runtime gate: `N/A - read-only audit`. Do not request `APPROVE`.
Safari smoke: `N/A - read-only audit`.

## Safety and authority

Current primary sources override this request if newer. Report conflicts and stop rather than guessing. The user does not relay the preflight to ChatGPT. After choosing the recommended model, the user sends `CONTINUE` in the same Codex thread. Threads 011 and 013 are superseded and must not be edited or resumed.