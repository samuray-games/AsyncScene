Use @asynchronia.

# BRIDGE-20260711-088 - Complete changed-task and repeated-preflight validator coverage

ISSUE: 216
CYCLE: CYCLE-20260711-003
GENERATION: 9
SLOT: 2
THREAD_ID: BRIDGE-20260711-088
LANE_ID: ASYNCHRONIA-PREFLIGHT-VALIDATOR-CORRECTION
TASK_ID: TASK-ASYNCHRONIA-PREFLIGHT-CHANGED-TASK-FIXTURE
EXECUTION_EPOCH: ASYNCHRONIA-PREFLIGHT-VALIDATOR-R1-20260711-1224JST
TASK_NONCE: ASPFV-088-1224
PHASE: MODEL_PREFLIGHT_REQUIRED
COORDINATOR_MEMORY_REVISION: 2026-07-11-1224-JST
AUTHORIZED_PRIMARY_BASELINE: 2007440347e48277c07572ff96fc2545efdefe19
SUPERSEDES_THREAD: BRIDGE-20260711-087
PREVIOUS_ATTEMPT_STATUS: SOURCE_AND_PUBLICATION_PASS_VALIDATOR_COVERAGE_INCOMPLETE
CLAIM_PATH: .ai-bridge/claims/BRIDGE-20260711-088-claim-v1-codex.md
EXPECTED_OUTBOX_PATH: .ai-bridge/outbox/BRIDGE-20260711-088-02-codex.md
EXPECTED_RECEIPT_PATH: .ai-bridge/receipts/BRIDGE-20260711-088-03-codex.md
MODEL_PREFLIGHT_POLICY: .ai-bridge/MODEL_PREFLIGHT_POLICY.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
PLUGIN_IDENTIFIER: asynchronia
PLUGIN_AUTO_START_REQUIRED: true
ACTIVE_INSTALLED_PLUGIN_WRITE_REQUIRED: false
ACTIVE_INSTALLED_PLUGIN_READBACK_REQUIRED: true
PLUGIN_TOOL_TELEMETRY_REQUIRED: false
MODEL_RECOMMENDATION_REQUIRED: true
MODEL_RECOMMENDATION_PAUSE_REQUIRED: true
SAME_THREAD_CONTINUE_REQUIRED: true
CONTINUE_TOKEN: CONTINUE
ACTUAL_MODEL_STATUS: USER_SELECTED_UNVERIFIED
SAFARI_STATUS: N/A_VALIDATOR_ONLY
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true

## Independent verifier result for Bridge 087

Bridge 087 source and publication mechanics are accepted as current predecessor evidence:

- remote main equals `2007440347e48277c07572ff96fc2545efdefe19`;
- it is exactly one direct child of baseline `26493abe8b66dfe29b0dd799129c1f11acb77238`;
- it changes exactly the 11 authorized paths;
- manifest and marketplace both declare Asynchronia `1.0.7`;
- root authority and three conflicting plugin skills now use blocking same-thread model preflight semantics;
- outbox and receipt exist as separate fast-forward mailbox commits;
- repository manifest SHA-256 matches the immutable installation evidence recorded by Bridge 087.

Do not rewrite or republish those accepted source changes.

Bridge 087 is not fully accepted because its task-specific validator misses exact required adversarial coverage.

## Exact verifier defect

`tools/validate-asynchronia-auto-model-preflight.py` currently:

1. checks task mismatch in `PreflightContract.resume()`;
2. but does not execute a `changed_task` adversarial fixture;
3. executes changed scope, baseline, claim, and wrong-thread cases;
4. but asserts only their returned status;
5. does not assert the required `repeated_preflight == true` result for stale identity cases.

The Bridge 087 contract required changed task, scope, baseline, or claim to produce stale recommendation **and repeated preflight**. A code branch without an executed fixture is not acceptance evidence.

## Mandatory model preflight

On the first fresh `мост 2` for Bridge 088:

1. fetch main and mailbox;
2. read current authority, STATE, this inbox, and the claim;
3. perform read-only discovery only;
4. automatically apply `task-router`, `scope-isolation-check`, and `model-selector`;
5. show the complete model recommendation;
6. do not edit, create a worktree, commit, push, or publish;
7. stop with `WAITING_FOR_MODEL_SELECTION`;
8. end with one standalone fenced `CONTINUE` block and no trailing text.

This is one deterministic validator-file correction. The cheapest plausible frontier must begin at `GPT-5.4-Mini / Light`. Promotion requires one concrete task-specific failure mode, not general nervousness around Git.

Implementation begins only after exact trimmed `CONTINUE` in the same Codex conversation, followed by fresh unchanged-authority verification.

## Atomic objective after CONTINUE

Correct and validate the missing adversarial coverage without changing the already accepted plugin package, policy files, installed plugin, or bridge block.

## Frozen primary write scope

Exactly one primary path may change:

- tools/validate-asynchronia-auto-model-preflight.py

No other primary path may change.

## Required code correction

The validator must:

1. add an explicit `changed_task` `ResumeAttempt` fixture with:
   - valid thread;
   - exact token `CONTINUE`;
   - a task value different from `VALID_TASK`;
   - otherwise valid scope, baseline, claim, and fresh-authority evidence;
2. assert that the changed-task result is exactly `STALE_RECOMMENDATION`;
3. assert that changed task sets `repeated_preflight == true`;
4. assert `repeated_preflight == true` for every stale identity case:
   - wrong thread;
   - changed task;
   - changed scope;
   - changed baseline;
   - changed claim;
5. assert `implementation_allowed == false` for every adversarial rejection fixture;
6. keep wrong token, embedded token, quoted token, blocked state, and missing fresh authority as non-resuming failures;
7. add explicit bounded PASS output fields proving at least:
   - `changedTaskStale: PASS`;
   - `staleIdentityRepeatsPreflight: PASS`;
   - `adversarialImplementationDenied: PASS`;
8. preserve every existing successful transition and text/package check.

Do not weaken the identity comparison, terminal fence check, manifest/marketplace parity, authority scan, or no-mutation-before-resume control.

## Stable read dependencies

Read and preserve without edits:

- AGENTS.override.md
- AGENTS.md
- ORCHESTRATION.md
- BRIDGE.md
- CODEX_BRIDGE_BOOTSTRAP.md
- CODEX_BRIDGE_RECOVERY.md
- .agents/plugins/marketplace.json
- plugins/asynchronia/.codex-plugin/plugin.json
- plugins/asynchronia/skills/model-selector/SKILL.md
- plugins/asynchronia/skills/task-router/SKILL.md
- plugins/asynchronia/skills/acceptance-pipeline-controller/SKILL.md
- plugins/asynchronia/skills/pipeline-state-and-resume-contract/SKILL.md

## Package and installation boundary

The plugin package remains `1.0.7`. Do not bump it because no plugin package file changes.

Do not rewrite the active installed plugin or user-level managed block. Read back and verify:

- installed manifest path and version remain `1.0.7`;
- repository manifest SHA-256 still equals the installed manifest SHA-256 recorded by current evidence;
- managed block remains `ASYNCHRONIA_BRIDGE_ALIAS_V2_4`.

If readback differs, return an exact blocker. Do not perform an unrequested installation repair in this one-file lane.

## Required validation after CONTINUE

Run from the final clean committed tree:

- `python3 tools/validate-asynchronia-auto-model-preflight.py`;
- `python3 -m py_compile tools/validate-asynchronia-auto-model-preflight.py`;
- explicit changed-task fixture result;
- explicit repeated-preflight assertions for all five stale identity fixtures;
- explicit implementation-denied assertions for every adversarial rejection fixture;
- all existing positive and adversarial fixtures;
- manifest/marketplace parity remains `1.0.7`;
- conflicting-clause scan remains PASS;
- exact one-path diff proof;
- sole-parent proof from `2007440347e48277c07572ff96fc2545efdefe19`;
- no mutation before same-thread `CONTINUE`;
- fresh post-resume authority verification;
- installed plugin and managed-block readback;
- `git diff --check 2007440347e48277c07572ff96fc2545efdefe19...<FINAL_PRIMARY_SHA>`.

## Publication

Immediately before push, refetch main and prove it still equals the authorized baseline.

Push one direct-child, one-path commit by explicit non-force SHA refspec to `refs/heads/main`.

Then publish only:

- `.ai-bridge/outbox/BRIDGE-20260711-088-02-codex.md`;
- `.ai-bridge/receipts/BRIDGE-20260711-088-03-codex.md` as a separate fast-forward mailbox child.

Outbox and receipt must preserve:

- exact preflight and same-thread `CONTINUE` evidence;
- primary SHA, parent, and one changed path;
- full validation results;
- explicit changed-task and repeated-preflight fixture evidence;
- read-only installed-plugin and managed-block identity;
- skill application evidence;
- no Safari claim.

Codex must not edit `.ai-bridge/STATE.md`.

Terminal success action code: `OPEN_FRESH_CHATGPT_VERIFIER_AND_SEND_SAME_BRIDGE_COMMAND`.
