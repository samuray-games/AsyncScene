BRIDGE_PROTOCOL: 1.0
THREAD_ID: BRIDGE-20260705-026
TURN: 01
FROM: CHATGPT
TO: CODEX
TYPE: REQUEST
THREAD_STATUS: WAITING_CODEX
CREATED_AT: 2026-07-07T13:43:00+09:00
REPLY_TO: N/A
PRIMARY_REPO_REF: main
PRIMARY_REPO_COMMIT: 1f4e8cf26bea8ca7494ea727fcca317b5c7f8f27
MAILBOX_PARENT_COMMIT: 6efd4d55702833957ff1f624a4f8e61bb3b780bb
TASK_ID: BRIDGE-ALIAS-V2-MIGRATION
MODE: MODEL_PREFLIGHT_ONLY
USER_COMMAND_ALIAS: мост

Use @asynchronia runtime-safety-gate.

## Goal

Complete the exact bridge-command migration from the former phrase to the single word `мост` for Codex, matching the already-updated ChatGPT rule and main `BRIDGE.md`.

## Current state

- Main `BRIDGE.md` already uses only `мост` on commit `1f4e8cf26bea8ca7494ea727fcca317b5c7f8f27`.
- Root `AGENTS.md` still names the former phrase.
- `CODEX_BRIDGE_BOOTSTRAP.md` still installs the former V1 global alias.
- The active user-level Codex instruction file under `${CODEX_HOME:-$HOME/.codex}` may still contain the former managed V1 block.

## Mandatory preflight

Return `MODEL_PREFLIGHT_ONLY` only. Do not edit, back up, stage, commit, push, or publish an outbox before valid same-thread `CONTINUE`.

Model selection belongs exclusively to `model-selector`. Report the complete task-router and model-selector contracts, full 12/12 matrix, current-thread plugin resolver/load evidence, and `actual active model: USER_SELECTED_UNVERIFIED`.

## Authorized writes after CONTINUE

Repository:

- `AGENTS.md`
- `CODEX_BRIDGE_BOOTSTRAP.md`

User-level Codex configuration:

- the selected active instruction file under `${CODEX_HOME:-$HOME/.codex}` using normal Codex precedence: `AGENTS.override.md` if it already exists as a regular file, otherwise `AGENTS.md`;
- one timestamped backup of that selected instruction file beside it before modification, when it already exists.

Mailbox:

- `.ai-bridge/outbox/BRIDGE-20260705-026-02-codex.md`

No other path may change.

## Exact migration contract

1. In root `AGENTS.md`, reserve the exact trimmed command `мост` for the mailbox bridge.
2. Remove the former phrase as an active alias. Historical audit records are not rewritten.
3. Update `CODEX_BRIDGE_BOOTSTRAP.md` to V2 so its managed block reserves exactly `мост`.
4. Discover the active user-level Codex instruction file without creating `AGENTS.override.md` merely to gain precedence.
5. Back up an existing selected instruction file before the first write.
6. Remove exactly one complete V1 managed alias block if present.
7. Install exactly one V2 managed alias block for `мост`.
8. Preserve all content outside the managed blocks byte-for-byte.
9. If only one marker from a managed pair exists, stop with `BLOCKED_MALFORMED_EXISTING_ALIAS` and make no user-level change.
10. Do not claim the current Codex thread reloaded the new global instruction. The new alias is accepted only in a new Codex thread.

## Validation

- exact repository diff limited to `AGENTS.md` and `CODEX_BRIDGE_BOOTSTRAP.md`;
- `git diff --check`;
- root policy contains active alias `мост` and does not present the former phrase as active;
- bootstrap contains exactly one complete V2 template and no active V1 installation instruction;
- selected user-level instruction file has exactly one V2 begin marker and one V2 end marker, no V1 markers, and unchanged content outside managed blocks;
- report backup path or `N/A - newly created`;
- runtime gate, Safari smoke and deployment: `N/A - policy and local configuration only`.

## Publication lifecycle

Codex may finish repository edits with `commit: N/A - pending user publication`. Distinguish this from later user publication. Publish the required mailbox outbox through the fail-closed mailbox branch guard. Do not declare the bridge task fully accepted until ChatGPT verifies the later repository commit and the immutable outbox.
