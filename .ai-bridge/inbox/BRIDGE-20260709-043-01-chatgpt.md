Use @asynchronia runtime-safety-gate.

# Process correction - unify model-selection semantics and validator coverage

BRIDGE_PROTOCOL: 3.2
THREAD_ID: BRIDGE-20260709-043
LANE_ID: PROCESS-MODEL-SELECTION-SEMANTICS-FIX
TASK_ID: TASK-PROCESS-MODEL-SELECTION-SEMANTICS-FIX
EXECUTION_EPOCH: MSF-E1-20260709-1627JST
PHASE: CORRECTION_REQUIRED
BRIDGE_SLOT: 1
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260709-043-claim-v1-codex.md
REMOTE_MAIN_BASELINE: 5505f19b379cdf9a4559c1e6d5dd8292160e599b
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-043-02-codex.md
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
SAFARI_STATUS: N/A_PROCESS_ONLY
SUPERSEDES_THREAD: BRIDGE-20260709-041
REJECTION_CLOSURE: .ai-bridge/inbox/BRIDGE-20260709-041-10-chatgpt.md

## Frozen objective

Remove the internal contradiction between non-blocking model recommendations and retired model-preflight stopping semantics. Expand orchestration validation so every installed Asynchronia plugin skill is scanned automatically and future stale semantics fail closed.

This is one atomic process/plugin correction. Do not modify product/runtime files, Stage 6 copy, Boomer smoke code, audit artifacts, `TASKS.md`, `PROJECT_MEMORY.md`, or primary `.ai-bridge/**` files.

## Explicit policy resolution

The accepted Protocol 3.2 one-command bridge policy remains authoritative:

- only `plugins/asynchronia/skills/model-selector/SKILL.md` may originate, rank, or name a Codex model and reasoning recommendation;
- the user alone selects the actual active model in the Codex interface;
- the actual active model remains `USER_SELECTED_UNVERIFIED` unless externally verified;
- the selector recommendation is informational evidence for the current run and guidance for later equivalent runs;
- it must never pause, block, authorize, or resume implementation;
- remove `MODEL_PREFLIGHT_ONLY`, `WAITING_FOR_MODEL_SELECTION`, fenced `CONTINUE`, mandatory model-selection pauses, and valid-preflight resume dependencies from active contracts;
- a stale or scope-mismatched recommendation is recomputed in the same execution response when applicable, not converted into a user round trip;
- if the recommendation differs from the user-selected model, record the difference truthfully but do not stop the current collision-free lane;
- scope isolation, ownership, dependency, lock, deployment, static validation, and user-owned Safari boundaries remain unchanged.

This explicitly chooses the already-accepted no-preflight policy. Do not reintroduce a hidden stop under a synonym.

## Exact primary write scope

Only these paths may change:

- `AGENTS.md`
- `.github/workflows/orchestration-policy.yml`
- `tools/validate-orchestration-policy.py`
- `plugins/asynchronia/.codex-plugin/plugin.json`
- `plugins/asynchronia/skills/model-selector/SKILL.md`
- `plugins/asynchronia/skills/task-router/SKILL.md`
- `plugins/asynchronia/skills/acceptance-pipeline-controller/SKILL.md`
- `plugins/asynchronia/skills/pipeline-state-and-resume-contract/SKILL.md`

All other primary paths are protected.

## Required contract changes

1. `model-selector` must retain the full 12/12 pair analysis and plugin-only recommendation authority, but have no stopping mode or continuation token.
2. `task-router` must state that it may carry only the selector's exact output without originating, altering, or turning it into an execution prerequisite.
3. `acceptance-pipeline-controller` must remove `WAITING_FOR_MODEL_SELECTION`, `model preflight result`, preflight-complete examples, and every equivalent stop or gate while preserving truthful recommendation reporting.
4. `pipeline-state-and-resume-contract` must preserve recommendation evidence and `USER_SELECTED_UNVERIFIED`, not a blocking preflight gate.
5. `AGENTS.md` must state the same plugin-only, non-blocking recommendation boundary without naming a recommended pair itself.
6. Bump the plugin patch version from `1.0.3` to `1.0.4` and keep metadata coherent.

## Validator requirements

`tools/validate-orchestration-policy.py` must:

- discover and scan every current `plugins/asynchronia/skills/*/SKILL.md` automatically rather than using a handpicked subset;
- fail if any skill is omitted from scanning or workflow trigger coverage;
- reject active semantic variants including `MODEL_PREFLIGHT_ONLY`, `WAITING_FOR_MODEL_SELECTION`, mandatory `CONTINUE`, `model preflight`, `valid preflight selection`, `waiting for model selection`, mandatory model-selection pause, and model-selection-as-authorization language;
- require positive plugin-only recommendation authority, non-blocking execution semantics, `USER_SELECTED_UNVERIFIED`, and same-response recomputation on material scope change;
- validate plugin version `1.0.4`, JSON syntax, all skill frontmatter/name pairs, and workflow coverage;
- preserve all existing Protocol 3.2 runtime-gate-removal checks;
- print the exact dynamically discovered and scanned paths.

The workflow must trigger for all current and future plugin skill contract files, preferably through a safe skills-path glob plus the existing root-policy paths.

## Required validation

Run and report:

- `python3 tools/validate-orchestration-policy.py`
- comprehensive semantic search across all root process files and every plugin skill for the retired model-preflight markers and variants
- positive search proving plugin-only recommendation authority and non-blocking semantics
- plugin JSON validation for `1.0.4`
- dynamic skill discovery versus scanned-path equality
- changed-skill frontmatter/name validation
- workflow trigger coverage validation
- exact eight-path changed-scope verification
- `git diff --check`

The negative semantic search must distinguish unrelated product uses of words such as `continue` from process-control tokens. Do not weaken validation through broad exclusions.

## Execution and publication

On the first exact `мост 1` in a fresh Codex conversation:

1. fetch current main and mailbox;
2. verify this thread, epoch, inbox, claim, baseline, scope, and expected outbox;
3. run `scope-isolation-check`; execute immediately if the exact lane is collision-free;
4. invoke `model-selector` as the only recommendation authority, report its output without creating a stop, and continue in the same response;
5. implement only the eight authorized primary paths;
6. run all required validation;
7. create one direct-child primary commit and push main fast-forward;
8. publish only `.ai-bridge/outbox/BRIDGE-20260709-043-02-codex.md` from a clean current mailbox worktree;
9. refetch both refs and return `PASS_PUSHED` with exact identities, parent, changed paths, validation evidence, selector-originated recommendation, actual model status `USER_SELECTED_UNVERIFIED`, and `Safari smoke status: N/A_PROCESS_ONLY`.

Do not modify or repair Step 4.4B in this task. Do not stop for model selection, `CONTINUE`, runtime approval, `APPROVE`, or Safari.
