Use @asynchronia.

# Implement the complete fresh-thread ChatGPT-Codex closed loop

BRIDGE_PROTOCOL: 3.3
CYCLE_ID: CYCLE-20260709-001
CYCLE_GENERATION: 1
THREAD_ID: BRIDGE-20260709-046
LANE_ID: PROCESS-CLOSED-LOOP-ORCHESTRATION
TASK_ID: TASK-PROCESS-CLOSED-LOOP-IMPLEMENTATION
EXECUTION_EPOCH: CLOSED-LOOP-E1-20260709-1930JST
TASK_NONCE: CLV1-046-E1-83FF-7C9A
BRIDGE_SLOT: 2
PHASE: IMPLEMENTATION_REQUIRED
BASELINE: 83ff4668bd2a7401a933794668c16b7ea62c08e2
COORDINATOR_MEMORY_REV: 2026-07-09-1918-JST
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260709-046-claim-v1-codex.md
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-046-02-codex.md
PUBLICATION_POLICY: .ai-bridge/PUBLICATION_POLICY_CLOSED_LOOP_V1.md
CLOSED_LOOP_PROTOCOL: .ai-bridge/CLOSED_LOOP_PROTOCOL.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
PLUGIN_BOOTSTRAP_FALLBACK: AUTHORIZED
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true
SAFARI_STATUS: N/A_PROCESS_ONLY
SUPERSEDES_THREAD: BRIDGE-20260709-045
SUPERSEDES_EPOCH: PLUGIN-E5-20260709-1918JST

## Absolute priority

The completed resumable ChatGPT-Codex cycle is the highest-priority project task. Do not resume product work and do not run Safari.

This task supersedes thread 045 and incorporates every unfinished plugin-first, installed-package, validator, runtime-gate-removal, outbox and recovery requirement from that thread.

One task instance equals one fresh Codex conversation. Do not use or continue any conversation that executed thread 045. This thread may perform only this implementation task and expires after its final response.

## Required plugin route

Read the current remote task package first. Then invoke, in order:

1. `task-router`;
2. `scope-isolation-check`;
3. `model-selector`;
4. `parallel-scope-planner` because this task governs three slots and shared process files;
5. `closed-loop-controller` from source after creating or staging it when necessary;
6. `failure-routing-and-corrective-loop`;
7. every additional skill selected by `task-router`.

Current installed plugin may be older than the required final package. Use governed `SOURCE_PLUGIN_FALLBACK_BOOTSTRAP` from baseline source version 1.0.4, then build and install final Asynchronia plugin version 1.0.5.

## Implementation objective

Encode one deterministic state machine that synchronizes fresh ChatGPT coordinator chats, fresh Codex execution chats, complete outboxes, fresh ChatGPT verification chats, correction epochs and the next task generation.

The implementation must enforce:

- one atomic epoch per fresh Codex conversation;
- one outbox verification per fresh ChatGPT conversation;
- no conversation history as authority;
- unique cycle, generation, thread, task, epoch and nonce;
- inbox and claim published and verified before STATE activates them;
- STATE updated last;
- live memory updated and refetched before Codex handoff;
- Codex remote-state fetch before routing;
- complete immutable outbox published, remotely refetched and byte-compared before handoff;
- rejected work converted into a new task instance rather than old-chat continuation;
- missing outbox converted into correction, report recovery or publication recovery after primary inspection;
- implementation acceptance automatically leading to a separate canary task;
- product work blocked until implementation and canary are both independently accepted.

## Required primary design

Create root `CLOSED_LOOP_PROTOCOL.md` matching the active mailbox protocol and make it authoritative from AGENTS, ORCHESTRATION, BRIDGE, bootstrap and recovery.

Create plugin skill `closed-loop-controller` that validates task identity, state transition, fresh-chat isolation, memory-sync readiness, publication ordering, failure classification, recovery selection and canary gating.

Bump plugin manifest to version 1.0.5. Install 1.0.5 as a sibling version under the discovered user-level Asynchronia package root. Preserve older versions and unrelated user content.

Create a machine-readable Python closed-loop contract and deterministic tests. `tools/validate-orchestration-policy.py` must call or import this contract rather than duplicating drifting string checks.

## Required state transitions

The validator and controller must recognize only:

- CLOSED
- PREPARING
- READY_FOR_CODEX
- EXECUTING
- PRIMARY_PUBLISHED
- OUTBOX_PUBLISHING
- AWAITING_CHATGPT_VERIFICATION
- ACCEPTED
- CORRECTION_REQUIRED
- RECOVERY_REQUIRED
- BLOCKED_EXTERNAL
- SUPERSEDED

Illegal transitions fail closed. Unreferenced task artifacts are inert. STATE referencing absent artifacts is recovery-required. MEMORY_SYNC_PENDING forbids Codex execution.

## Automatic repair rules

Implement bounded in-place repair before terminal output:

- stale remote state: refetch and restart identity resolution;
- main or mailbox non-fast-forward: refetch, create clean worktree and retry;
- moved main with disjoint writes and stable reads: reapply patch to latest main and rerun all checks;
- moved main with overlap or changed dependency: complete blocker outbox;
- dirty worktree: use new task-owned worktree, never stash/reset/clean unrelated work;
- stale plugin during plugin repair: pinned source fallback, validated install through temporary sibling and atomic rename;
- failed validation inside scope: diagnose and repair, maximum three distinct passes per root cause;
- transient connector/fetch/push failure: maximum three bounded retries with remote identity revalidation;
- outbox missing or byte mismatch: preserve immutable response bytes, republish from latest mailbox, maximum three clean attempts.

When repair is impossible, publish a complete blocker outbox whenever mailbox publication works. If mailbox publication itself fails after retries, return `BLOCKED_OUTBOX_PUBLICATION`, preserve the report locally and do not hand off to ChatGPT.

## Required failure coverage

Deterministic negative controls must cover at least:

1. memory unavailable;
2. memory stale against primary sources;
3. memory sync pending;
4. mailbox STATE split-brain;
5. STATE referencing missing inbox;
6. STATE referencing missing claim;
7. orphan inbox or claim;
8. duplicate task identity or nonce;
9. wrong bridge slot;
10. stale thread, epoch or nonce;
11. previous conversation identity reuse;
12. claim collision;
13. plugin missing on ordinary task;
14. source fallback used without authorization;
15. invalid source plugin manifest;
16. missing task-router or required skill;
17. reintroduced runtime-safety-gate;
18. moved main with safe disjoint reapply;
19. moved main with overlap;
20. dirty unrelated worktree;
21. source change outside frozen scope;
22. validator claim not reproducible on committed tree;
23. false no-delta inference;
24. failing tests requiring outside scope;
25. primary push non-fast-forward;
26. mailbox push non-fast-forward;
27. empty, partial or handoff-only outbox;
28. wrong outbox identity or nonce;
29. historical outbox reuse;
30. absent remote outbox after local success;
31. remote byte mismatch;
32. missing retry evidence;
33. missing remote refetch evidence;
34. primary commit missing, wrong parent or outside main ancestry;
35. main advanced after a valid task commit;
36. duplicate bridge command after closure;
37. missing Safari evidence when required;
38. product task attempted before canary acceptance;
39. external auth, permission or service outage;
40. report-recovery and publication-recovery selection.

Positive controls must prove legal coordinator, Codex, verifier, correction, report-recovery, publication-recovery and canary transitions.

## Exact primary scope

Only these primary paths may change or be created:

- `CLOSED_LOOP_PROTOCOL.md`
- `AGENTS.md`
- `AGENTS.override.md`
- `PROCESS_ROOT_SYNC.md`
- `ORCHESTRATION.md`
- `BRIDGE.md`
- `CODEX_BRIDGE_BOOTSTRAP.md`
- `CODEX_BRIDGE_RECOVERY.md`
- `GIT_PUSH.md`
- `STAGE6_PARALLEL_EXECUTION_PLAN.md`
- `PROJECT_MEMORY.md`
- `TASKS.md`
- `.github/workflows/orchestration-policy.yml`
- `tools/validate-orchestration-policy.py`
- `tools/closed_loop_contract.py`
- `tools/test_closed_loop_contract.py`
- `plugins/asynchronia/.codex-plugin/plugin.json`
- `plugins/asynchronia/skills/closed-loop-controller/SKILL.md`
- `plugins/asynchronia/skills/task-router/SKILL.md`
- `plugins/asynchronia/skills/scope-isolation-check/SKILL.md`
- `plugins/asynchronia/skills/parallel-scope-planner/SKILL.md`
- `plugins/asynchronia/skills/model-selector/SKILL.md`
- `plugins/asynchronia/skills/acceptance-pipeline-controller/SKILL.md`
- `plugins/asynchronia/skills/pipeline-state-and-resume-contract/SKILL.md`
- `plugins/asynchronia/skills/evidence-bundle-and-artifact-identity/SKILL.md`
- `plugins/asynchronia/skills/failure-routing-and-corrective-loop/SKILL.md`
- `plugins/asynchronia/skills/runtime-safety-gate/SKILL.md` if present, deletion only.

All product/runtime paths are protected.

Managed user-level Codex instruction and plugin installation paths may change only for the closed-loop managed block and versioned Asynchronia 1.0.5 installation, with timestamped backup, permission preservation and unrelated-byte preservation.

## Validation required on exact final committed tree

Report:

- final commit and actual first parent;
- clean worktree status;
- Python compile of all changed Python files;
- full orchestration validator;
- full closed-loop unit test suite;
- all positive and negative controls;
- dynamic discovered skill inventory and workflow coverage equality;
- plugin manifest 1.0.5 and frontmatter validation;
- source and installed runtime-gate absence;
- installed 1.0.5 path, inventory and source checksum equality;
- plugin-first positive smoke and unavailable-plugin negative smoke;
- exact changed paths versus frozen scope;
- protected product/runtime blob equality;
- `git diff --check`.

Do not use validation run before the final commit as completion evidence.

## Publication and acceptance

Publish one direct-child exact-scope primary commit or safely rebuild on a newer disjoint main as defined by policy.

Prepare one complete immutable final response and publish the exact bytes only to `.ai-bridge/outbox/BRIDGE-20260709-046-02-codex.md`. Refetch remote mailbox and prove schema and byte equality before showing the same response.

The exact next instruction must be: open a fresh ChatGPT conversation and send `мост 2`.

This implementation task does not itself declare the loop complete. After independent ChatGPT acceptance, ChatGPT must automatically create a separate CLOSED_LOOP_CANARY task in a new thread. Only independent canary acceptance may release product work.
