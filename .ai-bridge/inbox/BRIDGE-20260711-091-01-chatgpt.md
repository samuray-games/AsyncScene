Use @asynchronia.

# BRIDGE-20260711-091 - Fix4 adversarial evaluator and evidence correction

ISSUE: 216
CYCLE: CYCLE-20260711-003
GENERATION: 12
SLOT: 2
THREAD_ID: BRIDGE-20260711-091
LANE_ID: S6-BOOMER-STEP4_4B-FIX4-VALIDATION-CORRECTION
TASK_ID: TASK-S6-BOOMER-STEP4_4B-FIX4-ADVERSARIAL
EXECUTION_EPOCH: S6-BOOMER-4_4B-FIX4-R2-20260711-1415JST
TASK_NONCE: S6B44B-091-ADV-1415
COORDINATOR_MEMORY_REVISION: 2026-07-11-1620-JST
AUTHORIZED_PRIMARY_BASELINE: 42c56c023e3b60f3b9e534dbb720057e78ef0c77
BRIDGE_PROTOCOL: 4.0
BRANCH_MAILBOX: coordination/chatgpt-codex-bridge-2
BRANCH_TASK: bridge/2/BRIDGE-20260711-091
SUPERSEDES_THREAD: BRIDGE-20260711-090
PREVIOUS_ATTEMPT_STATUS: SOURCE_PARTIAL_PASS_ADVERSARIAL_AND_EVIDENCE_REJECTED
CLAIM_PATH: .ai-bridge/claims/BRIDGE-20260711-091-claim-v1-codex.md
EXPECTED_OUTBOX_PATH: .ai-bridge/outbox/BRIDGE-20260711-091-02-codex.md
EXPECTED_RECEIPT_PATH: .ai-bridge/receipts/BRIDGE-20260711-091-03-codex.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
REQUIRE_COMPLETE_SCOPE_DELTA: true
MODEL_RECOMMENDATION_PAUSE_REQUIRED: true
SAME_THREAD_CONTINUE_REQUIRED: true
SAFARI_STATUS: PENDING_USER

## Bridge v4 isolation

Read only `main` and `coordination/chatgpt-codex-bridge-2` for bridge control. Do not read, write, compare, or depend on bridge 1 or bridge 3 mailbox refs.

Perform implementation only on `bridge/2/BRIDGE-20260711-091`. Direct task-lane writes to `main` are forbidden. After validation, publish the task branch and evidence for independent integration/review.

## Accepted predecessor

Bridge 090 source commit `42c56c023e3b60f3b9e534dbb720057e78ef0c77` is accepted as partial predecessor evidence:

- sole parent `c12e0dacf92ae629215dbcc520b62a10f1d89846`;
- exactly four runtime paths changed;
- source/docs dev-check blobs match;
- both index blobs match and use Fix4 cache-bust;
- restoration implementation and harness evidence exist.

Bridge 090 is not fully accepted because:

1. `runAdversarialRestorationFixtures()` proves cloned digest inequality but does not invoke the production comparator.
2. plugin evidence identifies Asynchronia 1.0.5 instead of independently accepted installed 1.0.7.
3. receipt evidence is incomplete.

## Mandatory model preflight

First fresh `мост 2` is read-only. Apply `task-router`, `scope-isolation-check`, and `model-selector`; show the recommendation; stop at `WAITING_FOR_MODEL_SELECTION`; end with a standalone fenced `CONTINUE` block. No mutation before exact same-thread `CONTINUE`.

Start the cost frontier at `GPT-5.4 / Light`. Promote only for a concrete task-specific failure mode.

## Frozen task-branch scope

Exactly these four paths must change on `bridge/2/BRIDGE-20260711-091`:

- AsyncScene/Web/dev/dev-checks.js
- docs/dev/dev-checks.js
- AsyncScene/Web/index.html
- docs/index.html

No fifth path may change.

## Atomic objective after CONTINUE

Keep accepted Fix4 restoration logic, replace inequality-only adversarial checks with real production-comparator rejection fixtures, bump runtime identity/cache-bust to Fix5, preserve source/docs equality, and publish corrected evidence using installed Asynchronia 1.0.7.

For each category `profile`, `economy`, `battleState`, `moneyLog`, `persistence`, and `localStorage`:

- clone the before digest;
- make one category-local mutation;
- call the exact production comparison/evaluator path;
- require false for the mutated digest and true for the untouched control.

Expose:

- profileComparatorRejectsMutation
- economyComparatorRejectsMutation
- battleStateComparatorRejectsMutation
- moneyLogComparatorRejectsMutation
- persistenceComparatorRejectsMutation
- localStorageComparatorRejectsMutation
- allComparatorsRejectMutation

Overall smoke PASS is forbidden unless all comparator fields and all restoration fields are true.

## Validation

After one direct-child commit on the task branch, run from a clean committed tree:

- node syntax checks for both dev-check files;
- static generator and validator;
- exact four-path diff proof;
- sole-parent proof from `42c56c023e3b60f3b9e534dbb720057e78ef0c77`;
- source/docs dev-check blob equality;
- index blob equality and Fix5 cache-bust;
- executable ephemeral harness against source and docs;
- deliberate mutation of every category must make the actual comparator return false;
- untouched control digest must compare true for every category;
- full restoration fields remain true;
- `git diff --check`.

Read back active installed Asynchronia without writing. Expected version is 1.0.7. If not, return `BLOCKED_ACTIVE_PLUGIN_READBACK` with exact evidence.

## Publication

Publish implementation only to `bridge/2/BRIDGE-20260711-091`. Do not push the task commit directly to main.

Publish exact Bridge 091 outbox and receipt as separate fast-forward commits only on `coordination/chatgpt-codex-bridge-2`. Receipt must preserve validation, preflight, skill, comparator, restoration, mirror, harness, installed-plugin, task-branch SHA, parent, and exact-path evidence, plus final receipt refetch and byte-equality proof.

Codex must not edit `.ai-bridge/STATE.md`.

Terminal action: `OPEN_FRESH_CHATGPT_VERIFIER_AND_SEND_SAME_BRIDGE_COMMAND`.
