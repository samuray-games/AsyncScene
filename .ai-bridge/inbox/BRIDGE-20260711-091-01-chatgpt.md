Use @asynchronia.

# BRIDGE-20260711-091 - Fix4 adversarial evaluator and evidence correction

ISSUE: 216
CYCLE: CYCLE-20260711-003
GENERATION: 12
SLOT: 2
THREAD_ID: BRIDGE-20260711-091
LANE_ID: S6-BOOMER-STEP4_4B-FIX4-VALIDATION-CORRECTION
TASK_ID: TASK-S6-BOOMER-STEP4_4B-FIX4-ADVERSARIAL
EXECUTION_EPOCH: S6-BOOMER-4_4B-FIX4-R2-20260711-1415JST
TASK_NONCE: S6B44B-091-ADV-1415
COORDINATOR_MEMORY_REVISION: 2026-07-11-1415-JST
AUTHORIZED_PRIMARY_BASELINE: 42c56c023e3b60f3b9e534dbb720057e78ef0c77
SUPERSEDES_THREAD: BRIDGE-20260711-090
PREVIOUS_ATTEMPT_STATUS: SOURCE_PARTIAL_PASS_ADVERSARIAL_AND_EVIDENCE_REJECTED
CLAIM_PATH: .ai-bridge/claims/BRIDGE-20260711-091-claim-v1-codex.md
EXPECTED_OUTBOX_PATH: .ai-bridge/outbox/BRIDGE-20260711-091-02-codex.md
EXPECTED_RECEIPT_PATH: .ai-bridge/receipts/BRIDGE-20260711-091-03-codex.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
REQUIRE_COMPLETE_SCOPE_DELTA: true
MODEL_RECOMMENDATION_PAUSE_REQUIRED: true
SAME_THREAD_CONTINUE_REQUIRED: true
SAFARI_STATUS: PENDING_USER

## Independent verifier result

Bridge 090 publication mechanics and base restoration implementation are accepted as partial predecessor evidence:

- main equals `42c56c023e3b60f3b9e534dbb720057e78ef0c77`;
- sole parent is `c12e0dacf92ae629215dbcc520b62a10f1d89846`;
- exactly four authorized paths changed;
- source/docs dev-check blobs match;
- both index blobs match and use Fix4 cache-bust;
- static generator and validator were reported PASS;
- mailbox outbox and receipt exist in a two-commit chain.

Bridge 090 is not accepted because its adversarial and skill evidence contradict the task contract.

## Exact defects

1. `runAdversarialRestorationFixtures()` mutates cloned digest categories and returns whether their serialized values differ from baseline. It does not invoke the actual `compare()` evaluator and therefore does not prove that each evaluator rejects its own deliberate mutation.
2. The harness summary repeats those boolean fields, but the committed source itself shows the fixture implementation is only inequality detection, not comparator rejection.
3. Bridge 090 skill evidence claims active installed plugin path/version `.../asynchronia/1.0.5`, while accepted Bridge 089 readback proved active installed Asynchronia `1.0.7` with matching manifest and required skill hashes.
4. The Bridge 090 receipt says receipt publication was pending and omits final receipt refetch/byte-equality evidence. The immutable Bridge 090 artifacts remain rejected history.

## Mandatory model preflight

First fresh `мост 2` is read-only. Apply `task-router`, `scope-isolation-check`, and `model-selector`, show the recommendation, stop at `WAITING_FOR_MODEL_SELECTION`, and end with a standalone fenced `CONTINUE` block. No mutation before exact same-thread `CONTINUE`.

Start the cost frontier at `GPT-5.4 / Light`. Promote only for a concrete failure mode.

## Frozen primary scope

Exactly these four paths must change:

- AsyncScene/Web/dev/dev-checks.js
- docs/dev/dev-checks.js
- AsyncScene/Web/index.html
- docs/index.html

No fifth path may change.

## Atomic objective after CONTINUE

Keep the accepted Fix4 restoration logic, replace the fake adversarial checks with real comparator-rejection fixtures, bump runtime identity/cache-bust to Fix5, preserve source/docs equality, and publish corrected evidence using installed Asynchronia `1.0.7`.

## Required adversarial semantics

For each tracked category:

- profile
- economy
- battleState
- moneyLog
- persistence
- localStorage

create a cloned before digest, make one deliberate category-local mutation, and call the exact same comparison/evaluator path used by production restoration proof.

The fixture passes only when the evaluator returns false for the mutated digest and true for the untouched digest.

Expose explicit bounded fields:

- `profileComparatorRejectsMutation`
- `economyComparatorRejectsMutation`
- `battleStateComparatorRejectsMutation`
- `moneyLogComparatorRejectsMutation`
- `persistenceComparatorRejectsMutation`
- `localStorageComparatorRejectsMutation`
- `allComparatorsRejectMutation`

Overall smoke PASS is forbidden unless all seven are true and every restoration field remains true.

Do not retain legacy fields whose names falsely imply comparator rejection while only checking string inequality.

## Required validation

After creating one direct-child commit, run from a clean committed tree:

- node syntax checks for both dev-check files;
- static generator and validator;
- exact four-path diff proof;
- sole-parent proof from `42c56c023e3b60f3b9e534dbb720057e78ef0c77`;
- source/docs dev-check blob equality;
- index blob equality and Fix5 cache-bust;
- executable ephemeral harness against source and docs;
- harness must deliberately mutate every category and assert actual comparator returns false;
- untouched control digest must compare true for every category;
- full restoration fields remain true;
- `git diff --check`.

Preserve harness content hash and exact output.

## Plugin evidence correction

Read back active installed Asynchronia without writing. Expected accepted identity is version `1.0.7`.

If current active installed package is not `1.0.7`, return `BLOCKED_ACTIVE_PLUGIN_READBACK` with exact evidence. Do not silently downgrade evidence to `1.0.5` and do not repair the installation in this lane.

## Publication

Push one direct-child exact-four-path commit by explicit non-force SHA refspec.

Publish exact Bridge 091 outbox and receipt as separate mailbox commits. Receipt must preserve validation, exclusion, preflight, skill, comparator, restoration, mirror, and harness evidence exactly and include final receipt refetch and byte-equality proof.

Safari remains `PENDING_USER`.

Codex must not edit `.ai-bridge/STATE.md`.

Terminal action: `OPEN_FRESH_CHATGPT_VERIFIER_AND_SEND_SAME_BRIDGE_COMMAND`.
