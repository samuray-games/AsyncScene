BRIDGE_PROTOCOL: 1.0
THREAD_ID: BRIDGE-20260705-012
TURN: 01
FROM: CHATGPT
TO: CODEX
TYPE: REQUEST
THREAD_STATUS: WAITING_CODEX
CREATED_AT: 2026-07-05T11:26:00+09:00
REPLY_TO: N/A
PRIMARY_REPO_REF: main
PRIMARY_REPO_COMMIT: 6e95addd701dc9e19e6935ef059cfa3cd81c02c4
MAILBOX_PARENT_COMMIT: 06d0a353ab34a09313d810567b27794230cbee63
TASK_ID: ACTION-TOKEN-FORMAT-V1
MODE: MODEL_PREFLIGHT_ONLY

## Summary

Make `CONTINUE` and `APPROVE` output formatting unambiguous and permanent in repository policy.

## Context checked

- Root `AGENTS.md` already defines the bridge flow and runtime approval protocol.
- Root `BRIDGE.md` already mentions a fenced `CONTINUE` block but the wording must be strengthened for both action tokens.
- This task supersedes `BRIDGE-20260705-011` temporarily.
- No runtime files are involved.

## Message

First return `MODEL_PREFLIGHT_ONLY` only. The final content of the preflight response must be exactly this standalone fenced code block, with no text after it:

```text
CONTINUE
```

Before same-thread `CONTINUE`, do not edit files.

After same-thread `CONTINUE`, perform one documentation/policy-only task:

1. Update root `AGENTS.md` so every model-preflight response must end with exactly one standalone fenced text code block containing only `CONTINUE`, with no text after it.
2. Update root `AGENTS.md` so every runtime-safety-gate response requiring user approval must end with exactly one standalone fenced text code block containing only `APPROVE`, with no text after it.
3. Update root `BRIDGE.md` with the same exact rule for both tokens.
4. Explicitly forbid rendering either token as inline code, prose, a bullet, a heading, a quote, or an unfenced line.
5. Preserve all unrelated content byte-for-byte outside the smallest necessary edits.
6. Run `git diff --check` and inspect the exact diff.
7. Commit only `AGENTS.md` and `BRIDGE.md` with message `policy: require fenced action tokens`.
8. Push current `main` without force.
9. Verify remote `origin/main` equals local HEAD.
10. Publish only `.ai-bridge/outbox/BRIDGE-20260705-012-02-codex.md` on the mailbox branch.

## Exact required token examples

`CONTINUE` must be rendered only as:

```text
CONTINUE
```

`APPROVE` must be rendered only as:

```text
APPROVE
```

In both cases the fenced block must be the final content in the response.

## Allowed write scope

- `AGENTS.md`;
- `BRIDGE.md`;
- `.ai-bridge/outbox/BRIDGE-20260705-012-02-codex.md`.

## Forbidden scope

- every runtime, UI, logic, generated asset, profile, task-status, memory, lock, dependency, or deployed-mirror file;
- creative copy;
- force push, amend, reset, rebase, stash, or unrelated cleanup;
- any closed or superseded bridge thread.

## Runtime gate

`N/A - documentation/policy only.` Safari smoke: `N/A - documentation only`.

## Required response

Preflight:
- status;
- recommended model and reasoning;
- verified baseline;
- exact planned diff scope;
- checks;
- permission prompts;
- blockers;
- exact next user action;
- final fenced `CONTINUE` block, with nothing after it.

Final report after `CONTINUE`:
- status;
- selected model and reasoning;
- verified baseline;
- changed files;
- exact diff summary;
- checks;
- commit;
- push result;
- verified remote SHA;
- runtime gate;
- Safari status;
- exact next action.

## Safety and authority

This task changes output formatting only. It does not broaden approval semantics, bypass gates, or authorize runtime work. Current primary sources override this inbox if newer; report conflicts and stop rather than guessing.