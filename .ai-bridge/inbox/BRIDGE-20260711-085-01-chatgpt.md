Use @asynchronia.

# BRIDGE-20260711-085 - Fix3 publication with target-state delta semantics

ISSUE: 216
CYCLE: CYCLE-20260711-003
GENERATION: 6
SLOT: 2
THREAD_ID: BRIDGE-20260711-085
LANE_ID: S6-BOOMER-STEP4_4B-PUBLICATION-RECOVERY
TASK_ID: TASK-S6-BOOMER-STEP4_4B-FIX3-PUBLISH3
EXECUTION_EPOCH: S6-BOOMER-4_4B-FIX3-PUB-R3-20260711-1057JST
TASK_NONCE: S6B44B-085-PUB3-1057
PHASE: RECONSTRUCT_COMMIT_VALIDATE_AND_PUBLISH
COORDINATOR_MEMORY_REVISION: 2026-07-11-1057-JST
AUTHORIZED_PRIMARY_BASELINE: c5ef0ecea5c96a54604e6d130207ac4fd52789df
HISTORICAL_LOCAL_COMMIT: 0f7ecd69fb5857dbb965e6b4ee57dc2737f16f29
SUPERSEDES_THREAD: BRIDGE-20260711-084
PREVIOUS_ATTEMPT_STATUS: INVALID_BLOCKED_NO_SOURCE_DELTA_WITH_NONEMPTY_DIFF
CLAIM_PATH: .ai-bridge/claims/BRIDGE-20260711-085-claim-v1-codex.md
EXPECTED_OUTBOX_PATH: .ai-bridge/outbox/BRIDGE-20260711-085-02-codex.md
EXPECTED_RECEIPT_PATH: .ai-bridge/receipts/BRIDGE-20260711-085-03-codex.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
REQUIRE_COMPLETE_SCOPE_DELTA: false
DELTA_SCOPE_POLICY: .ai-bridge/DELTA_SCOPE_POLICY.md
PLUGIN_IDENTIFIER: asynchronia
PLUGIN_CONTEXT_MODE: USER_ATTACHED_OR_REPOSITORY_FALLBACK
PLUGIN_INVOCATION_REQUIRED: false
PLUGIN_TOOL_TELEMETRY_REQUIRED: false
SKILL_APPLICATION_EVIDENCE_REQUIRED: true
MODEL_RECOMMENDATION_VISIBLE_REQUIRED: true
MODEL_RECOMMENDATION_PAUSE: false
VALIDATOR_APPLICABILITY_GATE: REQUIRED
RUNTIME_CLASSIFICATION: RUNTIME_SMOKE_INFRASTRUCTURE
SAFARI_STATUS: PENDING_USER
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true
ACTUAL_MODEL_STATUS: USER_SELECTED_UNVERIFIED

## Root-cause correction

The Bridge 084 result is rejected.

It reported a three-path baseline-to-candidate diff and simultaneously returned `BLOCKED_NO_SOURCE_DELTA`. A non-empty diff is not `NO_SOURCE_DELTA`.

The previous contract also incorrectly required every path in the four-file authorized scope to appear in the commit. Read and apply `.ai-bridge/DELTA_SCOPE_POLICY.md`.

For this task:

- the four paths below are the complete authorized write scope and target-state verification set;
- the final commit may contain any non-empty subset of those four paths;
- every target path absent from the commit must have target no-delta proof;
- no cosmetic edit may be added merely to force a four-path diff.

## Visible model recommendation

Before executing repository commands, apply `model-selector` and print a concise visible recommendation containing:

- recommended model;
- recommended reasoning level;
- one concrete reason;
- `actual active model: USER_SELECTED_UNVERIFIED`.

Do not pause for confirmation. Continue execution in the same response.

The expected cost frontier should begin at `GPT-5.4 / Light`. Promote only if the selector identifies a concrete task-specific Light failure mode. Do not default to Medium merely because Git and validation are involved.

## Skill route

Apply in this order:

1. task-router
2. scope-isolation-check
3. model-selector
4. parallel-scope-planner
5. closed-loop-controller
6. failure-routing-and-corrective-loop
7. mirror-audit
8. deployment-verifier
9. smoke-orchestrator
10. acceptance-evidence-gate

Use attached plugin context or repository fallback. Do not seek callable-plugin telemetry.

## Authorized write scope and target-state paths

- AsyncScene/Web/dev/dev-checks.js
- AsyncScene/Web/index.html
- docs/dev/dev-checks.js
- docs/index.html

`ACTUAL_CHANGED_PATHS` must be the exact non-empty baseline-to-final subset of these paths. No fifth path is permitted.

For each authorized target path not present in `ACTUAL_CHANGED_PATHS`, publish `targetNoDeltaEvidence` with baseline blob SHA, candidate target blob or deterministic content hash, equality proof, target identity checks, and the reason it requires no commit delta.

## Historical commit handling

Do not require historical commit `0f7ecd69fb5857dbb965e6b4ee57dc2737f16f29` to be a direct child of the current baseline.

It is only a possible source of target file blobs. Its prior provenance claims are contradictory and non-authoritative.

Create a clean detached task-owned worktree at baseline `c5ef0ecea5c96a54604e6d130207ac4fd52789df` and reconstruct the verified Fix3 target state using:

- readable blobs from the historical commit when available;
- the prior task-owned reconstructed worktree when provenance is clear;
- deterministic reimplementation from the Fix3 objective when neither source is reliable.

Copy or edit only the four authorized paths. Do not rebase, cherry-pick, merge, amend, reset, stash, clean, force-push, or mutate the user's primary checkout.

Before committing, report the exact candidate diff paths. If three paths differ and the fourth already matches the target, commit the three real paths and prove the fourth path's no-delta target state.

## Final commit and validator sequence

1. Build the target state in the detached worktree.
2. Confirm candidate diff is a non-empty subset of authorized scope.
3. Create one direct-child commit whose sole parent is the authorized baseline.
4. Confirm `git status --short` is empty.
5. Only then run validators and checks from the committed tree.

The prior run appears to have invoked a working-tree validator before legally committing the three-path delta. That sequencing is invalid.

Required checks after final commit:

- empty `git status --short`;
- `python3 tools/generate-boomer-step4-4-economy-conflict-audit.py .`;
- `python3 tools/validate-boomer-step4-4-economy-conflict-audit.py .`;
- `node --check AsyncScene/Web/dev/dev-checks.js`;
- `node --check docs/dev/dev-checks.js`;
- exact sole-parent proof;
- actual changed paths are a non-empty subset of the four authorized paths;
- target-state proof for all four paths;
- protected-path proof;
- source/docs semantic mirror verification with intentional docs Alpha differences preserved;
- served entrypoint, Fix3 identity, and cache-bust verification for both index files;
- adversarial fixture evidence for each new evaluator family;
- restoration evidence for profile/text mode, points, REP, Influence, battle state, moneyLog, persistence, and relevant localStorage keys;
- `git diff --check c5ef0ecea5c96a54604e6d130207ac4fd52789df...<FINAL_PRIMARY_SHA>`.

The static audit must remain `STATIC_PASS / READY_FOR_RUNTIME_SMOKE`, 147 PASS, 0 FAIL, 0 structural failures, and 8 non-live rows.

If `tools/validate-boomer-step4-4-economy-conflict-audit.py` fails after the final commit with a genuinely clean tree, preserve exact command, HEAD, empty status output, exit code, complete stdout, and complete stderr. Do not paraphrase it as an approved-scope failure.

## Excluded validator

Only `tools/validate-orchestration-policy.py` remains `NOT_APPLICABLE` because it is pinned to historical Bridge 062 identity and baseline. Preserve the same exclusion schema and replacement controls as the previous task.

## Publication

Immediately before push:

- fetch `origin/main`;
- prove remote main still equals the authorized baseline;
- prove final commit has the baseline as sole parent;
- prove baseline is an ancestor of final commit.

Push without force using:

`git push origin <FINAL_PRIMARY_SHA>:refs/heads/main`

After successful source push:

- refetch remote main and verify SHA, parent, actual changed subset, and target state for all four files;
- publish only `.ai-bridge/outbox/BRIDGE-20260711-085-02-codex.md` as a fast-forward mailbox child;
- refetch it;
- publish only `.ai-bridge/receipts/BRIDGE-20260711-085-03-codex.md` as a separate fast-forward child;
- refetch mailbox and return bytes identical to the remote receipt;
- preserve `validationResults`, `excludedValidationEvidence`, `targetNoDeltaEvidence`, and `skillApplicationEvidence` exactly;
- do not edit `.ai-bridge/STATE.md`.

## Illegal blockers

The following are illegal when contradicted by evidence:

- `BLOCKED_NO_SOURCE_DELTA` when candidate diff is non-empty;
- `BLOCKED_PLUGIN_UNAVAILABLE` while repository skill fallback is readable;
- exact-four-path mismatch when the actual changed subset is target-correct and every unchanged target path has no-delta proof.

Safari remains `PENDING_USER`.

Terminal success action code: `OPEN_FRESH_CHATGPT_VERIFIER_AND_SEND_SAME_BRIDGE_COMMAND`.
