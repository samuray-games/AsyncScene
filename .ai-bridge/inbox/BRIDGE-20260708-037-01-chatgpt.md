Use @asynchronia runtime-safety-gate.

# Bridge Slot 1 - Stage 6 Wave V profile acceptance preparation

BRIDGE_PROTOCOL: 3.0
ORCHESTRATION_VERSION: 3.0
BRIDGE_SLOT: 1
THREAD_ID: BRIDGE-20260708-037
LANE_ID: S6-V5-PROFILE-ACCEPTANCE-PREP
TASK_ID: TASK-S6-PAR-V5-PREP
PHASE: MODEL_PREFLIGHT_ONLY
USER_COMMAND: мост 1
MAILBOX_PARENT_COMMIT: 90a2e33d759ced18d6c3fd124ea524bc140f948f
CURRENT_MAIN_BASELINE: 41ce4aa7ca6b9d287d47aee7d5a6384673054a36
PRIMARY_WRITE_SCOPE: NONE
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260708-037-02-codex.md
RUNTIME_CLASSIFICATION: READ_ONLY_PROFILE_ACCEPTANCE_PREP
CONFIRMATION_RULE: SAME_THREAD_CONTINUE_CONFIRMS_MODEL_AND_EXACT_READ_ONLY_SCOPE
MODEL_SELECTION: PENDING_CODEX_PREFLIGHT_12_OF_12
SAFARI_RUNTIME_STATUS: PENDING_USER

## Atomic objective

Prepare the exact Wave V Boomer and Alpha runtime acceptance package from current remote `main`, without editing implementation or smoke code.

1. Reproduce the current Boomer 4.4A static audit on baseline `41ce4aa7ca6b9d287d47aee7d5a6384673054a36`.
2. Verify whether `failRowCount === 0` and `structuralFailureCount === 0` on the current source and exact deployed artifact.
3. Run the current Alpha static aggregate and verify all accepted Alpha presentation decisions, including escape, teach, social actions and the frozen vote/report evidence contract.
4. Identify the exact existing Boomer 4.4B runtime entrypoint and the exact existing Alpha deployed aggregate / vote / report runtime entrypoints.
5. Resolve the exact current deployed artifact identity from `docs/index.html`, loaded cache-busts and current remote blobs.
6. If all required runtime entrypoints already exist and static checks pass, produce one minimal ordered iPhone Safari console package for the user, with exact expected PASS fields and artifact identity.
7. If an entrypoint is absent, stale or incapable of proving the frozen contract, return a precise `FAIL_RUNTIME_GAP` package naming the missing evidence and smallest later write scope. Do not implement the fix in this task.
8. Do not claim Safari PASS. User runtime evidence remains mandatory.

## Mandatory authorities

Read fresh remote versions of:

- `AGENTS.override.md`
- `ORCHESTRATION.md`
- `AGENTS.md`
- `BRIDGE.md`
- `TASKS.md`
- `PROJECT_MEMORY.md`
- `STAGE6_PARALLEL_EXECUTION_PLAN.md`
- `STAGE6_TRACEABILITY_CORRECTIONS.md`
- mailbox `STATE.md`
- this inbox and its immutable claim
- `.ai-bridge/inbox/BRIDGE-20260708-036-09-chatgpt.md`
- current `docs/index.html`
- current source and deployed `dev/dev-checks.js`
- current Boomer 4.4A/4.4B audit artifacts and generators
- current Alpha aggregate, mapping and vote/report evidence artifacts
- all runtime sources reached by the discovered smoke entrypoints.

Current repository sources override historical status prose. Report any stale `TASKS.md`, `PROJECT_MEMORY.md`, plan or audit statement, but do not edit those documents.

## Required model protocol

Evaluate all 12 available model/reasoning pairs and recommend the cheapest reliable pair for this read-only evidence task.

Return compact preflight only. End with exactly one fenced `CONTINUE` block and nothing after it.

No tests, edits, commits or pushes before the user selects the model and sends `CONTINUE` in the same Codex thread.

Under Protocol 3.0, `CONTINUE` confirms the selected model and this exact frozen read-only scope. No second approval is required.

## Strict prohibitions

- no primary implementation edits;
- no `dev-checks.js`, registry, export, global, boot, HTML or cache-bust edits;
- no generated audit rewrite;
- no `TASKS.md` or `PROJECT_MEMORY.md` edits;
- no mechanical, economy, battle, profile-selection, persistence or state edits;
- no synthetic Safari PASS;
- no acceptance based only on historical reports;
- no broad new smoke design when an existing accepted entrypoint can prove the requirement.

## Required execution after `CONTINUE`

### A. Remote and artifact identity

- prove `origin/main` remains exactly `41ce4aa7ca6b9d287d47aee7d5a6384673054a36`;
- report current `docs/index.html` blob and every relevant loaded script/cache-bust identity;
- prove the inspected deployed files are the files actually loaded by the current entrypoint;
- report source/deployed parity or intentional transformation for every inspected smoke/runtime file.

### B. Boomer static gate

- rerun the authoritative current 4.4A generator/audit;
- report audited row count, pass count, fail count and structural failures;
- PASS requires `failRowCount: 0`, `structuralFailureCount: 0` and no unresolved forbidden/mapped/unmapped/wrong-profile rows;
- verify the accepted Wave IV Boomer outputs are included, especially conflict labels, invitation error, dynamic escape label, cooldown reply and battle fallbacks.

### C. Alpha static gate

- run the authoritative current Alpha aggregate/static checks;
- verify exact accepted Alpha escape, teach and social-action outputs;
- verify the vote/report runtime evidence contract is still mapped to real handlers and emitters;
- verify Zoomer baseline preservation and Millennial default behavior;
- PASS requires empty failure and missing-coverage arrays.

### D. Runtime entrypoint audit

For Boomer and Alpha, report:

- exact `Game.__DEV` / `Game.Dev` function names;
- exact defining files and blobs;
- exact prerequisite state/profile setup;
- exact invocation order;
- exact expected object fields, buildTag, commit and smokeVersion;
- whether each function runs against the exact deployed artifact;
- whether vote/report evidence is observed rather than inferred.

### E. User Safari package

If all local/static gates and runtime-entrypoint audits pass, output one concise copy-paste Safari console package that:

1. confirms loaded artifact identity;
2. runs Boomer 4.4B/deployed acceptance;
3. runs Alpha deployed aggregate including vote/report evidence;
4. returns a compact combined result;
5. treats any missing function, wrong build identity, non-empty failure array or false required boolean as FAIL.

Do not require the user to inspect long logs manually. State exactly what full result the user must return to ChatGPT.

### F. Failure behavior

If static or runtime-entrypoint preparation fails:

- status must be `FAIL_RUNTIME_GAP`;
- identify the exact failing requirement and source path;
- distinguish stale documentation from real runtime absence;
- propose the smallest serialized correction scope for a later task;
- do not edit or push primary files.

## Publication

After execution, publish only the immutable mailbox outbox `.ai-bridge/outbox/BRIDGE-20260708-037-02-codex.md`.

No primary commit is allowed. `origin/main` must remain unchanged.

The outbox must include:

- selected model/reasoning and compact 12/12 preflight summary;
- exact remote main and artifact identity;
- complete Boomer static verdict;
- complete Alpha static verdict;
- runtime entrypoint inventory;
- exact Safari package if ready, or exact minimal correction package if blocked;
- empty/non-empty failure arrays;
- explicit `SAFARI_RUNTIME_STATUS: PENDING_USER`;
- exact next action: return to ChatGPT and write `мост 1`.
