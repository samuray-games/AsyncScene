Use @asynchronia runtime-safety-gate.

# Bridge Slot 1 - Wave IV correction contract supersession

BRIDGE_PROTOCOL: 3.0
ORCHESTRATION_VERSION: 3.0
BRIDGE_SLOT: 1
THREAD_ID: BRIDGE-20260708-036
LANE_ID: S6-I4-DATA-BATTLE-CONFLICT-PRESENTATION
TASK_ID: TASK-S6-PAR-I4
PHASE: EXECUTE_NOW_CORRECTION
SUPERSEDES: .ai-bridge/inbox/BRIDGE-20260708-036-03-chatgpt.md
CURRENT_MAIN_BASELINE: 374a47048425d9035e81f81304cb41211eba3475
EXPECTED_CORRECTION_OUTBOX: .ai-bridge/outbox/BRIDGE-20260708-036-06-codex.md
MODEL_PROTOCOL: SAME_THREAD_CORRECTION_NO_NEW_PREFLIGHT
CONFIRMATION: ALREADY_SATISFIED_BY_PRIOR_CONTINUE

## Coordinator correction

The prior correction incorrectly treated `default` and `millennial` as required distinct presentation identities.

Authoritative product semantics:

- Millennial is the default presentation profile.
- `default` is allowed to resolve to `millennial`.
- unknown presentation input is allowed to resolve to the default Millennial presentation.
- visible default and Millennial output must remain identical.

Therefore, do not alter the current profile-resolution behavior solely to create a separate `default` identity. Retract Defect A from the superseded inbox.

## Remaining valid defect

Only the fallback-routing defect remains:

1. use exact semantic keys:
   - `battle.draw_fallback`
   - `battle.escaped_fallback`
   - `battle.ignored_fallback`
2. route the full visible escape-vote sentence, including its prefix, through the profile matrix;
3. exact Boomer sentence results:
   - `Решает голосование: Выйти.`
   - `Решает голосование: Вызов отклонён.`
4. default/Millennial/Zoomer remain:
   - `Толпа решает: Свалить.`
   - `Толпа решает: Отвали.`
5. Alpha remains:
   - `Толпа решает: Уйти.`
   - `Толпа решает: Отвали.`
6. bare result lines remain bare outcome text.

## Exact scope

Change only:

- `AsyncScene/Web/conflict/conflict-core.js`
- `docs/conflict/conflict-core.js`

Do not change `data.js` or either `ui-battles.js` file.

Preserve all accepted Alpha/Boomer data overrides, Zoomer baseline, handlers, economy, REP, influence, crowd, timers, report flow, persistence, state transitions, SystemCopy routing and semantic result values.

## Required proof

- exact three `battle.*` keys only;
- all owned fallback call sites use those exact keys;
- full sentence matrix across default/Millennial/Zoomer/Alpha/Boomer/unknown;
- bare fallback matrix unchanged;
- profile resolver behavior remains unchanged from main baseline;
- conflict-core source/deployed mirror byte-identical;
- data and ui-battles blobs unchanged;
- node checks and diff check pass;
- empty failure arrays.

## Publication

- Publish one direct-child correction commit to `main` from `374a47048425d9035e81f81304cb41211eba3475`.
- Only the two conflict-core mirror files may change.
- Publish immutable outbox `.ai-bridge/outbox/BRIDGE-20260708-036-06-codex.md`.
- No new preflight, no new `CONTINUE`, no second approval.
- Exact next action after publication: return to ChatGPT and write `мост 1`.
