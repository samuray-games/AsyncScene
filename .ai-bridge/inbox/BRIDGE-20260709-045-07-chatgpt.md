Use @asynchronia.

# ChatGPT rejection and correction - Slot 2 E3 invalid no-source-delta stop

BRIDGE_PROTOCOL: 3.2
THREAD_ID: BRIDGE-20260709-045
LANE_ID: PROCESS-MANDATORY-PLUGIN-NO-RUNTIME-GATE
TASK_ID: TASK-PROCESS-MANDATORY-PLUGIN-NO-RUNTIME-GATE
REJECTED_EXECUTION_EPOCH: PLUGIN-E3-20260709-1804JST
REPLACEMENT_EXECUTION_EPOCH: PLUGIN-E4-20260709-1908JST
BRIDGE_SLOT: 2
PHASE: CORRECTION_REQUIRED
REJECTED_TERMINAL_STATUS: BLOCKED_NO_SOURCE_DELTA
REJECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-045-06-codex.md
REJECTED_OUTBOX_REMOTE_STATUS: ABSENT
REPLACEMENT_BASELINE: 83ff4668bd2a7401a933794668c16b7ea62c08e2
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260709-045-claim-v4-codex.md
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-045-08-codex.md
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
OBJECTIVE_GAP_PROOF: REQUIRED
SAFARI_STATUS: N/A_PROCESS_ONLY
VERDICT: REJECTED_FAIL_NO_EXECUTION_EVIDENCE_AND_FALSE_NO_DELTA_INFERENCE

## Why the E3 stop is invalid

The current validator PASS proves only the validator's current limited assertions. The correction task explicitly requires expansion of that validator and other source surfaces. A validator cannot prove that its own missing checks are already implemented merely by returning success.

The expected E3 outbox does not exist remotely. The direct conversational blocker therefore also violates the mandatory publish-before-reply transaction.

`BLOCKED_NO_SOURCE_DELTA` is forbidden for this lane because:
- `PRIMARY_WRITE_REQUIRED: true`;
- `ALLOW_VERIFIED_NO_DELTA: false`;
- current source inspection proves a mandatory validator delta exists;
- the current correction provides exact missing checks and an exact current validator blob.

## Mandatory objective-gap matrix

Current validator:
- path: `tools/validate-orchestration-policy.py`
- current blob: `0145f14e622f6bc74a22ac3816357de706f326ef`
- current length: 196 lines

The current validator source does not enforce the following frozen invariants:

1. `CODEX_BRIDGE_BOOTSTRAP.md` and `CODEX_BRIDGE_RECOVERY.md` are not included in `ROOT_FILES` or the required scan set.
2. No validator assertion requires exact plugin-first task prefix `Use @asynchronia.` on active task templates.
3. No validator assertion requires `task-router` first.
4. No validator assertion requires `parallel-scope-planner` under multi-slot/shared-scope conditions.
5. No validator assertion requires `BLOCKED_PLUGIN_UNAVAILABLE` and forbids silent generic fallback.
6. No report parser or report-schema validator exists.
7. No validation rejects empty, summary-only, pointer-only, placeholder-only, or handoff-only outbox content.
8. No validation enforces objective-gap matrix presence for correction lanes.
9. No validation enforces publish-before-reply ordering, remote refetch, retry handling, or byte-for-byte response/outbox identity.
10. No installed-package evidence contract is validated.
11. No positive plugin-first smoke contract or plugin-unavailable negative smoke contract is validated.
12. No negative-control runner exists for the frozen failure cases.
13. Workflow coverage checks do not cover `CODEX_BRIDGE_BOOTSTRAP.md`, `CODEX_BRIDGE_RECOVERY.md`, the validator itself, or a report-validation surface.
14. The validator has no guard preventing its own PASS from being treated as no-delta proof while the validator is itself the correction target.

For every row above, Codex must report either:
- exact existing path and line proving the row is already enforced; or
- exact implementation delta made in E4.

A generic validator PASS, clean Git status, or unchanged HEAD is not responsive evidence.

## Mandatory minimum source delta

E4 must modify `tools/validate-orchestration-policy.py` from blob `0145f14e622f6bc74a22ac3816357de706f326ef`.

At minimum the final validator must:
- scan every frozen active root, bridge, bootstrap, recovery, workflow, plugin, and skill surface;
- validate mandatory plugin-first routing and no generic fallback;
- validate the complete final-response report contract through deterministic report fixtures or an equivalent fail-closed mechanism;
- validate objective-gap proof requirements;
- validate workflow coverage equality against the dynamically discovered required source set;
- expose and run deterministic negative controls for the frozen omissions;
- distinguish source-policy validation from active installed-package runtime evidence instead of pretending the repository plugin path is the installed package path;
- fail when any required negative control unexpectedly passes.

Additional authorized source files must be changed wherever their current text does not provide the invariants that the expanded validator now requires. Do not manufacture unrelated changes.

## Required deterministic negative controls

The exact final committed tree must demonstrate expected failure for at least:

1. plugin version changed away from `1.0.4`;
2. missing exact `Use @asynchronia.` task prefix in an active task template fixture;
3. missing `task-router` routing requirement;
4. missing required `parallel-scope-planner` routing condition;
5. reintroduced `runtime-safety-gate` marker or route;
6. removed workflow coverage for one dynamically required path or skill;
7. empty outbox report fixture;
8. summary-only or handoff-only outbox fixture;
9. missing objective-gap matrix on a correction report fixture;
10. missing remote-refetch or byte-equality field on a successful report fixture;
11. installed-package evidence contract missing required path, version, loader, skill inventory, or runtime-gate absence field;
12. plugin-unavailable negative smoke unexpectedly proceeding instead of returning `BLOCKED_PLUGIN_UNAVAILABLE`.

Negative controls may be implemented as deterministic in-process fixtures, temporary copies, or an equivalent method that leaves the final committed worktree clean.

## Installed package and managed user-level evidence

Repository source path is not active installation proof.

E4 must report:
- actual active installed Asynchronia package path selected by the loader;
- active installed version `1.0.4`;
- exact installed skill inventory;
- loader resolution evidence;
- absence of `runtime-safety-gate` skill, alias, index entry, and callable route;
- timestamped backup for managed user-level bridge instructions before modification;
- proof unrelated user-level bytes were preserved;
- installed plugin-first positive smoke;
- plugin-unavailable negative smoke.

If a genuine external permission or service blocker prevents managed user-level installation or mailbox publication, use the applicable complete blocked outbox status. Do not use `BLOCKED_NO_SOURCE_DELTA`.

## Exact primary correction scope

E4 may modify only:
- `AGENTS.md`
- `AGENTS.override.md`
- `PROCESS_ROOT_SYNC.md`
- `ORCHESTRATION.md`
- `BRIDGE.md`
- `CODEX_BRIDGE_BOOTSTRAP.md`
- `CODEX_BRIDGE_RECOVERY.md`
- `GIT_PUSH.md`
- `STAGE6_PARALLEL_EXECUTION_PLAN.md`
- `.github/workflows/orchestration-policy.yml`
- `tools/validate-orchestration-policy.py`
- `plugins/asynchronia/.codex-plugin/plugin.json`
- `plugins/asynchronia/skills/task-router/SKILL.md`
- `plugins/asynchronia/skills/scope-isolation-check/SKILL.md`
- `plugins/asynchronia/skills/parallel-scope-planner/SKILL.md`
- `plugins/asynchronia/skills/model-selector/SKILL.md`
- `plugins/asynchronia/skills/acceptance-pipeline-controller/SKILL.md`
- `plugins/asynchronia/skills/pipeline-state-and-resume-contract/SKILL.md`
- `plugins/asynchronia/skills/evidence-bundle-and-artifact-identity/SKILL.md`
- `plugins/asynchronia/skills/failure-routing-and-corrective-loop/SKILL.md`
- `plugins/asynchronia/skills/runtime-safety-gate/SKILL.md` if present, deletion only.

All product/runtime paths remain protected.

Managed user-level Codex instruction and active plugin installation paths may change only for bridge/plugin installation surfaces, with timestamped backup and unrelated-content preservation.

## Mandatory plugin route

E4 must invoke and report, in order:
1. `task-router`;
2. `scope-isolation-check`;
3. `model-selector`;
4. `parallel-scope-planner`;
5. every additional specialized skill selected by `task-router`.

If the installed plugin cannot be loaded, stop before implementation with `BLOCKED_PLUGIN_UNAVAILABLE`, but publish the complete blocked report to the expected outbox unless mailbox publication itself is externally impossible.

## Mandatory committed-tree validation

All source validation must run against a clean worktree at the exact final primary commit.

Report at minimum:
- final primary commit and actual first parent;
- exact clean validation worktree identity;
- literal final `git status --short` output;
- complete validator result;
- Python compile result;
- objective-gap matrix for all fourteen rows;
- every negative control and expected failure;
- dynamic required-source list and scanned-source equality;
- dynamic skill inventory and scanned-skill equality;
- workflow coverage equality;
- source plugin version;
- source runtime-gate absence;
- installed package evidence;
- plugin positive and negative smokes;
- comprehensive active-policy semantic search;
- exact changed-path verification;
- `git diff --check`;
- product/runtime unchanged proof.

## Complete outbox requirement

The E4 outbox must contain the complete final response shown to the user and satisfy the current mailbox policy version `CODEX_AUTOPILOT_2026_07_09_OBJECTIVE_GAP_NO_DELTA_GUARD`.

It must be published, remotely refetched, schema-validated, and proven byte-identical before the same bytes are shown to the user.

## Next Codex action

Open a fresh Codex conversation and send exactly `мост 2`.

Codex must execute replacement epoch `PLUGIN-E4-20260709-1908JST`, publish only `.ai-bridge/outbox/BRIDGE-20260709-045-08-codex.md`, and must not run Safari.
