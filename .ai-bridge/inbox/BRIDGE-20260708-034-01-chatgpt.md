Use @asynchronia runtime-safety-gate.

# Bridge Slot 1 - Stage 6 serialized Wave II shared DM action registry

BRIDGE_PROTOCOL: 2.4
BRIDGE_SLOT: 1
THREAD_ID: BRIDGE-20260708-034
LANE_ID: S6-I2-DM-ACTIONS
TASK_ID: TASK-S6-PAR-I2
PHASE: MODEL_PREFLIGHT_ONLY_THEN_RUNTIME_SENSITIVE_IMPLEMENTATION
USER_COMMAND: мост 1
MAILBOX_PARENT_COMMIT: e9aee56e13519eb37d23c7b97c224390dd470756
CURRENT_MAIN_BASELINE: 0775b8d10ad8f2311ac9d3aa953706d73174bc15
DECISION_FREEZE: .ai-bridge/decisions/STAGE6_WAVE2_COPY_FREEZE.md
PRIMARY_WRITE_SCOPE:
- AsyncScene/Web/ui/ui-dm.js
- docs/ui/ui-dm.js
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260708-034-02-codex.md
RUNTIME_CLASSIFICATION: RUNTIME_SENSITIVE
RUNTIME_SAFETY_CONFIRMATION_REQUIRED: YES
MODEL_SELECTION: PENDING_CODEX_PREFLIGHT_12_OF_12

## Atomic objective

Implement only serialized Wave II: separate Boomer and Alpha DM action labels, apply the frozen DM decisions, and route the hardcoded Like CTA through a profile action ID without changing any handler, mechanics, economy, state machine, or file outside the exact mirror pair.

Do not start Wave III or later work.

## Mandatory authorities

1. Freshly fetch `origin/main` and `origin/coordination/chatgpt-codex-bridge`.
2. Read current mailbox STATE and immutable claim.
3. Read `.ai-bridge/decisions/STAGE6_WAVE2_COPY_FREEZE.md`.
4. Read accepted collision package `.ai-bridge/outbox/BRIDGE-20260707-030-02-codex.md`.
5. Read accepted Boomer package `.ai-bridge/outbox/BRIDGE-20260707-031-02-codex.md`.
6. Read accepted Alpha package `.ai-bridge/outbox/BRIDGE-20260707-032-02-codex.md`.
7. Inspect fresh current-main `AsyncScene/Web/ui/ui-dm.js` and `docs/ui/ui-dm.js`.

## Required model protocol

Before any edit, evaluate all 12 available model/reasoning pairs:

- GPT-5.4-Mini: Light, Medium, High, Extra High
- GPT-5.4: Light, Medium, High, Extra High
- GPT-5.5: Light, Medium, High, Extra High

Optimize for the cheapest reliable pair. Return compact preflight only and stop with exactly one fenced `CONTINUE` block. The user selects the model in Codex. Same-thread `CONTINUE` is the explicit runtime-safety confirmation for this exact two-file task.

No edit, test, commit or push before same-thread `CONTINUE`.

## Required implementation

### Profile routing

- DM action routing must distinguish `default`, `millennial`, `zoomer`, `boomer`, and `alpha`.
- Alpha must not share the Zoomer label object.
- Boomer must not share the base/Millennial label object for rows changed below.
- Unknown profiles retain the existing safe `default` fallback.
- Existing default, Millennial and Zoomer visible output must remain unchanged.

### Frozen Boomer DM targets

Implement exact Boomer profile labels:

- `dm.battle` -> `Конфликт`
- `dm.report.open` -> `Сообщить`
- `dm.report.submit.idle` -> `Сообщить`
- `dm.report.submit.pending` -> `Проверка...`
- `dm.report.submit.cooldown` -> `Повторите позже`

All other Boomer DM action labels remain exactly current unless an accepted frozen row explicitly says otherwise.

### Frozen Alpha DM targets

Implement exact Alpha profile labels:

- `dm.respect` -> `Уважение`
- `dm.teach.toggle` -> `Передать аргумент`
- `dm.invite` -> `Указать имя`
- `dm.p2p.give` remains `Передать 💰`
- `dm.p2p.request` remains `Попросить 💰`

The teach panel helper remains exactly current: `Выбери аргумент. Он одноразовый. Цена видна.`

Do not change Alpha report-state copy in this wave. Vote and Report remain later runtime-required contracts.

### Like routing

- Introduce one stable action ID for the current hardcoded Like CTA, preferably `dm.like` unless the existing architecture already has an equivalent canonical ID.
- Default, Millennial, Zoomer and Boomer visible Like text must remain exactly `Лайк`.
- Alpha visible Like text must be exactly `Поддержать ❤️`.
- Preserve the existing reaction handler, target identity, heart semantics, DOM behavior, event wiring and accessibility. Do not create a second reaction mechanic.
- Route the existing visible CTA through `resolveDmActionLabel(...)` or the existing canonical registry mechanism rather than a profile conditional at the render site.

### Architecture constraints

- Extend the existing `DM_ACTION_LABEL_BASE`, `DM_ACTION_LABELS`, `resolveDmProfile`, and `resolveDmActionLabel` architecture.
- Prefer immutable profile overrides composed from the existing base instead of duplicating unrelated labels.
- Preserve action IDs, handlers, report state transitions, costs, rewards, cooldowns, one-use teaching behavior, P2P behavior and emitted system messages.
- Do not change `system.js`, `data.js`, events, battle UI, conflict core, index, dev-checks, boot/cache-bust, shared status docs or mailbox STATE.
- No broad refactor and no unrelated cleanup.
- Source and deployed mirror must remain byte-identical.

## Exact allowed changed paths

- `AsyncScene/Web/ui/ui-dm.js`
- `docs/ui/ui-dm.js`

Any other changed path is automatic FAIL.

## Required verification

After same-thread `CONTINUE` and implementation:

1. Prove source/mirror byte equality.
2. Run syntax/static validation for both files.
3. Probe `resolveDmActionLabel` for all five profiles and unknown fallback.
4. Prove exact Boomer target matrix above.
5. Prove exact Alpha target matrix above.
6. Prove default/Millennial/Zoomer labels remain unchanged, including `баттл`, `Сдать`, `Проверяю...`, `Занят`, `Передать`, `Позвать`, `Лайк`, P2P labels and Respect.
7. Prove Alpha Teach is distinct from P2P Give.
8. Prove Like uses the same existing handler and only visible label routing changed.
9. Prove report state transitions and submit behavior are unchanged.
10. Run relevant existing DM/profile smoke commands without editing dev-checks or boot.
11. Run `git diff --check`.
12. Return empty `failures`, `forbiddenRemaining`, `missingCoverage`, and `failedChecks` for PASS.

Do not claim Safari acceptance. Safari remains deferred until the serialized deployed acceptance wave.

## Publication

- Commit and push the exact two-file primary change to `main` according to current repository policy.
- Publish one mailbox outbox at the expected path.
- On credentials-only mailbox publication failure, return the complete intended UTF-8 outbox payload and exact path.
- Do not edit mailbox STATE.

## Required outbox fields

Include:

- status;
- selected model and reasoning;
- preflight 12/12 summary;
- exact main parent and implementation commit;
- exact changed paths;
- source/mirror blob SHAs and equality;
- exact resolved matrix for all profiles;
- unchanged-label proof;
- Like action ID and handler-preservation proof;
- commands/tests/results;
- failures, forbiddenRemaining, missingCoverage, failedChecks;
- runtime classification;
- Safari status `N/A - later serialized deployment smoke`;
- mailbox parent/commit or complete auth-fallback payload;
- exact next user action `Return to ChatGPT and write мост 1`.
