# Slot 1 verification correction

BRIDGE_PROTOCOL: 2.4
BRIDGE_SLOT: 1
THREAD_ID: BRIDGE-20260708-033
LANE_ID: S6-I1-SYSTEM-RESOLVER
TASK_ID: TASK-S6-PAR-I1
PHASE: CORRECTION_REQUIRED_POST_VERIFICATION
CURRENT_MAIN_HEAD: aa32180cfcbc82ec00b351887cb8853e3f120e1f
REJECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260708-033-02-codex.md
CURRENT_BASELINE_INBOX: .ai-bridge/inbox/BRIDGE-20260708-033-03-chatgpt.md
CLAIM_PATH: .ai-bridge/claims/BRIDGE-20260708-033-claim-codex.md
PRIMARY_WRITE_SCOPE:
- AsyncScene/Web/system.js
- docs/system.js
EXPECTED_CORRECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260708-033-04-codex.md
RUNTIME_CLASSIFICATION: RUNTIME_SENSITIVE
MODEL_PREFLIGHT: ALREADY_COMPLETE
SELECTED_MODEL: GPT-5.4 / High
RUNTIME_SAFETY_CONFIRMATION: ALREADY_CONFIRMED_BY_SAME_THREAD_CONTINUE

## Verification result

The implementation commit is not accepted yet.

The intended Wave I routing and frozen copy targets appear present, main is exactly one commit ahead of the authorized baseline, and only the two authorized paths changed. However, the deployed mirror was broadly overwritten to match the source:

- `AsyncScene/Web/system.js`: 59 changed lines.
- `docs/system.js`: 531 changed lines.
- The baseline deployed mirror contained pre-existing `reputation_*`, `respect_*`, and `disrespect_*` profile-copy entries that were deleted by the implementation.
- The baseline deployed mirror also contained the Zoomer/genz escape fixture `Свалить за взятку {X} 💰` -> `Свалить: {X} 💰`; the implementation replaced it with `Выйти ...`, although no accepted Wave I decision authorized a Zoomer/genz copy change.

This violates:

- no broad refactor;
- preserve existing Millennial and Zoomer output unless explicitly frozen;
- no change outside the exact frozen target matrix;
- zero semantic loss during mirror reconciliation.

## Required correction

Continue in the same Slot 1 Codex thread. Do not run another model preflight and do not request another CONTINUE.

1. Start from current `origin/main` at `aa32180cfcbc82ec00b351887cb8853e3f120e1f`.
2. Keep the accepted Wave I resolver/profile-routing implementation and exact frozen Boomer/Alpha targets.
3. Compute the complete deletion set from baseline `873e67387e7c4db88c1b07772ef928ee17187210:docs/system.js` to current main `docs/system.js`.
4. Restore every deleted baseline line or semantic entry that was not explicitly authorized by the frozen Wave I target matrix.
5. Preserve source/mirror byte equality by carrying restored pre-existing deployed behavior into both `AsyncScene/Web/system.js` and `docs/system.js` where required.
6. At minimum, restore in both files:
   - all prior `reputation_increased`, `reputation_decreased`, `reputation_unchanged` entries;
   - `respect_gained`, `respect_lost`, `disrespect_event`;
   - `reputation_high`, `reputation_low`, `reputation_recovered`, `reputation_damaged`;
   - their Millennial and Zoomer texts exactly as present in the baseline deployed mirror;
   - the pre-existing Zoomer/genz escape smoke fixture `Свалить за взятку {X} 💰` -> `Свалить: {X} 💰` in both files.
7. Inspect all remaining baseline deletions, not only the examples above. No unrelated baseline behavior may remain deleted.
8. Do not alter any frozen Boomer or Alpha target, route, placeholder, taxonomy code, public API, handler, economy, state, or fallback behavior.
9. Change no path outside the same two-file scope.

## Required verification

- Compare baseline deployed mirror against corrected files and list every baseline deletion with disposition: restored or explicitly authorized replacement.
- Prove there are zero unexplained deletions.
- Prove source/mirror byte equality.
- Prove exact four-profile routing and frozen target matrix still PASS.
- Prove all restored reputation/respect keys resolve exactly as before for Millennial and Zoomer.
- Prove the Zoomer/genz escape fixture is restored exactly.
- Run syntax checks, targeted probes, relevant existing system-copy smoke, and `git diff --check`.
- Return empty `failures`, `forbiddenRemaining`, `missingCoverage`, `failedChecks`, and `unexplainedBaselineDeletions`.

## Publication

- Publish one corrective commit to `main` as a direct child of current main.
- Publish corrected mailbox outbox at `.ai-bridge/outbox/BRIDGE-20260708-033-04-codex.md`.
- Do not edit mailbox STATE.
- Exact next user action after publication: return to ChatGPT and write `мост 1`.
