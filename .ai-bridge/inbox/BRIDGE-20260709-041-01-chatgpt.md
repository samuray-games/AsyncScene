Use @asynchronia runtime-safety-gate.

# Stage 6 Wave V-B - Boomer Step 4.4B runtime aggregate smoke

BRIDGE_PROTOCOL: 3.1
THREAD_ID: BRIDGE-20260709-041
LANE_ID: S6-V5B-BOOMER-RUNTIME-AGGREGATE
TASK_ID: TASK-S6-PAR-V5B
EXECUTION_EPOCH: S6V5B-E1-20260709-0141JST
SLOT: 1
PHASE: MODEL_PREFLIGHT_ONLY
BASELINE: c0e2f891076f3e8e280941edbe0e241d9931dd0f
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
SAFARI_OWNER: USER
SAFARI_STATUS: PENDING_USER

## Frozen objective

Add one isolated Step 4.4B runtime aggregate smoke for the accepted Boomer economy and conflict terminology. Do not change product copy, economy, battle, NPC, persistence, state, routing, profile selection or gameplay behavior.

The new public Safari command must be:

`Game.__DEV.smokeBoomerEconomyConflictTerminologyOnce()`

The smoke must verify the exact deployed Boomer runtime presentation across these feature zones:

1. Points / 💰
2. REP / ⭐
3. Influence
4. voting
5. majority and minority outcomes
6. rematch
7. NPC-vs-NPC conflict text
8. conflict results
9. DM
10. reports and report resolution

The accepted static prerequisite is already satisfied by `UI_PROFILE_BOOMER_STEP_4_4_ECONOMY_CONFLICT_TERMINOLOGY_AUDIT.md`: `STATIC_PASS / READY_FOR_RUNTIME_SMOKE`, 147 PASS, 0 FAIL, 0 structural failures, 8 non-live.

## Exact read scope

Repository authority:

- `AGENTS.md`
- `TASKS.md`
- `PROJECT_MEMORY.md`
- `plugins/asynchronia/skills/runtime-safety-gate/SKILL.md`
- `UI_PROFILE_BOOMER_STEP_4_4_ECONOMY_CONFLICT_TERMINOLOGY_AUDIT.md`
- `docs/UI_PROFILE_BOOMER_STEP_4_4_ECONOMY_CONFLICT_TERMINOLOGY_AUDIT.md`
- `docs/UI_PROFILE_BOOMER_ALLOWED_LEXICON.md`
- `docs/UI_PROFILE_BOOMER_TABOO_LIST.md`
- `docs/UI_PROFILE_BOOMER_STEP_4_3_MILLENNIAL_TO_BOOMER_MAPPING.md`
- `docs/UI_PROFILE_BOOMER_RUNTIME_GAP_TARGETS.md`
- `docs/UI_PROFILE_BOOMER_RUNTIME_GAP_COPY_DECISIONS.md`

Runtime source and deployed mirrors:

- `AsyncScene/Web/system.js`
- `docs/system.js`
- `AsyncScene/Web/data.js`
- `docs/data.js`
- `AsyncScene/Web/events.js`
- `docs/events.js`
- `AsyncScene/Web/state.js`
- `docs/state.js`
- `AsyncScene/Web/conflict/conflict-core.js`
- `docs/conflict/conflict-core.js`
- `AsyncScene/Web/conflict/conflict-economy.js`
- `docs/conflict/conflict-economy.js`
- `AsyncScene/Web/ui/ui-core.js`
- `docs/ui/ui-core.js`
- `AsyncScene/Web/ui/ui-dm.js`
- `docs/ui/ui-dm.js`
- `AsyncScene/Web/ui/ui-battles.js`
- `docs/ui/ui-battles.js`
- `AsyncScene/Web/ui/ui-events.js`
- `docs/ui/ui-events.js`
- `AsyncScene/Web/ui/ui-menu.js`
- `docs/ui/ui-menu.js`
- `AsyncScene/Web/dev/dev-checks.js`
- `docs/dev/dev-checks.js`
- `AsyncScene/Web/index.html`
- `docs/index.html`

Existing helper fixtures that may be called but must not be edited:

- `Game.__DEV.__smokeBoomerTermsStep42Menu`
- `Game.__DEV.__smokeBoomerTermsStep42Dm`
- `Game.__DEV.__smokeBoomerTermsStep42Battles`
- `Game.__DEV.__smokeBoomerTermsStep42Events`

## Exact protected scope

All read-scope runtime and product files are protected except the four authorized write files below. Do not edit any copy registry, resolver, UI emitter, economy handler, battle handler, state, persistence, NPC file, profile selector or existing helper fixture.

## Exact write scope after approval only

- `AsyncScene/Web/dev/dev-checks.js`
- `docs/dev/dev-checks.js`
- `AsyncScene/Web/index.html`
- `docs/index.html`

No `TASKS.md`, `PROJECT_MEMORY.md`, audit artifact, lexicon, mapping or product runtime file belongs to this runtime implementation commit. Status-document synchronization is a separate serialized documentation task after runtime acceptance.

## Runtime smoke contract

The aggregate must:

- install and export `Game.__DEV.smokeBoomerEconomyConflictTerminologyOnce`;
- expose the same callable through the existing accepted `Game.Dev` / `Game.__DEV` export pattern without creating a new global API shape;
- run only against the Boomer presentation path and verify default/unknown fallback remains non-Boomer;
- call existing pure helper fixtures where available and actual runtime resolvers where required;
- cover every frozen feature zone above;
- verify exact placeholders, protected numbers and emoji;
- detect Millennial, Zoomer or Alpha leakage;
- detect accepted Boomer taboo phrases;
- preserve all game state, points, REP, Influence, battle state, money log, persistence and local storage;
- perform no synthetic economy transfer and no gameplay action;
- include deployment evidence from `location.href` and the loaded dev-checks script URL;
- fail closed on missing helper, missing feature zone, stale build, export failure, state drift or mirror-target drift;
- return `ok:true` only when `failures`, `forbiddenRemaining`, `missingCoverage` and `failedChecks` are all empty.

Required result fields:

- `ok`
- `buildTag`
- `commit`
- `smokeVersion`
- `profile`
- `checkedCount`
- `coveredFeatureZones`
- `stateRestored`
- `runtimeSourceUrl`
- `pageUrl`
- `failures`
- `forbiddenRemaining`
- `missingCoverage`
- `failedChecks`

Frozen identity:

- `buildTag`: `build_2026_07_09_step4_4b_boomer_runtime_aggregate`
- `commit`: `step4_4b_boomer_runtime_aggregate`
- `smokeVersion`: `boomer_step4_4b_runtime_aggregate_v20260709_001`
- cache-bust: `step4_4b_boomer_runtime_aggregate_20260709a`

## Source/deployed requirements

- Apply the same task-owned smoke block to both `dev-checks.js` copies.
- Preserve unrelated pre-existing differences. Do not wholesale replace either 54k-line file.
- The new aggregate block and exports must be byte-equivalent across the two copies.
- Keep `AsyncScene/Web/index.html` and `docs/index.html` byte-identical after the task.
- Update only the dev-checks cache-bust and the task-owned build identity required for this smoke.

## Validation after approval

- syntax-check both changed JavaScript files;
- prove the exact changed-file list is the four authorized paths;
- prove the aggregate block and exports match across source/deployed mirrors;
- prove both index files are identical and reference the frozen cache-bust;
- run the existing static Step 4.4A generator and validator without changing their outputs;
- run relevant repository smoke/static validators that do not require Safari;
- publish one direct-child primary commit and exact current outbox;
- report Safari as `PENDING_USER`.

## Model preflight for this command

On the first `мост 1`:

1. make no edits;
2. freshly fetch main and mailbox;
3. inspect every exact file above;
4. verify the four-file write scope is sufficient;
5. verify no slot or runtime singleton collision exists;
6. verify helper/export/boot wiring;
7. return a 12-of-12 preflight result;
8. independently recommend one available model and one reasoning level from the inspected task;
9. stop with exact next action `CONTINUE`.

No model or reasoning level is preselected by ChatGPT. The user selects the Codex-recommended option after the read-only preflight and then sends `CONTINUE` in the same Codex conversation.

After same-thread `CONTINUE`, run the runtime safety gate. Because all four write files are runtime-sensitive, make no edits and return `RUNTIME_SAFETY_GATE_REQUIRED`, list all four files and end with the standalone fenced `APPROVE` token required by repository policy.

Only after same-thread approval may implementation begin.
