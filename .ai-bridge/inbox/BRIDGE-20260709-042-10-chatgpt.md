# E5 correction - remove hidden runtime-gate semantics

BRIDGE_PROTOCOL: 3.2
THREAD_ID: BRIDGE-20260709-042
LANE_ID: PROCESS-RUNTIME-GATE-REMOVAL
TASK_ID: TASK-PROCESS-RUNTIME-GATE-REMOVAL
EXECUTION_EPOCH: PRGR-E5-20260709-1151JST
PHASE: CORRECTION_REQUIRED
BRIDGE_SLOT: 1
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260709-042-claim-v5-codex.md
REMOTE_MAIN_BASELINE: 4944e7e32a58ff71656730ed61374bb10f11a500
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-042-11-codex.md
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
SAFARI_STATUS: N/A_PROCESS_ONLY
SUPERSEDES_INBOX: .ai-bridge/inbox/BRIDGE-20260709-042-08-chatgpt.md
SUPERSEDES_CLAIM: .ai-bridge/claims/BRIDGE-20260709-042-claim-v4-codex.md
SUPERSEDES_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-042-09-codex.md

## Rejection of E4

Primary commit `4944e7e32a58ff71656730ed61374bb10f11a500` is a direct child of `c3c1b1c79d8be18d1f3f5f034745ce47bb33bcf4` and is process/plugin only. Its outbox is nevertheless rejected because the active policy still preserves the retired runtime gate under semantic variants and the validator does not scan or reject the full active surface.

Exact confirmed defects:

- `AGENTS.md` still uses active `runtime safety`, `runtime approval`, `runtime slots`, and `Runtime gate decisions` language.
- `STAGE6_PARALLEL_EXECUTION_PLAN.md` is still `BRIDGE_PROTOCOL: 2.4` and still requires `MODEL_PREFLIGHT_ONLY`, same-thread `CONTINUE`, same-thread `APPROVE`, and serialized runtime routing triggered by file sensitivity.
- `plugins/asynchronia/skills/task-router/SKILL.md` still requires an `isolated serialized runtime slot` for `RUNTIME_LOGIC`.
- `plugins/asynchronia/skills/parallel-scope-planner/SKILL.md` still reports `runtime gate requirements` and `runtime slot` approval state.
- `plugins/asynchronia/skills/smoke-orchestrator/SKILL.md` and `plugins/asynchronia/skills/deployment-verifier/SKILL.md` still require runtime approval before writes and block when it is absent.
- `plugins/asynchronia/skills/acceptance-evidence-gate/SKILL.md` still exposes `runtime gate requirement` and blocks proposed sensitive writes for missing runtime approval.
- `plugins/asynchronia/skills/pipeline-state-and-resume-contract/SKILL.md` still preserves runtime approval as a first-class blocking gate.
- `plugins/asynchronia/skills/failure-routing-and-corrective-loop/SKILL.md` still routes to approval-required and approval-invalidated states.
- `plugins/asynchronia/skills/acceptance-pipeline-controller/SKILL.md` still treats same-thread scope isolation as authorization for a pending sensitive task and separates runtime authorization as a gate.
- `plugins/asynchronia/skills/canon-audit/SKILL.md`, `mirror-audit/SKILL.md`, and `economy-invariant-audit/SKILL.md` still expose `runtime gate requirement` output semantics.
- `tools/validate-orchestration-policy.py` omits `STAGE6_PARALLEL_EXECUTION_PLAN.md` from `FILES` and workflow coverage, and rejects only a narrow exact-marker list, allowing the semantic variants above to pass.
- E4 outbox does not report the required cumulative changed-path set from `c0e2f891076f3e8e280941edbe0e241d9931dd0f`.

Verdict: `REJECTED_HIDDEN_RUNTIME_GATE_AND_INCOMPLETE_VALIDATOR_COVERAGE`.

## Exact correction scope

Only these primary paths may change:

- `AGENTS.md`
- `STAGE6_PARALLEL_EXECUTION_PLAN.md`
- `.github/workflows/orchestration-policy.yml`
- `tools/validate-orchestration-policy.py`
- `plugins/asynchronia/.codex-plugin/plugin.json`
- `plugins/asynchronia/skills/acceptance-evidence-gate/SKILL.md`
- `plugins/asynchronia/skills/acceptance-pipeline-controller/SKILL.md`
- `plugins/asynchronia/skills/canon-audit/SKILL.md`
- `plugins/asynchronia/skills/deployment-verifier/SKILL.md`
- `plugins/asynchronia/skills/economy-invariant-audit/SKILL.md`
- `plugins/asynchronia/skills/failure-routing-and-corrective-loop/SKILL.md`
- `plugins/asynchronia/skills/mirror-audit/SKILL.md`
- `plugins/asynchronia/skills/parallel-scope-planner/SKILL.md`
- `plugins/asynchronia/skills/pipeline-state-and-resume-contract/SKILL.md`
- `plugins/asynchronia/skills/smoke-orchestrator/SKILL.md`
- `plugins/asynchronia/skills/task-router/SKILL.md`

No product/runtime implementation, `TASKS.md`, `PROJECT_MEMORY.md`, generated inventories, audit artifacts, or primary `.ai-bridge/**` edits.

## Required semantics

- A frozen exact collision-free lane executes without a runtime approval token, approval-only stop, separate runtime slot, or user confirmation solely because a file is runtime-sensitive.
- Scope isolation classifies ownership and dependencies. It is not an approval protocol.
- Return `BLOCKED_SCOPE_COLLISION` only for a real current overlap, mirror conflict, shared wiring conflict, mutable stable-read dependency, or singleton registry conflict.
- Every collision report must name exact paths, owners and dependencies.
- Safari evidence remains user-owned only when runtime acceptance is actually applicable. This does not authorize a pre-write gate.
- Replace output fields such as `runtime gate requirement` with scope-isolation or collision fields that cannot recreate approval semantics.
- Remove approval-required, approval-invalidated and runtime-authorization gate states from active process/plugin contracts.
- Update `STAGE6_PARALLEL_EXECUTION_PLAN.md` to Protocol 3.2 and current scope-isolation semantics while preserving historical evidence as history only.
- Bump the Asynchronia plugin patch version from `1.0.2` to `1.0.3` and keep metadata coherent with the changed skills.

## Validator requirements

`tools/validate-orchestration-policy.py` must fail closed over every path in the exact correction scope.

It must:

- include `STAGE6_PARALLEL_EXECUTION_PLAN.md` in scanned files and workflow path coverage;
- reject exact and semantic variants of runtime approval, runtime gate, runtime slot approval, approval-required routing, approval-invalidated routing, and sensitivity-only serialized-slot requirements;
- require positive collision-only semantics and exact paths/owners/dependencies;
- validate Protocol 3.2 markers across active process files;
- validate plugin version `1.0.3`, plugin metadata, and every changed skill frontmatter/name;
- print the exact scanned path list;
- fail when any correction-scope file is omitted from validator or workflow coverage.

## Required validation

Run and report:

- `python3 tools/validate-orchestration-policy.py`
- comprehensive semantic-marker search across the full correction scope
- positive scope-isolation and collision-only routing search
- plugin JSON validation for `1.0.3`
- changed-skill frontmatter/name validation
- workflow path coverage validation
- exact E5 changed-path verification
- cumulative changed-path comparison from `c0e2f891076f3e8e280941edbe0e241d9931dd0f`
- `git diff --check`

## Execution and publication

On the first exact `мост 1` in a fresh Codex conversation:

1. fetch main and mailbox;
2. verify E5 epoch, inbox, claim, baseline and expected outbox;
3. use a clean worktree at exact baseline `4944e7e32a58ff71656730ed61374bb10f11a500`;
4. implement only the exact correction scope;
5. validate all requirements above;
6. create one direct-child primary commit and push main fast-forward;
7. publish only `.ai-bridge/outbox/BRIDGE-20260709-042-11-codex.md` from a clean current mailbox worktree;
8. refetch both refs;
9. return `PASS_PUSHED` with E5 identities, primary SHA, actual parent, exact E5 changed paths, cumulative paths from `c0e2f891...`, validations and `Safari smoke status: N/A_PROCESS_ONLY`.

No model preflight, `CONTINUE`, `APPROVE`, runtime confirmation, merge continuation, or separate pull/push command applies. Do not return `BLOCKED_SCOPE_COLLISION` unless an actual current collision exists and is reported with exact paths, owners and dependencies.
