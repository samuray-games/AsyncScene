BRIDGE_PROTOCOL: 1.0
THREAD_ID: BRIDGE-20260705-009
TURN: 01
FROM: CHATGPT
TO: CODEX
TYPE: REQUEST
THREAD_STATUS: WAITING_CODEX
CREATED_AT: 2026-07-05T10:36:00+09:00
REPLY_TO: N/A
PRIMARY_REPO_REF: main
PRIMARY_REPO_COMMIT: e7eeb5113d25233d0314e9fa30a4eefbc2e1ca26
MAILBOX_PARENT_COMMIT: 4546d96c2c6cbddef96732608e6da6e7cdaf4c3e
TASK_ID: TASK-S6-001-R3
MODE: MODEL_PREFLIGHT_ONLY

## Summary

Perform the full read-only Stage 6 reconciliation audit after the user updates the local `main` checkout. The previous alias failures were caused by a stale local checkout, not by the mailbox task definition. This supersedes the temporary global-bootstrap task BRIDGE-20260705-008.

## Required first phase

Return `MODEL_PREFLIGHT_ONLY` only. Do not run the audit before same-thread `CONTINUE`.

Preflight must:
1. verify local `main` includes commit `e7eeb5113d25233d0314e9fa30a4eefbc2e1ca26` or a newer current `origin/main`;
2. verify the root `AGENTS.md` contains the exact `проверь мост` alias and root `BRIDGE.md` exists;
3. verify the sole active mailbox thread is BRIDGE-20260705-009;
4. recommend the cheapest reliable available model and reasoning level for a broad cross-system Stage 6 reconciliation audit;
5. confirm no product writes, locks, runtime edits or Safari smoke occur in this task;
6. end with the standalone `CONTINUE` block.

## Audit scope after CONTINUE

A. current main, local worktree status, uncommitted changes, branches and worktrees;
B. all active, stale or conflicting locks, including BOOMER-B4T44A-ECONFLICT-AUDIT-20260705T0223JST;
C. canonical Stage 6 scope and separation from future caps/Living World scope;
D. exact implementation and acceptance state for Millennial, Zoomer, Boomer, Alpha and UI Profile Selection;
E. exact Boomer 4.4A failure inventory and artifacts;
F. exact Alpha 4.4 PASS, FAIL and RUNTIME_REQUIRED surfaces;
G. current UI Profile Selection implementation, resolver, onboarding, privacy/storage behavior and acceptance evidence;
H. exact future source files, deployed mirrors, intentional transformations, shared registries, exports, globals, boot/cache-bust wiring and runtime-sensitive boundaries;
I. stable-read dependencies, write overlaps, safe task splitting and whether one consolidated presentation runtime task is safe;
J. corrections required in the Execution Plan before implementation.

## Mirror rule

- Do not assume byte identity unless a current contract requires it.
- Distinguish byte parity, semantic parity, intentional transformation and actual deployment wiring defects.
- Do not turn this into a generic mirror audit.

## Allowed eventual write

- only `.ai-bridge/outbox/BRIDGE-20260705-009-02-codex.md` on `coordination/chatgpt-codex-bridge`.

## Forbidden

- every primary repository edit;
- runtime, UI, logic, generated asset, audit artifact or status-document change;
- lock acquisition;
- creative copy or terminology invention;
- implementation or repair;
- any change to closed or superseded threads.

## Runtime gate

`N/A - read-only audit.` Do not output `APPROVE`.

## Final response contract after CONTINUE

Publish the immutable outbox and report:
1. status;
2. selected model and reasoning;
3. verified baseline;
4. inspected files;
5. changed files;
6. task-owned diff;
7. unrelated concurrent changes;
8. tests/checks;
9. failures;
10. missing coverage;
11. buildTag;
12. commit;
13. push result;
14. smokeVersion;
15. Safari status;
16. exact next action.

The user does not relay the preflight to ChatGPT. Current primary sources override this inbox if newer; report conflicts and stop rather than guessing.