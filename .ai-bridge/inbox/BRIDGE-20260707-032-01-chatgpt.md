Use @asynchronia runtime-safety-gate.

# Bridge Slot 3 - Wave 2 Alpha decision package

BRIDGE_PROTOCOL: 2.4
BRIDGE_SLOT: 3
THREAD_ID: BRIDGE-20260707-032
LANE_ID: S6-A2-ALPHA-DECISION
TASK_ID: TASK-S6-PAR-A2
PHASE: MODEL_PREFLIGHT_ONLY_THEN_READ_ONLY_DECISION_PACKAGE
USER_COMMAND: мост 3
MAILBOX_PARENT_COMMIT: aa9e64e90e70c97b3960012736dedf88d992c6f5
CURRENT_POLICY_HEAD: 72aaf0459dea2f5d1d90f8e1eae5423dc45b4385
AUTHORIZED_PRODUCT_EVIDENCE_BASELINE: a8a7e3acacbe2ba7fe6a387cb647d09d7d701a4d
PRIMARY_WRITE_SCOPE: NONE
RUNTIME_CLASSIFICATION: READ_ONLY
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260707-032-02-codex.md
RECOMMENDED_MODEL: GPT-5.5
RECOMMENDED_REASONING: High
ACTUAL_ACTIVE_MODEL: USER_SELECTED_UNVERIFIED

## Atomic objective

Build the exact Wave 2 Alpha decision package for the three accepted FAIL groups and the two RUNTIME_REQUIRED groups. This is a read-only decision task. Produce source-backed candidate targets and runtime evidence contracts without changing product, runtime, mirrors, mappings, documentation, smoke wiring or STATE.

## Mandatory authorities

1. Fetch current `origin/main` and current mailbox STATE.
2. Read current policy and Stage 6 plan/status files.
3. Read accepted Slot 3 evidence outbox `.ai-bridge/outbox/BRIDGE-20260705-029-02-codex.md`.
4. Read current authoritative Alpha contracts from the frozen product evidence baseline:
   - `UI_PROFILE_ALPHA_TERM_RULES.md`;
   - `ALPHA_LEXICON.md`;
   - `docs/UI_PROFILE_ALPHA_WORD_INVENTORY.md`;
   - `docs/UI_PROFILE_ALPHA_ALLOWED_LEXICON.md`;
   - `docs/UI_PROFILE_ALPHA_TABOO_LIST.md`;
   - `docs/UI_PROFILE_ALPHA_Z_TO_ALPHA_MAPPING.md`;
   - exact current source and mirror files for the five groups below.
5. Do not use `docs/ui/ui-profile-alpha-new-features.js` or `docs/UI_PROFILE_ALPHA_NEW_FEATURES.md` as current implementation authority. They may be classified only as stale historical evidence.
6. `UI_PROFILE_ALPHA_TERM_RULES.md` says Codex must not invent Alpha phrases. Therefore every proposed target must be traceable to existing accepted lexicon terms or explicit accepted mappings. If a unique target cannot be derived, classify the row `BLOCKED_COORDINATOR_COPY_DECISION` and offer only bounded source-backed options.

## Accepted surface matrix

PASS and out of scope for rewriting:
- Attack: `Влететь`, action `battle_action_attack`.
- Rematch: `Ещё раунд`, action `battle_action_rematch`.
- P2P give: `Передать 💰`, action `dm.p2p.give`.
- P2P request: `Попросить 💰`, action `dm.p2p.request`.
- Cop flow: accepted representative Alpha templates from Slot 3 evidence.

FAIL groups requiring decision package:

1. Escape
- current conflicting visible copy: `Свалить -{X} 💰` and routed `Уйти: 1💰`.
- action key: `escape_button_label`; routed notification: `escapePaid`.
- preserve `{X}`, `1`, `💰`, `⭐`, Unicode minus, handler and economy.
- required decision: one canonical Alpha escape term across all Alpha surfaces, no slang collision, no semantic loss.

2. Teach
- current action copy: `Передать`.
- current panel: `Выбери аргумент. Он одноразовый. Цена видна.`
- leaked templates: `TEACH {student} {arg} -{cost}` and `TEACH {teacher}->{student}`.
- action: `dm.teach.toggle`; keys: `teach_sent_dm`, `teach_sent_chat`.
- preserve `{student}`, `{arg}`, `{cost}`, `{teacher}`, one-use semantics, price and handler.
- required decision: source-backed Alpha labels/templates with no English leakage and no ambiguity with P2P `Передать 💰`.

3. Social actions
- current shared copy: `Уважение`, `Позвать`, `Лайк`.
- action IDs: `dm.respect`, `dm.invite`; Like is currently a direct hardcoded CTA without a profile action ID.
- all profiles currently reuse `DM_ACTION_LABEL_BASE`, so Alpha has no distinct social-action routing.
- preserve costs, rewards, handlers, `💰`, `⭐`, `❤️` and other profiles.
- required decision: exact Alpha target per action and explicit classification of the Like hardcoded-copy dependency.

RUNTIME_REQUIRED groups:

4. Vote
- preserve current action/state and economy.
- later runtime proof must cover visible Alpha copy, one-vote lock, point charge, REP participation, continued NPC voting, event/battle crowd mirroring, cap behavior and exact deployed artifact identity.

5. Report
- preserve current action/state and economy.
- later runtime proof must cover open -> idle -> pending -> cooldown/resolved, duplicate-submit blocking, cop DM result, reward/penalty copy and exact deployed artifact identity.

Shared wrong-profile dependency:
- `activeSystemTextProfile()` currently routes Alpha through Zoomer.
- classify this as `SERIALIZED_EXTERNAL_DEPENDENCY` owned by a later shared resolver singleton, not by this read-only lane.

## Required deliverable

1. An exact decision table for every concrete Escape, Teach and Social-action surface:
   - source location and mirror;
   - current text;
   - action ID/key;
   - placeholders and protected tokens;
   - accepted lexicon or mapping evidence for each candidate;
   - candidate target or `BLOCKED_COORDINATOR_COPY_DECISION`;
   - collision with existing canonical terms;
   - future file ownership classification.
2. Enforce Alpha rules:
   - stable terms use one or two lexical words;
   - one meaning uses one canonical term;
   - surface-specific synonyms are forbidden;
   - no slang, diminutives, artificial-youth tone or semantic loss;
   - mechanics and protected tokens remain unchanged.
3. Produce one preferred source-backed candidate per surface only when uniquely justified. Do not silently treat it as user-accepted.
4. Produce a compact coordinator decision card listing only unresolved choices.
5. Produce exact later runtime acceptance contracts for Vote and Report, including source/mirror identity, deployment identity and Safari evidence fields.
6. Mark resolver, shared system copy, DM action registry, Like routing, events/report wiring, mirrors, dev-checks, exports, globals, boot and aggregate smoke as serialized dependencies where applicable. Defer the authoritative cross-lane collision map to Slot 1.
7. Confirm no PASS surface is reopened without new primary contradiction.

## Prohibited

- No source, mirror, mapping, lexicon, inventory, docs, plan, TASKS, PROJECT_MEMORY, dev-checks, registry, boot or STATE edits.
- No runtime execution and no Safari claim.
- No use of stale Alpha replacement artifacts as current authority.
- No invented phrases outside accepted lexicon/mapping evidence.
- No resolver or DM registry implementation.

## Required execution protocol

Adopt the immutable claim from current STATE. Return the compact Protocol 2.4 preflight and stop with exactly one fenced `CONTINUE` block. After same-thread `CONTINUE`, perform the read-only task and publish exactly the expected outbox as a one-path direct-child mailbox commit. On credentials-only failure, return `BLOCKED_MAILBOX_AUTH` with the complete intended payload.

## Required outbox fields

Include status, selected model and reasoning, inspected files, changed files `NONE`, exact decision table, source-backed candidates, unresolved coordinator card, Vote and Report runtime contracts, serialized dependencies, failures, missing coverage, primary commit `N/A`, mailbox parent and commit, Safari status `N/A - read-only decision package`, and exact next user action `Return to ChatGPT and write мост 3`.
