Use @asynchronia.

# Recover semantic controls, exact identity, plugin invocation, and receipt proof

BRIDGE_PROTOCOL: 3.3
CYCLE_ID: CYCLE-20260709-001
CYCLE_GENERATION: 11
THREAD_ID: BRIDGE-20260710-056
LANE_ID: PROCESS-CLOSED-LOOP-SEMANTIC-CONTROLS-AND-RECEIPT-RECOVERY
TASK_ID: TASK-PROCESS-CLOSED-LOOP-SEMANTIC-CONTROLS-AND-RECEIPT-RECOVERY
EXECUTION_EPOCH: CLOSED-LOOP-RECOVERY-R2-20260710-0106JST
TASK_NONCE: CLV1-056-RECOVERY-E599-0106
BRIDGE_SLOT: 3
PHASE: RECOVERY_REQUIRED
BASELINE: e599beddbbd03c8585f9c44f0f7c190338e123e7
COORDINATOR_MEMORY_REV: 2026-07-10-0106-JST
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260710-056-claim-v1-codex.md
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260710-056-02-codex.md
EXPECTED_RECEIPT: .ai-bridge/receipts/BRIDGE-20260710-056-03-codex.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
FRESH_CODEX_CONVERSATION_REQUIRED: true
ACTUAL_PLUGIN_INVOCATION_REQUIRED: true
SOURCE_PLUGIN_FALLBACK_AUTHORIZED: false
SAFARI_STATUS: N/A_PROCESS_ONLY
SUPERSEDES_THREAD: BRIDGE-20260710-055

## Independent verifier verdict for thread 055

`RECOVERY_REQUIRED_FALSE_SEMANTIC_CONTROLS_AND_FALSE_STATE_SHA`.

Accepted partial progress:
- remote main `e599beddbbd03c8585f9c44f0f7c190338e123e7` is exactly one direct child of baseline `388048b8878b6b362dda9518a8ea9f079277ade9`;
- its eight changed paths are inside the authorized thread-055 scope;
- outbox 055 exists at the exact expected path and has blob `97db84f6464b5f5ca351649996c37ffb2a10486f`;
- outbox publication commit is `2bc10a017cedf100ed01b9117b7161d04bbf9711`, directly after mailbox parent `9bc3ce8e20e79238a3110088a302ed1b0d401e6d`;
- the mailbox branch is one commit beyond the outbox commit and that commit adds only receipt 055;
- final response used the receipt-shaped report and source validations were reported PASS.

Rejected report evidence:
1. The actual current STATE blob is `a322f0647b340bd61d829c1b153b2978b3f8f7a1`, but outbox 055 reports `remoteStateSha: 9bc3ce8e20e79238a3110088a302ed1b0d401e6d` and receipt 055 reports `remoteStateSha: 2bc10a017cedf100ed01b9117b7161d04bbf9711`. Both values are commit SHAs, not the STATE blob SHA.
2. `remoteMailboxCommit` changes meaning between outbox and receipt. The contract must use distinct unambiguous fields such as `mailboxParentCommit`, `outboxPublicationCommit`, `stateBlobSha`, `outboxBlobSha`, and `receiptPath`.
3. `recoveryClassification: CORRECTION_REQUIRED` is inconsistent with `status: PASS_PUSHED` and `completionMode: PRIMARY_DELTA` unless the schema explicitly defines that combination. Status, completion mode, verifier phase, and recovery class must be exact and consistent.
4. `nextAction` is free text and only checked for non-emptiness. A successful receipt must carry an exact machine-readable action code and a precise human instruction that this is an independent ChatGPT verification handoff.

Rejected source implementation:
1. `evaluate_control()` still returns unconditional `True` for route controls and identity controls. These are labels, not executable semantics.
2. Several negative controls merely inspect one payload value rather than proving that the real validator rejects a mutated invalid report.
3. `validate_report_schema()` still permits forbidden `BLOCKED_NO_SOURCE_DELTA`.
4. One universal outbox schema is used. There are no exact status-specific outbox schemas and no executable receipt schema.
5. Active identity is incomplete. The contract does not require and compare cycle id, generation, inbox, claim, expected receipt, or the full exact thread-056 identity.
6. SHA validation lowercases before checking, so uppercase values are accepted. It does not reject repeated synthetic 40-hex placeholders.
7. Passing tests and validator fixtures still use stale thread 052/053/054 identities and repeated `cafebabe`, `deadbeef`, and `feedface` values.
8. Changed paths and authorized paths are only type-checked or checked as non-empty, not compared as exact sets against commit evidence and frozen scope.
9. `accept_product_work()` still requires only `canaryAccepted` and `currentState`; it does not require `implementationAccepted`, `installedPackageAccepted`, and `canaryAccepted` together.
10. The validator checks substrings and the contract's own mutable transition table. It does not compare against an immutable canonical transition table, execute every control with adversarial fixtures, prove exact route order, verify workflow triggers, verify active artifact absence from main, or implement actual mutation tests.
11. The report claims mutation-style proofs, but the committed test file contains no mutation-style tests.
12. The primary and mailbox protocols remain inconsistent: primary docs mention outbox plus receipt, while mailbox protocol and generic publication policy still describe final-response identity with the outbox only.
13. A line `Use @asynchronia.` inside a fetched inbox does not activate a Codex plugin. Root bridge instructions must distinguish actual UI/plugin invocation from inert text inside repository files.
14. No independent CI run or commit status exists for `e599beddbbd03c8585f9c44f0f7c190338e123e7`. Local validation claims are evidence to reproduce, not acceptance by themselves.

## Required correction

### Exact schemas and identity
- Define separate exact status-specific terminal outbox schemas and a separate exact receipt schema.
- Define one immutable active-identity structure containing cycle, generation, slot, thread, lane, task, epoch, nonce, coordinator memory revision, baseline, inbox, claim, expected outbox, and expected receipt.
- Compare reports to the exact active identity. Syntax-only checks are insufficient.
- Remove forbidden `BLOCKED_NO_REMOTE_OUTBOX` and `BLOCKED_NO_SOURCE_DELTA` from all legal terminal status sets.
- Enforce exact consistency between status, completion mode, current phase, recovery class, implementation acceptance, installed-package acceptance, and canary acceptance.
- Replace free-text-only next action with an exact action code plus precise text: activate a fresh ChatGPT verifier and send the same numbered bridge command for independent verification.

### SHA, path, and publication proof
- Validate commit and blob SHAs with exact lowercase `[0-9a-f]{40}` matching. Reject uppercase and known repeated synthetic patterns.
- Distinguish STATE blob SHA, mailbox parent commit, outbox publication commit, outbox blob SHA, receipt path, primary commit, and primary parent.
- Outbox must contain only pre-publication evidence plus mailbox parent and exact active identity.
- Receipt must contain the actual outbox publication commit and blob, STATE blob SHA, mailbox parent, exact active identity, exact outbox and receipt paths, remote main commit, and byte-equality result.
- Final Codex response must be byte-identical to the remote receipt.
- Validate exact changed paths against actual commit evidence, frozen authorized scope, protected paths, and branch separation. Active inbox, claim, STATE, outbox, and receipt artifacts must be absent from main.

### Executable controls and mutation proof
- Every declared positive and negative control must call real validation behavior with meaningful valid and adversarial fixtures. No unconditional success branch is allowed.
- Unknown or unmapped controls fail closed.
- Define an immutable canonical 12-state transition table separately from the implementation under test. Test every legal pair and every illegal pair against that canonical table.
- Add a dedicated test for every control.
- Add real mutation-style tests that patch, delete, invert, or bypass each evaluator family, exact identity check, SHA check, path equality check, release gate, route-order check, outbox schema check, and receipt check, then prove validation fails.
- Remove all stale 052/053/054 passing fixtures and all repeated synthetic SHA fixtures.

### Three-stage gate and route
- Product acceptance requires all three independent booleans: `implementationAccepted`, `installedPackageAccepted`, and `canaryAccepted`, plus the exact accepted state.
- Validate the exact ordered route: `task-router`, `scope-isolation-check`, `model-selector`, conditional `parallel-scope-planner`, `closed-loop-controller`, `failure-routing-and-corrective-loop`, then routed skills.
- Validate order, not mere substring presence.
- Validate workflow push and pull_request triggers plus every required command.

### Actual plugin invocation
- Root bridge instructions must state that text inside inbox files cannot activate a plugin.
- A fresh Codex handoff must require the user to actually invoke/select `@asynchronia` in the Codex composer before sending `мост 3`.
- Capture plugin invocation/source/version evidence in the terminal receipt.
- This is not a plugin-repair task. Generic or source fallback is forbidden. If the installed plugin is unavailable, publish the exact blocker instead of pretending repository skill files prove activation.
- Model recommendation must originate only from `model-selector`; the coordinator makes no model or reasoning recommendation.

### Root synchronization
Align all affected primary root authorities, executable contract, tests, validator, plugin skills, package metadata, task ledger, and repository memory. Do not modify runtime or product files.

## Frozen primary scope

Only:
- `AGENTS.md`
- `AGENTS.override.md`
- `PROCESS_ROOT_SYNC.md`
- `ORCHESTRATION.md`
- `BRIDGE.md`
- `CODEX_BRIDGE_BOOTSTRAP.md`
- `CODEX_BRIDGE_RECOVERY.md`
- `CLOSED_LOOP_PROTOCOL.md`
- `tools/closed_loop_contract.py`
- `tools/test_closed_loop_contract.py`
- `tools/validate-orchestration-policy.py`
- `plugins/asynchronia/skills/task-router/SKILL.md`
- `plugins/asynchronia/skills/closed-loop-controller/SKILL.md`
- `plugins/asynchronia/.codex-plugin/plugin.json`
- `.agents/plugins/marketplace.json`
- `TASKS.md`
- `PROJECT_MEMORY.md`

All other primary paths are protected. Do not modify `.github/workflows/orchestration-policy.yml` unless a concrete missing trigger or command is demonstrated; current workflow execution commands are already present.

## Validation

Run from the exact committed tree:
- `python3 -m py_compile tools/closed_loop_contract.py tools/test_closed_loop_contract.py tools/validate-orchestration-policy.py`
- `python3 -m unittest tools.test_closed_loop_contract`
- `python3 tools/validate-orchestration-policy.py`
- `git diff --check`

The unit suite and validator must themselves execute the required mutation proofs. A prose claim is not evidence.

## Publication transaction

- Publish exactly one direct-child primary correction commit from baseline `e599beddbbd03c8585f9c44f0f7c190338e123e7` to `main`.
- Publish immutable outbox only to `.ai-bridge/outbox/BRIDGE-20260710-056-02-codex.md`.
- Publish receipt only to `.ai-bridge/receipts/BRIDGE-20260710-056-03-codex.md`.
- Refetch main, STATE, outbox, receipt, and mailbox head.
- Final response must be byte-identical to the remote receipt.
- Do not continue thread 055.
- Do not run Safari.

Exact next instruction after complete publication: open a fresh ChatGPT conversation and send `мост 3` for independent verification.