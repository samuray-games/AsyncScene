Use @asynchronia.

# Complete the closed-loop implementation on fresh Slot 3

BRIDGE_PROTOCOL: 3.3
CYCLE_ID: CYCLE-20260709-001
CYCLE_GENERATION: 3
THREAD_ID: BRIDGE-20260709-048
LANE_ID: PROCESS-CLOSED-LOOP-ORCHESTRATION
TASK_ID: TASK-PROCESS-CLOSED-LOOP-IMPLEMENTATION-R2-SLOT3
EXECUTION_EPOCH: CLOSED-LOOP-R2-E1-20260709-1955JST
TASK_NONCE: CLV1-048-R2-CA2A-9F31
BRIDGE_SLOT: 3
PHASE: CORRECTION_REQUIRED
BASELINE: ca2a3f88ac00a7e4fae47459758e7b09099a3f41
COORDINATOR_MEMORY_REV: 2026-07-09-1955-JST
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260709-048-claim-v1-codex.md
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-048-02-codex.md
PUBLICATION_POLICY: .ai-bridge/PUBLICATION_POLICY_CLOSED_LOOP_V1.md
CLOSED_LOOP_PROTOCOL: .ai-bridge/CLOSED_LOOP_PROTOCOL.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
PLUGIN_BOOTSTRAP_FALLBACK: AUTHORIZED
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true
SAFARI_STATUS: N/A_PROCESS_ONLY
SUPERSEDES_THREAD: BRIDGE-20260709-046
SUPERSEDES_EPOCH: CLOSED-LOOP-E1-20260709-1930JST
ORPHANED_INERT_ARTIFACT: .ai-bridge/inbox/BRIDGE-20260709-047-01-chatgpt.md
ORPHAN_REASON: CREATED_CONCURRENTLY_BUT_NEVER_ACTIVATED_BY_STATE
SLOT_2_STATUS: USER_REPORTED_BUSY_DO_NOT_ASSIGN

## Fresh-state facts

Current main is `ca2a3f88ac00a7e4fae47459758e7b09099a3f41` (`Harden bridge policy validator`). It is the immutable baseline and partial process progress.

Thread 046 produced no remote outbox and is superseded before acceptance. The concurrently created 047 inbox is not referenced by current STATE and is therefore inert. Do not execute 046 or 047. Do not reuse their Codex conversations.

This task is a new atomic instance on Slot 3 with a new generation, thread, task, epoch, nonce, claim and outbox. Its Codex conversation expires after its final response.

## Absolute objective

Finish and prove the completed resumable ChatGPT-Codex cycle. Product work and Safari remain suspended until this implementation and a later separate canary are independently accepted.

The final architecture must enforce:

- one atomic epoch per fresh Codex conversation;
- one independent verification per fresh ChatGPT conversation;
- no conversation history as authority;
- immutable cycle/generation/thread/task/epoch/nonce identity;
- inbox and claim verified before STATE activation;
- STATE as the only activation pointer;
- unreferenced artifacts treated as inert orphans;
- memory synchronized and refetched before handoff;
- remote-state fetch and nonce verification before Codex routing;
- complete immutable outbox, remote refetch and byte equality before handoff;
- every correction or recovery becoming a new task instance;
- missing outbox classified as correction, report recovery or publication recovery;
- separate canary automatically created after implementation acceptance;
- product work released only after canary acceptance.

## Mandatory plugin route

Freshly fetch main and mailbox and verify this exact identity before work. Then invoke in order:

1. `task-router`;
2. `scope-isolation-check`;
3. `model-selector`;
4. `parallel-scope-planner`;
5. `closed-loop-controller` from governed source after staging it when needed;
6. `failure-routing-and-corrective-loop`;
7. every additional routed skill.

Use governed `SOURCE_PLUGIN_FALLBACK_BOOTSTRAP` from baseline source plugin 1.0.4 when installed plugin is stale. Build and install exact final Asynchronia plugin version 1.0.5. Ungoverned generic fallback is forbidden.

## Required implementation

Within frozen scope:

- create root `CLOSED_LOOP_PROTOCOL.md` and make it authoritative from root process files;
- create `plugins/asynchronia/skills/closed-loop-controller/SKILL.md`;
- bump plugin manifest to 1.0.5;
- create `tools/closed_loop_contract.py` as the shared machine-readable state/report/recovery contract;
- create `tools/test_closed_loop_contract.py` with deterministic positive and negative controls;
- make `tools/validate-orchestration-policy.py` import or invoke the shared contract;
- update workflow coverage dynamically for every process authority and plugin skill;
- update task router, scope, parallel, model, pipeline, evidence and failure skills where required;
- update `TASKS.md` and `PROJECT_MEMORY.md`;
- remove every source and installed `runtime-safety-gate` skill, alias, route, index and approval-only equivalent;
- install and prove user-level Asynchronia 1.0.5 while preserving older versions and unrelated bytes.

## Legal task states

Only: CLOSED, PREPARING, READY_FOR_CODEX, EXECUTING, PRIMARY_PUBLISHED, OUTBOX_PUBLISHING, AWAITING_CHATGPT_VERIFICATION, ACCEPTED, CORRECTION_REQUIRED, RECOVERY_REQUIRED, BLOCKED_EXTERNAL, SUPERSEDED.

Illegal transitions fail closed. Missing referenced artifacts require recovery. `MEMORY_SYNC_PENDING` forbids execution.

## Automatic repair

Before terminal output:

- stale state: refetch and restart identity resolution;
- main/mailbox non-fast-forward: refetch and recreate clean worktrees;
- moved main: safely reapply only when writes and stable reads remain disjoint, then rerun all checks;
- overlap or changed dependency: publish complete blocker evidence;
- dirty worktree: use a fresh task-owned worktree and preserve unrelated changes;
- stale plugin during repair: validate pinned source and install via temporary sibling plus atomic rename when supported;
- validation failure inside scope: diagnose and repair, at most three distinct passes per root cause;
- transient connector/fetch/push error: at most three retries with identity revalidation;
- outbox absence or byte mismatch: preserve immutable response bytes and retry from latest mailbox up to three clean attempts.

Every terminal blocker publishes a complete expected outbox when mailbox publication works. `BLOCKED_OUTBOX_PUBLICATION` gives no ChatGPT handoff.

## Deterministic controls

The exact final committed tree must positively prove legal coordinator, Codex, verifier, correction, report-recovery, publication-recovery and canary transitions.

Negative controls must cover memory unavailable/stale/pending; STATE split-brain; missing referenced artifacts; inert orphan artifacts; duplicate identity/nonce; wrong slot; stale thread/epoch/nonce; old-conversation reuse; claim collision; missing/invalid/illegally bootstrapped plugin; missing required skill; runtime-gate reintroduction; moved-main safe reapply and collision; dirty unrelated worktree; scope overflow; protected path change; non-reproducible validator; false no-delta; outside-scope test failure; primary/mailbox non-fast-forward; empty/partial/foreign/historical/wrong-nonce outbox; remote outbox absence; byte mismatch; missing retry/refetch proof; missing commit/wrong parent/lost ancestry/main advancement; duplicate bridge after closure; missing required Safari evidence; product work before canary; external auth/permission/service outage; correction/report/publication recovery choice; memory identity mismatch; and partial primary publication.

## Exact primary scope

Only:

- `CLOSED_LOOP_PROTOCOL.md`
- `AGENTS.md`
- `AGENTS.override.md`
- `PROCESS_ROOT_SYNC.md`
- `ORCHESTRATION.md`
- `BRIDGE.md`
- `CODEX_BRIDGE_BOOTSTRAP.md`
- `CODEX_BRIDGE_RECOVERY.md`
- `GIT_PUSH.md`
- `STAGE6_PARALLEL_EXECUTION_PLAN.md`
- `PROJECT_MEMORY.md`
- `TASKS.md`
- `.github/workflows/orchestration-policy.yml`
- `tools/validate-orchestration-policy.py`
- `tools/closed_loop_contract.py`
- `tools/test_closed_loop_contract.py`
- `plugins/asynchronia/.codex-plugin/plugin.json`
- `plugins/asynchronia/skills/closed-loop-controller/SKILL.md`
- `plugins/asynchronia/skills/task-router/SKILL.md`
- `plugins/asynchronia/skills/scope-isolation-check/SKILL.md`
- `plugins/asynchronia/skills/parallel-scope-planner/SKILL.md`
- `plugins/asynchronia/skills/model-selector/SKILL.md`
- `plugins/asynchronia/skills/acceptance-pipeline-controller/SKILL.md`
- `plugins/asynchronia/skills/pipeline-state-and-resume-contract/SKILL.md`
- `plugins/asynchronia/skills/evidence-bundle-and-artifact-identity/SKILL.md`
- `plugins/asynchronia/skills/failure-routing-and-corrective-loop/SKILL.md`
- `plugins/asynchronia/skills/runtime-safety-gate/SKILL.md` if present, deletion only.

All product/runtime paths are protected. Managed user-level instructions and versioned plugin installation may change only with backup, permission preservation and unrelated-byte preservation.

## Final validation and publication

Validate on the exact final committed tree: final commit and actual parent, clean worktree, Python compile, full orchestration validator, closed-loop tests, every positive/negative control, dynamic skill/workflow equality, plugin 1.0.5 and frontmatter, source/installed runtime-gate absence, installed path/inventory/checksum equality, plugin positive and unavailable negative smokes, exact changed paths, protected product/runtime blob equality and `git diff --check`.

Publish one direct-child exact-scope primary commit from baseline `ca2a3f88ac00a7e4fae47459758e7b09099a3f41`, unless a later disjoint main is safely adopted under policy.

Publish the complete immutable response only to `.ai-bridge/outbox/BRIDGE-20260709-048-02-codex.md`, refetch it and prove schema and byte equality before showing the same response.

The exact next instruction is: open a fresh ChatGPT conversation and send `мост 3`.

Do not run Safari. Independent acceptance must create a separate fresh-thread canary before the loop can be declared complete.
