Use @asynchronia runtime-safety-gate.

# Bridge Slot 1 - Wave V-A3 truthful derivation correction

BRIDGE_PROTOCOL: 3.1
ORCHESTRATION_VERSION: 3.1
BRIDGE_SLOT: 1
THREAD_ID: BRIDGE-20260708-040
LANE_ID: S6-V5A3-BOOMER-STATIC-AUDIT
TASK_ID: TASK-S6-PAR-V5A3
PHASE: CORRECTION_REQUIRED
PUBLICATION_MODE: CODEX_AUTO_PULL_PUSH
REMOTE_MAIN_BASELINE: 57dae3f7942ba60996604c39115dad0cb4fa2238
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260708-040-claim-codex.md
REJECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260708-040-02-codex.md
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260708-040-04-codex.md
RECOMMENDED_MODEL: GPT-5.4-Mini / Medium
CONFIRMATION: MATCHING_NUMBERED_BRIDGE_COMMAND
RUNTIME_CLASSIFICATION: STATIC_AUDIT_ONLY
SAFARI_RUNTIME_STATUS: PENDING_USER

## Coordinator verdict

`BRIDGE-20260708-040-02-codex.md` is rejected.

Two independent defects remain:

1. The outbox reports nonexistent primary SHA `57dae3fad0f7b1be8ef14e0aa66e90d6d0bb3e8b`, while remote `main` is `57dae3f7942ba60996604c39115dad0cb4fa2238`.
2. The generator still embeds observed current text for live rows that the frozen contract requires it to derive from current production registries and resolvers.

The current primary commit is otherwise one direct child of the previous baseline and changes only the authorized three paths. Do not revert it and do not touch production/runtime files.

## Exact write scope

Only:

1. `tools/generate-boomer-step4-4-economy-conflict-audit.py`
2. `UI_PROFILE_BOOMER_STEP_4_4_ECONOMY_CONFLICT_TERMINOLOGY_AUDIT.md`
3. `docs/UI_PROFILE_BOOMER_STEP_4_4_ECONOMY_CONFLICT_TERMINOLOGY_AUDIT.md`

Any other changed primary path is automatic FAIL.

## Required correction

Preserve two independent values for every live row:

- `currentText` from current production/profile source;
- `acceptedBoomerTarget` from frozen authority.

The accepted-target overlay may affect only `acceptedBoomerTarget` and authority. It must never supply `currentText`.

Correct at least these proven derivation defects:

### A. Battle fallback rows

For `AUD_0153`, `AUD_0154`, and `AUD_0155`, derive current Boomer text by semantic key directly from `BATTLE_FALLBACK_TEXTS.boomer`.

Do not embed these observed current values in `build_manual_specs`:

- `Решает голосование`
- `Выйти`
- `Вызов отклонён`

The target authority remains independent. A changed current fallback must change the row's `currentText` and produce a computed mismatch or structural failure rather than retaining PASS.

### B. Report submit states

For `AUD_0145` and `AUD_0146`, derive current values from the parsed current `DM_REPORT_SUBMIT_LABELS` registry.

Do not embed `Проверка...` or `Повторите позже` as observed current values in the row specifications.

### C. Escape payment surface

For `AUD_0125`, derive current text through the current System/profile route for `notifications.escapePaid` or the exact current production resolver that owns the surface.

Do not embed `Выйти за 1 💰.` as observed current text.

### D. Data.SYS result templates

For `AUD_0132` and `AUD_0133`, extract or structurally prove the current `Data.SYS.npcBattleEndWin` and `Data.SYS.npcBattleEndDraw` templates from current source.

Do not use accepted targets as current values. If extraction cannot be proven, record a structural failure.

### E. Preserve existing truthful routes

Keep current extraction from:

- `SYSTEM_PROFILE_TEXT_COPY.boomer` plus `SYSTEM_PROFILE_TEXT_ROUTE_MAP`;
- `DM_ACTION_LABEL_BOOMER`;
- `DM_REPORT_SURFACE_COPY.boomer`;
- `TOPBAR_STAT_TITLES.boomer`;
- current Data/System dictionaries and resolvers.

Keep `make_row` authoritative for classification and verdict. Do not hardcode PASS, final counts, status, or empty failure arrays.

## Frozen accepted targets

The exact `stage6_accepted_decision:<AUD_ID>` overlay from the original inbox remains unchanged. Do not revise any copy.

## Required derived result

Only if actual independent derivation supports it:

- `auditStatus: STATIC_PASS / READY_FOR_RUNTIME_SMOKE`
- `auditedRowCount: 147`
- `passRowCount: 147`
- `failRowCount: 0`
- `structuralFailureCount: 0`
- `nonLiveEvidenceCount: 8`
- all live classifications `allowed_exact`
- root/docs mirrors byte-identical

Do not force these values.

## Required checks

Run and report:

1. `python3 -B tools/generate-boomer-step4-4-economy-conflict-audit.py .`
2. `python3 -B tools/validate-boomer-step4-4-economy-conflict-audit.py .`
3. `python3 -m py_compile tools/generate-boomer-step4-4-economy-conflict-audit.py`
4. root/docs byte comparison
5. deterministic second generation with zero diff
6. exact changed-path check
7. `git diff --check`
8. protected production blob verification for every source file read by the generator
9. temporary negative-control proof, without committed production changes:
   - change one `BATTLE_FALLBACK_TEXTS.boomer` value in a disposable copy and prove the corresponding current row changes or fails;
   - change `DM_REPORT_SUBMIT_LABELS.pending` in a disposable copy and prove `AUD_0145` changes or fails;
   - change one routed `SYSTEM_PROFILE_TEXT_COPY.boomer` value in a disposable copy and prove the corresponding current row changes or fails.

Restore every temporary mutation before commit.

## Automatic publication

On the next `мост 1`:

1. Fetch current remote refs.
2. Use a clean task-owned worktree from exact baseline `57dae3f7942ba60996604c39115dad0cb4fa2238`.
3. Apply only this correction.
4. Run all checks.
5. Create one atomic direct-child primary commit changing exactly the three authorized paths.
6. Fast-forward push `main` and verify exact remote SHA.
7. Create and fast-forward push only `.ai-bridge/outbox/BRIDGE-20260708-040-04-codex.md` as a direct child of the latest mailbox head.
8. Report the exact verified primary SHA without transcription error.

Do not repeat model preflight. Do not request `CONTINUE`, `APPROVE`, separate `пул`, separate `пуш`, payload copy, merge, rebase, reset, stash, clean, cherry-pick, amend or force-push.

Exact next action after publication: return to ChatGPT and send `мост 1`.