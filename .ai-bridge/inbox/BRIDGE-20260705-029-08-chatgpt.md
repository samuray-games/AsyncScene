Use @asynchronia runtime-safety-gate.

# BRIDGE-20260705-029 publication fallback correction

BRIDGE_PROTOCOL: 2.4
BRIDGE_SLOT: 3
THREAD_ID: BRIDGE-20260705-029
LANE_ID: S6-A1-ALPHA-EVIDENCE
TASK_ID: TASK-S6-PAR-A1
PHASE: MAILBOX_AUTH_FALLBACK
AUTHORIZED_PRIMARY_BASELINE: a8a7e3acacbe2ba7fe6a387cb647d09d7d701a4d
PRIMARY_WRITE_SCOPE: NONE
RUNTIME_CLASSIFICATION: READ_ONLY
CLAIM_PATH: .ai-bridge/claims/BRIDGE-20260705-029-claim-codex.md
CLAIM_TOKEN: s6a1-recovery-20260707-029-a
EXPECTED_OUTBOX_PATH: .ai-bridge/outbox/BRIDGE-20260705-029-02-codex.md

## Coordinator decision

The reported Git credential failure is not a valid slot blocker under Bridge Protocol 2.4. The remote outbox is absent, and the prior response did not include the complete intended immutable outbox payload. Therefore the slot is not accepted yet.

The local-only outbox commit `01a10e532a8ff5250ebecb63cb4132c65b21abcd` is audit evidence only. Do not push, reuse, amend, cherry-pick or treat it as authoritative publication.

## Required same-thread action

Use the existing logical Codex thread and the existing recovery claim. Do not create another claim. Do not request GitHub credentials from the user.

Return the complete intended UTF-8 contents of:

`.ai-bridge/outbox/BRIDGE-20260705-029-02-codex.md`

The payload must be exact and publication-ready, not a summary. It must preserve the completed read-only Alpha evidence package and include:

- slot, thread, lane, task, existing claim path and token;
- authorized primary baseline;
- no-primary-write declaration;
- actual model recorded as `USER_SELECTED_UNVERIFIED` unless externally proven;
- PASS surfaces: attack, rematch, P2P give, P2P request, cop flow;
- FAIL surfaces: escape, teach, social actions;
- RUNTIME_REQUIRED surfaces: vote, report;
- for every surface: exact visible copy, action ID, handler, emitter, source file, deployed mirror, placeholders, protected tokens and runtime evidence requirement;
- current profile resolver path and any wrong-profile result;
- static checks, missing runtime coverage and stale Alpha documentation;
- no replacement Alpha copy;
- Safari status `N/A - read-only evidence lane`.

If the detached worktree file still exists, print its complete contents exactly. If it no longer exists, reconstruct the same evidence package from the already-inspected current baseline without changing primary files.

Return one fenced block containing only the complete intended outbox payload. Nothing may follow the block.

Afterward, return to ChatGPT and write `мост 3`.