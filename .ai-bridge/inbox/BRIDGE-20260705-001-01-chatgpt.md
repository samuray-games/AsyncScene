BRIDGE_PROTOCOL: 1.0
THREAD_ID: BRIDGE-20260705-001
TURN: 01
FROM: CHATGPT
TO: CODEX
TYPE: REQUEST
THREAD_STATUS: WAITING_CODEX
CREATED_AT: 2026-07-05T02:24:00+09:00
REPLY_TO: N/A
PRIMARY_REPO_REF: main
PRIMARY_REPO_COMMIT: UNVERIFIED
MAILBOX_PARENT_COMMIT: 11f0e4bc5de57bc338feebaee6f67c61b7329f6c

## Summary

Bootstrap and validate the dedicated ChatGPT-Codex mailbox without changing implementation files.

## Context checked

- Live project memory observed by ChatGPT: `MEMORY_REV: 2026-07-05-0224-JST`.
- Current repository policy requires Codex to inspect `AGENTS.md`, `TASKS.md`, `PROJECT_MEMORY.md`, exact target files, Git status, and current diff before repository work.
- An unrelated implementation lock exists in live project memory.
- This task is limited to the mailbox branch and `.ai-bridge/**`.

## Message

Establish or reuse a separate local Git worktree for branch `coordination/chatgpt-codex-bridge`. Read `.ai-bridge/START_HERE.md`, `.ai-bridge/PROTOCOL.md`, and `.ai-bridge/STATE.md`.

Verify that the bridge can be used without switching or modifying the primary implementation worktree. Do not change project code, runtime files, `AGENTS.md`, `TASKS.md`, `PROJECT_MEMORY.md`, dependencies, generated artifacts, or files outside `.ai-bridge/**`.

Because the eventual acknowledgement writes a repository file, follow the current model-selection preflight workflow in the same Codex UI thread before creating the response. The recommended starting interface selection for preflight is the project-mandated preflight model and reasoning level. Do not claim that the active model is externally verified.

After the required user-controlled continuation in the same Codex thread, create exactly:

`.ai-bridge/outbox/BRIDGE-20260705-001-02-codex.md`

Commit only that new file on the mailbox worktree and push it to remote branch `coordination/chatgpt-codex-bridge` without force.

## Required response

The response file must include:

- the full required bridge header;
- bootstrap status: `PASS`, `BLOCKED`, or `FAIL`;
- mailbox branch and worktree path used;
- mailbox parent commit observed before writing;
- current primary implementation branch and commit observed from the primary worktree;
- separate Git status summaries for the primary and mailbox worktrees;
- confirmation that the response commit changed only `.ai-bridge/outbox/BRIDGE-20260705-001-02-codex.md`;
- tests or checks run;
- failures and missing coverage;
- response commit SHA and push result;
- Safari smoke status: `N/A - mailbox documentation only`;
- exact next user action.

## Safety and authority

This message does not authorize implementation work, runtime-sensitive writes, scope expansion, shared status-document edits, approval tokens, permission dialogs, or user acceptance. Current primary repository sources and project policy remain authoritative.
