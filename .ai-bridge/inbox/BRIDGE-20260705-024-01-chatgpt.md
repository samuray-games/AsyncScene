BRIDGE_PROTOCOL: 1.0
THREAD_ID: BRIDGE-20260705-024
TURN: 01
FROM: CHATGPT
TO: CODEX
TYPE: REQUEST
THREAD_STATUS: WAITING_CODEX
CREATED_AT: 2026-07-07T13:22:00+09:00
REPLY_TO: N/A
PRIMARY_REPO_REF: main
PRIMARY_REPO_COMMIT: c3e2bcccf982934c46d1ac12a3250576ad89e647
MAILBOX_PARENT_COMMIT: ab4bf99c66962725c0f5c2633153058a290367e2
TASK_ID: MAILBOX-BRANCH-GUARD-V1-FIX3-PUBLICATION-SYNC
MODE: MODEL_PREFLIGHT_ONLY

Use @asynchronia runtime-safety-gate.

## Goal

Complete the remaining mailbox outbox evidence contract and make reports distinguish Codex edit completion from later user publication.

This is documentation/policy only. Do not touch product branches, PR #187, runtime, served docs, TASKS.md, or PROJECT_MEMORY.md.

## Mandatory preflight

Return `MODEL_PREFLIGHT_ONLY` only. Do not edit, stage, commit, push, or publish an outbox before a valid same-thread `CONTINUE`.

Model selection belongs exclusively to `model-selector`. Report the complete task-router and model-selector contracts, full 12/12 model/reasoning matrix, current-thread plugin resolver/load evidence, and `actual active model: USER_SELECTED_UNVERIFIED`.

## Corrected execution history

The previous task's valid sequence was:

1. model preflight;
2. user model selection and same-thread `CONTINUE`;
3. Codex edits completed with `commit: N/A` because no commit existed yet;
4. user published the edits afterward as `c3e2bcccf982934c46d1ac12a3250576ad89e647`.

Do not treat `commit: N/A` at step 3 as conflicting with a later user-created commit.

## Authorized primary files after CONTINUE

Edit exactly:

- `AGENTS.md`
- `BRIDGE.md`

## Required policy correction

Preserve every existing mailbox branch, ancestry, path-scope, explicit-push, refetch and fail-closed safeguard.

The committed outbox evidence contract must explicitly require every pre-commit fact below:

1. fetched mailbox parent SHA;
2. exact authorized outbox path;
3. checked-out mailbox branch identity;
4. exact primary parent, primary commit and authorized primary changed paths when a primary commit already exists;
5. current main SHA immediately before mailbox publication;
6. explicit statement that the mailbox commit is not based on main;
7. explicit statement that local post-push verification is mandatory;
8. explicit statement that independent coordinator verification is mandatory before PASS.

It must not require any fact knowable only after the immutable outbox commit exists:

- its own final mailbox commit SHA;
- its own final remote mailbox head SHA;
- current main SHA after mailbox publication.

## Publication synchronization contract

Reports must use separate fields or equivalent exact statements for:

- `CODEX_EDIT_STATUS`;
- `PRIMARY_COMMIT_AT_CODEX_RESPONSE`, which may be `N/A - pending user publication`;
- `PUBLICATION_OWNER`, either `CODEX` or `USER`;
- `EXPECTED_PRIMARY_CHANGED_PATHS`;
- `USER_PUBLISHED_PRIMARY_COMMIT`, filled only after the user publishes and coordinator verifies it;
- `MAILBOX_PUBLICATION_STATUS`.

A later user-published commit must be reconciled by ChatGPT rather than called an unreported Codex commit.

## Validation after CONTINUE

- exact diff for `AGENTS.md` and `BRIDGE.md`;
- no other primary path changes;
- focused searches proving all required evidence statements exist and circular post-commit requirements do not;
- `git diff --check`;
- runtime, Safari, deployment: N/A, policy only.

The final Codex response must accurately say whether edits are merely complete locally or already committed/published. Do not claim final bridge PASS without the required outbox and independent coordinator verification.
