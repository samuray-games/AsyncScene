# BRIDGE-20260705-029 Protocol 2.3 update

BRIDGE_PROTOCOL: 2.3
BRIDGE_SLOT: 3
USER_COMMAND: мост 3
LANE_ID: S6-A1-ALPHA-EVIDENCE
TASK_ID: TASK-S6-PAR-A1
PHASE: MODEL_PREFLIGHT_ONLY
AUTHORIZED_PRIMARY_BASELINE: 5024b4241fd8f1afd92078939aa638e76666ebd1
PRIMARY_WRITE_SCOPE: NONE
RUNTIME_CLASSIFICATION: READ_ONLY
CLAIM_PATH: .ai-bridge/claims/BRIDGE-20260705-029-claim-codex.md
OUTBOX_PATH: .ai-bridge/outbox/BRIDGE-20260705-029-02-codex.md

The earlier stale-worktree `BLOCKED_MAILBOX_BRANCH_GUARD` response is invalidated. An existing stale mailbox worktree is not a blocker under Protocol 2.3.

For a fresh thread:

1. resolve Asynchronia skills from installed package cache or repository fallback;
2. verify Slot 3 is still open and unclaimed;
3. create the immutable claim in a fresh detached worktree at the fetched mailbox parent, leaving the stale existing mailbox worktree untouched;
4. push the direct-child claim commit without force and verify the remote head;
5. return the compact preflight with actual claim token/path, skill source, runtime verdict, collision verdict, evaluated pair count 12/12, recommendation, frontier reasons, scope, dependencies, and blockers;
6. end with `CONTINUE` only when not blocked.

After same-thread `CONTINUE`, execute the read-only Alpha evidence task and publish only the expected outbox. Final next action: return to ChatGPT and write `мост 3`.
