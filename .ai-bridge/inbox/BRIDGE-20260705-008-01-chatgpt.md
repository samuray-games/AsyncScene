BRIDGE_PROTOCOL: 1.0
THREAD_ID: BRIDGE-20260705-008
TURN: 01
FROM: CHATGPT
TO: CODEX
TYPE: REQUEST
THREAD_STATUS: WAITING_CODEX
CREATED_AT: 2026-07-05T09:12:00+09:00
REPLY_TO: N/A
PRIMARY_REPO_REF: main
PRIMARY_REPO_COMMIT: e7eeb5113d25233d0314e9fa30a4eefbc2e1ca26
MAILBOX_PARENT_COMMIT: 769e4b9d934197dbc2008d98bdf75d54c032d06e
TASK_ID: BRIDGE-BOOTSTRAP-V1
MODE: MODEL_PREFLIGHT_ONLY

## Summary

Install a persistent user-level Codex instruction for the exact command `проверь мост`. This is a one-time local Codex configuration task. It supersedes the current Stage 6 audit thread temporarily because repository-level AGENTS.md cannot fix a stale local checkout before the command is interpreted.

## Context checked

- Official Codex behavior loads global user instructions from `$CODEX_HOME/AGENTS.override.md` first, otherwise `$CODEX_HOME/AGENTS.md`.
- Current primary main expected: `e7eeb5113d25233d0314e9fa30a4eefbc2e1ca26`.
- Bootstrap contract: `CODEX_BRIDGE_BOOTSTRAP.md` on `origin/main`.
- Product/runtime implementation is out of scope.
- Previous Stage 6 thread `BRIDGE-20260705-007` is superseded while this bootstrap is active.

## Message

First return `MODEL_PREFLIGHT_ONLY`. Do not modify user configuration before same-thread `CONTINUE`.

During preflight:
1. verify installed Codex version if available;
2. determine `${CODEX_HOME:-$HOME/.codex}` without exposing secrets;
3. inspect only whether `AGENTS.override.md` and `AGENTS.md` exist and which is active;
4. fetch `origin/main` and read `CODEX_BRIDGE_BOOTSTRAP.md` with `git show`;
5. recommend the cheapest reliable available model and reasoning level;
6. list the exact selected future write path, backup path pattern, expected native permission prompt, and validation checks;
7. confirm runtime safety gate is `N/A - Codex user instructions only`;
8. end with a standalone `CONTINUE` block.

After same-thread `CONTINUE`:
1. re-fetch `origin/main` and verify the bootstrap file is still at commit `e7eeb5113d25233d0314e9fa30a4eefbc2e1ca26` or report a newer primary source;
2. execute `CODEX_BRIDGE_BOOTSTRAP.md` exactly;
3. preserve all pre-existing global instructions outside the managed block;
4. create a timestamped backup if the target file existed;
5. install or replace exactly one `ASYNCHRONIA_BRIDGE_ALIAS_V1` managed block;
6. validate marker counts, managed contents, and unchanged surrounding content;
7. create exactly `.ai-bridge/outbox/BRIDGE-20260705-008-02-codex.md` on the mailbox branch;
8. do not run Stage 6 audit, mirror audit, runtime smoke, or product implementation in this task.

## Allowed write scope

- active global Codex user instruction file under `${CODEX_HOME:-$HOME/.codex}`;
- one timestamped backup beside it if needed;
- `.ai-bridge/outbox/BRIDGE-20260705-008-02-codex.md` on the mailbox branch.

## Forbidden scope

- every product/runtime/UI/logic file;
- repository `AGENTS.md`, `BRIDGE.md`, `TASKS.md`, `PROJECT_MEMORY.md`;
- locks, dependencies, approval policy, sandbox policy, broad command allowlists;
- any closed or superseded bridge thread;
- Stage 6 audit execution.

## Runtime gate

`N/A - Codex user instruction configuration only.` Native permission to write under Codex home may still appear and remains user-controlled.

## Required preflight response

1. status;
2. recommended model and reasoning;
3. verified bootstrap source and expected main commit;
4. Codex home and active instruction-file selection logic;
5. planned exact write and backup scope;
6. preservation strategy;
7. planned validation;
8. expected native permission prompt;
9. blockers;
10. exact next user action.

End with exactly:

```text
CONTINUE
```

## Required final response and outbox

After CONTINUE, report:
- status;
- selected model/reasoning as `USER_SELECTED_UNVERIFIED` unless externally proven;
- Codex home;
- selected instruction path;
- created or updated;
- backup path;
- marker counts;
- preservation validation;
- changed paths;
- outbox commit and push result;
- runtime gate `N/A`;
- exact next user action: open a new Codex thread in AsyncScene and write only `проверь мост`.

## Safety and authority

Do not improvise a mirror audit. Do not treat repository AGENTS.md as sufficient. The purpose of this task is to install the alias into the global Codex instruction layer that is loaded for every new thread.