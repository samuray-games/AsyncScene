Use @asynchronia.

# BRIDGE-20260711-086 - Mandatory automatic plugin preflight and active installation sync

ISSUE: 216
CYCLE: CYCLE-20260711-003
GENERATION: 7
SLOT: 2
THREAD_ID: BRIDGE-20260711-086
LANE_ID: ASYNCHRONIA-PLUGIN-PREFLIGHT-ROOT-SYNC
TASK_ID: TASK-ASYNCHRONIA-PLUGIN-AUTO-PREFLIGHT
EXECUTION_EPOCH: ASYNCHRONIA-PLUGIN-PREFLIGHT-R1-20260711-1120JST
TASK_NONCE: ASPF-086-1120
PHASE: MODEL_PREFLIGHT_REQUIRED
COORDINATOR_MEMORY_REVISION: 2026-07-11-1120-JST
AUTHORIZED_PRIMARY_BASELINE: 93995ab4e599ebcb8881a6dc391e2a8ffdd07555
SUPERSEDES_THREAD: BRIDGE-20260711-085
PREVIOUS_ATTEMPT_STATUS: REJECTED_FALSE_RUNTIME_AND_MIRROR_EVIDENCE
CLAIM_PATH: .ai-bridge/claims/BRIDGE-20260711-086-claim-v1-codex.md
EXPECTED_OUTBOX_PATH: .ai-bridge/outbox/BRIDGE-20260711-086-02-codex.md
EXPECTED_RECEIPT_PATH: .ai-bridge/receipts/BRIDGE-20260711-086-03-codex.md
MODEL_PREFLIGHT_POLICY: .ai-bridge/MODEL_PREFLIGHT_POLICY.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
PLUGIN_IDENTIFIER: asynchronia
PLUGIN_AUTO_START_REQUIRED: true
ACTIVE_INSTALLED_PLUGIN_SYNC_REQUIRED: true
PLUGIN_TOOL_TELEMETRY_REQUIRED: false
MODEL_RECOMMENDATION_REQUIRED: true
MODEL_RECOMMENDATION_PAUSE_REQUIRED: true
SAME_THREAD_CONTINUE_REQUIRED: true
CONTINUE_TOKEN: CONTINUE
ACTUAL_MODEL_STATUS: USER_SELECTED_UNVERIFIED
SAFARI_STATUS: N/A_PROCESS_AND_PLUGIN_ONLY
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true

## Why Bridge 085 is rejected

Bridge 085 published primary commit `93995ab4e599ebcb8881a6dc391e2a8ffdd07555`, outbox, and receipt, but its acceptance claims are false.

Independent verification found:

1. `AsyncScene/Web/dev/dev-checks.js` changed from baseline blob `1e50fc811572c43eb65aabd15fe0947216a57440` to `8e1001ac617dde83d0fbc1f48e34b40383eed454`.
2. `docs/dev/dev-checks.js` remains baseline blob `1e50fc811572c43eb65aabd15fe0947216a57440`.
3. `docs/index.html` now loads `dev/dev-checks.js?v=step4_4b_boomer_runtime_aggregate_fix3_20260711a`, while the unchanged docs dev-check script still contains the prior aggregate identity and expected cache-bust.
4. Therefore the claimed source/docs semantic mirror PASS and target-state-all-four-paths PASS are false.
5. The final primary commit changed only Fix3 identifiers and two index cache-busts. It did not add restoration for battle state, moneyLog, persistence, or localStorage.
6. The predecessor code snapshots those surfaces but restores only profile, text mode, points, REP, and Influence. Therefore the receipt's full restoration PASS is unsupported.
7. `tools/validate-boomer-step4-4-economy-conflict-audit.py` validates deterministic terminology audit artifacts and clean working-tree scope. It does not execute or prove runtime restoration, adversarial evaluator behavior, or deployed mirror correctness.

Bridge 085 is not authorized for Safari. Runtime correction remains pending after this plugin-policy lane completes.

## Direct user requirement

The user requires that Asynchronia start automatically for every Asynchronia/Codex task.

For every implementation task, `model-selector` must run before implementation, show a competent model and reasoning recommendation, pause, end with a standalone fenced code block containing exactly `CONTINUE`, and perform no implementation until the user sends exact `CONTINUE` in the same Codex conversation.

This direct user requirement supersedes every repository or mailbox statement that:

- model recommendation is informational or non-blocking;
- no preflight is required;
- no continuation token is allowed;
- execution begins on the first numbered command;
- plugin invocation or skill resolution is optional for Asynchronia work.

Read `.ai-bridge/MODEL_PREFLIGHT_POLICY.md` and follow it exactly.

## Mandatory phase 1 - recommendation and pause

On the first `мост 2` for this thread:

1. Freshly fetch main and mailbox and read current authority, STATE, this inbox, and the claim.
2. Perform read-only scope discovery only.
3. Apply `task-router`, `scope-isolation-check`, and `model-selector` from the active installed Asynchronia plugin; use repository fallback only if the installed skill source cannot be read.
4. Print the visible model recommendation required by the model-selector contract.
5. Do not edit any file, create any commit, push any ref, alter the installed plugin, or publish outbox/receipt.
6. End the response with a standalone fenced code block containing exactly:

```text
CONTINUE
```

Nothing may appear after that fenced block.

## Resume boundary

Implementation begins only after the user sends exact `CONTINUE` in the same Codex conversation.

On resume, refetch both remotes and verify unchanged thread, task, baseline, claim, and frozen scope. If any changed, rerun recommendation and pause again.

A fresh Codex conversation cannot consume the prior `CONTINUE` authorization.

## Atomic objective after CONTINUE

Synchronize the direct user requirement across all affected primary repository authorities and plugin skills, bump package identity consistently, validate the contract with positive and adversarial fixtures, publish one direct-child primary commit, synchronize the active installed plugin and user-level bridge block, then publish immutable outbox and receipt evidence.

Do not touch runtime game files in this lane.

## Frozen primary write scope

Exactly these primary repository paths may change:

- AGENTS.override.md
- AGENTS.md
- ORCHESTRATION.md
- BRIDGE.md
- CODEX_BRIDGE_BOOTSTRAP.md
- CODEX_BRIDGE_RECOVERY.md
- plugins/asynchronia/.codex-plugin/plugin.json
- .agents/plugins/marketplace.json
- plugins/asynchronia/skills/task-router/SKILL.md
- plugins/asynchronia/skills/model-selector/SKILL.md
- plugins/asynchronia/skills/acceptance-pipeline-controller/SKILL.md
- plugins/asynchronia/skills/pipeline-state-and-resume-contract/SKILL.md
- tools/validate-asynchronia-model-preflight.py

No other primary path may change.

## Required repository behavior

The final repository must establish all of these rules consistently:

1. Every Asynchronia/Codex task automatically starts the Asynchronia skill route without requiring the user to type a plugin mention.
2. Read-only scope discovery may occur before recommendation, but no implementation mutation may occur.
3. `model-selector` is mandatory for every file-changing task.
4. The selector recommendation is a blocking user gate.
5. The recommendation response ends with a standalone fenced code block containing exactly `CONTINUE`.
6. No text appears after that block.
7. Exact same-thread `CONTINUE` is the only resume authorization.
8. Wrong-thread, stale-scope, changed-baseline, malformed-token, quoted-token, or longer-message cases do not resume.
9. A changed scope requires a fresh recommendation and a new pause.
10. Blocked responses never include `CONTINUE`.
11. After valid resume, Codex freshly refetches authority before implementation.
12. Actual model remains `USER_SELECTED_UNVERIFIED` unless independently proven.
13. The model recommendation compares adjacent plausible pairs and gives a concrete rejection failure mode for the next cheaper candidate.
14. The installed plugin and repository package must have matching content identity.

Remove or replace every conflicting authoritative statement in frozen scope, including current phrases such as:

- `MODEL_PREFLIGHT_GATE: RETIRED` where applicable to the active bridge policy model;
- `no preflight or separate bridge token is required`;
- `no preflight or continuation token is allowed`;
- `execution starts immediately on the matching numbered command`;
- `model selection ... is not a required approval stop`;
- `must not become an execution prerequisite, pause, or resume token`;
- `not a separate approval gate`;
- `Do not emit ... CONTINUE`;
- `must not be represented as a blocking preflight gate`.

## Package identity

Bump the Asynchronia plugin from `1.0.5` to `1.0.6` unless fresh inspection proves a newer consistent version already exists.

The plugin manifest and marketplace declaration must match exactly.

The manifest description, prompts, and capabilities must make automatic Asynchronia routing and mandatory model preflight discoverable without claiming a callable tool surface.

## Validator

Create `tools/validate-asynchronia-model-preflight.py` as a task-specific validator.

It must fail closed and include positive and adversarial fixtures proving at least:

- initial Asynchronia task -> recommendation phase only;
- no primary or installed-plugin mutation before `CONTINUE`;
- exact same-thread `CONTINUE` -> resume allowed;
- wrong thread -> rejected;
- wrong token -> rejected;
- token embedded in a longer message -> rejected;
- scope or baseline change -> recommendation stale and pause repeated;
- blocked response -> no token;
- all frozen authoritative files contain the required current semantics;
- no frozen authoritative file retains a conflicting non-blocking/no-preflight rule;
- manifest and marketplace versions match;
- required skill route and recommendation output contract are present.

The validator must not merely search for one magic phrase. It must parse a declared contract table or fixtures and prove transition behavior.

## Active installation and user-level bridge sync

After the primary commit is pushed and refetched:

1. Determine `${CODEX_HOME:-$HOME/.codex}`.
2. Discover the active installed Asynchronia plugin source/cache and active user-level instruction file.
3. Back up only the exact user-level file or installed package paths that the current bootstrap contract authorizes.
4. Synchronize the active installed plugin to repository version `1.0.6` using the supported local installation mechanism.
5. Run the updated bootstrap to install the new managed bridge block.
6. Verify installed manifest version, relevant skill hashes, and managed-block identity match the committed repository.
7. Preserve unrelated user-level configuration byte-for-byte.

If active installation cannot be found or updated after one non-destructive supported attempt, return `BLOCKED_ACTIVE_PLUGIN_SYNC` with exact paths, commands, stdout, stderr, and preserved repository commit evidence. Do not pretend installation succeeded.

## Required validation after CONTINUE

Run from the final clean committed tree:

- `python3 tools/validate-asynchronia-model-preflight.py .`;
- syntax or parse validation for every changed JSON and Python file;
- exact frozen-scope diff proof;
- direct-child proof from baseline `93995ab4e599ebcb8881a6dc391e2a8ffdd07555`;
- conflicting-rule scan across frozen authority;
- manifest/marketplace version equality;
- positive and adversarial transition fixture results;
- active installed plugin version and skill-hash equality;
- active managed user-level block identity;
- proof no implementation mutation occurred before same-thread `CONTINUE`;
- `git diff --check 93995ab4e599ebcb8881a6dc391e2a8ffdd07555...<FINAL_PRIMARY_SHA>`.

## Publication

Push the final direct-child commit without force using an explicit SHA refspec to `refs/heads/main`.

After remote source and active installation verification, publish only:

- `.ai-bridge/outbox/BRIDGE-20260711-086-02-codex.md`;
- then `.ai-bridge/receipts/BRIDGE-20260711-086-03-codex.md` as a separate fast-forward mailbox commit.

Outbox and receipt must preserve:

- full preflight recommendation evidence;
- same-thread exact `CONTINUE` evidence;
- no-mutation-before-resume evidence;
- primary commit, parent, and exact changed paths;
- validator and adversarial fixture results;
- repository package identity;
- active installed plugin path, version, and skill hashes;
- installed managed-block identity;
- all required skill application evidence;
- no Safari claim.

Codex must not edit `.ai-bridge/STATE.md`.

Terminal success action code: `OPEN_FRESH_CHATGPT_VERIFIER_AND_SEND_SAME_BRIDGE_COMMAND`.
