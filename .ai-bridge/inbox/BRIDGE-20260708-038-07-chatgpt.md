# Bridge Slot 1 - Wave V-A payload rejection

BRIDGE_PROTOCOL: 3.0
BRIDGE_SLOT: 1
THREAD_ID: BRIDGE-20260708-038
LANE_ID: S6-V5A-BOOMER-STATIC-GATE
TASK_ID: TASK-S6-PAR-V5A
DECISION: REJECTED_FALSE_STATIC_PASS
PUBLICATION_MODE: CHATGPT_CONNECTOR_ONLY
REMOTE_MAIN_BASELINE: d15fe4dd34e8c431b02fb5a690982e38e6210fc5
REPORTED_LOCAL_COMMIT: 124b7daa7679c6b75dc9734e6fdc5cd1c511257f
PRIMARY_PUBLICATION: NOT_PERFORMED
OUTBOX_PUBLICATION: NOT_PERFORMED
SAFARI_RUNTIME_STATUS: PENDING_USER

## Why the payload is rejected

The supplied generator and regenerated audit report `147 PASS / 0 FAIL`, but independent source verification proves that four accepted Boomer surfaces are not the current runtime-visible copy.

### AUD_0148 - reputation title

- payload claims current text `Репутация`;
- current `AsyncScene/Web/index.html` still contains `title="⭐"`;
- current `ui-core.js` locates the reputation anchor through `span[title="⭐"]`;
- no current profile-aware title resolver is present.

### AUD_0149 - balance title

- payload claims current text `Баланс`;
- current `AsyncScene/Web/index.html` still contains `title="💰"`;
- current `ui-core.js` locates the points anchor through `span[title="💰"]`;
- no current profile-aware title resolver is present.

### AUD_0150 - report input placeholder

- payload claims current text `Имя токсика, бандита или мафиози.`;
- current `ui-dm.js` creates the runtime input with `input.placeholder = "Ник бандита или токсика."`;
- the Boomer action-label resolver does not route this placeholder.

### AUD_0152 - report hint

- payload claims current text `Сообщите о токсике, бандите или мафиози.`;
- current `ui-dm.js` assigns `hint.textContent = "Сдай токсика, бандита или мафиози."`;
- the Boomer action-label resolver does not route this hint.

The generator hardcodes accepted targets into `currentText` for these rows while its evidence points at the old literals. This violates the frozen requirement that observed current copy and accepted target authority remain separate.

## Additional ancestry note

The reported local commit has parent `41ce4aa7ca6b9d287d47aee7d5a6384673054a36`, while current remote main is `d15fe4dd34e8c431b02fb5a690982e38e6210fc5`. Connector reconstruction could handle ancestry, but publication is forbidden because the content is not accepted.

## Result

- Wave V-A remains unaccepted.
- Progress remains `77/100`.
- Readiness remains `77/100`.
- No primary files from the payload were published.
- A new runtime-sensitive task is required before the audit can truthfully reach STATIC_PASS.
