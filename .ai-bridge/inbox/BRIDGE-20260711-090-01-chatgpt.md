Use @asynchronia.

# BRIDGE-20260711-090 - Boomer Step 4.4B runtime restoration Fix4

ISSUE: 216
CYCLE: CYCLE-20260711-003
GENERATION: 11
SLOT: 2
THREAD_ID: BRIDGE-20260711-090
LANE_ID: S6-BOOMER-STEP4_4B-RUNTIME-RESTORATION
TASK_ID: TASK-S6-BOOMER-STEP4_4B-FIX4
EXECUTION_EPOCH: S6-BOOMER-4_4B-FIX4-R1-20260711-1335JST
TASK_NONCE: S6B44B-090-FIX4-1335
COORDINATOR_MEMORY_REVISION: 2026-07-11-1335-JST
AUTHORIZED_PRIMARY_BASELINE: c12e0dacf92ae629215dbcc520b62a10f1d89846
SUPERSEDES_THREAD: BRIDGE-20260711-089
PREVIOUS_ATTEMPT_STATUS: PREFLIGHT_PROCESS_ACCEPTED_RUNTIME_CORRECTION_PENDING
CLAIM_PATH: .ai-bridge/claims/BRIDGE-20260711-090-claim-v1-codex.md
EXPECTED_OUTBOX_PATH: .ai-bridge/outbox/BRIDGE-20260711-090-02-codex.md
EXPECTED_RECEIPT_PATH: .ai-bridge/receipts/BRIDGE-20260711-090-03-codex.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
REQUIRE_COMPLETE_SCOPE_DELTA: true
MODEL_RECOMMENDATION_PAUSE_REQUIRED: true
SAME_THREAD_CONTINUE_REQUIRED: true
SAFARI_STATUS: PENDING_USER

## Current verified defect

Current primary repository evidence shows:

- `AsyncScene/Web/dev/dev-checks.js` blob: `8e1001ac617dde83d0fbc1f48e34b40383eed454`;
- `docs/dev/dev-checks.js` blob: `1e50fc811572c43eb65aabd15fe0947216a57440`;
- both index files are identical blob `2e5b776c301b51f6a2c73aa353af98cbaa85c6a8` and load `step4_4b_boomer_runtime_aggregate_fix3_20260711a`;
- the Fix3 source changed identities and cache-busts but did not implement full restoration for battle state, moneyLog, persistence, or relevant localStorage;
- previous receipt claims of full restoration and source/docs mirror were rejected.

## Mandatory model preflight

The first fresh `мост 2` is read-only. Apply, in order:

1. task-router
2. scope-isolation-check
3. model-selector

Show the complete recommendation, stop at `WAITING_FOR_MODEL_SELECTION`, and end with one standalone fenced `CONTINUE` block. No mutation before exact same-thread `CONTINUE`.

Start the cost frontier at `GPT-5.4 / Light`. Promote only for a concrete failure mode caused by the runtime-state restoration complexity.

After `CONTINUE`, refetch main and mailbox and verify unchanged thread, task, claim, baseline, and scope.

## Frozen primary write scope

Exactly these four paths must change:

- AsyncScene/Web/dev/dev-checks.js
- docs/dev/dev-checks.js
- AsyncScene/Web/index.html
- docs/index.html

No fifth primary path may change.

## Atomic objective

Implement a side-effect-free Fix4 runtime smoke for Boomer Step 4.4B and publish it as one direct child of the authorized baseline.

The smoke must preserve existing terminology checks while making runtime state restoration real and fail-closed.

## Required restoration semantics

Identify the actual runtime objects and keys touched by the smoke. Snapshot before mutation and restore in a `finally` path even when an assertion or evaluator throws.

At minimum restore exactly:

- selected profile and text mode;
- points, REP, and Influence;
- every battle-state object, array, counter, or selection touched by the smoke;
- moneyLog, including order and object contents;
- persistence/save state and counters touched by the smoke;
- every localStorage key read, written, removed, or indirectly changed by the smoke.

Restoration must preserve original absence versus presence. Keys absent before the smoke must be removed afterward. Existing values must be restored byte-for-byte.

Where runtime code may retain references, restore arrays and objects in place rather than replacing identity. Do not JSON-roundtrip values whose types or identity semantics would change.

## Required state proof

The smoke must capture a canonical before digest and after digest for every tracked surface and expose a bounded result containing at least:

- `profileRestored`
- `economyRestored`
- `battleStateRestored`
- `moneyLogRestored`
- `persistenceRestored`
- `localStorageRestored`
- `stateRestored`

Overall PASS is forbidden unless every restoration field is true.

Add isolated adversarial fixtures proving that each comparator/evaluator rejects a deliberate mutation of its own surface. Run those fixtures on cloned or sandboxed data, not on live player state.

A snapshot field without corresponding restore and comparison logic is a failure.

## Source/docs mirror

The Boomer Fix4 implementation must be semantically mirrored in source and docs.

Preserve only documented pre-existing intentional docs Alpha differences outside the Boomer block. The Boomer snapshot, restore, digest, adversarial evaluator, result schema, version, and command behavior must match.

Both index files must load the new Fix4 cache-bust and must remain byte-identical unless an existing documented deployment difference requires otherwise.

## Required clean-tree validation

Create one direct-child commit first, then run from the clean committed tree:

- `git status --short` is empty;
- `node --check AsyncScene/Web/dev/dev-checks.js`;
- `node --check docs/dev/dev-checks.js`;
- `python3 tools/generate-boomer-step4-4-economy-conflict-audit.py .`;
- `python3 tools/validate-boomer-step4-4-economy-conflict-audit.py .`;
- static audit remains 147 PASS, 0 FAIL, 0 structural failures, 8 non-live rows;
- exact four-path diff proof;
- sole-parent proof from `c12e0dacf92ae629215dbcc520b62a10f1d89846`;
- protected-path proof;
- source/docs Boomer semantic mirror proof;
- both index cache-bust and byte-equality proof;
- deterministic ephemeral Node harness proving snapshot/restore/digest and all adversarial fixtures without committing a fifth file;
- `git diff --check c12e0dacf92ae629215dbcc520b62a10f1d89846...<FINAL_PRIMARY_SHA>`.

The ephemeral harness content and SHA-256 must be preserved in outbox evidence. A static string scan is not a substitute for executing the restoration helper and adversarial fixtures.

If a headless browser already exists, run the smoke there as supplementary evidence. Do not install new browser dependencies and do not claim Safari PASS.

`tools/validate-orchestration-policy.py` remains NOT_APPLICABLE because it is pinned to historical Bridge 062 identity. Preserve the exact exclusion and replacement controls.

## Skill route after resume

Apply in order:

1. task-router
2. scope-isolation-check
3. model-selector
4. parallel-scope-planner
5. closed-loop-controller
6. failure-routing-and-corrective-loop
7. mirror-audit
8. smoke-orchestrator
9. deployment-verifier
10. acceptance-evidence-gate

Use installed Asynchronia or repository fallback. Do not seek callable-plugin telemetry.

## Publication

Immediately before push, refetch main and prove it still equals the authorized baseline.

Push one direct-child exact-four-path commit by explicit non-force SHA refspec to `refs/heads/main`.

Then publish the exact Bridge 090 outbox and receipt as separate fast-forward mailbox commits. Preserve validation results, excluded-validator evidence, model preflight evidence, ordered skill evidence, restoration fields, adversarial results, harness identity, source/docs mirror proof, and no Safari claim.

Codex must not edit `.ai-bridge/STATE.md`.

Terminal action: `OPEN_FRESH_CHATGPT_VERIFIER_AND_SEND_SAME_BRIDGE_COMMAND`.
