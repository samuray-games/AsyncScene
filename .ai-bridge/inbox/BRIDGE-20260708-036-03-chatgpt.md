Use @asynchronia runtime-safety-gate.

# Bridge Slot 1 - Wave IV narrow correction

BRIDGE_PROTOCOL: 3.0
ORCHESTRATION_VERSION: 3.0
BRIDGE_SLOT: 1
THREAD_ID: BRIDGE-20260708-036
LANE_ID: S6-I4-DATA-BATTLE-CONFLICT-PRESENTATION
TASK_ID: TASK-S6-PAR-I4
PHASE: EXECUTE_NOW_CORRECTION
CORRECTION_OF: .ai-bridge/outbox/BRIDGE-20260708-036-02-codex.md
CURRENT_MAIN_BASELINE: 374a47048425d9035e81f81304cb41211eba3475
EXPECTED_CORRECTION_OUTBOX: .ai-bridge/outbox/BRIDGE-20260708-036-04-codex.md
RUNTIME_CLASSIFICATION: RUNTIME_SENSITIVE
MODEL_PROTOCOL: SAME_THREAD_CORRECTION_NO_NEW_PREFLIGHT
CONFIRMATION: ALREADY_SATISFIED_BY_PRIOR_CONTINUE

## Decision

Wave IV is not accepted yet. Independent remote inspection confirmed publication, ancestry, authorized paths and mirror equality, but found two frozen-contract violations.

## Defect A - `default` presentation identity collapsed

The task required five distinct presentation identities: `default`, `millennial`, `zoomer`, `alpha`, `boomer`.

Current `resolveUiTextMode("default")` returns `millennial`, and unknown input also returns `millennial`. Visible default/Millennial copy may remain identical, but the resolver identity itself must preserve `default`.

Required correction:

- explicit `default` -> `default`;
- explicit `millennial` -> `millennial`;
- explicit `zoomer` -> `zoomer`;
- explicit `alpha` -> `alpha`;
- explicit `boomer` -> `boomer`;
- explicit unknown -> `default`;
- omitted profile must continue to use the active runtime profile and must not be forced to Millennial by the default `Data.TEXT_MODE = "millennial"` value;
- visible default and Millennial output must remain byte-for-byte equivalent.

Do not change `Data.getUiProfile`, `setUiProfile`, birth-year selection or stored profile identity.

## Defect B - fallback semantic keys and full note routing

The frozen semantic keys were exactly:

- `battle.draw_fallback`
- `battle.escaped_fallback`
- `battle.ignored_fallback`

Current implementation instead registers and calls `draw_fallback`, `escaped_fallback`, `ignored_fallback`.

Also, the escape-vote note still hardcodes `Толпа решает:`. Under Boomer this produces mixed-profile copy such as `Толпа решает: Выйти.` instead of routing both visible fallback fragments through the exact matrix.

Required correction:

1. Registry keys and call sites must use the exact three `battle.*` semantic keys.
2. The resolver must accept only those three keys and fall back safely to the default table.
3. For the escape-vote sentence note, route both components:
   - prefix through `battle.draw_fallback`;
   - outcome through `battle.escaped_fallback` or `battle.ignored_fallback`;
   - preserve colon and final period.
4. Exact Boomer sentence results must therefore be:
   - `Решает голосование: Выйти.`
   - `Решает голосование: Вызов отклонён.`
5. Default/Millennial/Zoomer sentence results must remain exactly:
   - `Толпа решает: Свалить.`
   - `Толпа решает: Отвали.`
6. Alpha sentence results must remain:
   - `Толпа решает: Уйти.`
   - `Толпа решает: Отвали.`
7. Bare `resultLine` values remain the bare outcome target without punctuation.

## Exact correction scope

Change only:

- `AsyncScene/Web/data.js`
- `docs/data.js`
- `AsyncScene/Web/conflict/conflict-core.js`
- `docs/conflict/conflict-core.js`

Do not touch either `ui-battles.js` file. They are already equal at blob `9b11eeffeff3091c4933130d73d96843301f0b08`.

Preserve all previously accepted Alpha and Boomer overrides, complete Zoomer baseline, handlers, economy, REP, influence, crowd, timers, report flow, persistence, state transitions, SystemCopy routing and semantic result values.

## Required regression proof

Prove:

1. exact six-input identity matrix: default, millennial, zoomer, alpha, boomer, unknown;
2. default and Millennial visible output remains identical and unchanged;
3. omitted runtime resolution still follows the active profile;
4. exact three `battle.*` semantic keys are the only fallback registry keys;
5. all fallback call sites use exact `battle.*` keys;
6. full sentence-note matrix above across all five profiles plus unknown;
7. bare fallback matrix remains exact;
8. all five Alpha and five Boomer data overrides remain exact;
9. complete Zoomer table remains unchanged from baseline;
10. both changed source/deployed pairs are byte-identical;
11. both `ui-battles.js` files remain unchanged at the accepted blob;
12. `node --check` all six owned files;
13. `git diff --check`;
14. no mechanics, SystemCopy, report-flow or state change;
15. empty `failures`, `forbiddenRemaining`, `missingCoverage`, `failedChecks`.

## Publication

- Publish one direct-child correction commit to `main` from `374a47048425d9035e81f81304cb41211eba3475`.
- Only the four correction-scope files may change.
- Refetch `origin/main` after push and report fresh remote blob SHAs for all six ownership-group files.
- Publish immutable outbox `.ai-bridge/outbox/BRIDGE-20260708-036-04-codex.md`.
- Do not edit mailbox STATE.
- No new preflight, no new `CONTINUE`, no second approval.
- On auth failure, use Protocol 3.0 `BLOCKED_PUSH_AUTH` with the complete recovery bundle.
- Exact next action after publication: return to ChatGPT and write `мост 1`.
