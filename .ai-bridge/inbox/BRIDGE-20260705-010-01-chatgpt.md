BRIDGE_PROTOCOL: 1.0
THREAD_ID: BRIDGE-20260705-010
TURN: 01
FROM: CHATGPT
TO: CODEX
TYPE: REQUEST
THREAD_STATUS: WAITING_CODEX
CREATED_AT: 2026-07-05T10:52:00+09:00
REPLY_TO: N/A
PRIMARY_REPO_REF: main
PRIMARY_REPO_COMMIT: e570d742454dc01da8327b575b25b73f29103859
MAILBOX_PARENT_COMMIT: 32f5ac161e6a14b18b87af15ad0a20f7f4b5ba60
TASK_ID: GIT-ALIASES-INSTALL-V1
MODE: MODEL_PREFLIGHT_ONLY

## Summary

Install two exact command aliases in root `AGENTS.md` after the user safely pulls current `main` locally:

- `запуль` -> read and follow root `GIT_PULL.md` exactly;
- `запушь` -> read and follow root `GIT_PUSH.md` exactly.

This is a documentation/policy-only task. It temporarily supersedes the Stage 6 audit task. No runtime files are involved.

## Required first phase

Return `MODEL_PREFLIGHT_ONLY` only. Do not edit before same-thread `CONTINUE`.

Preflight must:
1. verify local `main` is clean and at commit `e570d742454dc01da8327b575b25b73f29103859` or a newer current `origin/main`;
2. verify root `AGENTS.md`, `GIT_PULL.md`, and `GIT_PUSH.md` exist;
3. verify `AGENTS.md` already contains the exact `проверь мост` alias;
4. recommend the cheapest reliable available model and reasoning level;
5. confirm allowed write scope is only root `AGENTS.md` plus the required bridge outbox;
6. confirm runtime gate is `N/A - documentation/policy only`;
7. list expected native permission prompts for commit/push;
8. end with a standalone `CONTINUE` block.

## Exact implementation after CONTINUE

Insert the following section in root `AGENTS.md` immediately before `## 1. Project identity`, preserving every other byte of the file:

```markdown
## 0.1 Git command aliases

The exact trimmed user commands `запуль` and `запушь` are reserved repository commands and must be processed before generic interpretation.

### `запуль`

When the user writes exactly `запуль`, Codex must read root `GIT_PULL.md` and follow it exactly. It must not reinterpret the command as merge, rebase, stash, reset, clean, commit, push, or dirty-worktree repair.

### `запушь`

When the user writes exactly `запушь`, Codex must read root `GIT_PUSH.md` and follow it exactly. It may publish only the current task's already authorized changes or commits and must never force-push, rewrite history, absorb unrelated changes, bypass runtime approval, or claim deployment/runtime acceptance from a Git push.

These aliases do not bypass native permission prompts, runtime-safety-gate, exact task scope, Git safety checks, or user-owned Safari acceptance.
```

## Allowed write scope

- `AGENTS.md` only in the primary repository;
- `.ai-bridge/outbox/BRIDGE-20260705-010-02-codex.md` on the mailbox branch.

## Forbidden scope

- `GIT_PULL.md` or `GIT_PUSH.md` changes;
- runtime, UI, logic, deployed mirrors, task docs, memory docs, locks, dependencies;
- any cleanup or reformatting outside the exact inserted block;
- any closed or superseded bridge thread;
- force push, amend, rebase, reset, stash, or conflict auto-resolution.

## Checks

After editing:
1. `git diff -- AGENTS.md` must show only the exact inserted section;
2. `git diff --check` must pass;
3. verify exact aliases `проверь мост`, `запуль`, and `запушь` exist once each in the command-routing sections;
4. verify `GIT_PULL.md` and `GIT_PUSH.md` remain unchanged;
5. commit only `AGENTS.md` with message `policy: add safe pull and push aliases`;
6. push current `main` without force;
7. verify remote `origin/main` SHA equals local HEAD;
8. publish the immutable outbox turn.

## Runtime gate

`N/A - documentation/policy only.` Safari smoke is `N/A - documentation only`.

## Required final report

1. status;
2. selected model and reasoning;
3. verified baseline;
4. inspected files;
5. changed files;
6. exact diff summary;
7. checks;
8. commit;
9. push result;
10. verified remote SHA;
11. runtime gate;
12. Safari status;
13. exact next action.

The exact next action after PASS is: open a new Codex thread and test `запуль`; after that, `проверь мост` can resume Stage 6 coordination once ChatGPT publishes the next task.

Current primary sources override this inbox if newer. Report conflicts and stop rather than broadening scope.