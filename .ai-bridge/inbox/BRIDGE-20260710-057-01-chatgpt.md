# Complete the closed loop without plugin delivery as a gate

BRIDGE_PROTOCOL: 3.3
CYCLE_ID: CYCLE-20260709-001
CYCLE_GENERATION: 12
THREAD_ID: BRIDGE-20260710-057
LANE_ID: PROCESS-CLOSED-LOOP-CORE-COMPLETION
TASK_ID: TASK-PROCESS-CLOSED-LOOP-CORE-COMPLETION
EXECUTION_EPOCH: CLOSED-LOOP-CORE-R1-20260710-1033JST
TASK_NONCE: CLV1-057-CORE-E599-1033
BRIDGE_SLOT: 3
PHASE: RECOVERY_REQUIRED
BASELINE: e599beddbbd03c8585f9c44f0f7c190338e123e7
COORDINATOR_MEMORY_REV: 2026-07-10-1033-JST
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260710-057-claim-v1-codex.md
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260710-057-02-codex.md
EXPECTED_RECEIPT: .ai-bridge/receipts/BRIDGE-20260710-057-03-codex.md
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
PLUGIN_DELIVERY_LANE: SEPARATE_EXTERNAL_NON_GATING
PLUGIN_INVOCATION_REQUIRED: false
PLUGIN_SOURCE_CHANGES_AUTHORIZED: false
SAFARI_STATUS: N/A_PROCESS_ONLY
SUPERSEDES_THREAD: BRIDGE-20260710-056

## User scope correction

The user explicitly clarified that Asynchronia plugin installation and activation are being handled in another chat. This lane exists only to make the ChatGPT-Codex closed loop complete reliably.

Therefore:
- plugin installation, package delivery, activation, UI invocation, loader telemetry, and installed-package acceptance are out of scope;
- plugin unavailability must not block this loop-only lane;
- `BLOCKED_PLUGIN_UNAVAILABLE` is illegal for this task;
- no claim of plugin activation may be made;
- repository authority may be read directly as ordinary project files;
- do not modify `plugins/asynchronia/**`, `.agents/plugins/**`, or any plugin installation/cache path;
- no model recommendation is required in this lane.

## Thread 056 verdict

`RECOVERY_REQUIRED_WRONG_CROSS_LANE_PLUGIN_GATE`.

Thread 056 correctly detected that inbox text cannot activate a plugin, but it incorrectly made actual plugin availability a prerequisite for completing the loop. That requirement belongs to the separate plugin lane and created a circular blocker. Thread 056 must not continue.

## Frozen objective

Complete the executable closed-loop core so one cycle can progress deterministically through:

`ChatGPT prepares -> Codex executes -> primary publishes -> immutable outbox publishes -> receipt publishes -> fresh ChatGPT verifies -> accepted or fresh correction -> STATE and memory update -> next cycle or closure`

The loop must complete without requiring plugin delivery evidence.

## Required implementation

### 1. Exact active identity
- Define one immutable active identity containing cycle, generation, slot, thread, lane, task, epoch, nonce, coordinator memory revision, baseline, inbox, claim, expected outbox, and expected receipt.
- Validate exact equality against active task data. Syntax-only validation is insufficient.
- Remove stale thread 052/053/054 fixtures from passing tests.

### 2. Exact outbox and receipt schemas
- Define separate exact status-specific outbox schemas.
- Define a separate exact receipt schema.
- Distinguish `stateBlobSha`, `mailboxParentCommit`, `outboxPublicationCommit`, `outboxBlobSha`, `primaryCommitSha`, and `primaryParent`.
- Outbox contains only evidence knowable before its own publication.
- Receipt contains actual outbox publication identity and exact byte-equality evidence.
- Receipt does not require its own publication commit SHA.
- Final Codex reply must be byte-identical to the remote receipt.

### 3. Executable semantic controls
- Every declared control must call real validation behavior with meaningful valid and adversarial fixtures.
- Remove unconditional-success branches for route, identity, schema, path, and publication controls.
- Unknown and unmapped controls fail closed.
- Add a dedicated test for every declared control.
- Add mutation-style tests that patch, remove, invert, or bypass each evaluator family and prove validation fails.

### 4. Canonical transition machine
- Define an immutable canonical 12-state transition table independently from the implementation table.
- Test every legal pair and every illegal pair against the canonical table.
- Verify the complete terminal path through outbox publication, receipt publication, awaiting verification, acceptance, correction, recovery, and closure.

### 5. SHA, path, and branch proof
- Require exact lowercase `[0-9a-f]{40}` values.
- Reject uppercase and known repeated synthetic placeholder patterns.
- Compare changed paths exactly with actual primary commit evidence and frozen authorized scope.
- Verify protected paths are unchanged.
- Verify active inbox, claim, STATE, outbox, and receipt are absent from main.
- Verify outbox and receipt each change only their exact mailbox path and preserve fast-forward ancestry.

### 6. Status and recovery consistency
- Remove forbidden `BLOCKED_NO_REMOTE_OUTBOX` and `BLOCKED_NO_SOURCE_DELTA` from legal terminal status sets for primary-required tasks.
- Define exact allowed combinations of status, completion mode, current phase, verifier classification, and next action.
- Replace arbitrary free-text next action with a machine-readable action code and exact human instruction.
- A success receipt must use action code `OPEN_FRESH_CHATGPT_VERIFIER_AND_SEND_SAME_BRIDGE_COMMAND` and explicitly request independent verification of the remote outbox and receipt.

### 7. Loop completion gate
- Closed-loop completion requires source implementation acceptance and a separate closed-loop canary acceptance.
- Plugin installation or installed-package acceptance is not part of this loop's completion gate.
- Track plugin readiness only as `OUT_OF_SCOPE_SEPARATE_LANE`.
- Product/plugin release policy may remain separately blocked by the plugin lane, but the closed-loop cycle itself must be able to reach COMPLETE.

### 8. Root synchronization
- Align all affected primary root authorities, executable contract, tests, validator, TASKS, and PROJECT_MEMORY.
- Root text must explicitly state that plugins may assist tasks but plugin availability is not bridge transport and cannot block a loop-only execution lane.
- Do not edit plugin source, manifests, marketplace metadata, runtime, product code, or Safari surfaces.

## Frozen primary scope

Only:
- `AGENTS.md`
- `AGENTS.override.md`
- `PROCESS_ROOT_SYNC.md`
- `ORCHESTRATION.md`
- `BRIDGE.md`
- `CODEX_BRIDGE_BOOTSTRAP.md`
- `CODEX_BRIDGE_RECOVERY.md`
- `CLOSED_LOOP_PROTOCOL.md`
- `tools/closed_loop_contract.py`
- `tools/test_closed_loop_contract.py`
- `tools/validate-orchestration-policy.py`
- `TASKS.md`
- `PROJECT_MEMORY.md`

All other primary paths are protected. In particular, all plugin paths are protected because another lane owns plugin work.

## Validation

Run from the exact committed tree:
- `python3 -m py_compile tools/closed_loop_contract.py tools/test_closed_loop_contract.py tools/validate-orchestration-policy.py`
- `python3 -m unittest tools.test_closed_loop_contract`
- `python3 tools/validate-orchestration-policy.py`
- `git diff --check`

The unit suite and validator must execute the required mutation proofs. Prose summaries are not evidence.

## Publication transaction

- Publish exactly one direct-child primary correction commit from baseline `e599beddbbd03c8585f9c44f0f7c190338e123e7` to `main`.
- Publish immutable outbox only to `.ai-bridge/outbox/BRIDGE-20260710-057-02-codex.md`.
- Publish receipt only to `.ai-bridge/receipts/BRIDGE-20260710-057-03-codex.md`.
- Refetch main, STATE, outbox, receipt, and mailbox head.
- Final response must be byte-identical to the remote receipt.
- Do not continue thread 056.
- Do not run Safari.

Exact next instruction after complete publication: open a fresh ChatGPT conversation and send `мост 3` for independent verification.