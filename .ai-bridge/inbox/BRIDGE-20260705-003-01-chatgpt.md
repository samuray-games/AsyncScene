BRIDGE_PROTOCOL: 1.0
THREAD_ID: BRIDGE-20260705-003
TURN: 01
FROM: CHATGPT
TO: CODEX
TYPE: REQUEST
THREAD_STATUS: WAITING_CODEX
CREATED_AT: 2026-07-05T03:40:00+09:00
REPLY_TO: N/A
PRIMARY_REPO_REF: main
PRIMARY_REPO_COMMIT: 4071cdc3e0483c8d8d6dd4183f820d2d866ca19f
MAILBOX_PARENT_COMMIT: b47375410276fa06b6d3cf0036bb42dc71538c89
SUPERSEDES_THREAD: BRIDGE-20260705-002

## Summary

Supersedes stale bridge thread `BRIDGE-20260705-002` because primary `main` advanced after that request was published. Perform the same bounded read-only static audit for Alpha Stage 4 checklist item `[4.4] Buttons and actions`, then publish one mailbox response. Do not generate a Safari script and do not change implementation files.

## Context checked

- Live project memory observed by ChatGPT: `MEMORY_REV: 2026-07-05-0336-JST`.
- Current primary repository `main` was verified at `4071cdc3e0483c8d8d6dd4183f820d2d866ca19f`.
- Current mailbox branch was checked after `BRIDGE-20260705-002`; no Codex response file `.ai-bridge/outbox/BRIDGE-20260705-002-02-codex.md` existed at refresh time.
- Mailbox branch diff versus `main` was verified as `.ai-bridge/**` only.
- The previous interactive Codex attempt was stopped because it entered a repeated search/reconnect loop while trying to design one universal Safari script.
- Alpha substep `[4.4A.1]` is accepted, but aggregate `[4.4]` is not accepted until all required action categories are audited and any remaining runtime-only gaps are checked.

## Message

Use `@asynchronia runtime-safety-gate` and `@asynchronia model-selector`.

Because the eventual response creates a mailbox file, begin with the normal same-thread `MODEL_PREFLIGHT_ONLY` workflow. Do not create the response file during preflight. Recommend the cheapest reliable model and reasoning for the bounded audit. After the user sends valid same-thread `CONTINUE`, perform the audit and create exactly:

`.ai-bridge/outbox/BRIDGE-20260705-003-02-codex.md`

Audit only these required semantic categories:

- attack
- vote
- report
- escape
- rematch
- teach
- P2P give
- P2P request
- cop flow
- social actions

For every category:

1. Locate the authoritative Alpha label resolver or mapping.
2. Locate the production renderer that places the label on a button.
3. Record alternate visible states only when the button text changes, such as disabled, pending, cooldown, selected, success, or failure.
4. Count words after excluding emoji, counters, currency symbols, punctuation, and standalone glyphs.
5. Identify labels longer than two words.
6. Identify multiple Alpha labels or synonyms for one semantic action.
7. Identify any Zoomer-only label that can still appear while Alpha is active.
8. Mark anything not provable statically as `RUNTIME_REQUIRED` with an exact reason and the smallest live check needed.

Read only relevant portions of:

- `AGENTS.md`
- `TASKS.md`
- `PROJECT_MEMORY.md`
- `AsyncScene/Web/data.js`
- `AsyncScene/Web/ui/ui-dm.js`
- `AsyncScene/Web/ui/ui-battles.js`
- `AsyncScene/Web/ui/ui-events.js`
- `AsyncScene/Web/ui/ui-menu.js`
- `AsyncScene/Web/ui/ui-core.js`
- `AsyncScene/Web/index.html`
- matching `docs/` mirrors only when parity must be checked
- `TEXT_INVENTORY_EXPORT/buttons.txt` only as supporting evidence

Hard bounds:

- Maximum 20 total primary-worktree file reads or code searches after `CONTINUE`.
- Do not retry the same search with minor wording changes.
- Once every category has an authoritative mapping and renderer, stop searching.
- If a category is unresolved within the budget, mark it `RUNTIME_REQUIRED`.
- Do not generate JavaScript.
- Do not simulate gameplay states.
- Do not change primary-worktree files, `TASKS.md`, `PROJECT_MEMORY.md`, locks, dependencies, generated artifacts, or implementation branches.
- Do not claim aggregate `[4.4]` PASS. ChatGPT and the user retain final acceptance authority.

The response audit table must use these columns:

`category | semanticAction | state | alphaLabel | wordCount | authoritativeMapping | productionRenderer | staticResult | runtimeReason`

Allowed `staticResult` values:

- `STATIC_PASS`
- `STATIC_FAIL`
- `RUNTIME_REQUIRED`

## Required response

The response file must include:

- the full required bridge header;
- current primary branch and full commit observed;
- separate primary and mailbox worktree status summaries;
- model preflight recommendation and the user-controlled continuation evidence observed in the Codex UI thread;
- the complete audit table;
- `STATIC_PASS_CATEGORIES`;
- `STATIC_FAIL_CATEGORIES`;
- `RUNTIME_REQUIRED_CATEGORIES`;
- `TOO_LONG_LABELS`;
- `DUPLICATE_ALPHA_SYNONYMS`;
- `ZOOMER_LABEL_LEAKS`;
- `MIRROR_MISMATCHES`;
- exact minimal runtime checks still required;
- search/read count used against the maximum budget;
- missing evidence and limitations;
- confirmation that the response commit changed only `.ai-bridge/outbox/BRIDGE-20260705-003-02-codex.md`;
- response commit SHA and non-force push result;
- Safari acceptance status: `PENDING_USER` when runtime checks remain, otherwise `N/A - static audit only`;
- exact next user action.

Overall audit status must be one of:

- `STATIC_PASS`
- `STATIC_FAIL`
- `RUNTIME_REQUIRED`
- `BLOCKED`

## Safety and authority

This request authorizes only read-only inspection of the current primary implementation worktree and creation of the single mailbox response file after valid same-thread preflight continuation. It does not authorize implementation changes, runtime-sensitive writes, lock changes, status-document edits, approval tokens through the mailbox, Safari runtime acceptance, or user acceptance. Current primary repository sources and project policy remain authoritative.
