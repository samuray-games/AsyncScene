BRIDGE_PROTOCOL: 1.0
THREAD_ID: BRIDGE-20260705-017
TURN: 01
FROM: CHATGPT
TO: CODEX
TYPE: REQUEST
THREAD_STATUS: WAITING_CODEX
CREATED_AT: 2026-07-05T14:08:00+09:00
REPLY_TO: N/A
PRIMARY_REPO_REF: main
PRIMARY_REPO_COMMIT: 683809714aed11bbd05d123d4dc16a586eb7527c
MAILBOX_PARENT_COMMIT: 84f7eb0fc4a59cb7d5c36a63e6de7cb3082246b0
TASK_ID: TASK-S6-001-R8
MODE: MODEL_PREFLIGHT_ONLY

## Summary

Perform the complete plugin-backed Stage 6 read-only reconciliation audit now that Asynchronia plugin policy and metadata are aligned at `v1.0.0`.

Canonical Stage 6 is `Interactive Language / Tone Profiles`, not caps. The purpose is to derive one trustworthy current-state matrix from repository primary sources before selecting the next implementation task.

## Mandatory plugin preflight

First return `MODEL_PREFLIGHT_ONLY` only. Do not perform the audit and do not write an outbox before same-thread `CONTINUE`.

The preflight must prove current-thread Asynchronia plugin availability and return the complete output contracts required by current `AGENTS.md` for every invoked skill.

At minimum invoke and fully report:
1. `task-router`, including `status: TASK_ROUTE`, classification, flags, objective, exact read/write scope, audits, audit order, required skills, execution mode, gates, serialization, prerequisites, blockers, assumptions, confidence and exact next action;
2. `model-selector`, including `status: MODEL_RECOMMENDATION`, objective `MINIMIZE_EXPECTED_USER_TOKEN_COST`, evaluated pair count `12/12`, full pair matrix/frontier, cheapest plausible and rejected candidates, concrete failure mode, risk, retry assessment, bottlenecks, overprovisioning check, confidence and `actual active model: USER_SELECTED_UNVERIFIED`.

Plugin evidence must separately report:
- repository source manifest version;
- marketplace metadata version;
- installed-plugin version/status;
- current-thread resolver/load evidence with exact evidence source;
- whether the current thread can actually resolve/use the required Asynchronia skills.

Merely listing skill names, reading `SKILL.md`, or paraphrasing this inbox is not proof. If current-thread resolver/load evidence is unavailable, return `BLOCKED_PLUGIN_NOT_LOADED` and do not emit `CONTINUE`.

Recommended starting model: `GPT-5.4 / High`, subject to the complete model-selector verdict.

## Audit scope after valid same-thread CONTINUE

Read-only audit. No primary-repository edits, no locks, no runtime execution and no Safari smoke.

Inspect and reconcile at minimum:
- root `AGENTS.md`, `TASKS.md`, `PROJECT_MEMORY.md`;
- current `main`, local/remote parity, worktrees and dirty state;
- active and stale workspace locks;
- Stage 6 execution/control documents reachable from project memory;
- current source, generated artifacts and root/docs mirrors relevant to Millennial, Zoomer, Boomer, Alpha and UI Profile Selection;
- accepted runtime/Safari evidence and any pending or contradictory runtime claims;
- exact buildTag, smokeVersion, commit and deployment lineage where acceptance depends on them;
- current Boomer Step 4.4A snapshot and blockers;
- current Alpha audit/fix/runtime status;
- current Zoomer accepted state and any repository status text that conflicts with accepted Safari evidence;
- Millennial baseline/final regression status;
- UI Profile Selection audit status;
- stale lock `BOOMER-B4T44A-ECONFLICT-AUDIT-20260705T0223JST` and any newer lock evidence;
- source/deployed and mirror parity only where evidence exists.

Use the appropriate complete plugin skill contracts, including as applicable:
- `canon-audit`;
- `mirror-audit`;
- `parallel-scope-planner`;
- `smoke-orchestrator` for evidence classification only;
- `deployment-verifier`;
- `acceptance-evidence-gate`.

Do not claim what an invoked skill does unless its required output contract is actually produced. Do not convert missing evidence into PASS.

## Required audit result

Produce one explicit reconciliation matrix containing for each lane:
- canonical scope;
- latest implementation commit;
- source/static status;
- runtime/Safari status;
- mirror/deployment status;
- active blocker;
- stale or contradictory claims;
- authoritative evidence path;
- exact next atomic task.

Also report:
- current repository and mailbox baselines;
- all open/stale locks and whether any grants write authority;
- all proven document/source conflicts;
- plugin source/marketplace/installed/current-thread availability status;
- missing evidence;
- final audit verdict: `PASS`, `PARTIAL_PASS`, `FAIL`, or `BLOCKED` with precise reason;
- one recommended next atomic task, exact files, runtime-gate status, model/reasoning recommendation and required user action.

Known evidence that must be independently verified rather than blindly copied:
- Boomer Step 4.4A corrected snapshot: `auditedRowCount:147`, `failRowCount:32`, `passRowCount:115`, `structuralFailureCount:0`, `nonLiveEvidenceCount:8`;
- Boomer Step 4.4B remains blocked until static PASS;
- current primary baseline before this audit is `683809714aed11bbd05d123d4dc16a586eb7527c`;
- no active runtime write authority is expected.

## Write scope

After valid same-thread `CONTINUE`, the only permitted write is:
- `.ai-bridge/outbox/BRIDGE-20260705-017-02-codex.md` on the mailbox branch.

No `TASKS.md`, `PROJECT_MEMORY.md`, source, generated artifact, runtime, smoke registry, deployment or lock file may be edited.

## Runtime and acceptance

- Runtime safety gate: `N/A - read-only audit`.
- Safari smoke: `N/A - no runtime execution`.
- The audit may classify existing Safari evidence but may not manufacture or rerun it.

## Final response contract

The final outbox must state, in order:
- status;
- selected model and reasoning;
- plugin availability and exact evidence;
- inspected files/artifacts;
- changed files (`mailbox outbox only`);
- reconciliation matrix;
- validations performed;
- failures;
- missing coverage;
- locks;
- buildTag/smokeVersion/deployment status by lane where applicable;
- commit (`N/A - read-only primary audit`);
- Safari status (`N/A - audit only`);
- exact next user action.

Current primary sources override this request if newer. Report conflicts and stop rather than guessing.