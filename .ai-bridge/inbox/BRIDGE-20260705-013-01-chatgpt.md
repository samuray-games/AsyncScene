BRIDGE_PROTOCOL: 1.0
THREAD_ID: BRIDGE-20260705-013
TURN: 01
FROM: CHATGPT
TO: CODEX
TYPE: REQUEST
THREAD_STATUS: WAITING_CODEX
CREATED_AT: 2026-07-05T12:12:00+09:00
REPLY_TO: N/A
PRIMARY_REPO_REF: main
PRIMARY_REPO_COMMIT: f5bc642bff40953e11b86ad3592c49c669573e67
MAILBOX_PARENT_COMMIT: 064f5d66a0ede2e2a1d57217b5d16efa0e3e3381
TASK_ID: TASK-S6-001-R5
MODE: MODEL_PREFLIGHT_ONLY

## Summary

Perform the complete current-baseline read-only reconciliation audit for canonical Stage 6: Interactive Language / Tone Profiles. This is a fresh thread and replaces, but does not resume, superseded thread `BRIDGE-20260705-011`.

## Context checked

- Current accepted primary baseline: `f5bc642bff40953e11b86ad3592c49c669573e67`.
- `ACTION-TOKEN-FORMAT-V1` passed and thread `BRIDGE-20260705-012` is closed by final decision turn.
- Root `AGENTS.md` and `BRIDGE.md` now contain the accepted exact fenced action-token policy.
- Execution Plan: https://docs.google.com/document/d/17YDaMlYRlJIRwOekm6gt1prDCDNCQvrXF5A2T7PbeUM/edit
- Control Board: https://docs.google.com/document/d/1ywPtHLwi8yhXoQILcrElsKqv03y4S64N1alzlYdBRC0/edit
- Live Project Memory observed before coordinator synchronization at `MEMORY_REV: 2026-07-05-1130-JST`; verify the current value directly and prefer newer primary repository sources for implementation state.
- Current known Boomer 4.4A static snapshot from repository records: 147 audited live rows, 32 FAIL, 115 PASS, 0 structural failures, 8 non-live evidence rows.
- The historical lock `BOOMER-B4T44A-ECONFLICT-AUDIT-20260705T0223JST` is treated as stale pending reconciliation and grants no write authority.

## Message

First return `MODEL_PREFLIGHT_ONLY` only. Do not run the audit before same-thread `CONTINUE`.

The preflight must:
1. verify local and remote `main`, current branch, local Git status, uncommitted changes, worktrees and applicable repository policies;
2. verify mailbox `STATE.md`, this active inbox and that thread 013 is the sole open thread;
3. recommend the cheapest reliable available model and reasoning level for a broad cross-system reconciliation audit;
4. list the exact planned read scope, including repository files, relevant generated/audit artifacts, mirrors, locks and status sources;
5. confirm that changed files in the primary repository will remain none;
6. confirm the only eventual write is `.ai-bridge/outbox/BRIDGE-20260705-013-02-codex.md` on the mailbox branch;
7. list native permission prompts, missing access and blockers;
8. end with exactly one standalone fenced text code block containing only `CONTINUE`, with no text after it.

After same-thread `CONTINUE`, perform the complete read-only audit:

A. Repository baseline and concurrency
- current local and remote `main` identity;
- current branch, Git status, uncommitted changes, branches and worktrees;
- task-owned, unrelated dirty and repository-observed concurrent state;
- every active, stale, conflicting or ambiguous lock, including the historical Boomer lock.

B. Canonical Stage 6 boundary
- prove that Stage 6 is Interactive Language / Tone Profiles;
- separate it from caps and NPC-vs-NPC / Living World work currently assigned to future Stage 8;
- identify any stale plan, task or status text that still mixes these scopes.

C. Profile-by-profile implementation and acceptance
- Millennial baseline and remaining final-regression obligations;
- Zoomer exact implementation and accepted Safari evidence;
- Boomer exact implementation, Step 4.4A failure inventory, accepted copy sources, missing fixes and blocked runtime 4.4B;
- Alpha exact implementation, audit PASS/FAIL/RUNTIME_REQUIRED surfaces, corrective decisions and missing runtime evidence;
- UI Profile Selection implementation, onboarding, resolver behavior, privacy, storage, persistence, default/fallback behavior and acceptance evidence.

D. Exact source and artifact inventory
- all relevant primary source files;
- root/docs or source/deployed counterparts;
- accepted lexicons, taboo lists, mapping tables, inventories, audit outputs, generators and validators;
- intentional transformations versus required byte identity;
- current boot entrypoints, cache-bust wiring and deployed reachability.

E. Runtime-sensitive boundaries
- every future file involving runtime JavaScript, UI runtime, state, persistence, routing, shared smoke registries, every `dev-checks.js` copy, `Game.__DEV`, `Game.Dev`, exports, globals, boot wiring and mirrors;
- exact ownership groups and serialization requirements;
- whether one consolidated presentation runtime slot is safe, or atomicity requires multiple gates.

F. Dependency and task plan
- stable-read dependencies;
- overlapping writes and mirror ownership;
- safe task splitting and order;
- exact copy-decision prerequisites before implementation;
- smallest reliable model and reasoning recommendation for each future lane;
- exact corrections required in the Stage 6 Execution Plan and Control Board before implementation begins.

G. Audit verdict
- produce a source-backed reconciliation table or equivalent structured result;
- distinguish confirmed facts, conflicts, stale status, missing evidence and blockers;
- give the exact next atomic task for ChatGPT to authorize after this audit;
- do not implement, repair, create copy or acquire a lock.

Mirror rules:
- do not assume byte identity unless the current contract requires it;
- distinguish byte parity, semantic parity, intentional deterministic transformation and actual deployment-wiring defects;
- do not reduce this task to a generic two-file mirror audit;
- matching file contents alone do not prove runtime reachability or deployed acceptance.

## Required response

Preflight and final audit report must follow current `AGENTS.md` and clearly distinguish:
- mailbox evidence;
- primary repository evidence;
- local-worktree evidence;
- task-owned scope;
- unrelated or concurrent state;
- static validation;
- runtime acceptance status;
- missing coverage;
- exact next user action.

Recommended starting model: `GPT-5.4 / High`, subject to the required model preflight comparison.

Allowed eventual write:
- only `.ai-bridge/outbox/BRIDGE-20260705-013-02-codex.md`.

Forbidden:
- every primary repository edit;
- runtime, UI, logic, generated asset, audit artifact, status-document or lock change;
- creative copy, slang, terminology invention or copy application;
- implementation, repair, commit, push to `main`, force push, amend, reset, rebase, stash or cleanup;
- modification of any closed or superseded thread.

Runtime gate: `N/A - read-only audit`. Do not request `APPROVE`.
Safari smoke: `N/A - read-only audit`.

## Safety and authority

Current primary sources override this request if newer. Report conflicts and stop rather than guessing. The user does not relay the preflight to ChatGPT. After choosing the recommended model, the user sends `CONTINUE` in the same Codex thread. Historical thread 011 remains superseded and must not be edited or resumed.