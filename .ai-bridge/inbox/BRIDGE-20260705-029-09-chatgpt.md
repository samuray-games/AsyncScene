Use @asynchronia runtime-safety-gate.

# BRIDGE-20260705-029 Alpha evidence matrix correction

BRIDGE_PROTOCOL: 2.4
BRIDGE_SLOT: 3
THREAD_ID: BRIDGE-20260705-029
LANE_ID: S6-A1-ALPHA-EVIDENCE
TASK_ID: TASK-S6-PAR-A1
PHASE: EVIDENCE_PAYLOAD_CORRECTION
EVIDENCE_BASELINE: a8a7e3acacbe2ba7fe6a387cb647d09d7d701a4d
CURRENT_POLICY_HEAD: c2d23b4de1182850d0d1af1b702564c24f7c830f
PRIMARY_WRITE_SCOPE: NONE
RUNTIME_CLASSIFICATION: READ_ONLY
CLAIM_PATH: .ai-bridge/claims/BRIDGE-20260705-029-claim-codex.md
CLAIM_TOKEN: s6a1-recovery-20260707-029-a
EXPECTED_OUTBOX_PATH: .ai-bridge/outbox/BRIDGE-20260705-029-02-codex.md

## Coordinator decision

The returned payload is not publication-ready. It contains useful grouped evidence, but it does not provide the required explicit per-surface matrix. Slot 3 remains unaccepted and the remote immutable outbox remains absent.

The main branch moved from the evidence baseline only through Git/bridge policy files. No product or runtime file changed. Continue to audit the exact evidence baseline above while reading current command policy from CURRENT_POLICY_HEAD.

Do not create a new claim. Do not push, reuse, amend, cherry-pick or rebase local commit `01a10e532a8ff5250ebecb63cb4132c65b21abcd`.

## Required same-thread correction

Return one complete publication-ready UTF-8 outbox payload. Include one explicit row for every required surface:

- PASS: attack;
- PASS: rematch;
- PASS: P2P give;
- PASS: P2P request;
- PASS: cop flow;
- FAIL: escape;
- FAIL: teach;
- FAIL: social actions;
- RUNTIME_REQUIRED: vote;
- RUNTIME_REQUIRED: report.

Every row must include all of these fields:

1. classification;
2. exact visible copy;
3. action ID;
4. handler;
5. emitter;
6. source file and exact location;
7. deployed mirror and exact location;
8. placeholders;
9. protected tokens, numbers and emoji;
10. runtime evidence requirement or exact reason it is N/A.

Also include:

- current profile resolver path and the Alpha-to-Zoomer wrong-profile result;
- static checks performed and exact verdicts;
- missing runtime coverage;
- stale Alpha documentation classification;
- explicit statement that no replacement Alpha copy was created;
- `ACTUAL_ACTIVE_MODEL: USER_SELECTED_UNVERIFIED`;
- Safari status `N/A - read-only evidence lane`.

Do not summarize rows or combine multiple surfaces into one row. Do not invent copy or action IDs. Use source-backed facts only.

## Publication command

After the exact payload exists, the universal command is `пуш`, not `запушь`.

Read current `origin/main:GIT_PUSH.md`. Detached HEAD is allowed only when the exact mailbox target, direct-parent proof, exact changed path and fast-forward proof pass. If Git authentication is unavailable, return the complete payload and exact path without asking for credentials.

Return exactly one fenced block containing only the complete intended outbox payload. Nothing may follow the block.
