Use @asynchronia runtime-safety-gate.

# Bridge Slot 1 - Wave V-A auth-fallback publication package

BRIDGE_PROTOCOL: 3.0
ORCHESTRATION_VERSION: 3.0
BRIDGE_SLOT: 1
THREAD_ID: BRIDGE-20260708-038
LANE_ID: S6-V5A-BOOMER-STATIC-GATE
TASK_ID: TASK-S6-PAR-V5A
PHASE: AUTH_FALLBACK_EXPORT_NOW
CORRECTION_OF: .ai-bridge/inbox/BRIDGE-20260708-038-03-chatgpt.md
REMOTE_MAIN_BASELINE: d15fe4dd34e8c431b02fb5a690982e38e6210fc5
LOCAL_VALIDATED_COMMIT_REPORTED_BY_CODEX: 124b7daa7679c6b75dc9734e6fdc5cd1c511257f
EXPECTED_REMOTE_OUTBOX: .ai-bridge/outbox/BRIDGE-20260708-038-04-codex.md
MODEL_PROTOCOL: SAME_THREAD_AUTH_FALLBACK_NO_NEW_PREFLIGHT
CONFIRMATION: ALREADY_SATISFIED_BY_PRIOR_CONTINUE
SAFARI_RUNTIME_STATUS: PENDING_USER

## Coordinator decision

Do not retry GitHub authentication, rebase, force-push or branch publication in this environment.

The current environment reported:

- local validated commit `124b7daa7679c6b75dc9734e6fdc5cd1c511257f`;
- remote `main` still at `d15fe4dd34e8c431b02fb5a690982e38e6210fc5`;
- push blocked by invalid GitHub credentials;
- local code and audit validations complete.

ChatGPT has an independent GitHub connector and will publish the authorized files after receiving a complete UTF-8 fallback payload.

## Required response format

Return one self-contained final response beginning exactly:

`AUTH_FALLBACK_PAYLOAD_BEGIN`

and ending exactly:

`AUTH_FALLBACK_PAYLOAD_END`

Do not attempt another push. Do not publish a mailbox outbox locally as a substitute.

Include these sections in order.

### 1. Identity and validation

- remote baseline SHA;
- local validated commit SHA;
- local commit parent SHA;
- exact changed paths between remote baseline and local commit;
- generator/validator/compile/diff-check results;
- final audit counts;
- root/docs audit byte-identity result;
- protected production blob verification;
- complete empty/non-empty failure arrays.

### 2. Full UTF-8 file payloads

Provide complete file contents, not diffs, for exactly:

1. `tools/generate-boomer-step4-4-economy-conflict-audit.py`
2. `UI_PROFILE_BOOMER_STEP_4_4_ECONOMY_CONFLICT_TERMINOLOGY_AUDIT.md`

The docs audit mirror must be byte-identical to item 2, so do not repeat its full content. Instead state:

`docs/UI_PROFILE_BOOMER_STEP_4_4_ECONOMY_CONFLICT_TERMINOLOGY_AUDIT.md = BYTE_IDENTICAL_COPY_OF_ROOT_AUDIT`

For each full file include:

- path;
- UTF-8 byte length;
- Git blob SHA from local commit;
- one fenced code block containing the entire exact file.

Do not truncate, summarize or replace content with ellipses.

### 3. Exact outbox payload

Provide the complete intended UTF-8 contents for:

`.ai-bridge/outbox/BRIDGE-20260708-038-04-codex.md`

The outbox must truthfully state that local validation passed but direct publication was blocked by authentication, and that ChatGPT connector publication is required. It must not claim a remote primary commit or remote outbox commit yet.

Include byte length, expected path, and the entire content in one fenced code block.

### 4. Connector publication manifest

Provide a compact manifest:

- base remote SHA: `d15fe4dd34e8c431b02fb5a690982e38e6210fc5`;
- root generator current remote blob SHA;
- root audit current remote blob SHA;
- docs audit current remote blob SHA;
- intended new local blob SHA for each authorized primary file;
- expected root/docs audit identity;
- no other primary paths changed.

## Strict prohibitions

- no new preflight;
- no new `CONTINUE`;
- no request for user reauthentication;
- no force push;
- no history rewrite;
- no production/runtime edits;
- no `TASKS.md`, `PROJECT_MEMORY.md` or mailbox STATE edits;
- no omitted or truncated file payloads;
- no base64 unless a UTF-8 code block is impossible.

## Next action

After producing the complete auth-fallback payload, tell the user to paste the entire response into ChatGPT and write `мост 1`.
