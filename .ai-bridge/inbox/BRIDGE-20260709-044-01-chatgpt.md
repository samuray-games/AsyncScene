Use @asynchronia. Start with task-router and invoke every required Asynchronia skill for this task.

# Process correction - three-slot orchestration, mandatory plugin routing and full outbox replay

BRIDGE_PROTOCOL: 3.2
THREAD_ID: BRIDGE-20260709-044
LANE_ID: PROCESS-BRIDGE-OUTBOX-AND-PARALLEL-CONTRACT
TASK_ID: TASK-PROCESS-THREE-SLOT-FULL-OUTBOX-HARDENING
EXECUTION_EPOCH: OBX-E1-20260709-1720JST
PHASE: CORRECTION_REQUIRED
BRIDGE_SLOT: 1
CURRENT_CLAIM: .ai-bridge/claims/BRIDGE-20260709-044-claim-v1-codex.md
REMOTE_MAIN_BASELINE: 5505f19b379cdf9a4559c1e6d5dd8292160e599b
EXPECTED_OUTBOX: .ai-bridge/outbox/BRIDGE-20260709-044-02-codex.md
THREAD_ROTATION_REQUIRED: true
FRESH_CODEX_CONVERSATION_REQUIRED: true
PRIMARY_WRITE_REQUIRED: true
ALLOW_VERIFIED_NO_DELTA: false
SAFARI_STATUS: N/A_PROCESS_ONLY
SUPERSEDES_THREAD: BRIDGE-20260709-043
SUPERSEDES_EPOCH: MSF-E1-20260709-1627JST
MAILBOX_POLICY_COMMIT: 5b93e620884c8a661a8df9ce61620b52967baf76

## Frozen objective

Create one coherent Protocol 3.2 process contract that simultaneously:

1. preserves the accepted non-blocking model recommendation policy;
2. documents and validates all three fixed bridge slots and safe parallel execution;
3. makes Asynchronia plugin routing mandatory for every numbered bridge task;
4. retires the contradictory `runtime-safety-gate` task prefix in favor of active `scope-isolation-check` routing;
5. makes an empty, partial, summary-only or missing outbox impossible to classify as success;
6. requires the expected outbox to contain the complete final response shown to the user;
7. requires publish, remote refetch and byte-for-byte outbox verification before Codex sends the final response or tells the user to return to ChatGPT;
8. retries every recoverable outbox publication failure automatically until remote verification succeeds;
9. fails closed as `BLOCKED_OUTBOX_PUBLICATION` without a ChatGPT handoff only for a non-recoverable external authentication, permission or service outage.

This is one atomic process/plugin correction. Do not modify product runtime files, Stage 6 Boomer copy or smoke implementation, `TASKS.md`, `PROJECT_MEMORY.md`, or primary `.ai-bridge/**` paths.

## User requirements that are authoritative for this task

### Three bridges and parallel work

- `мост 1`, `мост 2` and `мост 3` are all permanent fixed commands.
- Each command addresses only its matching slot.
- Every slot has its own thread, lane, task, epoch, inbox, claim, baseline, exact scope, worktree and outbox.
- Whenever multiple tasks or lanes exist, `parallel-scope-planner` is mandatory.
- Open slots may run concurrently only with proven disjoint writes, stable reads, mirrors, shared wiring, registries, generated outputs and documentation ownership.
- Shared process files, shared docs, mirror pairs, aggregate smoke, registries, exports, globals and boot wiring are serialized.
- Closed Slots 2 and 3 must be reported explicitly as intentionally unavailable when fewer collision-free lanes exist.

### Mandatory plugin routing

Every numbered bridge task must invoke the installed Asynchronia plugin. The active minimum route is:

1. `task-router` for every task;
2. `scope-isolation-check` for every implementation lane;
3. `model-selector` for every implementation lane;
4. `parallel-scope-planner` whenever multiple tasks or bridge lanes exist;
5. all other specialized Asynchronia skills required by the routed task.

The final report must list the skills actually invoked and their material results. A generic statement that the plugin was used is not evidence.

The obsolete line `Use @asynchronia runtime-safety-gate.` must not remain a mandatory prefix because that retired skill name conflicts with current Protocol 3.2. Replace it in templates and active process guidance with plugin-level routing beginning at `task-router` and active `scope-isolation-check` semantics.

### Full final-response outbox identity

For every success, verified-no-delta, correction, rejection or blocked completion that can write the mailbox branch:

- assemble the complete final response before publication;
- validate it against the required report schema;
- write those exact bytes to the exact expected outbox path;
- publish from a clean current mailbox worktree;
- refetch the remote mailbox branch and exact outbox;
- prove the remote bytes exactly equal the prepared final response bytes;
- only then send the same complete response to the user.

The outbox and visible final response must be byte-for-byte identical. Do not publish a summary, receipt, pointer, placeholder, whitespace-only file, empty file or one-line handoff.

The complete response must contain all applicable fields:

- status and completion mode;
- slot, thread, lane, task and execution epoch;
- invoked Asynchronia skills and results;
- selector-originated recommendation and actual model status;
- inspected and changed files;
- tests and validators with results;
- failures and recovery attempts;
- missing coverage;
- baseline, primary commit and parent, or explicit N/A reasons;
- buildTag, commit label and smokeVersion, or explicit N/A reasons;
- Safari status;
- expected outbox path;
- exact next user action.

The final response must not contain self-referential mailbox commit data that would make byte identity impossible. ChatGPT independently verifies mailbox commit identity after receiving `мост N`.

### Retry and handoff rule

- Any recoverable outbox failure must trigger automatic refetch, clean mailbox worktree recreation, exact response reapplication, fast-forward push and remote byte verification.
- Stale mailbox parents, non-fast-forward mailbox updates, stale worktrees, first-push failures and remote byte mismatches are recoverable and must not end the run.
- No success status and no instruction to return to ChatGPT is allowed until the exact non-empty outbox is remotely verified.
- If mailbox publication is impossible only because of a non-recoverable external auth, permission or service outage after allowed repair, return `BLOCKED_OUTBOX_PUBLICATION` in Codex, preserve the prepared response locally and do not tell the user to send `мост N` to ChatGPT.

## Accepted model-selection resolution retained from superseded thread 043

- only `plugins/asynchronia/skills/model-selector/SKILL.md` may originate, rank or name a Codex model and reasoning recommendation;
- the user alone selects the actual active model in the Codex interface;
- actual model remains `USER_SELECTED_UNVERIFIED` unless externally verified;
- recommendation is informational and cannot pause, block, authorize or resume implementation;
- remove `MODEL_PREFLIGHT_ONLY`, `WAITING_FOR_MODEL_SELECTION`, mandatory `CONTINUE`, model-preflight stops and valid-preflight resume dependencies;
- a material scope change recomputes the recommendation in the same response and does not create a user round trip.

## Exact primary write scope

Only these primary paths may change:

- `AGENTS.md`
- `AGENTS.override.md`
- `PROCESS_ROOT_SYNC.md`
- `ORCHESTRATION.md`
- `BRIDGE.md`
- `CODEX_BRIDGE_BOOTSTRAP.md`
- `CODEX_BRIDGE_RECOVERY.md`
- `GIT_PUSH.md`
- `STAGE6_PARALLEL_EXECUTION_PLAN.md`
- `.github/workflows/orchestration-policy.yml`
- `tools/validate-orchestration-policy.py`
- `plugins/asynchronia/.codex-plugin/plugin.json`
- `plugins/asynchronia/skills/task-router/SKILL.md`
- `plugins/asynchronia/skills/parallel-scope-planner/SKILL.md`
- `plugins/asynchronia/skills/model-selector/SKILL.md`
- `plugins/asynchronia/skills/acceptance-pipeline-controller/SKILL.md`
- `plugins/asynchronia/skills/pipeline-state-and-resume-contract/SKILL.md`
- `plugins/asynchronia/skills/evidence-bundle-and-artifact-identity/SKILL.md`
- `plugins/asynchronia/skills/failure-routing-and-corrective-loop/SKILL.md`

All other primary paths are protected.

## Required implementation

1. Unify all root process files with the three-slot contract and explicit slot-specific acceptance behavior.
2. State mandatory Asynchronia plugin routing for every numbered bridge task without requiring every skill for every task.
3. Remove or replace stale `runtime-safety-gate`, `CONTINUE`, preflight and approval semantics in every changed process surface.
4. Add a canonical complete-report schema and exact final-response/outbox identity contract.
5. Add publish-before-reply ordering and recoverable retry behavior.
6. Make `BLOCKED_OUTBOX_PUBLICATION` the only no-handoff result for non-recoverable mailbox publication failure.
7. Update bridge bootstrap and recovery guidance so user-level aliases follow current remote Protocol 3.2 and never return the user to ChatGPT before verified outbox publication.
8. Update `parallel-scope-planner` and task routing contracts to map safe lanes to Slots 1, 2 and 3 and to explain why unused slots remain closed.
9. Update evidence, pipeline and failure-routing skills to require complete outbox replay and reject empty or partial reports.
10. Preserve plugin-only model recommendation authority and bump plugin version from `1.0.3` to `1.0.4`.

## Validator requirements

`tools/validate-orchestration-policy.py` must fail closed and must:

- dynamically discover every `plugins/asynchronia/skills/*/SKILL.md` file;
- prove every discovered skill is scanned and covered by workflow triggers;
- validate plugin version `1.0.4`, JSON syntax and every skill frontmatter/name pair;
- require all three commands `мост 1`, `мост 2`, `мост 3` in root bridge authority;
- require slot-specific inbox, claim, epoch, scope and outbox semantics;
- require `task-router`, `scope-isolation-check`, `model-selector` and conditional `parallel-scope-planner` routing;
- reject the obsolete mandatory `runtime-safety-gate` prefix;
- reject model-preflight, mandatory `CONTINUE`, approval-only and hidden stopping variants;
- require `EMPTY_OUTBOX: FORBIDDEN` and `FINAL_RESPONSE_OUTBOX_IDENTITY: REQUIRED` in authoritative process files;
- require complete-report fields, publish-before-reply ordering, remote refetch, byte equality and retry semantics;
- reject one-line return-to-ChatGPT completion before outbox verification;
- require `BLOCKED_OUTBOX_PUBLICATION` to forbid a ChatGPT handoff;
- print the exact dynamically discovered and scanned paths.

Add validator self-tests or negative fixtures sufficient to prove failure for:

- empty outbox contract;
- missing report field;
- summary-only outbox;
- missing byte-equality verification;
- success before remote refetch;
- missing retry rule;
- missing bridge 2 or bridge 3;
- missing mandatory plugin route;
- reintroduced `runtime-safety-gate`, model preflight or `CONTINUE` stop.

## Required validation

Run and report:

- `python3 tools/validate-orchestration-policy.py`;
- validator negative-control tests for every failure class above;
- comprehensive semantic search across all root process files and all plugin skills;
- dynamic skill discovery versus scanned-path equality;
- workflow trigger coverage equality;
- plugin JSON validation for `1.0.4`;
- every changed-skill frontmatter/name validation;
- exact nineteen-path changed-scope verification;
- `git diff --check`;
- proof that product/runtime paths are unchanged.

## Execution and publication

On the first exact `мост 1` in a fresh Codex conversation:

1. fetch current main and mailbox;
2. verify this thread, epoch, inbox, claim, baseline, scope and expected outbox;
3. invoke Asynchronia `task-router`, `scope-isolation-check`, `model-selector` and `parallel-scope-planner` because three bridge slots and concurrent-lane policy are in scope;
4. execute immediately if the exact serialized process lane is collision-free;
5. implement only the nineteen authorized primary paths;
6. run all required validation and negative controls;
7. create one direct-child primary commit and push main fast-forward;
8. assemble the complete final response before mailbox publication;
9. publish those exact bytes only to `.ai-bridge/outbox/BRIDGE-20260709-044-02-codex.md` from a clean latest mailbox worktree;
10. retry every recoverable publication failure automatically;
11. refetch and byte-compare the remote outbox;
12. only after equality is proven, return that same complete response to the user with `PASS_PUSHED` and exact next action `Return to ChatGPT and send мост 1`.

Do not modify or repair Step 4.4B in this task. Do not stop for model selection, `CONTINUE`, runtime approval, `APPROVE` or Safari.
